[{"u":"getting-started/introduction.html","t":"Introduction","s":"Getting Started","d":"What this handbook covers, why it targets the Community CLI, and what changed in React Native 0.87.","h":["Why the CLI path, and a note on Expo","What changed, and why most tutorials are now wrong","The 0.87 change that breaks existing code","What this handbook assumes","How the handbook is organised","Conventions in this handbook","Common mistakes","Related topics"],"b":"This is a practical handbook for building, shipping and maintaining React Native apps with the React Native Community CLI . It targets React Native and assumes the New Architecture, because as of 0.82 there is no other architecture to assume. Everything here was checked against the npm registry and the installed type definitions on 2026 09 12. Where a version number appears, it is a real one that was read from the registry rather than remembered. Why the CLI path, and a note on Expo React Native has two mainstream setups. Expo is a framework built on top of React Native, and the official React Native documentation recommends it for most new apps. It is a reasonable default, and if you have no specific reason to avoid it, you should read their docs instead of these. This handbook deliberately covers the other path: the framework less Community CLI , where you own the and folders outright. That path is the right one when you are adding React Native to an existing native app, when you depend on native code that no managed workflow will accommodate, when your build has to run inside an existing native CI pipeline, or when your organisation needs the native projects under its own control. That is the entire scope note. Expo, EAS and packages do not appear anywhere else in this handbook — every command, dependency and example from here on uses the Community CLI. What changed, and why most tutorials are now wrong If you search for React Native help, most of what you find describes an architecture that no longer exists. This is the single biggest source of wasted time for people picking the framework back up. Version Date What happened 0.76 Oct 2024 New Architecture became the default 0.81 Aug 2025 Last version that supported both architectures 0.82 Oct 2025 Runs entirely on th"},{"u":"getting-started/environment-setup.html","t":"Environment Setup","s":"Getting Started","d":"Install Node, the JDK, Android Studio and — on macOS — Xcode, so that React Native 0.87 builds on the first try.","h":["What you actually need, and what you do not","Node, and why the version floor bites","Install Node with a version manager","Pin the version per project","Watchman","The JDK","Android Studio and the Android SDK","SDK Platforms tab","SDK Tools tab","Environment variables","Create an emulator","macOS only: Xcode and the iOS toolchain","Xcode","Command line tools","An iOS simulator runtime","Ruby and Bundler, for CocoaPods","Remove any global React Native CLI","Verify your setup","The one command that checks everything","Common mistakes","Related topics"],"b":"React Native 0.87 builds real native apps, so the toolchain is a native toolchain: a JavaScript runtime, a JDK, the Android SDK, and on macOS the Xcode toolchain as well. Almost every \"it does not build\" report on a fresh machine traces back to one of four things — a Node version below the floor, the wrong JDK, a missing Android SDK component, or not being set where the shell can see it. This page installs each piece, explains why it is needed, and ends with a set of commands you can run to prove the setup is real before you create a project. What you actually need, and what you do not Piece Needed for Optional? Node.js Everything — Metro, the CLI, the package manager No A Node version manager Switching Node per project Strongly recommended Watchman Faster file watching on macOS and Linux Recommended, not required JDK 17 Compiling Android and running Gradle No, for Android Android Studio + SDK Android builds, the emulator, No, for Android Xcode 26 + iOS runtime iOS builds and the simulator No, for iOS. macOS only Ruby + Bundler CocoaPods, which installs iOS native dependencies No, for iOS iOS requires macOS. There is no way around it Building or running an iOS app requires Xcode, and Xcode runs only on macOS. There is no supported path on Windows or Linux — not a VM you can license, not a cross compiler, not a container. If you are on Windows or Linux you can build the complete Android app and write all of the shared JavaScript, but the iOS half of the project needs a Mac (your own, a colleague's, or a hosted macOS CI runner). Android development works on all three operating systems. Node, and why the version floor bites React Native 0.87.1 declares this in its own : Read that carefully, because it is not \"Node 22 or newer\". It is three disjoint ranges: — Node 22, but o"},{"u":"getting-started/creating-a-project.html","t":"Creating a Project","s":"Getting Started","d":"Create a React Native 0.87 app with the Community CLI, pin the version deliberately, and get it running on both platforms.","h":["Remove any global CLI first","Create the project","Pin the version deliberately","What version did you actually get","Install native dependencies","Run it","The first ten minutes after init","1. Commit immediately","2. Opt out of the AGP 9 defaults","3. Confirm the Strict TypeScript API is active","4. Add .env-style secrets to .gitignore before you need them","Upgrading later","Common mistakes","Related topics"],"b":"With the toolchain in place, creating a project is one command. The parts worth understanding are which command, which version it gives you, and what to do in the first ten minutes after it finishes — because a project created carelessly is a project you will re create. This page assumes you have worked through Environment Setup. If reports missing pieces, fix those first; will succeed regardless and then fail at build time, which is a much worse place to debug. Remove any global CLI first This is not optional housekeeping. was the old global package, and a copy left on your machine from 2021 intercepts commands and reports errors from a CLI that predates the New Architecture entirely. Symptoms include , templates that do not match the docs, and Gradle configuration errors referencing files your project does not have. The supported invocation is , which runs a specific version and leaves nothing behind. Create the project That gives you the latest stable React Native — 0.87.1 at the time of writing — with the 20.2.0 toolchain, a TypeScript entry point, and both native projects generated in full. The project name becomes the Android application ID and the iOS bundle identifier, and renaming it afterwards means touching Gradle files, Xcode project settings, Java package directories and . Choose a name you can live with. Use PascalCase with no spaces, hyphens or leading digits. pnpm and React Native pnpm's symlinked layout works, but some native libraries assume a flat tree and autolinking occasionally needs in . If you have no specific reason to choose pnpm for a React Native project, npm or yarn is the lower friction default. Pin the version deliberately is right when you are starting something new today. It is wrong when you are recreating a project to match an existing"},{"u":"getting-started/project-structure.html","t":"Project Structure","s":"Getting Started","d":"Every file and folder a React Native 0.87 init generates, what each one is for, and which ones you will actually edit.","h":["The generated tree","The files you will edit constantly","App.tsx","package.json","android/ and ios/","The files you edit rarely, but must understand","index.js","app.json","metro.config.js","babel.config.js","tsconfig.json","jest.config.js","Gemfile and .bundle/",".watchmanconfig",".eslintrc.js and .prettierrc.js","__tests__/App.test.tsx","Where your own code goes","Common mistakes","Related topics"],"b":"A freshly generated React Native project has about a dozen top level files and two large native folders. Most tutorials skip straight past them, which is why so many people are unsure whether they are allowed to edit (you are), whether matters (it does), or why there are three different config files that all seem to be about bundling. This page walks the tree a 0.87 actually produces and says, for each entry, what it is for and how often you will touch it. The generated tree , , and the dotfiles In the template repository these are stored as , and , because npm strips a real from a published package. The CLI renames them during . If you are reading the template source and wondering where the dotfiles went, that is why. The files you will edit constantly The root component, and the only application code the template ships. It renders a starter screen whose entire purpose is to prove the toolchain works. Delete its contents and replace them the moment you have a real first screen — nothing else depends on what is inside it. The file is because the template is TypeScript by default in 0.87. There is no separate \"TypeScript template\" to opt into any more. Dependencies plus the scripts you run every day: runs Metro. and build and install. These scripts call the project local CLI, which is exactly why a global CLI install causes so much confusion — the scripts and your muscle memory end up running different binaries. and Two complete native projects, checked into your repository, owned by you. This is the defining property of the Community CLI path: there is no managed layer regenerating them, so nothing stops you editing them, and nothing regenerates them if you break them. The files you will realistically open: File What you change there App ID, version code/name, signing c"},{"u":"getting-started/running-on-android.html","t":"Running on Android","s":"Getting Started","d":"What `npm run android` actually does, how to pick an emulator or a device, and how to read the Gradle, Metro and adb failures you will hit on the way.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Choosing a target","Common patterns","Reading a Gradle failure","The failures that are not Gradle's fault","When to clean, and when not to","After adding a native dependency","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"builds a debug APK with Gradle, installs it on a target, launches the main activity, and makes sure Metro is serving the JavaScript bundle. Four moving parts, four different places a failure can come from. This page is about telling them apart. The single most useful habit is reading the first error, not the last. Gradle prints a long tail of \"Execution failed for task\" lines after the real cause, and the real cause is usually twenty lines above where your eye lands. Why it exists / when to use it — and when NOT to The CLI command is for the development loop: build, install, attach to Metro, iterate. It always produces a debug build that loads JavaScript from the Metro server over the network. It is the wrong tool when you want to inspect the release build, test startup time, or reproduce a bug that only happens without Metro. For those, build the release variant and let the bundle be embedded in the APK: That takes noticeably longer because it bundles and minifies JavaScript into the APK, and it needs a signing config. See Android Signing. Basic example Two terminals. Metro in the first, and leave it running: The build in the second: Metro's terminal accepts three key commands once it is running, which is faster than reaching for the device: Key Effect <kbd r</kbd Reload the connected app(s) <kbd d</kbd Open the Dev Menu on the connected app(s) <kbd j</kbd Open React Native DevTools On the device itself, reload by pressing <kbd R</kbd twice, and open the Dev Menu with <kbd Ctrl</kbd +<kbd M</kbd (or by shaking a physical device). How it works runs from the project local CLI. In order, it: 1. Looks for a running Metro server on port 8081 . If there is not one, it starts one in a new terminal window. 2. Picks a target — the device you named with , or the single connected"},{"u":"getting-started/running-on-ios.html","t":"Running on iOS","s":"Getting Started","d":"Building and running the iOS app on a simulator and a real device, and reading the CocoaPods, Xcode and signing failures that come with it.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Choosing a target","Common patterns","CocoaPods drift — the single biggest source of iOS build failures","Derived data, and when clearing it is justified","Building to a physical device","Port 8081 is in use","After adding a native dependency","Escalating a build failure, in order","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"iOS builds require macOS and Xcode Xcode runs only on macOS, and there is no supported alternative — not a virtual machine you can license, not a cross compiler, not a container. If you are on Windows or Linux you can build the whole Android app and write all of the shared JavaScript, but the iOS half needs a Mac: your own, a colleague's, or a hosted macOS CI runner. Everything on this page assumes macOS. compiles the Xcode workspace, installs the app on a simulator or device, launches it, and makes sure Metro is serving the bundle. Unlike Android, iOS has a separate dependency step — CocoaPods — that you run by hand, and most of the confusing failures on this page come from that step being out of date. Why it exists / when to use it — and when NOT to The CLI command is the development loop. It always builds the Debug configuration, which loads JavaScript from Metro over the network. Use Xcode directly instead when you need something the CLI does not expose: reading a native crash in the debugger, changing signing settings, inspecting the view hierarchy, or profiling with Instruments. Open — never the , which does not include the pod targets. For a build that behaves like the shipped app: That embeds the JavaScript bundle in the app instead of loading it from Metro, so it is also how you reproduce \"works in development, broken in release\". Basic example First run, from a freshly created or freshly cloned project: Then, in two terminals: Metro's terminal accepts the same keys as on Android: <kbd r</kbd to reload, <kbd d</kbd to open the Dev Menu, <kbd j</kbd to open React Native DevTools. On the simulator itself, the Dev Menu is <kbd Cmd</kbd +<kbd D</kbd , and reload is <kbd R</kbd pressed twice. How it works runs from the project local CLI, which: 1. Starts Metro on po"},{"u":"getting-started/dev-menu-and-fast-refresh.html","t":"Dev Menu and Fast Refresh","s":"Getting Started","d":"The in-app Dev Menu, what Fast Refresh does and does not preserve, and how to open React Native DevTools in 0.87.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What is preserved","What is not preserved","Forcing a remount on purpose","Error resilience","When Fast Refresh silently stops","Common patterns","Adding your own Dev Menu entry","Structuring files so Fast Refresh keeps working","Reload versus refresh versus rebuild","React Native DevTools","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"The development loop in React Native is: save a file, see the change, keep your place in the app. Fast Refresh is what makes that work, the Dev Menu is where you control it, and React Native DevTools is where you inspect what happened. All three are development only — none of them exists in a release build. Most of the frustration people report with this loop comes from one thing: Fast Refresh sometimes resets your state, and the reason is never random. This page is mostly about making that predictable. Why it exists / when to use it — and when NOT to Fast Refresh replaces the edited modules inside the running app and asks React to re render, instead of restarting the app. That keeps you inside the screen you are working on — three levels deep in a form, with a modal open — which is exactly where a reload hurts. It is on by default and you should leave it on. Turn it off temporarily when you are debugging something that Fast Refresh itself perturbs: module level initialisation, a subscription set up at import time, or a bug you can only reproduce from a cold start. Then reload rather than refresh. Basic example Open the Dev Menu: <kbd Cmd</kbd +<kbd D</kbd on the simulator, or shake a physical device. <kbd Ctrl</kbd +<kbd M</kbd on the emulator, or shake a physical device. From the Metro terminal, without touching the device at all: Key Effect <kbd r</kbd Reload the connected app(s) <kbd d</kbd Open the Dev Menu on the connected app(s) <kbd j</kbd Open React Native DevTools These are the three keys Metro registers in interactive mode; it prints them itself when it starts. On the device, reload by pressing <kbd R</kbd twice. How it works Fast Refresh is React Native's integration of React Refresh . Metro compiles each module with a Babel transform that registers every Re"},{"u":"getting-started/your-first-screen.html","t":"Your First Screen","s":"Getting Started","d":"Replace the generated template with a real, typed screen — layout, text input, a list and press handling — using the 0.87 APIs that actually exist.","h":["Why it exists / when to use it — and when NOT to","What you need first","Basic example","How it works","Layout is flexbox, with different defaults","Text input, and holding its value","Pressable, not a button element","Lists scroll; View does not","The complete screen","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"The screen generates is a demo of the framework, not a starting point. This page replaces it with something small but genuinely real: a task list with an input, a list, and press handling. By the end you will have used every primitive that most screens are built from, and you will have seen where React Native stops looking like the web. Every code block on this page is type checked against the installed React Native 0.87 types with the Strict TypeScript API active. If it appears here, it compiles. Why it exists / when to use it — and when NOT to This is the \"hello world\" that teaches something. It is deliberately not a navigation example, not a state management example and not a networking example — each of those has its own section, and mixing them in makes it impossible to tell which part broke. Skip to Components instead if you already know React Native and just want the reference for a specific primitive. What you need first A project that builds and runs on at least one platform. If or is not working yet, fix that first — see Running on Android and Running on iOS. Debugging a build failure and a layout bug at the same time is twice as hard as debugging either alone. One package is worth installing before you start, because the core alternative is deprecated: Then rebuild: it contains native code, so restarting Metro is not enough. Core is deprecated in 0.87 The installed 0.87 type definitions mark from as , and annotate it — it does nothing on Android. Use , which handles both platforms and gives you the inset values directly when you need them. Basic example Start by replacing the body of with the smallest thing that renders. Get this on screen before adding anything else, so that when something breaks later you know it was the thing you just typed. Four things in"},{"u":"getting-started/learning-path.html","t":"Learning Path","s":"Getting Started","d":"A suggested reading order through this handbook, what to read now, what to read when you hit the problem, and what you can honestly skip.","h":["Why it exists / when to use it — and when NOT to","Stage 1 — Before you write any code","Stage 2 — Understand what is running","Stage 3 — Build screens","Stage 4 — More than one screen","Stage 5 — Make it do something","Stage 6 — Motion and gestures","Stage 7 — Native code","Stage 8 — Make it good","Stage 9 — Ship it","Stage 10 — Keep it alive","If you are coming back after a few years","Common mistakes","Related topics"],"b":"This handbook has fifteen sections and well over a hundred pages. Read in order, front to back, that is a poor use of your time: half of it is reference material that only makes sense once you have the problem it solves. What follows is a reading order that tracks how a real project grows, with an honest note on each stage about what you can skip. The only genuinely non optional part is the first stage. Why it exists / when to use it — and when NOT to Use this page when you are new to the handbook, or when you are picking React Native back up after a couple of years and need to know which of your habits are now wrong. Skip it if you arrived looking for one specific API — go straight to Component Reference or API Reference. Stage 1 — Before you write any code Read all of this. It is about two hours and it saves considerably more. 1. Introduction — the version timeline, and why most tutorials you will find are now wrong. The single most important page here. 2. Environment Setup — Node, JDK 17, the Android SDK, Xcode. Most \"React Native does not build\" reports are a toolchain problem, not a code problem. 3. Creating a Project — , version pinning, and the first run. 4. Project Structure — which of the generated files you will actually edit. 5. Running on Android and Running on iOS — read the one for the platform you are on now, and the other when you get to it. 6. Dev Menu and Fast Refresh — the development loop. 7. Your First Screen — a real screen, typed, on current APIs. What you can skip: nothing. If you are tempted to skip Environment Setup because you \"already have Node\", check the version anyway — the floor is 22.13.0, not 22.11.0. Stage 2 — Understand what is running Read The New Architecture now. Read the rest when you hit something that does not make sense. React "},{"u":"core-concepts/new-architecture.html","t":"The New Architecture","s":"Core Concepts","d":"What actually runs a React Native 0.87 app — JSI, Fabric, TurboModules, Codegen and Hermes — and what the removal of the Bridge changed.","h":["Why it exists — the problem with the Bridge","What replaced it","Basic example","How it works","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"\"The New Architecture\" is the name for the set of pieces that replaced React Native's original runtime between 2022 and 2025. In React Native 0.87 it is not a mode, a flag or an opt in. It is the only way React Native runs. Understanding it is the difference between reasoning about your app and guessing. Five things do the work, and each has its own page: Piece What it is What it replaced JSI A C++ interface that lets JavaScript hold and call native objects directly JSON messages over an async queue Fabric The renderer: a C++ shadow tree, layout, and a mount phase The old UIManager and its Java/ObjC shadow trees TurboModules Native modules as lazily created JSI objects with typed methods Eagerly instantiated, dynamically dispatched native modules Codegen A build time generator that turns TypeScript specs into C++/Java/ObjC interfaces Hand written glue and runtime type coercion Hermes The default JavaScript engine, running precompiled bytecode JavaScriptCore with runtime parsing Why it exists — the problem with the Bridge React Native originally connected JavaScript and native code with a component called the Bridge . Everything crossing between the two worlds went through it, and it had four properties that together set a ceiling on what the framework could do: 1. Serialised. Every argument was converted to JSON on one side and parsed on the other. A large list payload or a stream of scroll events paid that cost twice per message. 2. Asynchronous. There was no way to ask native a question and get an answer in the same tick. Measuring a view, reading a layout value, or deciding what to render based on a native value all had to become callbacks. 3. Batched. Messages were queued and flushed on a schedule rather than sent immediately, so latency was not just non zero, it wa"},{"u":"core-concepts/fabric.html","t":"Fabric","s":"Core Concepts","d":"React Native's renderer — the C++ shadow tree, Yoga layout off the main thread, atomic commits, and why it makes concurrent React work.","h":["Why it exists — and what it changed","Basic example","How it works","Shadow nodes are immutable","Three phases","Why concurrent React works here","Synchronous access, when it is warranted","View flattening","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Fabric is React Native's renderer. It is the code that turns the element tree your components return into real and objects, and it is written in C++ and shared by both platforms. The part worth internalising: Fabric owns a shadow tree — a C++ description of your UI, independent of both JavaScript and the platform view system. Layout happens on that tree, off the main thread. Only the final mount step touches platform views. Why it exists — and what it changed The previous renderer kept its shadow tree in Java on Android and Objective C on iOS, and drove it with asynchronous messages from JavaScript. Three consequences followed: Measurement was asynchronous. Asking how tall a view was meant a round trip, so layouts that depended on measured size flashed. React could not be interrupted safely. Updates were applied as they arrived, so a render React later abandoned had already touched the screen. Two implementations drifted. Every layout fix had to be made twice, and the platforms disagreed in the gaps. Fabric replaces all three: one C++ implementation, layout on a background thread, and commits that are atomic so a half finished render is never visible. You do not call Fabric. You get it by rendering components. It matters because it explains what is fast, what is not, and why some things that were impossible are now possible. Basic example Nothing here opts into Fabric — this is just a component. It is included to anchor the vocabulary used below. Rendering this produces three trees, and keeping them distinct is most of the battle: Tree Lives in Contains React element tree JavaScript , , — including your own components Shadow tree C++ Only host components ( , ), each with props and computed layout Host view tree Platform / instances exists only in the first. By the time "},{"u":"core-concepts/turbomodules.html","t":"TurboModules","s":"Core Concepts","d":"How native modules work in React Native 0.87 — lazy creation through TurboModuleRegistry, typed interfaces from Codegen, and synchronous calls over JSI.","h":["Why they exist — and when you need one","Basic example","How it works","Registration and lazy creation","Method dispatch","Sync versus async, concretely","Events","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"A TurboModule is a native module exposed to JavaScript as a JSI object . Calling one of its methods is a C++ call into Kotlin or Swift, not a message posted to a queue. The module itself is created the first time JavaScript asks for it, and its method signatures are generated from a TypeScript spec at build time. TurboModules are how every native module works in 0.87. There is no flag, and no alternative; the feature flag was removed. Why they exist — and when you need one The previous system had two structural problems. Every registered native module was instantiated when the app started, whether or not you used it — so install a library and pay for it on every cold start. And every call was dynamically dispatched with arguments coerced at runtime, so a wrong argument type produced a confusing failure deep inside native code, or silently did nothing. TurboModules fix both: lazy instantiation through a registry, and typed interfaces that Codegen derives from your spec so a mismatch is a build error. You need a TurboModule when JavaScript has to reach a platform capability that core does not expose — a vendor SDK, a hardware API, an existing native library in your own app. You do not need one to make JavaScript faster; JS and native are both fast, and the boundary is no longer the bottleneck it was. Basic example The spec file is the contract. It is ordinary TypeScript and lives in your app. Using it from a component is unremarkable, which is the point — the types come from the spec: The full native side — Kotlin, Swift, registration and autolinking — is in TurboModules End to End. How it works Registration and lazy creation At startup the runtime installs a TurboModule provider into the JavaScript global scope: a JSI object whose property lookups are intercepted by C++."},{"u":"core-concepts/jsi.html","t":"JSI","s":"Core Concepts","d":"The JavaScript Interface — a C++ API that lets JavaScript hold and call native objects directly, replacing the Bridge's serialised message queue.","h":["Why it exists — and why you rarely touch it directly","Basic example","How it works","The runtime is a C++ object","Values, and who owns them","Host objects and host functions","Shared memory","Synchronous means \"on the calling thread\"","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"JSI (the JavaScript Interface) is a small, engine agnostic C++ API . It defines what a JavaScript engine must be able to do — create values, read properties, call functions — and, crucially, it lets C++ install its own objects and functions into the JavaScript global scope. That last capability is the whole point. A JavaScript variable can hold a pointer to a live C++ object. Calling a method on it is a virtual C++ call on the calling thread. No serialisation, no queue, no callback. Everything else in the New Architecture — Fabric, TurboModules, Reanimated's worklets, JSI based storage libraries — is built on this one idea. Why it exists — and why you rarely touch it directly The Bridge could only move data. Anything you wanted native to do had to be encoded as a JSON message, queued, batched, and answered later through another message. Three things were therefore impossible: Synchronous results. You could not ask native a question and use the answer on the next line. Sharing memory. A 4 MB buffer had to be copied and re encoded to cross, so image pixels, audio samples and database rows were expensive by construction. Passing functions. A callback had to be registered by id and invoked by another message, so ownership and lifetime were manual and leaky. JSI removes all three by making the boundary a pointer , not a protocol. You will almost never write JSI code in an app. You use it constantly and indirectly. Write raw JSI only when you are building a library that needs to expose something the TurboModule and Fabric shapes cannot express — a memory mapped buffer, a custom runtime, a hot path measured in microseconds. For everything else, a TurboModule gives you the same mechanism with a generated, type safe surface. Reach for the generated layer first A hand written JSI"},{"u":"core-concepts/codegen.html","t":"Codegen","s":"Core Concepts","d":"How TypeScript spec files become C++, Java and Objective-C interfaces at build time, and why that makes native contracts compile-time checked.","h":["Why it exists — and when it runs","Basic example","A module spec","A component spec","Telling the build where to look","How it works","The pipeline","What the generator accepts","Where the output lands","Reading the generated interface","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Codegen is a build time code generator. It reads TypeScript spec files from your project and its dependencies and emits the native interfaces that TurboModules and Fabric components must implement: C++ bindings, a Java abstract class, an Objective C protocol. The spec is the source of truth. You write the contract once, in TypeScript, and the compiler on each platform enforces it. A method you declared but did not implement is a build error rather than a runtime . Why it exists — and when it runs Before Codegen, the glue between JavaScript and native was hand written on both sides and agreed on by convention. Argument types were coerced at runtime. The failure mode was a mismatch that compiled cleanly, shipped, and produced a null on one platform only. Codegen makes the contract machine checked in three places at once: Where What is checked TypeScript Your call sites match the spec Kotlin / Java Your implementation matches the generated abstract class Swift / Objective C Your implementation conforms to the generated protocol It runs as part of the native build , not as part of Metro. That is the single most common source of confusion: editing a spec and reloading JavaScript changes nothing, because generated native code is produced by Gradle and by the CocoaPods/Xcode build script. When you must rebuild Change a spec file and you need a native rebuild ( / ), not a Fast Refresh. If a newly added method is at runtime, a stale generated interface is the first thing to check. Basic example A spec file is recognised by convention: it lives in a directory declared in your , and its filename starts with for a module or ends with for a view. A module spec A component spec Telling the build where to look is , or . is scanned recursively for spec files. A library declares the sam"},{"u":"core-concepts/hermes.html","t":"Hermes","s":"Core Concepts","d":"The default JavaScript engine — ahead-of-time bytecode, no JIT, and the trade it makes between peak throughput, startup time and memory.","h":["Why it exists — and what it optimises for","Basic example","How it works","Development: source over the wire","Release: bytecode in the bundle","No JIT, concretely","Garbage collection","Debugging","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Hermes is the JavaScript engine React Native runs on. It is the default, and as of 0.87 it is effectively the only supported one: the Podfile option is marked deprecated in React Native's own build scripts, with the note that Hermes is the default engine and JavaScriptCore has moved to community support under . Two design decisions define it. Your JavaScript is compiled to bytecode ahead of time , and the engine has no JIT compiler . Everything else about Hermes follows from those. Why it exists — and what it optimises for Browser engines are tuned for a workload mobile apps do not have: a long lived tab where a JIT has minutes to observe hot code and generate optimised machine code, on a machine that is plugged in. A phone app is the opposite. It starts, does a burst of work, and is killed. Startup and memory dominate; peak throughput after thirty seconds of warm up rarely matters. Hermes takes the trade in that direction: Decision You gain You give up Bytecode compiled at build time No parse or compile at startup; the bundle is memory mapped and executed Build time cost, and a larger on disk artifact than minified source No JIT Lower memory, smaller engine binary, no warm up, no JIT writable pages Peak throughput on long running numeric loops Compact object model and a concurrent GC Smaller heap, shorter pauses Some allocation heavy patterns cost more than on a JIT engine For an app whose JavaScript mostly reacts to events and updates a UI, that trade is strongly positive. For an app doing sustained heavy computation in JavaScript, it is not — and the right answer there is usually to move the computation into native code behind a TurboModule, not to swap engines. Basic example You do not import Hermes. You observe it. The engine identifies itself through a global that"},{"u":"core-concepts/render-pipeline.html","t":"The Render Pipeline","s":"Core Concepts","d":"What happens between setState and a pixel changing — render, commit and mount, which thread runs each, and where jank actually comes from.","h":["Why it matters","Basic example","How it works","Phase 1 — Render","Phase 2 — Commit","Phase 3 — Mount","Putting it together","Events run the pipeline backwards","Why setState then measure gives the old value","State-driven animation versus native animation","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Between calling and a pixel changing, React Native does a fixed sequence of work. Knowing that sequence is what turns \"the list feels janky\" into a specific question with a specific answer. There are three phases — render , commit and mount — and they do not all run on the same thread. Why it matters Most performance advice for React Native is folklore attached to the wrong phase. \"Reduce re renders\" is good advice for the render phase and irrelevant if your cost is in mount. \"Flatten your view hierarchy\" targets mount and does nothing for a slow reducer. The pipeline is also the only way to answer questions that otherwise sound like magic: why fires when it does, why reading a measurement right after gives you the old value, and why an animation driven from JavaScript state is categorically more expensive than one driven natively. What this replaced Under the old Bridge, updates were serialised into JSON messages, batched, and applied to platform specific shadow trees as they arrived. There was no atomic commit, so an update React later abandoned had already reached the screen, and every measurement was a round trip. That architecture was removed in 0.82 and is described here only so you can recognise advice written for it. See New Architecture Migration. Basic example A concrete update to trace through the phases: How it works Phase 1 — Render Runs on the JS thread (or, for a concurrent update, on the JS thread in interruptible slices). 1. React reconciles: it calls your components, compares the element tree with the previous one, and determines which host elements changed. 2. For each change, Fabric creates a new shadow node in C++. Shadow nodes are immutable, so an update creates new nodes along the path from the root to the change and shares untouched subtrees by p"},{"u":"core-concepts/differences-from-web.html","t":"How RN Differs from the Web","s":"Core Concepts","d":"What a web developer has to unlearn — no DOM, no CSS files, no cascade, nothing scrolls by default, and units that are not pixels.","h":["Why it exists / when to use it — and when NOT to","There is no DOM","What the runtime does give you","There are no CSS files","Layout is flexbox, and the defaults are not the web's","Nothing scrolls by default","Units are density-independent pixels","Text is a component, not a tag","Events are not DOM events","The web-shaped aliases that do exist","What does transfer","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"React Native uses React, JSX and JavaScript, which makes it look far more like web development than it is. The component model transfers. Almost nothing below the component model does. This page is the list of assumptions that will cost you an afternoon each if you carry them over. Every claim here was checked against the installed type definitions and the official , not against memory. Why it exists / when to use it — and when NOT to Read this if you are coming from React on the web. It is the shortest path to understanding why your first screen looked wrong and why the fix was not the CSS you expected. Skip it if you have never done web development. Nothing here is a prerequisite for the rest of the handbook — it is a translation guide, and without the source language it is just a list of facts you already have to learn anyway. There is no DOM is not a . It is a handle on a real on iOS and a real on Android. There is no document, no element, no , and no way to reach a component except through a ref. This is not a stylistic difference — the DOM globals genuinely do not exist. The official that every project extends declares a curated list of ECMAScript libraries and deliberately omits : Adding to would silence the compiler and change nothing at runtime, which is worse than the error. Do not. What the runtime does give you React Native's own global type declarations define a specific, small set of web shaped APIs: Available Notes Logs go to Metro and to React Native DevTools , , , Plus their / counterparts , , , A polyfill over The real transport underneath , , , , here has a method the web's does not , The measurement shape, not a DOM node in development builds, in release Present when running on Hermes Not present, and not polyfillable in any way that means anything: "},{"u":"core-concepts/threading-model.html","t":"JS Thread vs UI Thread","s":"Core Concepts","d":"Which thread runs your JavaScript, which one draws, where layout actually happens, and what changed now that the Bridge is gone.","h":["Why it exists / when to use it — and when NOT to","The three places work happens","Basic example","How it works","An update, end to end","Not everything is asynchronous any more","Priorities, not a flush schedule","The second JavaScript runtime","Common patterns","Telling the two threads apart","Getting expensive work off the JS thread","Synchronous native calls are a loaded gun","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"A React Native app runs your code on more than one thread, and knowing which one is busy is the difference between fixing jank and guessing at it. There are three places work happens: the JS thread , the main (UI) thread , and C++ renderer work that runs on neither of them. The mental model most articles give you is from before React Native 0.82, when everything crossed a serialised, asynchronous Bridge. That description is now wrong in ways that will send you optimising the wrong thing. \"Everything is async over the bridge\" is no longer true The Bridge was removed in React Native 0.82. There is no serialised message queue between JavaScript and native, no batching schedule to minimise, and no per call JSON encoding cost. Synchronous calls in both directions are possible and routinely used. If advice you are reading is built on \"minimise bridge traffic\", it predates 0.82 and its conclusions do not transfer. See The New Architecture. Why it exists / when to use it — and when NOT to You need this model when something is janky and you have to decide what to fix: a slow render is a JS thread problem, a slow scroll with an idle JS thread is a main thread problem, and they have disjoint solutions. You also need it before touching animation, because the entire point of Reanimated is moving work off one of these threads. You do not need it to build screens. Nothing on this page changes how you write a component. The three places work happens Where What runs there What it means when it is busy JS thread Your JavaScript: components, hooks, effects, event handlers, promises, timers, data parsing. React's reconciler Handlers feel laggy, state updates arrive late, drifts. Native scrolling and native driven animation keep running, which makes this easy to misdiagnose Main / UI thread"},{"u":"core-concepts/platform-differences.html","t":"Platform Differences","s":"Core Concepts","d":"Platform.OS, Platform.select, Platform.Version and platform file extensions — plus the behaviours that genuinely differ between iOS and Android.","h":["Why it exists / when to use it — and when NOT to","The Platform API","Basic example","Platform.select","Platform-specific files","The exact resolution order","The differences that actually matter","Keyboard","Hardware back button","Shadows","Clipping and overflow","Safe areas","Status bar","Text rendering","Touch feedback","Permissions","Native configuration lives in different files","Common patterns","Push the branch as low as possible","Make the platform assumption explicit","Test both, early","Performance considerations","Common mistakes","Related topics"],"b":"React Native shares your JavaScript across two platforms. It does not make those platforms behave the same, and the places where they differ are not edge cases — the keyboard, the back button, shadows, clipping and safe areas all differ in ways that are visible on the first screen you build. This page covers the API for branching on platform, the file extension mechanism for splitting whole modules, and the specific behavioural differences worth knowing before you meet them in a bug report. Why it exists / when to use it — and when NOT to Branch on platform when the platforms genuinely differ: a native affordance, a system behaviour, a design language convention. Do not branch to paper over a layout you have not understood — a that adds 8 points of padding on Android to fix a misaligned row is almost always hiding a flexbox mistake that will come back on the next device. The order of preference is: write one implementation that works on both, then a conditional style, then a , then separate files. Separate files are the most powerful and the most expensive, because they double what you have to keep in sync. The API Verified against in the installed . Member Type Notes A literal per platform build, so it narrows on iOS, on Android On Android it is the API level (34, 35…), not a version name Keys are platform names plus Object, platform specific shape on every platform; , , , , on Android; , , , on iOS Present on both True under the test environment iOS only iOS and Android members; true only on Apple Vision Pro iOS only is a union type, and that changes how you write the check In 0.87 is typed as a union of per platform shapes ( ). Accessing a member that only one of them has is a compile error until you narrow: Checking first narrows the union and unlocks , and the iOS "},{"u":"components/view.html","t":"View","s":"Components","d":"The base layout primitive — what it maps to natively, how it lays out children, and when an extra View costs you.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Flex defaults are not the web's","Layout-only view removal","Measuring a view","Touch targets: hitSlop and pointerEvents","Platform differences","Common patterns","An absolutely positioned overlay","Spacing with gap instead of margins","Performance considerations","Common mistakes","Related topics"],"b":"is the container everything else sits in. It maps to a on iOS and an on Android, it lays its children out with flexbox, and it carries the touch, layout and accessibility props that most other components inherit. If you come from the web, treat as with three differences: it has no default scrolling, it has no text rendering (text must live inside Text), and its flex defaults are not the web's. Why it exists / when to use it — and when NOT to Use a when you need a box: to group children, to apply a background, border or padding, to position something absolutely, or to give flexbox something to lay out. Do not reach for when: The content scrolls. Use ScrollView for a small, bounded amount of content, or FlatList once the list can grow. You need text. A bare string cannot be a child of . It must be wrapped in . You only need it to hold a style you could put on the child. Every extra is an extra native view. React Native removes some layout only views automatically ( ), but it cannot remove one that paints a background or handles a touch. Basic example How it works Flex defaults are not the web's A is with , and . On the web the defaults are , and . That single difference accounts for most of the \"why is my layout wrong\" confusion when moving from web to native. There is no and no float. There is , and . Layout only view removal If a does nothing but position its children — no background, no border, no touch handler, no — React Native may drop it from the native hierarchy entirely and hoist its layout onto the parent. That is the prop, and it defaults to . You will notice it when you try to find the view in a native inspector and it is not there, or when a native library needs a real view to attach to. Set to force it to exist. does the same for every direct child at once. "},{"u":"components/text.html","t":"Text","s":"Components","d":"The only component that renders strings — nesting, truncation, font scaling, and the platform differences that actually bite.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Nesting and style inheritance","Truncation: numberOfLines and ellipsizeMode","Font scaling and accessibility","Measuring rendered lines","Platform differences","Common patterns","Selectable text","A link inside a sentence","Performance considerations","Common mistakes","Related topics"],"b":"is the only component that can render a string. It lays out with text layout rather than flexbox, so nested elements flow and wrap at the end of a line instead of being positioned as rectangles. It maps to family rendering on iOS and to a on Android. Every visible word in your app goes through this component, which makes its accessibility and font scaling behaviour worth understanding once rather than rediscovering per screen. Why it exists / when to use it — and when NOT to Use for any string. There is no alternative in core. Do not use it when: You want a tappable region larger than the glyphs. has , but the hit area is the text box. For a button, wrap it in Pressable, which gives you , pressed state and correct accessibility roles. You need an editable field. That is TextInput. You need rich text with images inline. React Native has no inline image support in . Lay out separate elements, or render with . Basic example How it works Nesting and style inheritance is the one place in React Native where style inherits, and it inherits only from a parent , not from a parent . A nested picks up the outer font family, size, colour and weight, then overrides what it sets itself. That is how you get a bold word inside a sentence without breaking the line box. There is no global \"app font\". If you want one, you build it yourself with a wrapper component that applies your base text style — not by setting on a , which does nothing. Truncation: and clamps the rendered line count and truncates the rest; means no limit. decides where the ellipsis goes: , , or (the default). truncates. It does not shrink the text. If you need the text to shrink to fit a fixed box, that is , and the two interact — see the platform tabs below. Font scaling and accessibility defaults to , so your text g"},{"u":"components/image.html","t":"Image","s":"Components","d":"Loading local and remote images, resize modes, sizing rules, caching, and the deprecated ImageBackground replacement.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Static assets versus remote sources","Resize modes","Load lifecycle","The static methods","Platform differences","Common patterns","Content on top of an image","Tinting a monochrome icon","Reserving space with aspectRatio","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"renders a bitmap from a bundled asset, a remote URL, a data URI or the device's file system. It is a leaf component: in the 0.87 types its prop is typed , so you cannot nest anything inside it. Images are usually the largest single contributor to an app's memory use and to its scroll jank, so the sizing and caching rules below matter more than the prop list. Why it exists / when to use it — and when NOT to Use for photographs, avatars, illustrations and raster icons. Do not use it when: The asset is an icon or a shape. Use (15.15.5) or an icon font. A vector scales to any density without shipping three PNGs, and it tints cleanly. You need content on top of an image. takes no children. Use a with an absolutely positioned behind the content — see Common patterns. You are rendering hundreds of remote images in a list. Core works, but a dedicated caching library gives you disk cache control and decode priority. Measure first; the core component with correct sizing handles more than people assume. Basic example How it works Static assets versus remote sources accepts two shapes, and they behave differently. A static asset is passed as . Metro resolves it at bundle time, so the image has an intrinsic width and height and you may omit the style dimensions. The / suffix convention selects the right density automatically. A remote or file source is passed as . It has no intrinsic size until the response arrives, so you must give it a width and a height in style . Otherwise the image renders at zero size, or the layout jumps when it loads. also accepts an array of objects. The native side picks the best match for the target size and the device's pixel density — the native equivalent of . There is also a web style string prop if you prefer that spelling. Resize modes decides how t"},{"u":"components/scrollview.html","t":"ScrollView","s":"Components","d":"Scrollable content that renders all of its children at once — when that is correct, and the keyboard and inset props that need care in 0.87.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","style versus contentContainerStyle","Scrolling programmatically","Scroll events and scrollEventThrottle","Keyboard behaviour","Sticky headers","Platform differences","Common patterns","A horizontal card carousel that snaps","Keeping position while content loads above","Pull to refresh","Performance considerations","Common mistakes","Related topics"],"b":"is a scrolling container that mounts every one of its children immediately . It maps to a on iOS and a / on Android, and it is the base that FlatList and SectionList are built on. The mount everything behaviour is the whole story. It makes the simplest thing that works for a settings screen, and the wrong choice for anything that grows. Why it exists / when to use it — and when NOT to Use when the content is bounded and small : a form, a settings page, a detail screen, an onboarding step, a horizontal row of five cards. Use a virtualized list instead when: The item count is driven by data. Anything backed by an API response can grow. A hundred rows is already noticeable on a low end Android device; a thousand is a frozen screen. Items are expensive. Even twenty rows, each with an image and a nested list, cost more to mount at once than they do to mount lazily. You need per item lifecycle. Virtualized lists give you ; gives you a scroll offset and nothing else. The cost is paid on mount, not on scroll A with 500 children feels fine once it is on screen. The damage is a blocked JS thread during the transition into the screen, which reads to users as \"the app froze when I tapped\". Profiling a smooth looking scroll will not find it. Basic example How it works versus This is the single most common source of confusion, and it is worth being precise. applies to the scroll view itself — the window you look through. belongs here. So does a background colour that should cover the whole viewport. applies to the inner view that holds the children. Padding, , and for the content belong here. Putting on clips the content instead of insetting it. Putting on makes the content exactly one screen tall and it stops scrolling. Centring content that is sometimes shorter than the screen cent"},{"u":"components/textinput.html","t":"TextInput","s":"Components","d":"Controlled and uncontrolled text entry, keyboard configuration, autofill on both platforms, and why controlled inputs drop characters.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Controlled versus uncontrolled","Choosing the keyboard","The return key","Focus management","Autofill, on both platforms","Platform differences","Common patterns","An auto-growing multiline composer","Formatting as the user types","Dismissing the keyboard","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"is the editable text field. It wraps / on iOS and on Android, and it is the component where the two platforms diverge most: keyboard types, autofill hints, return key behaviour and multiline layout all differ. It is also the component most likely to feel laggy if you wire it up naively, because every keystroke can become a React render. Why it exists / when to use it — and when NOT to Use for any text the user types: search fields, forms, chat composers, one time codes. Do not use it when: The value is a choice, not free text. A picker, a Switch or a segmented control communicates the constraint and avoids validation. You need rich text editing. Core has no bold/italic spans. That needs a native editor or a WebView. You want a read only label. still renders a focusable input on some paths. Use Text with instead. Basic example How it works Controlled versus uncontrolled A controlled input passes and updates it from . React owns the truth, and the native field is forced to match on every render. An uncontrolled input passes and reads the current text from into a ref, or not at all. The native field owns the truth. Controlled is the right default because validation, formatting and \"clear the form\" all become trivial. It has one real failure mode: if the state update is slow — a heavy parent re render, an expensive validation on every keystroke, a state library with a deep subscription — the native field can get ahead of React, and React then forces the value back, which looks like dropped or reordered characters. The fix is not to abandon controlled inputs. It is to keep the update path cheap: The prop is also how you block edits without flicker The 0.87 type comment says it plainly: setting the same to prevent edits causes flickering. Use or , or cap with . Choosing the k"},{"u":"components/pressable-and-touchables.html","t":"Pressable and Touchables","s":"Components","d":"The modern touch primitive, the four legacy Touchable wrappers, and how to pick between them without shipping a dead tap target.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","style and children can both be functions","The press lifecycle, and why onPress sometimes never fires","hitSlop versus pressRetentionOffset","unstable_pressDelay","Refs","Platform differences","Common patterns","One button that looks native on both platforms","A list row that does not fight the scroll","Choosing among the legacy wrappers","Dismissing the keyboard by tapping the background","Performance considerations","Common mistakes","Related topics"],"b":"is the component you wrap around anything that should respond to a tap. It renders a View, attaches the press recogniser to it, and hands you the press state so you can style the pressed appearance yourself. The four components predate it. They still ship in 0.87 and they still work, but each one bakes in one fixed visual effect, and was added precisely because that set turned out to be too narrow. Treat them as legacy leaning: fine in existing code, not the thing you reach for in new code. Why it exists / when to use it — and when NOT to Use for buttons, list rows, chips, icon targets — anything where a tap does something. It gives you , , , and , plus a flag you can feed into styles, so the feedback is yours to define rather than the component's. Do not use it when: The gesture is more than a tap. Pans, pinches, swipe to delete and anything that must run on the UI thread during a drag belong to Gesture Handler, not to the JS responder system. A platform control already exists. A Switch is a switch. Do not build one out of a and lose the platform's accessibility behaviour. You want a plain, unstyled system button. exists for that, at the cost of almost no styling control. The undocumented root export is gone 0.87 removed the bare export from . It was never documented and it was never meant to be imported. , , and are all still exported. Basic example How it works and can both be functions Both accept a callback that receives . The style callback is the normal way to render feedback. The children callback is for when the content changes — a label that swaps, an icon that fills in. The flag flips on press in and back on press out, so it is genuinely \"is a finger down right now\", not \"was this tapped\". The press lifecycle, and why sometimes never fires A press moves throu"},{"u":"components/flatlist.html","t":"FlatList","s":"Components","d":"The virtualized list for flat, data-driven content — keys, item layout, pagination, viewability, and the props that actually change performance.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The render window","Keys","getItemLayout — the single biggest win, when it applies","extraData","Refs and imperative scrolling","Platform differences","Common patterns","Infinite scroll","Pull to refresh","Tracking what is on screen","A grid","Performance considerations","Common mistakes","Related topics"],"b":"renders a long array of items while keeping only the ones near the viewport mounted. It is a thin wrapper around , which is itself built on ScrollView, so every prop is available on it too. The whole point is that mounting cost stops scaling with . A 10,000 row mounts 10,000 native subtrees at once and blocks for seconds. A 10,000 row mounts roughly a screenful. Why it exists / when to use it — and when NOT to Use when the content is an array and the array can grow. That is the whole test. Even a \"short\" list of user generated content is a candidate, because the length is not yours to decide. Do not use it when: The content is a fixed, small set of heterogeneous blocks. A settings screen with eight hand written rows is a . would add virtualization machinery and take away the ability to just write the JSX. The rows are grouped under headers. That is SectionList. The list nests inside another scroll view along the same axis. Two virtualized lists on the same axis break each other's windowing. Use the outer list's instead. Basic example How it works The render window keeps a window of mounted cells around the viewport and unmounts what falls outside it. Four props control the window, and their real defaults in 0.87 are: Prop Default What it controls Cells rendered in the very first pass. These are never unmounted. Cells added per incremental batch while scrolling. (ms) Gap between those batches. Window height in units of viewport height — 21 means the viewport plus ten screens above and ten below. Raising trades memory for fewer blank frames during fast scrolls. Lowering it does the opposite. Raising fills blanks faster but blocks the JS thread for longer per batch, which shows up as unresponsive taps. Virtualization and FlashList goes through the mechanism in detail, incl"},{"u":"components/sectionlist.html","t":"SectionList","s":"Components","d":"The virtualized list for grouped data — section shape, sticky headers, per-section renderers, and the index maths that trips people up.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The section shape","Sticky headers","Keys, at two levels","Refs and scrollToLocation","getItemLayout is harder here than on FlatList","Platform differences","Common patterns","Grouping a flat array","Different rows per section","Separators between sections","Performance considerations","Common mistakes","Related topics"],"b":"is FlatList with a second level: instead of one flat array you give it an array of sections, each holding its own array. It renders a header before each section's items and, on iOS by default, sticks that header to the top while its section is on screen. Underneath it is , which flattens everything — headers, items, separators and footers — into a single virtualized stream. Knowing that explains most of its odd corners. Why it exists / when to use it — and when NOT to Use it when the data genuinely has groups that the user needs to see: contacts by first letter, transactions by day, settings by category, search results by type. Do not use it when: There is only one group. Use and put the header in . The \"sections\" are really just two or three static blocks. A with three free chunks is simpler and has no virtualization to reason about. The grouping is purely visual and changes with a filter. Regrouping on every keystroke rebuilds the section array and throws away the render window. Consider a flat list with inline divider rows. Basic example The explicit type arguments are worth writing. Without them both and infer as , and every typo inside compiles. How it works The section shape A section is any object with a array. Everything else on it is yours — is a convention, not a requirement. The type that describes it in the 0.87 exports is , and is the part insists on. Three optional fields on a section override the list level behaviour for that section only: Field Effect Identity for the section itself. Without it the array index is used, so reordering sections reshuffles state. Renders this section's items instead of the list level . Separator used inside this section only. Key function for this section's items only. That per section is the reason handles heterogeneous dat"},{"u":"components/virtualization-and-flashlist.html","t":"Virtualization and FlashList","s":"Components","d":"How FlatList's render window actually works, which props move which number, and an honest account of where @shopify/flash-list wins and where it does not.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The lifecycle of a cell","The four window props, and what each one actually trades","getItemLayout: the one that is not a trade-off","keyExtractor is a performance prop","removeClippedSubviews","Blank cells","FlashList","What recycling changes about your row code","The ref API is different","Where FlashList wins, and where it does not","Platform differences","Common patterns","Deciding what to change, in order","Migrating a FlatList to FlashList","Performance considerations","Common mistakes","Related topics"],"b":"Virtualization is the reason a list of 50,000 rows can open instantly. Instead of mounting a native view subtree per row, the list mounts a window of rows around the viewport and unmounts the rest as they scroll away. Everything that makes lists fast or slow in React Native follows from that one idea. This page explains the mechanism, then covers 2.3.2, which replaces unmount and remount with view recycling. Why it exists / when to use it — and when NOT to You are already using virtualization if you use FlatList or SectionList. This page is about tuning it and about knowing when to swap the implementation. Reach for when you have measured a problem that is specifically about cell mounting: blank cells while scrolling fast, a visible hitch as each batch lands, or memory that climbs with scroll distance. Do not swap the list out when: You have not profiled. Most \"slow list\" reports are a slow row — an unoptimised image, a shadow on every cell, a re render storm from a missing . Changing the list component does not fix any of those, and you will carry a dependency for nothing. The list is short. Under a few hundred simple rows, with sensible props is indistinguishable. You need a core only dependency footprint. is a native library. It has to build, it has to be kept compatible across React Native upgrades, and it is one more thing between you and an upgrade. Basic example A with the window props stated explicitly rather than left at their defaults, so the trade offs are visible in the code. How it works The lifecycle of a cell 1. On mount, the list renders cells synchronously. These are special: they are never unmounted, so scrolling back to the top is always instant. 2. As you scroll, the list schedules low priority batches. Each batch renders at most more cells, and batc"},{"u":"components/modal.html","t":"Modal","s":"Components","d":"The core overlay component — how it presents on each platform, why onRequestClose is not optional, and what 0.87 removed from its prop list.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","visible is the only control","onRequestClose is not optional in practice","transparent and backdropColor","The lifecycle callbacks differ per platform","Refs","Platform differences","Common patterns","A bottom sheet","A form inside a modal","Chaining two modals on iOS","Performance considerations","Common mistakes","Related topics"],"b":"presents its children in a native window above the rest of the app. It is not a styled with a high ; on both platforms it is a separate presentation layer, which is why it escapes parent clipping, parent transforms and the navigation header. That separateness is also its main limitation. A is outside your navigation tree, so the back stack, deep links and the hardware back button do not know about it unless you wire them up. Why it exists / when to use it — and when NOT to Use for something short lived and self contained that must sit above everything: a confirmation, a picker, a bottom sheet, a full screen media viewer. Do not use it when: The overlay is a screen. If the user can navigate within it, or should be able to reach it by a link, or expects the back gesture to return from it, it belongs in the navigator as a modal screen. See Modals in the navigation section. You need a simple alert. gives you the real system dialog, with the platform's own button layout and accessibility behaviour, for one line of code. You want a tooltip or popover anchored to a control. A is a full window layer. Measure the anchor and render an absolutely positioned overlay instead. The prop was removed in 0.87 Older code sets a boolean prop named on this component. It no longer exists, and it is not a type error that shows up as a crash — the animation just never happens. The replacement is , which takes (the default), or . Basic example How it works is the only control There is no or . is declarative: render it with and it is not presented; flip the prop and it is. Keeping it mounted with costs almost nothing, and it is simpler than conditionally rendering the whole element. is not optional in practice The type marks it optional, but the prop's own documentation says it is required on An"},{"u":"components/switch.html","t":"Switch","s":"Components","d":"The platform boolean control — controlled-component rules, the colour props that only work on one platform, and how to label it for screen readers.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Controlled, strictly","onValueChange versus onChange","Sizing","Refs","Platform differences","Common patterns","Making the whole row tappable","A list of settings","Performance considerations","Common mistakes","Related topics"],"b":"renders the system on/off control: a on iOS, a on Android. It is a controlled component — it shows whatever you give it and does not change on its own, so a switch whose never updates will not move no matter how many times it is tapped. It is one of the few core components where using the real thing matters more than styling it. Users recognise it, screen readers announce it correctly, and the platforms animate it differently on purpose. Why it exists / when to use it — and when NOT to Use a for a setting that takes effect immediately: notifications on or off, dark mode, location sharing. Do not use it when: The change needs confirming or saving. A switch implies \"this is now on\". If the user has to press Save afterwards, a checkbox style row is the honest control — and there is no core checkbox, so that is a with your own mark. It is one of several mutually exclusive options. That is a radio group or a segmented control, not three switches. The design demands a custom look. You can tint a but you cannot restyle it. If the design needs a bespoke track and thumb, build it from a and an animated — and accept that you now own , and the animation. Basic example How it works Controlled, strictly drives the visual state. The switch does not keep its own. If the handler is asynchronous and fails, do not leave the state optimistically flipped — either revert it or hold the switch in its old position until the write succeeds. versus Both fire on the same interaction. receives the new boolean, which is what you want almost always. receives the full event, typed , whose is the same boolean and whose is the node id. Use it only when you need the target — for instance one shared handler across a row of switches. Both callbacks may return a in the 0.87 types, so an handler is accepte"},{"u":"components/activityindicator.html","t":"ActivityIndicator","s":"Components","d":"The platform spinner — its four props, why a numeric size only works on one platform, and when a spinner is the wrong loading UI.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The four props","animating versus conditional rendering","Size","Accessibility","Platform differences","Common patterns","A button that shows its own progress","A list footer while the next page loads","Not flashing for fast responses","An overlay that blocks interaction","Performance considerations","Common mistakes","Related topics"],"b":"is the system spinner: a on iOS, a circular on Android. It has four props and no state of its own. It spins whenever it is mounted and is true. It is the smallest component in the core set, and the interesting questions about it are not about its API — they are about when a spinner is the right thing to show at all. Why it exists / when to use it — and when NOT to Use it for an indeterminate wait whose duration you cannot estimate: a network request in flight, a form submitting, a page of a list loading. Do not use it when: You know the progress. A determinate bar tells the user something; a spinner tells them nothing. There is no cross platform progress bar in core — is Android only — so a determinate bar is a styled with a width you control. The content has a known shape. A skeleton placeholder that matches the layout reads as faster than a spinner, because the screen does not jump when content arrives. The wait is under about 200ms. A spinner that appears and vanishes is a flash of noise. Delay it, or do not show it. The whole screen is blocked by it every time. A full screen spinner on every navigation makes an app feel slower than it is. Prefer keeping the previous content and showing the spinner in place. Basic example How it works The four props Prop Type Notes Whether it spins. Defaults to , so an with no props is already spinning. Foreground colour. Defaults to on iOS and to the system accent colour on Android. is 20pt tall, is 36pt. A number is Android only . iOS only. Whether the view disappears when is false. Everything else is inherited from , so , and the layout props all work. versus conditional rendering Two ways to stop showing a spinner, and they are not equivalent. keeps the view mounted. On iOS defaults to , so the view becomes invisible but still oc"},{"u":"components/refreshcontrol.html","t":"RefreshControl","s":"Components","d":"Pull-to-refresh done with the platform's own control — the controlled `refreshing` prop, the split between iOS and Android styling, and why it belongs on the scroll view rather than inside it.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Where it attaches","The short form","refreshing is controlled","progressViewOffset","Refs","Platform differences","Common patterns","Keeping the spinner honest with a minimum duration","Distinguishing a refresh from the initial load","Refreshing a short screen","Performance considerations","Common mistakes","Related topics"],"b":"is the pull to refresh indicator. You do not render it as a child; you pass it to a scrollable component through its prop, and that component attaches it natively. It is a controlled component with one required prop: . The spinner shows exactly when is , which means you have to set it to yourself inside and back to when the work finishes. Forgetting the second half leaves a spinner turning forever. Why it exists / when to use it — and when NOT to Use it wherever a scrollable list of remote data should be refreshable by pulling down. It is the gesture both platforms' users already know, and using the real control gets you the platform's own animation and haptics. Do not use it when: There is nothing to refresh. A static settings screen with a pull to refresh spinner is confusing. The list refreshes itself. If a subscription or a query cache already keeps the data live, pull to refresh at best duplicates it and at worst fights it. The content is short and always visible. Pull to refresh needs a scroll gesture, and a screen with nothing to scroll gives the user nowhere to start one — unless the scroll view bounces, which is iOS only by default. You want a custom looking refresh animation. is the platform control. A bespoke animation means Gesture Handler and Reanimated, not this component. Basic example How it works Where it attaches is passed as an element to the prop of ScrollView, FlatList or SectionList. It is not a child and it does not render in the layout — the scroll view hands it to the native or . Only vertical scroll views support it. A list has no pull down gesture to attach to. The short form and also accept and directly, and build the for you. That is the shorter path when you do not need to style the control: Passing an explicit element overrides that: the l"},{"u":"components/keyboardavoidingview.html","t":"KeyboardAvoidingView","s":"Components","d":"Keeping inputs above the software keyboard — what each `behavior` actually does, why the right value differs by platform, and the Android manifest setting that changes everything.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The three behaviors","keyboardVerticalOffset","enabled","Refs","Platform differences","Common patterns","Correcting for a navigation header","A chat composer pinned above the keyboard","Inside a modal","Performance considerations","Common mistakes","Related topics"],"b":"is a that watches the keyboard and resizes, pads or shifts itself so its contents stay visible. It has four props — , , and — and getting wrong is the single most common cause of \"the keyboard covers my button\" bugs. It is also the component most likely to be unnecessary. On iOS a with often does the job on its own, and on Android the activity's may already be resizing the window before React Native sees anything. Why it exists / when to use it — and when NOT to Use it when a form has content that must stay on screen while the keyboard is up: the field being typed into, the submit button, an autocomplete list. Do not use it when: The screen already scrolls and the platform already insets it. Check what happens with no at all before adding one. Two mechanisms fighting each other produces double padding and a jumping layout. The content is a single centred field with plenty of room. Nothing is covered; nothing needs avoiding. The input is inside a Modal and you put the on the screen behind it. A modal is a separate window. The avoiding view has to be inside the modal. You need per frame tracking of the keyboard animation. This component reacts to the keyboard show/hide events. A toolbar that follows the keyboard frame exactly is a Reanimated job, or on iOS. Basic example How it works The component subscribes to the keyboard events, measures the keyboard's frame, and applies one of three strategies. The strategy is , and it is not defaulted — with no the component renders as a plain and does nothing. The three behaviors Value What it changes Works best when Adds bottom padding equal to the keyboard height The child fills the available space and can shrink — a , or a flex column Reduces the view's own height by the keyboard height The child is laid out with flex and should "},{"u":"components/safe-areas.html","t":"Safe Areas","s":"Components","d":"Keeping content clear of notches, status bars, home indicators and camera cutouts — what core SafeAreaView can and cannot do, and why react-native-safe-area-context is the real answer.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Core SafeAreaView","useSafeAreaInsets","The library's SafeAreaView","Reacting to inset changes without re-rendering","The status bar in 0.87","Platform differences","Common patterns","A custom header that respects the notch","A bottom action bar above the home indicator","Landscape, where left and right matter","Testing it","Performance considerations","Common mistakes","Related topics"],"b":"A phone screen is not a rectangle you own. Part of it is covered by the status bar, part by a notch or a camera cutout, part by the home indicator or the gesture bar, and on Android part may be under a translucent navigation bar. The safe area is what is left. React Native ships a in core, and it is not the component most apps should use. It is effectively iOS only, it has no way to say which edges you care about, and the 0.87 type definition marks it deprecated in favour of . This page covers both honestly, because you will meet the core one in existing code. Why it exists / when to use it — and when NOT to You need safe area handling on any screen that draws to the edges: a full screen list, a custom header, a bottom bar, a modal that covers the whole window. You do not need it when: A navigator is already handling it. React Navigation's headers and tab bars apply the insets themselves. Adding another safe area wrapper around a screen that already has a header produces a double gap. The content is inset from the edges anyway. A card with 24pt of margin on a centred screen is not going to collide with the home indicator. You are inside a scroll view on iOS and only need the content inset. on ScrollView already handles that case. Applying insets twice is a more common bug than not applying them at all, and it is harder to notice because it just looks like slightly wrong spacing. Basic example 5.9.1 is the maintained solution and the one React Navigation itself depends on. Install it and rebuild: Wrap the app once in a provider, then use the hook or the component anywhere below it. How it works Core The core component applies the safe area insets as padding, on all four edges, with no configuration. Its prop list is exactly — there is no prop and no prop. Two further lim"},{"u":"styling/stylesheet.html","t":"StyleSheet","s":"Styling and Layout","d":"How StyleSheet.create works in 0.87, what it actually does, and how to type styles under the Strict API.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The style prop takes more than one object","What StyleSheet exports in 0.87","Typing styles","There is no cascade and almost no inheritance","Platform differences","Common patterns","Derive a dynamic style from a static base","Reading a merged style back","Performance considerations","Common mistakes","Related topics"],"b":"is the styling entry point in React Native. You declare style objects in JavaScript, pass them to a component's prop, and the renderer applies them to the underlying native view. There is no CSS file, no selector engine and no cascade. Under the Strict TypeScript API in 0.87 you import it from the package root. The old deep import is a type error. Why it exists / when to use it — and when NOT to gives you three things that a bare object literal does not: 1. A stable reference. Styles declared once at module scope keep the same identity across renders, so and comparisons do not see a changed prop. 2. Type checking against the real style surface. A typo such as is a compile error inside , where a plain would swallow it. 3. A single place to look. Styles collected at the bottom of a file read as a unit. Reach for an inline object only when a value genuinely depends on props or state, and even then prefer combining a static base style with a small dynamic override. is an identity function The 0.87 type definitions describe as \"An identity function for creating style sheets\". It returns the object you passed. The pre Fabric behaviour where registered styles and handed back opaque numeric IDs to send across the Bridge is gone; the Bridge itself went away in 0.82. Advice that tells you is faster because it \"avoids serialising objects\" is describing an architecture that no longer exists. The reasons above still hold, but they are about references and types, not serialisation. Basic example How it works The prop takes more than one object Every core component accepts a single style object, an array, , or an empty string, and arrays may nest. React Native flattens the structure left to right, so later entries win. That is the entire \"cascade\" — there are no selectors and no speci"},{"u":"styling/flexbox.html","t":"Flexbox in React Native","s":"Styling and Layout","d":"How Yoga's flexbox differs from the web's, the four defaults that catch everyone, and what React Native's layout engine does not implement.","h":["Why it exists / when to use it — and when NOT to","The four defaults that differ from the web","Basic example","How it works","flex is one number, not the CSS shorthand","The main axis follows flexDirection","gap replaces margin arithmetic","Percentages and aspectRatio","Positioning","What React Native does not have","Platform differences","Common patterns","Centring","Push one item to the far end of a row","A wrapping tag list","A full-bleed overlay","Performance considerations","Common mistakes","Related topics"],"b":"React Native lays out every view with Yoga , a cross platform implementation of flexbox written in C++. Yoga runs on the native side, not in a browser engine, so what you get is flexbox shaped but not flexbox identical: a subset of the properties, four different defaults, and none of the surrounding CSS machinery. If you come from the web, the defaults are the thing that will cost you an afternoon. Learn them first. Why it exists / when to use it — and when NOT to Flexbox is the only general purpose layout system React Native has. There is no grid, no float, no . Every screen you build is nested flex containers, so the question is never whether to use flexbox but how to structure it. The one thing to avoid is reaching for to escape a layout you have not understood. Absolute children are removed from the flow, do not contribute to their parent's size, and stop responding to text scaling. They are correct for overlays, badges and floating buttons, and wrong for the main structure of a screen. The four defaults that differ from the web This is the whole reason a web developer's first React Native layout looks wrong. Verified against the 0.87 type definitions and the layout props reference on reactnative.dev. Property React Native CSS on the web Why it bites Children stack vertically unless you say otherwise. Wrapped lines bunch at the start of the cross axis instead of filling it. Children overflow their container instead of shrinking. Long text does not compress; it gets clipped. includes padding and border, so a padded box is not wider than you asked for. Everything else matches the web's defaults: is , is , is , is , is , and is . is the one that produces \"my text is cut off\" On the web, a flex child shrinks below its content size by default. In React Native it does not"},{"u":"styling/units-and-density.html","t":"Units and Density","s":"Styling and Layout","d":"What the numbers in a style object mean physically, how PixelRatio and hairlineWidth work, and how @2x/@3x assets are picked.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The unit on each platform","PixelRatio","Pixel grid snapping","Font scale is separate from pixel density","@2x and @3x asset resolution","Platform differences","Common patterns","Do not build a \"scale to design width\" helper","A crisp one-device-pixel border","Seamless adjacent views","Performance considerations","Common mistakes","Related topics"],"b":"Every size in a React Native style is a bare number. There is no , no , no and no suffix, and adding one is a type error for most props. The number is a density independent pixel : a logical unit that the platform scales to real device pixels. The consequence is that covers roughly the same physical distance on a low density tablet and a high density phone, but occupies a different number of hardware pixels on each. Why it exists / when to use it — and when NOT to Density independence is what lets one layout ship to a 1x tablet and a 3x phone without a per device stylesheet. You write one number and the platform maps it. You need to think about density explicitly in three situations, and in no others: 1. Drawing something that must land on the pixel grid — a one device pixel divider. Use . 2. Requesting a raster image or thumbnail at the right size from a server or a native API. Use . 3. Respecting the user's text size setting in a layout that has to reserve space for text. Use . Outside those cases, converting to device pixels yourself is a bug in the making. Write the logical number and let the platform scale it. Basic example How it works The unit on each platform Platform Native name examples iOS point (pt) 2 on most iPhones, 3 on Plus/Pro Max class devices Android density independent pixel (dp) 1 at mdpi, 1.5 at hdpi, 2 at xhdpi, 3 at xxhdpi A density independent pixel and a point are the same idea with different names, which is why one number works for both. The conversion is . All four methods below are read from the installed type definitions. Method Returns Use it for Device pixel density as a number. Deciding which asset variant to request. The user's text size multiplier. If no font scale is set, it returns the device pixel ratio. Reserving vertical space for"},{"u":"styling/dimensions.html","t":"Dimensions and useWindowDimensions","s":"Styling and Layout","d":"Reading the window and screen size in 0.87, why the hook is the default choice, and the type changes the Strict API introduced.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What comes back","window versus screen","Subscribing without the hook","Why the hook is the default","Platform differences","Common patterns","Orientation from the dimensions you already have","Measure the view, not the window","A carousel page width","Performance considerations","Common mistakes","Related topics"],"b":"React Native has no media queries, so any layout decision that depends on how much room you have is a runtime read. Two APIs expose it: the hook and the module. In a React component, use the hook. exists for the code that cannot use a hook — module level constants, utility functions and non React code — and it does not re render anything on its own. Why it exists / when to use it — and when NOT to Use one of these when you need a number in JavaScript: a breakpoint decision, a column count, a carousel page width, an animation distance. Do not use them to size a view that flex could size. , and are handled inside Yoga on the native side and stay correct through rotation, split screen and foldable transitions without a single re render. A layout built from measured window widths re renders the whole subtree every time the window changes. Also do not use them for insets. The window size tells you nothing about the notch, the home indicator or the status bar. That is — see Safe Areas. Basic example How it works What comes back Both APIs return the same shape. Read from the installed 0.87 type definitions: Field Meaning Logical width in density independent pixels. Logical height. Device pixel density, the same value as . The user's text size multiplier. Android only. Present on , absent from . Because Android adds a field, the return type is the union . Destructuring the four common fields works without narrowing; reading does not. is deprecated in 0.87 The type definitions mark as deprecated with the note \"Use DisplayMetrics\". It is still exported, so old code compiles, but new code should import (or ) instead. versus takes either. They differ on Android and are usually identical on iOS. is the area your app can draw in. On Android the type definitions note that it excludes "},{"u":"styling/responsive-layouts.html","t":"Responsive and Tablet Layouts","s":"Styling and Layout","d":"Breakpoints without media queries, honest tablet detection in 0.87, and layouts that survive rotation, split-screen and foldables.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Width is the breakpoint, not the device","Tablet detection, honestly","Orientation","Layout that adapts without a breakpoint","Platform differences","Common patterns","Put the decision in one place","Responsive columns in a list","Do not scale everything by a design width","Accessibility text size is part of responsiveness","Performance considerations","Common mistakes","Related topics"],"b":"A React Native app has to fit a 320 point phone in portrait, a 1366 point tablet in landscape, an app occupying a third of an iPad screen, and a foldable that changes size mid session. There are no media queries and no CSS container queries, so responsiveness is ordinary React: read the size, derive a decision, render accordingly. The goal is not to make the phone layout bigger. It is to use the extra space for extra content. Why it exists / when to use it — and when NOT to Build a responsive layout when the information architecture should change with the available space: a list that becomes a list plus detail, one column that becomes three, a bottom sheet that becomes a sidebar. Do not build one when flex already handles it. A row of cards, a image with an , and a with all adapt on their own, inside Yoga, with no re render. Every breakpoint you add is a subtree that re renders on rotation. Basic example How it works Width is the breakpoint, not the device Branch on the current window width , never on a device identity. A phone in landscape, a tablet in portrait and an iPad running your app in Split View can all report the same width, and in every one of those cases you want the same layout. Device checks get this wrong; width checks do not. Tablet detection, honestly There is no in core React Native. What exists in the 0.87 type definitions is , and it comes with a Strict API catch. is typed as a union of per platform shapes ( ). exists only on the iOS member, so reading it without narrowing on is a compile error. There is no Android equivalent at all. Prefer the size check even where works is for an iPad even when your app is running in a narrow Slide Over window where a phone layout is correct. Use it for genuinely device specific behaviour (an iPad only feature, a p"},{"u":"styling/platform-specific-styles.html","t":"Platform-Specific Styles","s":"Styling and Layout","d":"Platform.OS, Platform.select and the .ios.tsx / .android.tsx file extensions — which of the three to reach for, and the typing traps in each.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Platform is a union type, not one object","Platform.select picks the first key that matches","Platform file extensions","Platform differences","Common patterns","Put the branch in a token, not in the JSX","Branch once, at the top of a hook","Extension files for anything with its own imports","Performance considerations","Common mistakes","Related topics"],"b":"React Native gives you one component tree and two sets of platform conventions. Sometimes the difference is a number — 8 points of padding on Android, 12 on iOS. Sometimes it is a whole component. The three mechanisms below cover both ends of that range, and picking the wrong one is how a codebase ends up with scattered through two hundred lines of JSX. The rule that keeps this manageable: branch on values, not on structure. If the difference is a style value, use . If the difference is the component, use a platform file extension and let Metro pick. Why it exists / when to use it — and when NOT to Mechanism Use it for Cost A one off boolean check, usually inside a hook or helper Easy to over use; reads as noise in JSX Per platform values : a number, a colour, a style object None worth mentioning / Per platform implementations of a whole module Two files to keep in sync; no shared logic unless you extract it When not to reach for any of them: when the real difference is not the platform but the screen, the density or the user's settings. A tablet is not a platform — see Responsive and Tablet Layouts. A larger font because the user asked for one is not a platform difference either; that is , covered in Units and Density. Basic example How it works is a union type, not one object This is the part that surprises people coming from older type definitions. Read from the installed 0.87 types, is typed as , and each member has a literal field. That makes a discriminant, and it means several properties only exist on one member. , and are on every member, so they need no narrowing. and are iOS only. exists everywhere but has a different shape on each platform — , , and on Android; , and on iOS. picks the first key that matches The lookup order is the concrete platform key ( , , "},{"u":"styling/shadows-and-elevation.html","t":"Shadows and Elevation","s":"Styling and Layout","d":"The real 0.87 state of shadows — iOS-only shadow props, Android-only elevation, and the cross-platform boxShadow and filter props that now exist on both.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What the 0.87 type surface actually contains","BoxShadowValue","filter and dropShadow","Platform differences","Common patterns","One cross-platform shadow token","Keeping elevation for z-ordering","A border instead of a shadow","Performance considerations","Common mistakes","Related topics"],"b":"Shadows used to be the textbook React Native platform split: four props that only did anything on iOS, and an number that only did anything on Android. That split is still in the type definitions, and most existing code is written against it. It is no longer the whole story. React Native 0.87 ships and as ordinary, cross platform style props , registered in the base view configuration for both iOS and Android. This page reports what is actually supported where, read from the installed 0.87.1 types and the Android sources that ship inside the package. Why it exists / when to use it — and when NOT to Approach Platforms Use it when iOS and Android You want one declaration, multiple shadows, or an inset shadow / / / iOS only You are maintaining existing code, or need fine iOS only control Android only You need Android's z ordering behaviour, not just a shadow iOS, and Android 12+ The shadow should follow the pixels (a transparent PNG, an icon) rather than the box When not to use a shadow at all: on a long list. A shadow is an extra draw pass per view, and a of 200 shadowed cards is one of the reliable ways to make an Android mid range device stutter. A hairline border or a background colour step reads almost as well and costs nothing. Basic example How it works What the 0.87 type surface actually contains Verified by reading : Style prop Type Platform annotation in the types none — cross platform none — cross platform / / on none — cross platform The absence of a tag is not a guess: and are both registered in and in the installed package. They are real on both platforms. The four props carry and are ignored on Android. carries and is ignored on iOS. Both statements are unchanged from previous versions. , , and accept a or a . Numbers are density independent points, the same"},{"u":"styling/dark-mode.html","t":"Dark Mode","s":"Styling and Layout","d":"useColorScheme in 0.87, the Appearance override API, and PlatformColor / DynamicColorIOS for colours the OS resolves for you.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","useColorScheme() returns ColorSchemeName | null","The Appearance module","PlatformColor","DynamicColorIOS","Platform differences","Common patterns","One theme object, read through a hook","Respect the user's explicit choice over the system","Shadows do not survive the switch","Images and logos need a variant","Performance considerations","Common mistakes","Related topics"],"b":"Dark mode in React Native is a value you read and branch on. tells you which appearance the OS is in, you pick a palette, and the tree re renders. There is no media query, no CSS variable and no automatic inversion of your colours. Two things changed in 0.87 that break existing code in quiet ways: the hook's return type lost its third value, and the override API renamed its \"follow the system\" argument. Both are covered below. Why it exists / when to use it — and when NOT to Support dark mode because the OS level setting is now a system wide user preference, and an app that ignores it is a white rectangle at 2am. It is also an accessibility setting for some users, not a style choice. Do not build a dark mode by inverting your existing palette programmatically. Dark themes are not light themes with the luminance flipped: shadows stop working, brand colours lose contrast, and pure black backgrounds with pure white text are harder to read than a dark grey with a soft white. Define both palettes explicitly. Do not reach for when you have a brand palette. It is the right tool when you want the platform's own colours — a system background, a separator, a system blue — so your screen matches the OS chrome around it. It is the wrong tool for colours a designer chose. Basic example How it works returns Verified from the installed 0.87.1 types: The third return value was removed in 0.87 Through 0.86 the hook could return a third string meaning \"the system has expressed no preference\". In 0.87 the type is and that string is gone. Code written as still compiles — the middle branch just stops being reachable in the way you expected. Compare against and treat everything else, included, as light. The case is narrow. The type definition says it occurs only when the native module is una"},{"u":"styling/fonts-and-icons.html","t":"Fonts and Icons","s":"Styling and Layout","d":"What a custom font actually needs on the CLI path — native asset registration on both platforms — and how to draw icons without a font at all.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Registering a font, per platform","The asset-linking command","Why fontWeight sometimes does nothing","Icons without a font","Platform differences","Common patterns","A type scale, not scattered font sizes","Always set lineHeight with a custom font","Keep icons in one directory with one prop shape","Performance considerations","Common mistakes","Related topics"],"b":"A custom font in React Native is not a JavaScript concern. is a lookup against fonts the native side already knows about, so the font file has to be registered with the platform before any style can name it. On the Community CLI path that means real changes inside and , not an import. Icons are the same problem wearing a different hat. An icon font is a custom font. A vector icon is a component. This page covers both, and is honest about which parts of the tooling are stable. Why it exists / when to use it — and when NOT to Use a custom font when the brand genuinely requires one. The cost is real: every font file is bundle size on both platforms, a cold start cost while the typeface is loaded, and a per weight file you have to remember to ship. Do not ship four weights when you use two. Do not ship a font at all if the system font is acceptable — San Francisco on iOS and Roboto on Android are optimised for their platforms' rendering and cost nothing. For icons, prefer vector components over an icon font. A font gives you one colour, one glyph per code point, and the same native registration problem. A vector gives you multiple colours, any size without hinting artefacts, and no native setup. Basic example Once a font is registered natively, using it is unremarkable — and this is the only part that lives in JavaScript: How it works Registering a font, per platform This is the whole job. Both platforms need the file inside the native project and a declaration that it exists. 1. Add the font file to the Xcode project so it ends up in the app bundle's Copy Bundle Resources build phase. 2. List the filename under in the Info.plist. The string is the filename as it appears in the bundle. The you write in a style is the font's PostScript family name, which is often different. "},{"u":"styling/styling-approaches.html","t":"Styling Approaches Compared","s":"Styling and Layout","d":"StyleSheet, inline objects, styled-components-style libraries and utility libraries — what each costs per render, and what Fabric does and does not care about.","h":["Why it exists / when to use it — and when NOT to","The comparison","Basic example","How it works","Every approach ends at the same place","Where the cost lands under Fabric","What a template-literal library adds","What a utility-class library adds","What a theme-typed library adds","Platform differences","Common patterns","StyleSheet plus a typed theme, read through a hook","A static base plus one dynamic override","Do not mix three approaches in one codebase","Performance considerations","Common mistakes","Related topics"],"b":"React Native ships one styling primitive: a plain object handed to a prop. Everything else — , template literal libraries, utility class libraries — is a way of producing that object. Knowing that is most of the analysis, because it tells you where the cost of each approach actually lands: in the JavaScript that builds the object, not in the renderer that consumes it. This page compares the options honestly, including the ones this handbook does not recommend. Why it exists / when to use it — and when NOT to The decision matters more in React Native than on the web, for two reasons. There is no CSS file to cache and no selector engine, so every style is computed by your JavaScript on the JS thread — the same thread that handles touches. And screens stay mounted in a navigator, so an approach that re renders broadly re renders screens the user cannot see. If you are starting a project and have no strong requirement, use plus a theme object. It is in core, it has no peers to keep compatible across React Native upgrades, and it is the baseline every other option is measured against. Reach past it when you have a concrete problem it does not solve. The comparison Versions were read with on 2026 09 12. Only packages whose manifests were actually checked are listed; see the note after the table about what a peer range does and does not prove. Inline objects / Ships with React Native Yes Yes No No No No Version checked core 0.87.1 core 0.87.1 6.5.3 2.4.5 3.3.0 4.2.6 / 4.16.0 Declared peer — — none / Extra required peers none none , none Nitro Modules, Reanimated, edge to edge, normalize colors (nativewind) Style identity across renders Stable (module scope) New object every render New object when props change New object per render unless memoised Library managed Cached by clas"},{"u":"navigation/fundamentals.html","t":"React Navigation Fundamentals","s":"Navigation","d":"Installing React Navigation 7, the navigator/router/screen model, and the difference between the static and dynamic configuration APIs.","h":["Why it exists / when to use it — and when NOT to","Installing","Native configuration","Basic example","How it works","Static or dynamic configuration","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"React Navigation is the routing library this handbook uses. It is written in JavaScript on top of native primitives: supplies real native containers, and each navigator package supplies the transitions and chrome that belong to a platform. There is no router built into React Native itself, so something has to fill this role, and React Navigation is the option with first party New Architecture support across stack, tabs and drawer. This page covers installation, the mental model, and the one decision you make before writing any screens: static or dynamic configuration. Why it exists / when to use it — and when NOT to A React Native app has no URL bar and no browser history. Whatever you build instead has to own a serialisable state tree, map it onto native view containers, restore focus, handle the Android hardware back button and answer incoming deep links. React Navigation does all of that. You do not need it for a single screen app, or for a React Native view embedded inside an existing native app where the host app already owns navigation. In the embedded case, adding a navigator means two navigation stacks fighting over the back button. The main alternative is , which uses native navigation controllers directly rather than rendering screens inside a JavaScript managed tree. It is a different trade: more native fidelity, less flexibility, and a much smaller ecosystem. This handbook documents React Navigation only. Versions used throughout this section Every example was written against 7.3.18 and checked against the real installed type definitions. Version numbers on this page were read from the npm registry on 2026 09 12. Installing Install the core package plus its two required peers. and are peer dependencies of every navigator, not optional extras. Then install th"},{"u":"navigation/native-stack.html","t":"Native Stack","s":"Navigation","d":"The push/pop navigator built on UINavigationController and Fragments — options, actions, and the difference between navigate, push and replace.","h":["Why it exists / when to use it — and when NOT to","Installing","Basic example","How it works","Actions that change the stack","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"The native stack is the navigator most apps start with. It models navigation as a pile of screens: you push a new one on top, and going back pops it off. implements that by handing the stack to the platform through , so the transition, the swipe back gesture and the header are the real system ones rather than JavaScript imitations. This page assumes you have read React Navigation Fundamentals and have the container installed. Why it exists / when to use it — and when NOT to Every app that has a list and a detail view needs a stack. It is the only navigator that keeps a history, so it is also the only one that can answer \"go back\" meaningfully. Use the native stack unless you have a specific reason not to. Because the transition runs on the native side, it does not stutter when the JavaScript thread is busy — which is exactly when a JavaScript driven transition looks worst, since a busy thread is usually the result of the screen you are navigating to doing its initial work. The trade is customisation. , the JavaScript stack, lets you write an arbitrary transition with an interpolator because it animates in JavaScript. The native stack gives you a fixed set of platform animations instead. If you need a shared element transition or a bespoke curve for the whole app, the JavaScript stack is the escape hatch; for everything else the native one is faster and more correct. Do not reach for a stack when the destinations are siblings rather than a hierarchy. Five top level areas of an app are Tabs or a Drawer, not five pushes. Installing 7.3.18, 4.27.0 and 5.9.1 are peer dependencies — they are declared in the package's , so a package manager that does not auto install peers leaves you with a missing module at first render. Run afterwards. Basic example Two things to notice. on "},{"u":"navigation/tabs.html","t":"Tabs","s":"Navigation","d":"Bottom tabs with @react-navigation/bottom-tabs 7 — lazy screens, badges, per-tab stacks, hiding the bar and the tabPress event.","h":["Why it exists / when to use it — and when NOT to","Installing","Basic example","How it works","Icons","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"A tab navigator shows several sibling destinations at once and switches between them without a history. renders the bar in JavaScript and each tab's content in a container, which is why it can keep inactive tabs mounted and still avoid paying for their renders. Tabs are a flat structure. Nothing in a tab navigator pushes or pops — that is the job of a stack inside each tab, which is the subject of Nesting Navigators. Why it exists / when to use it — and when NOT to Tabs are for three to five top level areas that a user moves between constantly and in no particular order: a feed, search, a profile. The value is that all of them are one tap away and each remembers where it was. They stop working past about five items, because the labels stop fitting and the tap targets get too small. At that point the answer is a Drawer, or a \"More\" tab that opens a list. Do not use tabs for a linear flow. Checkout steps, onboarding and anything with a required order belong in a stack, where back means something. Material top tabs are a separate package provides the swipeable tabs that sit under a header. This handbook covers bottom tabs; the concepts below (lazy, , nesting) transfer, but the options do not. Installing It depends on 2.9.40 internally and declares 7.3.18, 4.27.0 and 5.9.1 as peers. Unlike the drawer, it needs no gesture or animation library — the bar is a row of pressables, not a gesture surface. Basic example How it works The tab router holds an array of routes and an index, plus a of visited route keys. There is no push: switching tabs changes the index and records the move in that history, which is what the Android back button walks through. on the navigator controls that walk. The values the router accepts are (the default), , , , and . drops duplicate entries; keeps t"},{"u":"navigation/drawer.html","t":"Drawer","s":"Navigation","d":"The side drawer navigator — its real install cost (gesture-handler, Reanimated 4 and the separate worklets runtime), custom content, and when a drawer is the wrong answer.","h":["Why it exists / when to use it — and when NOT to","Installing","Basic example","How it works","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"A drawer is a panel that slides in from the side of the screen, usually holding a list of destinations that would not fit in a tab bar. implements it on top of , which means a real gesture and a real animation — and therefore the heaviest install of the three navigators in this section. Read the installation section carefully. The drawer is where most React Native projects first meet the Reanimated 4 packaging change, and almost every tutorial written before it still gets this wrong. Why it exists / when to use it — and when NOT to A drawer buys screen space. Six or ten destinations, account switching, a settings link and a sign out do not fit in a bottom bar, and a drawer holds all of them without stealing a row of pixels from every screen. It costs discoverability. Content behind an edge swipe or a hamburger button is content most users never open. If your app has three or four main areas, Tabs are better: always visible, one tap, no gesture to learn. The honest rule is to use a drawer for secondary navigation — the things a user needs occasionally — and tabs or a stack for the things they need constantly. Many apps use both, with the drawer attached above the tabs. Not React Native core still exports , but it is deprecated in 0.87, and it is Android only. — which the drawer navigator already depends on — is the cross platform replacement. Do not build a drawer on the core component. Installing This is four packages, not one, and the order of the traps matters. Reanimated 4 does not bundle its worklets runtime declares as a peer dependency . It is a separate install and a separate native build. Installing Reanimated alone gives you a build that fails the moment a worklet runs, with an error about a missing worklets module that does not name the package you are missing"},{"u":"navigation/nesting.html","t":"Nesting Navigators","s":"Navigation","d":"Combining stacks, tabs and drawers — how nested state works, navigating across boundaries with NavigatorScreenParams, and the tab-bar-hiding problem.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Headers stack up","Navigating across navigators","Typing a screen that reaches both ways","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"A real app is rarely one navigator. The usual shape is a stack at the root, tabs inside it, and another stack inside each tab. Nesting is how you express that: a navigator is a component, so it can be the of a screen in another navigator. The mechanics are simple. The parts that bite are navigating from a screen in one navigator to a screen in another, typing that call, and deciding which navigator a given screen belongs to. Why it exists / when to use it — and when NOT to Each navigator type answers one question: a stack answers \"where did I come from\", tabs answer \"which area am I in\", a drawer answers \"what else is there\". An app that needs more than one of those answers needs more than one navigator. Nest when the structures are genuinely different. Do not nest to group screens that share options — does that without a second navigator, and a group keeps the state tree flat, which keeps deep links and state restoration simpler. Every level of nesting adds a level to the state tree, a level to the linking config and a level to chains. Two levels is normal. Four is a sign that some of those navigators should be groups. Basic example The common shape: a root stack that owns full screen and modal presentations, with a tab navigator as its first screen, and a stack inside one tab. How it works Nesting produces a nested state object. The root stack's route for carries a of its own, whose route for carries another. Serialise it and you get exactly that shape — which is why deep links and state restoration have to describe the whole path, not just the destination. Only one navigator is focused at a time, and focus flows down. A screen is focused when it is the active route in its navigator and every navigator above it is focused too. That is why a tab screen's does not fire "},{"u":"navigation/params-and-typed-routes.html","t":"Params and Typed Routes","s":"Navigation","d":"Declaring a param list, typing screen props and hooks, and the React Navigation 7 global declaration that makes every navigate call checked without a generic.","h":["Why it exists / when to use it — and when NOT to","The param list","Typing a screen component","Typing the hooks","The global declaration — verified","The other form, and which to use","With the static API","Params must be serialisable","Security considerations","Common mistakes","Related topics"],"b":"Params are the data a screen is opened with: an id, a filter, a draft key. In React Navigation they live inside the navigation state, which means they are serialised, restored, and reconstructed from deep links. That single fact explains every rule on this page. This is also the page that decides whether the rest of your navigation code is checked or not. React Navigation ships full TypeScript types, but they only do their job once you have told the library what your routes are. Every pattern below was compiled against 7.3.18 and the installed type definitions. Why it exists / when to use it — and when NOT to Without types, compiles, ships, and does nothing at runtime. The action bubbles up, no router handles it, and — unless you happen to have wired to something visible — nothing tells you. Typed routes turn that into a compile error. Params are not a state container. Put in them what identifies the screen: an id, a query string, a tab name. Do not put in them the object that id refers to. The fetched record belongs in a cache or a store; duplicating it into params means a stale copy is restored days later when the app reopens. The param list A param list is a plain type mapping each route name to its params. means the screen takes none. The difference between and matters. The types check whether ; if it does, the params argument to becomes optional. alone still requires you to pass an object, even an empty one. Pass the list to the navigator factory and the navigator's props are checked against it: Typing a screen component A screen receives and . Each navigator package exports a props type that pairs them for one route name. The equivalents for the other navigators are from and from . Each is with that navigator's own navigation prop, so and exist on a stack's and on"},{"u":"navigation/deep-linking.html","t":"Deep Linking and Universal Links","s":"Navigation","d":"Mapping URLs onto navigation state, the Android and iOS native configuration that makes links work, and treating every link parameter as attacker-controlled input.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Native configuration","Security considerations","Threat","Exploit","Fix","Verification","Common patterns","Common mistakes","Related topics"],"b":"A deep link is a URL that opens a specific screen. React Navigation handles the JavaScript half: you give it a config, it converts an incoming URL into a navigation state tree and restores the app into it. The other half is native configuration, and that half is where deep links usually fail — silently, on one platform, in release builds only. This page covers both halves, and then treats the part most guides skip: a deep link parameter is input from whoever sent the link, which is not necessarily your user. Why it exists / when to use it — and when NOT to Deep links are how everything outside your app points into it: a push notification, an email, a shared link, a Google result, an OAuth redirect. Without them, every one of those lands on your home screen and the user has to find their way. There are two kinds, and they are not interchangeable: Kind Example Behaviour Custom scheme Only your app can open it. Nothing happens if the app is not installed. Any other app can also claim the same scheme. Universal / App Link A verified https URL. Opens your app if installed, your website if not. Ownership is proven by a file on your domain. Ship both. The custom scheme is what you use for internal redirects and for testing; the https link is what you put in emails and share sheets, because it degrades to a web page and because its ownership cannot be hijacked by another app. Skip deep links entirely only for an app with no external entry points at all — an internal tool launched from the home screen and nothing else. Basic example The prop on the container maps paths onto the screen tree. The shape of mirrors the shape of your navigators. is rendered while the initial URL is being resolved. Without it the app shows the initial route for a frame and then jumps, which reads as a"},{"u":"navigation/headers.html","t":"Headers","s":"Navigation","d":"Configuring the native stack header, the header options that are real on each platform, replacing it entirely, and laying out content under a transparent one.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Which options are real where","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"The header is the bar at the top of a screen with a title, a back button and actions. On the native stack it is a genuine platform header — a on iOS, a Fragment's app bar on Android — which is why it animates correctly during a push and why its options are a fixed list rather than arbitrary styles. On the tab and drawer navigators the header is instead a React component from , so those two share a different, more style friendly option set. Most of the confusion about header options comes from mixing the two lists up. Why it exists / when to use it — and when NOT to The header answers three questions at once: where am I, how do I get back, and what can I do here. Getting it from the navigator rather than rendering your own means the back button, the title truncation, the safe area inset and the transition are all handled. Turn it off with when the screen has its own full bleed design — a media viewer, an onboarding screen, a map. Turn it off on the outer navigator when you nest, so you do not get two. Replace it with a custom component when you need a shape the options cannot express: a search field that is part of the bar, a segmented control, a two line title with a subtitle. Accept what you give up first — see the trade below. Basic example as a function receives , which is how a title derived from params is written without an effect. Use only when the option depends on state inside the screen. How it works The native stack does not render a for the header. It renders a configuration element that translates into properties on the native navigation bar. That is the reason for every restriction on this page: you are describing a native component, not styling a React one. Consequences worth knowing: on the native stack accepts and little else. Padding, borders and flex h"},{"u":"navigation/modals.html","t":"Modals","s":"Navigation","d":"Presenting a screen modally with the native stack — presentation modes, form sheets and detents, transparent modals, and when the core Modal component is the better tool.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Form sheets and detents","Platform differences","Common patterns","When to use the core Modal component instead","Performance considerations","Common mistakes","Related topics"],"b":"A modal is a screen presented over the current one rather than pushed beside it. On the native stack this is one option, , and everything else follows from it: the transition comes from the top, the screen underneath stays visible, and dismissal is a swipe down rather than a swipe from the edge. Because a modal is still a screen in the navigator, it has a route, params, a linking path and a place in the state tree. That is the difference between this and React Native's component, which is covered at the end of this page. Why it exists / when to use it — and when NOT to Present modally when the task interrupts the current one and ends by being finished or cancelled: composing a message, picking a filter, signing in, confirming a destructive action. The visual language tells the user the same thing — the screen behind is still there and they are coming back to it. Do not present modally when the destination is a place rather than a task. A detail screen reached from a list is a push; making it a modal breaks the back gesture people expect and leaves the list visible behind it for no reason. Do not stack modals. A modal presenting another modal works, and on both platforms it looks wrong — the second sheet sits a few points below the first and there is no way back to the content without dismissing twice. Basic example Modal screens belong on the root stack, grouped so the option is written once. How it works is passed through to , which maps it onto the platform's own presentation style. The whole set, verified against 4.27.0: Value iOS Android Normal push Normal push Sheet over the previous screen Modal presentation Falls back to Falls back to , with detents Falls back to a bottom sheet Previous screen stays visible behind a translucent screen Same Falls back to Falls bac"},{"u":"navigation/state-persistence.html","t":"State Persistence","s":"Navigation","d":"Saving and restoring the navigation state across app restarts and Android process death — the wiring, the versioning, and why restored state is untrusted input.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Versioning against app upgrades","Storing it synchronously with MMKV","Platform differences","Security considerations","Threat","Exploit","Fix","Verification","Performance considerations","Common mistakes","Related topics"],"b":"Navigation state is a plain serialisable object, so it can be written to disk when it changes and handed back to the container the next time the app starts. The user returns to the screen they left instead of the home screen. That is the entire mechanism: out, in. The work is in deciding when restoring is right, versioning what you store, and remembering that a file on disk is not something you can trust. Why it exists / when to use it — and when NOT to There are two quite different reasons to do this. Development. Android and iOS both kill backgrounded apps, and Fast Refresh does not survive a full reload. Persisting state in development means editing a screen five pushes deep without re navigating to it every time. This is a near universal win and costs nothing in production. Production. Android reclaims memory by killing the process; when the user returns, the app starts cold and lands on the home screen having lost their place. Restoring state fixes that. It is the right call for an app where the user is in the middle of something — a long form, a reading position, a multi step flow. It is the wrong call when restoring can put the user somewhere that no longer makes sense: a screen behind an authentication wall after the session expired, a checkout for an order that has since been placed, a detail screen for a record that was deleted. Restoring blind into any of those is worse than the home screen. The pragmatic default is: on in development always, on in production selectively, with the restored state validated before it is used. Restoring disables initial deep link handling documents this directly: when is provided, deep links and URLs are not handled on the initial render. If the app was launched by a link, that link must win over the saved state — the example be"},{"u":"navigation/navigation-performance.html","t":"Navigation Performance","s":"Navigation","d":"Why native screen containers matter, what freezeOnBlur and lazy actually do, keeping work out of screen bodies, and the cost of re-rendering a navigator.","h":["Why it exists / when to use it — and when NOT to","Native screen containers","Freezing screens that are not visible","Lazy screens","Keeping work out of screen bodies","The cost of re-rendering a navigator","Platform differences","Common mistakes","Related topics"],"b":"Navigation is where an app feels fast or slow, because it is the only interaction that puts a transition animation and a screen's first render on the same thread at the same moment. Everything on this page is about reducing what happens during that window. Four things account for nearly all of it: whether screens are native containers, whether invisible screens are still rendering, when a screen is first built, and how often the navigator itself re renders. Why it exists / when to use it — and when NOT to Measure before you change anything. A transition that stutters has a cause — a large image decoding, a list rendering 200 rows, a context provider recomputing — and the options on this page are ineffective against the wrong cause. does nothing for a screen that is slow the first time it opens. Start with the profiler, identify which of the four categories below your problem is in, then apply the matching fix. Measuring Before Optimising is the method. Native screen containers is a peer dependency of every navigator, and it is not optional plumbing. It is what makes each screen a real native container — a on iOS, a Fragment on Android — instead of a that JavaScript positions. That buys three things: The transition runs natively. A busy JavaScript thread does not stutter the animation, which matters because the thread is busiest precisely when a new screen is mounting. Inactive screens are detached from the native view hierarchy. A screen five pushes down is not laid out, not composited, and its views are not measured. Without screens, every screen in the stack is a mounted the platform still walks on every layout pass. The platform's own behaviours come for free. The iOS swipe back gesture, the Android back dispatch, and the system's handling of memory pressure on offsc"},{"u":"state-and-data/hooks-in-react-native.html","t":"Hooks in a Native Context","s":"State and Data","d":"What changes about React hooks when there is no DOM — effect cleanup, screens that stay mounted, AppState, useLayoutEffect under Fabric, and the hooks React Native adds.","h":["What actually changes without a DOM","Basic example","How it works","Mounted is not the same as visible","The AppState interaction","useLayoutEffect under Fabric","React 19 is in play","The hooks React Native adds","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"The hooks API is the same one you already know. , , , , and behave identically, because they are implemented in , not in the renderer. React Native 0.87 pins to , so every React 19 hook is available to you. What changes is everything the hooks talk to . There is no , no , no , no event. A screen can stay mounted for the entire life of the app while the user is looking at something else. And the process itself can be suspended mid effect when the user switches apps. Those three facts account for nearly every native specific hooks bug. What actually changes without a DOM Web assumption React Native reality cleanup runs when the user navigates away It runs on unmount, and a stacked screen does not unmount when you push on top of it / tells you the app is hidden does, and it has states the web has no analogue for runs before the browser paints It runs before , but layout is computed in C++ and mounted on the UI thread — there is nothing to read yet read once is good enough The window resizes on rotation, split screen and foldables — read it from a hook is synchronous Storage is a native module; see AsyncStorage vs MMKV Basic example The shape of a data loading effect is the same as on the web, and so is the reason you need the cleanup: the component can go away before the request finishes, and writing state after that is a leak. In production you would not hand roll this. See Data Fetching and Caching for why fetching goes wrong once there is more than one screen. How it works Mounted is not the same as visible This is the single biggest difference from the web. In a native stack navigator, pushing screen B on top of screen A leaves A mounted . Its effects never clean up, its intervals keep firing, its subscriptions keep receiving events, and its state survives untouched. T"},{"u":"state-and-data/context.html","t":"Context","s":"State and Data","d":"How React Context behaves in a React Native app, where to place providers relative to navigators, and the re-render cost that makes people reach for a store instead.","h":["When to use it — and when not to","Basic example","How it works","Provider placement","Common patterns","Split state from actions","Fail loudly when the provider is missing","Keep a store in context, not the state","Performance considerations","Common mistakes","Related topics"],"b":"React Context is the built in way to pass a value down a tree without threading it through every component. The API is identical to React on the web, and React 19 simplifies it: you render directly instead of . What is different in a React Native app is the tree it sits in. Navigators keep screens mounted, so a context update repaints screens the user cannot see. And the things people usually put in context — theme, auth session, feature flags — are exactly the things that change rarely but are consumed everywhere, which is the worst possible shape for a naive implementation. When to use it — and when not to Context is a dependency injection mechanism. It answers \"how does this deeply nested component get hold of that object\". It is not a state manager, and it does nothing to help with update granularity. Use context for Use a store for Values that change rarely: theme, locale, auth session Values that change often: form drafts, live data, selections Injecting a service instance (an API client, a storage handle) Anything where components need to subscribe to a slice Scoping a value to a subtree (a form, a modal, a wizard) Global state read from unrelated corners of the app Test seams — swap a real client for a fake in one place State you also want to read outside React The rule of thumb: if you find yourself writing and then immediately destructuring one field out of a large object, you have outgrown context. See Zustand and Redux Toolkit. Basic example Consuming it is unremarkable, which is the point: How it works When a provider's changes by comparison, React re renders every consumer of that context, regardless of which part of the value they read. There is no selector, no shallow compare and no way to opt out. on an intermediate component does not stop it — context "},{"u":"state-and-data/zustand-and-redux.html","t":"Zustand and Redux Toolkit","s":"State and Data","d":"When a store beats context, how Zustand 5 and Redux Toolkit 2 differ in practice, and the native-specific concerns — selectors, persistence and reading state outside React.","h":["Why it exists / when to use it — and when NOT to","The comparison","Basic example","Zustand","Redux Toolkit","How it works","Subscription, not propagation","Selectors that return new objects","Reading and writing outside React","Persistence","Platform differences","Common patterns","One store per concern, not one store for everything","Keep actions in the store, not in components","Do not put server data in the store","Select actions separately from state","Performance considerations","Common mistakes","Related topics"],"b":"You reach for a store when Context runs out of granularity: a value changes often, is read from unrelated corners of the app, and every consumer re rendering on every change is no longer acceptable. A store solves exactly that problem by letting components subscribe to a slice rather than to the whole value. Two options are worth knowing in 2026. Zustand is a small subscription store with no boilerplate. Redux Toolkit is the modern Redux, with a structured architecture and the best devtools in the ecosystem. This page compares them on the axes that matter in a React Native app, which are not quite the axes web articles compare them on. Why it exists / when to use it — and when NOT to Do not start here. Most state in a React Native app belongs somewhere else: State Where it belongs Server data — lists, details, anything fetched A query cache. See Data Fetching and Caching Which screen you are on, and route params The navigator Form input, a toggle, a selected tab in one screen in that screen Theme, locale, an injected service Context Animation and gesture values Reanimated shared values, which never trigger a React render What is left is genuinely global client state: an auth session, a cart, a multi step draft, a queue of pending offline mutations, feature flags the user can toggle. That is the store's job, and it is usually a much smaller surface than people expect. The comparison Versions read with on 2026 09 12. Zustand Redux Toolkit Version 5.0.15 2.12.0 (plus 9.3.0 ) Peers (optional bindings) , and needs Packages to install 1 2 ( , ) Provider required No Yes — Boilerplate per feature A hook A slice, plus wiring into the root reducer Subscription granularity Per selector, opt in Per selector, opt in Shallow comparison from from Reading state outside React — trivial "},{"u":"state-and-data/data-fetching.html","t":"Data Fetching and Caching","s":"State and Data","d":"TanStack Query 5 in a native app — wiring focus refetch to AppState, online state to NetInfo, and retry behaviour that survives a real mobile network.","h":["Why it exists / when to use it — and when NOT to","What goes wrong with useEffect fetching","When not to use it","Basic example","How it works","Version 5, not version 4","Focus refetch: AppState, not window","Online state: NetInfo, not navigator.onLine","Retry and backoff on a real mobile network","Query keys are the cache","Platform differences","Common patterns","Wire both bridges once, at startup","Mutations that invalidate","Infinite lists","Pair the query cache with a persistent one","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Fetching data in React Native is , the same API as the browser. Everything hard about it is what happens around the request: the screen is still mounted when the user comes back three minutes later, the connection dropped halfway through, the process was suspended mid flight, and two screens want the same list. A query cache solves all of that once. This page uses TanStack Query 5.102.8 , which is installed in this handbook's type check harness, so every sample below is compiled against it and the real react native 0.87.1 types. Why it exists / when to use it — and when NOT to What goes wrong with fetching The pattern everyone writes first looks reasonable and fails in five specific ways on mobile: 1. No cache. Navigate away and back and it fetches again, showing a spinner over data you already had. 2. No deduplication. Two components that need the same list issue two requests. 3. No staleness model. The data is either \"fresh forever\" or \"refetched on every mount\". Neither is what you want. 4. No retry. On a mobile network a request fails for ten seconds and then would have succeeded. This gives up. 5. The screen stays mounted. In a stack navigator, pushing another screen on top does not unmount this one, so the cleanup never runs and the data silently ages for as long as the user is elsewhere. Fixing those by hand means writing a cache, a staleness clock, an in flight registry and a backoff policy. That is what a query library is. When not to use it Client state. A cart, a form draft, a toggled preference. See Zustand and Redux Toolkit. A single one off request with no caching requirement — a form submission with no cached representation, a fire and forget analytics call. Streaming or subscription data. A websocket pushing updates is a different shape; feed it into the"},{"u":"state-and-data/offline-first.html","t":"Offline-First","s":"State and Data","d":"Detecting connectivity with NetInfo 12, optimistic updates, a durable mutation queue, persisting and rehydrating a query cache, resolving conflicts, and the limits you cannot engineer away.","h":["Why it exists / when to use it — and when NOT to","Detecting the connection: NetInfo 12","Three states, not two","Reading offline: persist the query cache","Writing offline: optimistic updates","The mutation queue","The built-in version","The explicit version","Resolving conflicts","Security considerations","Threat","Exploit","Fix","Verification","Honest limits","Platform differences","Common mistakes","Related topics"],"b":"A mobile app is offline regularly and briefly: a lift, a tunnel, a basement, a handover between cells, a plane. \"Offline first\" is not a feature you bolt on for the rare user with no signal — it is the assumption that the network is an optimisation, not a precondition , applied to an app that most of the time has a perfectly good connection. That assumption produces four concrete pieces of work, and this page covers them in order: 1. Know the connection state — and know how little that tells you. 2. Read from a cache that survives a restart , so a cold launch is not a spinner. 3. Accept writes while offline , durably, and replay them in order later. 4. Resolve the conflicts that step 3 makes inevitable. There is also a fifth thing you cannot do, and the honest limits section at the end says what it is. Why it exists / when to use it — and when NOT to Full offline first is real work. It is worth it when the user creates content, when sessions are long, or when the app is used somewhere with bad signal by definition — field work, travel, warehouses, transit. It is not worth it when the data is meaningless offline. A live auction, a payment confirmation, a video call, a real time dashboard: for these, detecting offline and saying so clearly is the whole feature, and queueing a bid to send later is actively harmful. App Right level Note taking, task list, journal Full: local writes, queue, sync, conflict resolution Feed reader, documentation, catalogue Read cache only — persist queries, no write queue Messaging Full, with per message delivery state visible to the user Banking, payments, ticketing Cache reads; refuse writes offline and say why Live auction, live scores, trading Neither. Show a clear disconnected state Detecting the connection: NetInfo 12 12.0.1 is the verifi"},{"u":"state-and-data/asyncstorage-vs-mmkv.html","t":"AsyncStorage vs MMKV","s":"State and Data","d":"The two general-purpose key-value stores compared on API shape, synchronicity, performance and encryption — plus the MMKV 4 install trap almost every tutorial gets wrong.","h":["Why it exists / when to use it — and when NOT to","The comparison","Which to choose","The MMKV 4 install trap","Basic example","AsyncStorage","MMKV","How it works","AsyncStorage 3.x changed its API","MMKV is memory-mapped, which is why it is fast","MMKV's React hooks","Encryption, and what it is worth","Security considerations","Threat","Exploit","Fix","Verification","Platform differences","Common patterns","One adapter, so the choice is reversible","Migrating from AsyncStorage to MMKV","Namespaced keys","Performance considerations","Common mistakes","Related topics"],"b":"Every app needs somewhere to put small values that survive a restart: a theme preference, an onboarding flag, a last selected tab, a cached response. React Native has no , so this is a library decision, and there are two serious options. 3.1.1 is the community standard: asynchronous, promise based, widely assumed by other libraries. 4.3.2 wraps Tencent's MMKV through Nitro Modules: synchronous, considerably faster, and with an install step almost every tutorial gets wrong. Neither is secure storage. That is the first thing to settle, and it is settled below. Why it exists / when to use it — and when NOT to Use a key value store for small, structured, non sensitive values. Do not use one for: Instead of Use Credentials, tokens, API keys The Keychain or Keystore — Secure Storage Large binary blobs, images, downloads The file system Relational or queryable data A database Server data with a staleness model A query cache — Data Fetching and Caching Values that only matter while the app is open , or a store The comparison AsyncStorage 3.1.1 MMKV 4.3.2 Synchronicity Asynchronous — every call returns a Promise Synchronous — every call returns a value API shape / / / / / / / / / / / / / / / Value types Strings only — you serialise , , , natively Underlying storage SQLite (Android), files (iOS) Memory mapped file, both platforms Typical performance Milliseconds per call, plus a thread hop Microseconds; no thread hop Multiple instances Encryption None AES 128 or AES 256, opt in Size limits No documented hard limit; performance degrades with size Designed for many small values; not for megabyte blobs React hooks None built in , , , , , , Change notifications None Extra install steps None beyond Requires Ecosystem assumptions Many libraries default to it You write a small adapter M"},{"u":"state-and-data/secure-storage.html","t":"Secure Storage","s":"State and Data","d":"react-native-keychain 10 from the data-layer side — the real API surface, where a token store sits in startup, and what secure storage does not protect you from.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The API surface, verified","Where the read goes in startup","Keep the token out of React state","Biometric gating changes the shape of your data layer","What it does not protect","Platform differences","Common patterns","One module, one service string","A key for MMKV, not the data in the Keychain","Clear on sign-out, and clear everything","Mocking it in tests","Performance considerations","Common mistakes","Related topics"],"b":"Secure storage is the third tier of an app's data layer. Memory holds what only matters while the app is open, a key value store holds preferences and caches, and the platform's Keychain or Keystore holds the handful of values you would not want printed in a support log. 10.0.0 is the verified library for that tier. This page covers it from the data layer perspective: the API shape, where the read fits in startup, how the session state it produces connects to the rest of your state, and where its guarantees stop. Keychain and Keystore covers the same library from the attacker's side, with the exploit walkthrough and the platform by platform detail. Read both; this one is about architecture. Why it exists / when to use it — and when NOT to Value Tier Refresh token, access token, session cookie Secure storage A key used to encrypt something else at rest Secure storage A PIN or passphrase the user set for your app Secure storage Theme, onboarding flag, last tab, feature toggles AsyncStorage or MMKV Cached API responses A query cache — Data Fetching and Caching Anything you can fetch again in under a second Do not persist it at all Do not use it as a general store. Every read is a native call, some of them show a system prompt, and the platforms are explicit that it is for small secrets. Encrypt bulk data with a key held here rather than pushing the data itself through it. Do not use it for a value you can derive. A user id is not a secret; it is in every request you make. Basic example One module owns the credential. Nothing else in the app calls the library. How it works The API surface, verified Read from the installed 10.0.0 type definitions. These are the functions a data layer actually uses: Function Signature — Android — iOS There is a parallel set for internet crede"},{"u":"state-and-data/app-lifecycle.html","t":"App Lifecycle and AppState","s":"State and Data","d":"The AppState status union in 0.87, what each platform actually does when your app is backgrounded, when to save state, and what replaced InteractionManager.","h":["Why it exists / when to use it — and when NOT to","The status union, exactly","The events","Basic example","How it works","There is no \"app is closing\" callback","Time does not pass, and then it does","InteractionManager is gone","Platform differences","Common patterns","Refresh on return, if enough time passed","Stop expensive work when you leave","Blank sensitive screens before the snapshot","Subscribe once, share via context","Security considerations","Common mistakes","Related topics"],"b":"A web page is either loaded or gone. A mobile app is neither: it is foregrounded, transitioning, backgrounded, suspended, or killed without warning — and the transitions between those are where a surprising share of mobile bugs live. Unsaved drafts, timers that never stop, sockets that stay open, stale data presented as current, jumping forward by an hour. is the only lifecycle API React Native gives you, and it is smaller than people expect. This page is about what it reports, what it does not, and what each platform actually does behind it. Why it exists / when to use it — and when NOT to Use for things that must react to the app leaving or returning: pausing a camera or a location watch, closing a websocket, persisting a draft, refreshing data that is now stale, re checking authentication, clearing a sensitive screen before it is snapshotted. Do not use it as a navigation lifecycle. A screen that is not visible because the user pushed another one on top of it is still ; React Navigation's is the tool for that, and the distinction is covered in Hooks in a Native Context. And do not use it as a background scheduler. Nothing you register here runs while the app is suspended. Need Tool App went to the background / came back This screen became visible / hidden from React Navigation Defer non urgent work until the JS thread is idle The global Run code while the app is closed Not possible from JavaScript — see Background Refresh The device lost connectivity NetInfo — see Offline First The status union, exactly Read from the installed : Five members, and the last two are the ones nobody handles. Status Means Where you see it Running in the foreground, receiving events Both In the background: another app, the home screen, or (Android) another Activity including system ones li"},{"u":"state-and-data/background-refresh.html","t":"Background Refresh","s":"State and Data","d":"Keeping data fresh when the app is not in front of the user, and why the answer is usually to reconcile on foreground rather than to run in the background.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","1. Reconcile on foreground (start here)","2. Render stale data immediately, revalidate behind it","3. Actual background execution (last resort)","Platform differences","Common patterns","Refresh on foreground, with a staleness floor","Persist and hydrate the cache","Invalidate rather than refetch everything","Let the server decide","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Background refresh is the problem of having fresh data ready the moment someone opens your app, without them waiting for a network round trip. It is one of the most requested features and one of the most commonly over engineered. This page is about the data question: what to fetch, when, and how to reconcile it. The platform question — what the OS will actually let you execute while suspended — is covered in Background Tasks, and the short version is that the CLI path has no first party background execution API and doing it properly means native code. Read that page first if you have not. Almost everyone who asks for background refresh actually wants what this page describes instead. Why it exists / when to use it — and when NOT to Use a background refresh strategy when opening the app cold should not mean staring at a spinner : a feed, an inbox, a dashboard, a list of orders. Do not reach for actual background execution when: A foreground reconcile would do. This covers the overwhelming majority of cases and costs you no native code, no entitlements, and no battery complaints. Start here. The data must be current to the second. Then you want a push, not a poll — the server tells the device when something changed. See Push and Local Notifications. You are trying to sync writes. That is an outbox problem, not a refresh problem. See Offline First. The value is low. Both platforms ration background execution by how often the user opens your app. An app nobody opens gets almost no background time, which means the feature helps least exactly where it was supposed to help most. Basic example The pattern that solves most of the problem: refetch when the app comes back to the foreground, but only if the data is actually stale. The staleness check is the part people leave out. W"},{"u":"platform-apis/permissions.html","t":"Permissions","s":"Platform APIs","d":"Checking and requesting runtime permissions on both platforms, the result states that actually matter, and the native configuration that decides whether your app crashes or prompts.","h":["Why it exists — and when NOT to use it","Basic example","Native configuration","How it works","Check and request are not interchangeable","The five result states","blocked is where the user experience goes wrong","Rationale is an Android-only mechanism","Platform differences","Common patterns","Request a group in one dialog sequence","Model the three outcomes, not two","Ask in context, not at launch","Security considerations","Common mistakes","Related topics"],"b":"Every interesting platform API in this section is gated by a permission, and both platforms gate them differently. Android asks at runtime and lets the user say \"never ask again\". iOS asks once, ever, and then the only way back is the Settings app. Get this page right and the rest of the section is mostly plumbing. There is a second half that is easy to miss: a permission prompt only appears if the native project declares it. On Android an undeclared permission is silently denied. On iOS a missing usage description string is not a denial — it is an immediate crash the moment your code touches the API. Why it exists — and when NOT to use it React Native ships , which is exactly what its name says: an Android only wrapper around . It does not exist on iOS in any useful form, and it has no concept of the iOS permission states. (5.6.1) is the cross platform answer. One , one , one result vocabulary for both platforms, plus the states that iOS has and Android does not ( , ). Use when your app is Android only and you want no extra dependency. Use for anything shipping on both platforms. Do not mix them for the same permission — they report different vocabularies for the same underlying state and you will write two incompatible branches. The best time to use either is as late as possible . A permission requested on first launch, before the user knows what the app does, gets denied at a far higher rate than the same permission requested at the moment the feature needs it. Basic example The Android only equivalent, using core, for comparison: Note the difference in return types. Core's resolves to ; resolves to a five state union. That difference is the whole reason the library exists. Native configuration Nothing above prompts anyone until the native projects declare the permis"},{"u":"platform-apis/camera.html","t":"Camera","s":"Platform APIs","d":"Capturing photos and video with react-native-vision-camera 5, including the permissions, native configuration, device selection and frame-processor constraints.","h":["Why it exists — and when NOT to use it","Installation","Native configuration","Basic example","How it works","Permissions have their own vocabulary here","Devices, not cameras","Formats are constraints, not a list you pick from","Outputs compose","Frame processors need two more packages","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"React Native core has no camera. There is no export, no , nothing that opens a capture session — the entire surface comes from a third party native module. The one worth building on is 5.2.3 . Version 5 is a rewrite on top of Nitro Modules, which means it is New Architecture only by construction: there is no legacy bridge code path to fall back to, and nothing to check before you adopt it on 0.87. Why it exists — and when NOT to use it VisionCamera gives you the capture session itself: device enumeration, preview, photo and video outputs, focus, zoom, torch, and a frame stream you can run code against. That is the right tool when the camera is the feature — a scanner, a document capture flow, a video recorder, anything with a live preview. It is the wrong tool when you only need \"let the user pick or take a picture\". If a system photo picker would do, use one: it needs no camera permission at all on iOS, it is a smaller dependency, and it is a smaller attack surface. Opening a full capture session to take one avatar photo is a large amount of native code for a small amount of product. Version 5 is not version 4 The v4 API — returning a device with a array, , — was replaced. In v5 you compose outputs ( , , ) and pass them to the camera. Almost every tutorial and answer you will find online describes v4 or earlier. Installation VisionCamera Core depends on Nitro. Both peers are required, and both need a native rebuild: Verified from the published package: declares peers on , , and ( at time of writing). Native configuration This is the step that decides whether you get a dialog or a crash. Add a usage description for every capability you actually use. The camera one is mandatory; the microphone one is only needed if you record video with audio. Omitting does not produce a"},{"u":"platform-apis/geolocation.html","t":"Geolocation","s":"Platform APIs","d":"Getting the device's position with @react-native-community/geolocation, plus the foreground/background split and the extra justification iOS demands for \"Always\".","h":["Why it exists — and when NOT to use it","Installation","Native configuration","Basic example","How it works","The API surface","Watching, and cleaning up","Errors are a code, not an exception","Foreground versus background","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"React Native 0.87 does not export a geolocation API. There is no value in the Strict API type surface, and no polyfill installed for you. Grep the installed types yourself if you want to confirm it: That search returns nothing. The API used to live in core and was extracted to the community long before 0.82. Any tutorial that calls in a fresh project is describing a React Native that no longer exists. The maintained replacement is 3.4.0 . It ships a Codegen spec ( ) and a entry, so it is a real TurboModule rather than a legacy module running through the interop layer. Why it exists — and when NOT to use it Location is the permission users are most suspicious of, and the one platform vendors police hardest. Both app stores require a written justification for background access, and iOS re prompts users about apps that keep taking location in the background. Use it when the position genuinely changes the product: finding nearby things, attaching a place to a record, showing the user where they are. Do not use it for analytics, for \"personalisation\", or to infer a country — an IP based country guess on your server costs no permission and no review friction. If all you need is a coarse region, request only. It is a meaningfully smaller ask, users grant it more often, and iOS exposes the same idea as reduced accuracy. Installation Native configuration Foreground use needs one key. If the keys are missing, authorization requests fail immediately and silently — no dialog, no error you can see, just an error callback you will probably have ignored. Background or \"Always\" access needs a second key and a background mode. iOS treats this as a separate, higher cost grant and the string is shown in a distinct dialog, often days later. For the temporary precise location escalation (as"},{"u":"platform-apis/notifications.html","t":"Push and Local Notifications","s":"Platform APIs","d":"The honest split between core PushNotificationIOS, Notifee for rich local notifications, and the provider plus server work that remote push actually requires.","h":["What exists where","Native configuration","Basic example","How it works","Android channels are mandatory","Displaying and scheduling","Handling events","Badge counts with core","Remote push is mostly not a React Native problem","Platform differences","Security considerations","Common mistakes","Related topics"],"b":"\"Notifications\" is three different problems wearing one word, and most of the frustration on this topic comes from treating them as one. 1. Local notifications — your app schedules something the OS shows later. No server. 2. Remote push — a server sends a payload to APNs or FCM, which wakes the device. Needs a provider, a certificate or key, a token pipeline, and backend code. 3. Handling — what happens when a notification arrives or is tapped, in the foreground, the background, and from a cold start. React Native core covers a slice of (1) and (3) on iOS only. Everything else is a library plus infrastructure you write. What exists where Need Core 0.87 Library Server iOS local notification, iOS permission prompt, badge count — — Android notification channels nothing — Rich local notifications, triggers, actions, on both platforms nothing 9.1.8 — Cross platform permission state (Android only) 5.6.1 — Remote push delivery nothing an APNs/FCM client required is genuinely in core — it is in the 0.87 export surface and it has a complete type definition. It is also iOS only and old shaped : string keyed event names, callback types, and no Android story at all. It is the right tool for badge counts and the iOS permission prompt, and the wrong tool for anything you want on both platforms. Notifee runs through the interop layer 9.1.8 ships no and no spec — verified by inspecting the published package. It works on 0.87 through the New Architecture's interop layer for legacy modules, which is supported, but it is not a native TurboModule. Weigh that when you pick it, and watch its releases. Native configuration Notifications need no usage description — the permission is requested at runtime through the notification centre. What they do need is capabilities and, for , delegate forw"},{"u":"platform-apis/file-system.html","t":"File System","s":"Platform APIs","d":"React Native has no filesystem API — which library to use instead, where each platform lets you write, and the permissions modern Android actually requires.","h":["Which library","Why it exists — and when NOT to use it","Installation","Native configuration","Basic example","How it works","Directories are not interchangeable","The API is promise-based and flat","Downloading with progress and cancellation","Paths are strings, and that is a hazard","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"React Native core has no filesystem API. There is no export, no module, no or to disk path. Check the 0.87 export surface and you will find and — nothing that reads or writes a file. What core does give you is , which is enough to send a file you already have a URI for, and that is why a surprising number of apps never need a filesystem library at all. If yours does, you need a third party native module, and you need to pick one that has shipped Codegen support. Which library 2.40.2 is the one this handbook recommends. Verified from the published package: it declares with the spec name and ships a spec at , so it is a real TurboModule rather than a legacy module leaning on the interop layer. It is a maintained fork of the original , which is still published at 2.20.0, has no , and has not been updated in years. Do not install . Its name still dominates search results and its answers are the ones you will find first. If your actual job is \"download or upload a large file without loading it into JavaScript memory\", 0.24.11 is the alternative worth knowing. It also ships a Codegen spec ( ), and it is built around streaming to and from the network rather than general file manipulation. Both declare An open peer range is not a compatibility statement. What makes them safe on 0.87 is the Codegen spec, not the peer field. Apply the same test to anything else you consider — see Native Dependency Compatibility. Why it exists — and when NOT to use it Reach for a filesystem when you are handling files : a downloaded PDF, a captured photo, an export the user will share, a cache of images too large for a key value store. Do not reach for it for application state. Hand rolled JSON on disk persistence is slower than MMKV, has no atomicity, and gives you a corrupted file recovery path "},{"u":"platform-apis/clipboard-and-share.html","t":"Clipboard and Share","s":"Platform APIs","d":"Core Clipboard, Share and ActionSheetIOS — what each one actually does on each platform, and the paste-transparency and file-sharing details that are not in the API.","h":["Why it exists — and when NOT to use it","Native configuration","Basic example","How it works","Clipboard is two methods","Share opens the system sheet","ActionSheetIOS for iOS-native menus","Platform differences","Common patterns","Paste only when there is something to paste","Give feedback yourself","Security considerations","Common mistakes","Related topics"],"b":"These are two of the few platform APIs React Native still ships in core, and both are small enough to learn in a minute. has exactly two methods. has one. The interesting part is not the API — it is that both behave meaningfully differently on each platform, and that both are privacy relevant in ways the method signatures do not hint at. Core silently discards the you pass on Android. Reading the clipboard on iOS 14+ shows the user a banner naming your app. Why it exists — and when NOT to use it A \"Copy\" button is the cheapest useful affordance in a mobile app: no permission, no dialog, no network. The system share sheet is nearly as cheap and hands your content to every other app on the device without you integrating with any of them. What core does not cover: sharing files , sharing to a specific app, reading or writing images, and knowing when the clipboard changes. Those need a library or native code. Reach for one only when you actually need that; the core APIs cover the common cases without a dependency. Native configuration Neither API needs a permission on either platform. What native configuration exists is for the cases just past the edge of core. Nothing in is required for , or . Two entries become relevant nearby: See Linking for why that array is required and what happens without it. Since iOS 14 , reading the pasteboard shows the user a transient banner — . This is not configurable and cannot be suppressed. What you can do is avoid reading at all: the detection APIs ( , , , in the community clipboard package) answer \"is there something pasteable?\" without triggering the banner. Core has no such method. No permission is needed for or . Handing a file to another app does need configuration, because Android forbids passing a raw URI across app boundaries. You"},{"u":"platform-apis/linking.html","t":"Linking","s":"Platform APIs","d":"Opening URLs, registering your own scheme, handling incoming links from cold and warm starts, and why an incoming URL is untrusted input.","h":["Why it exists — and when NOT to use it","The API","Native configuration","Basic example","How it works","Well-known schemes","Custom schemes versus verified links","sendIntent is Android-only","Platform differences","Security considerations","Common mistakes","Related topics"],"b":"is the boundary between your app and every other app on the device. It goes both ways: outbound, you open a URL and the OS decides who handles it; inbound, someone else opens a URL and the OS hands it to you. The outbound half is four methods and one gotcha. The inbound half is where the interesting problems live — native configuration you cannot skip, a cold start path that behaves differently from the warm start path, and the fact that anything arriving through a link is input from outside your app . Why it exists — and when NOT to use it Use to leave the app deliberately: open a web page, start a phone call, compose an email, hand off to a maps app. Use the inbound half to let a push notification, an email, or a web page land the user on the right screen. Do not use for a web page you want the user to come back from — that is what an in app browser or a WebView is for; leaving the app for a help article is a bad flow and loses your navigation state. And do not use raw for deep link routing if you already use React Navigation: it has a linking configuration that maps URLs to routes and handles the cold start case for you. See Deep Linking. The API Verified from the 0.87.1 type definitions: Method Returns Notes Rejects if nothing can handle it On iOS, gated by The URL that cold started the app Links arriving while running Opens your app's settings page Android only Native configuration Outbound links need no configuration on Android and one array on iOS. Inbound links need real setup on both. To be opened by a custom scheme ( ), register it: To use at all , list every scheme you will ask about. iOS returns for any scheme not in this array, even when the app is installed — it is an anti enumeration measure, not a bug: The array is capped at 50 entries and its contents a"},{"u":"platform-apis/biometrics.html","t":"Biometrics","s":"Platform APIs","d":"Face ID, Touch ID and Android BiometricPrompt through react-native-keychain 10 — gating a secret rather than gating a boolean, and the usage description that crashes without it.","h":["Why gating a secret beats gating a boolean","Native configuration","Basic example","How it works","Find out what the device has, before you offer it","Access control decides what the prompt actually enforces","Handling the outcomes","Always keep a non-biometric path","Platform differences","Security considerations","Common mistakes","Related topics"],"b":"React Native core has no biometric API . There is no export, no , nothing in the 0.87 surface that prompts for a fingerprint or a face. The verified answer is 10.0.0 , which is already in this handbook's dependency table. It is not a \"prompt for biometrics\" library — it is secure storage where an item can be locked behind biometric authentication. That distinction is the most important idea on this page, and it is what makes the feature actually secure rather than decorative. Why gating a secret beats gating a boolean The tempting shape is: Nothing was protected. The session token was already readable; the biometric check only decided whether to render a screen. An attacker who can run code in your process — a patched build, a debugger, a hooking framework on a rooted device — flips that boolean and walks in. The strong shape stores the secret in the Keychain or Keystore with an access control policy, so the operating system refuses to return the bytes until biometric authentication succeeds. There is no boolean to flip: without the prompt, the value does not exist in your process. Use biometrics for exactly this: releasing a credential, a token, an encryption key, or authorising a transaction. Do not use it as a screen lock over data you have already decrypted. Native configuration Face ID requires a usage description. A missing is a crash , not a denied prompt — the process is terminated the moment the Face ID prompt would appear. Touch ID does not require it, which is exactly why this gets shipped: the bug only reproduces on Face ID hardware. After installing, run in . No entitlement is needed for local biometrics; the Keychain Sharing capability is only relevant if you use to share items between your own apps. The library's own manifest already declares what it need"},{"u":"platform-apis/haptics.html","t":"Haptics","s":"Platform APIs","d":"Why core Vibration is not haptics, what react-native-haptic-feedback 3 gives you instead, and the permission and hardware realities on each platform.","h":["Why it exists — and when NOT to use it","What core gives you","Which library","Native configuration","Basic example","How it works","Types are semantic, not physical","There is a fallback chain, and it is documented","Intensity and custom patterns","Give the user a switch","Platform differences","Performance considerations","Accessibility considerations","Common mistakes","Related topics"],"b":"React Native core ships , and it is not a haptics API. It has two methods — and — and on iOS it is implemented as : a single, fixed, roughly 400 ms buzz with no pattern support and no intensity control . That is verified in the 0.87.1 source, which says so in the doc comment. Real haptics — the crisp tick when a picker snaps, the distinct success and error taps — come from UIKit feedback generators and Core Haptics on iOS, and from and on Android. None of that is in core. You need a library or native code. Why it exists — and when NOT to use it Haptics are confirmation you feel rather than read. They earn their place on a small set of interactions: a selection snapping into position, a long press being recognised, a destructive action completing, a form failing validation. In each case the feedback removes a glance. They are noise everywhere else. A tap on every button, a buzz on every list scroll, a vibration on every notification — users turn haptics off system wide because of apps that do this, and then your genuinely useful feedback goes with it. Two hard rules: never use haptics as the only channel for information, because a user with haptics disabled or a device without hardware gets nothing; and never buzz for something the user did not initiate. What core gives you is the right tool for exactly one thing: a deliberate, attention grabbing buzz, such as an alarm. It is the wrong tool for interface feedback, because a 400 ms motor spin is not a tick — it is the opposite of subtle. Which library 3.0.0. Verified from the published package: it declares with the spec name and ships a spec, so it is a real TurboModule. Peer dependencies are and . It maps a named haptic type onto the best mechanism each device has, with a documented fallback chain rather than a silent no"},{"u":"platform-apis/background-tasks.html","t":"Background Tasks","s":"Platform APIs","d":"What the OS actually lets a backgrounded app do, why the Community CLI has no first-party background API, and what doing it properly with BGTaskScheduler and WorkManager involves.","h":["What \"background\" actually means","What core does give you","AppState — knowing which situation you are in","Headless JS — Android only, and it needs native code","Native configuration","Doing it properly means native code","Common patterns that avoid the problem","Reconcile on foreground","Let the server push","Schedule a local notification instead of a task","Finish the work before you are suspended","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"There is no JavaScript API in React Native for \"run this code later, in the background\". There is no scheduler, no periodic task, no first party wrapper around either platform's mechanism. That absence is not an oversight — it reflects the fact that both operating systems treat background execution as something the system schedules, not something the app requests. This page is about setting expectations correctly first, and then showing what the real mechanisms are and where the boundary with native code falls. What \"background\" actually means The word covers four different situations, and most confusion comes from treating them as one. Situation What runs Duration App visible Everything Unbounded App backgrounded, not suspended Briefly, then nothing Seconds App suspended Nothing at all. Timers do not fire — OS scheduled wake up A native entry point the OS calls 30 seconds The third row is the one that surprises people. A suspended app runs no JavaScript. Your does not fire. A pending promise does not settle. A in flight is not resumed. The JavaScript context is frozen, and when the app returns to the foreground it resumes mid tick as though no time passed — which is why jumps. The fourth row is the only real background execution, and it is entirely native: the OS calls into your app at a time it chooses, gives you a short window, and expects you to report completion. is not this 0.87 replaced with the global. That schedules work during idle frames while your app is running . It has nothing to do with background execution and does not fire when the app is suspended. What core does give you Two things, and it is worth knowing exactly what each is. — knowing which situation you are in is . is the iOS in between state — the app switcher, an incoming call, a system dialog. "},{"u":"platform-apis/accessibility.html","t":"Accessibility APIs","s":"Platform APIs","d":"The accessibility props React Native actually exports in 0.87, what AccessibilityInfo can tell you, and where TalkBack and VoiceOver genuinely differ.","h":["Why it exists — and when NOT to use it","Native configuration","Basic example","The props, and what each one is for","accessible","accessibilityLabel","accessibilityHint","accessibilityRole","accessibilityState","accessibilityValue","accessibilityActions and onAccessibilityAction","importantForAccessibility — Android","accessibilityLiveRegion — Android","The aria-* aliases","AccessibilityInfo","Announcing a change","Platform differences","Common patterns","Hide decoration, expose meaning","Adapt behaviour, not just labels","Security considerations","Common mistakes","Related topics"],"b":"Accessibility is the one area in this section where core covers the ground properly. There is no library to evaluate and no native module to write: the props are on every component, and reports the user's settings. What core cannot do is decide what your UI means. A with an icon inside it is a button to you and an unlabelled tap target to a screen reader. Every prop on this page exists to close that gap, and every one of them is something only you know. Everything below was verified against and in the installed react native 0.87.1. Why it exists — and when NOT to use it Use these props whenever the rendered output does not describe itself. Icon only controls, custom toggles, progress indicators, anything built from and rather than from a platform control — all of it is opaque to assistive technology until you label it. The part people get wrong is the opposite direction: do not annotate what is already clear. A node with visible words already has an accessible name. Adding to a reading \"Order total\" makes the screen reader say it, and nothing else changes. Adding a label that differs from the visible text is worse — a voice control user says what they see, and if the accessible name does not match, the command fails. Native configuration There are no permissions and no keys for accessibility. That is worth stating plainly, because it is the exception in this section. What the tabs below cover is the configuration that does affect accessibility, and how to turn the screen reader on to test. Nothing is required. Two settings do affect what is announced: To test, enable VoiceOver in Settings → Accessibility → VoiceOver. Bind it to the triple click Accessibility Shortcut, because navigating a device with VoiceOver on in order to turn it off is itself a lesson. The simulator"},{"u":"native-modules/when-you-need-native-code.html","t":"When You Need Native Code","s":"Native Modules","d":"How to decide between JavaScript, an existing library and writing your own TurboModule or Fabric component.","h":["Why it exists — and when NOT to reach for it","Basic example — choosing the right extension point","How it works","Platform differences","Common patterns","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Writing native code is the most expensive decision you can make in a React Native project. It doubles the number of languages, build systems, CI steps and upgrade risks you own. This page is about deciding whether you have to, and — once you have decided — which of the three native extension points you actually need. Everything in this section assumes React Native 0.87 and therefore the New Architecture: TurboModules, Fabric and Codegen. There is no other architecture to target. The old Bridge was removed in 0.82, so any tutorial that starts by registering a module with the Bridge is describing a runtime that no longer ships. Why it exists — and when NOT to reach for it React Native exposes a large amount of platform capability through core APIs and through community packages that have already done the native work. Before you write a line of Kotlin or Swift, work down this list in order. Step Question If yes 1 Can this be done in JavaScript alone? Do that. No build changes, no platform parity work. 2 Does a core API already cover it? Use it. , , , , , cover a lot. 3 Is there a maintained library with Fabric/TurboModule support? Use it, after checking compatibility. 4 Do you need a platform SDK with no JS surface, a background service, or C/C++ that must not cross into JS? Write native code. Step 3 has a hard filter attached to it. A package that has not shipped New Architecture support is not a valid answer in 0.87. The interop layers may keep it limping along, but you are then depending on a compatibility shim for your core functionality. Check before you depend; see Native Dependency Compatibility. The honest reasons to write native code are narrow: A vendor SDK exists only as an and an . Payment terminals, DRM players, hardware peripherals, enterprise MDM agents. The"},{"u":"native-modules/turbomodules-end-to-end.html","t":"TurboModules End to End","s":"Native Modules","d":"The complete path for a native module in React Native 0.87 — spec file, Codegen, Kotlin, Swift, registration, autolinking and the TypeScript call site.","h":["Why the whole path matters","Step 1 — the spec file","Step 2 — Codegen, and where its output lands","Telling the build about your specs","What is generated, and where","Step 3 — the Kotlin implementation","Step 4 — the iOS implementation","Why there is an Objective-C++ file","The Swift class","The Objective-C++ wrapper","Step 5 — registration","Step 6 — autolinking","Step 7 — calling it from TypeScript","Platform differences","Common patterns","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"This page builds one real TurboModule from an empty folder to a typed call site, with nothing skipped. The example is a calendar module: it reads a constant, creates an event asynchronously, reports a queue depth synchronously, and toggles a setting. Everything here is React Native 0.87 on the New Architecture. There is no Bridge to register against, no macro to export a method, and no runtime name lookup — the spec file is the contract and the compiler on each platform enforces it. Why the whole path matters A TurboModule is not one file. It is a spec, generated code, two platform implementations, two registrations and a build configuration that ties them together. Tutorials that show you the Kotlin class and stop are the reason most first attempts fail at link time rather than at compile time. The seven steps below are in dependency order. Doing them out of order is the single most common way to waste an afternoon, because step 3 and step 4 both consume output produced by step 2. Step Artefact Who writes it 1 You 2 (Java) / (ObjC protocol) Codegen 3 You 4 + You 5 / You 6 , autolinking Mostly the CLI 7 The call site You Get the build green before you write logic Implement every method as a stub that throws, run both builds, and only then write the real code. Almost all of the difficulty in a native module is in the build graph, and a stub isolates that difficulty from your logic. Step 1 — the spec file The spec is ordinary TypeScript in your app. Codegen finds it by convention: it lives under the directory named in , and its filename starts with . The filename is load bearing. produces a generated class called on both platforms. Rename the file and every generated name changes with it. and are both type only exports of , and is a value export. Under the Strict TypeScri"},{"u":"native-modules/fabric-native-components.html","t":"Fabric Native Components","s":"Native Modules","d":"The complete path for a native view — spec, Codegen, the Android ViewManager, the iOS component view, registration, autolinking and the JSX call site.","h":["Why a component rather than a module","Step 1 — the spec file","Step 2 — Codegen configuration and output","Step 3 — the Android implementation","The view","The view manager","Step 4 — the iOS implementation","Step 5 — registration","Step 6 — autolinking","Step 7 — using it from JSX","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"A Fabric native component is a platform view that takes part in React's render pipeline: it has props diffed by C++, a shadow node that Yoga lays out, an event emitter, and a mounting layer that creates and recycles the real or . This page builds one from an empty folder to a in JSX, with nothing skipped. The example is a colour wheel: a custom drawn control that the user drags. The drag has to be handled on the platform's UI thread — round tripping every touch into JavaScript would tie the control's responsiveness to the JavaScript thread — and it has to lay out like any other view. That combination is what a Fabric component is for. Why a component rather than a module A component costs more than a TurboModule. Codegen emits a props struct, a state struct, a shadow node, a component descriptor and an event emitter on each platform, and you write a view manager on Android and a component view on iOS. Take that cost only when all three of these are true: Condition Why it forces a component Something must appear on screen Only a component participates in mounting It must take part in layout Only a shadow node is measured by Yoga It owns gestures, accessibility or recycling Those live on the platform view, not in a method call If the platform work can happen off screen and hand back data, write a module. If you can draw it acceptably with , and , write JavaScript. Step 1 — the spec file A component spec describes props, events and commands . It never describes methods — there is no object to call methods on. Codegen finds it by filename: a component spec's name ends with . Two naming rules fall out of that string and that filename: The filename must end in or Codegen will not parse it. The component name passed to is what every generated C++, Java and Objective C name is "},{"u":"native-modules/codegen-specs.html","t":"Codegen and Spec Files","s":"Native Modules","d":"The reference for spec files — how Codegen finds them, every type it accepts, every field of codegenConfig, and exactly what it emits where.","h":["Why the spec language is so restricted","How Codegen finds a spec","Module specs","The shape the parser requires","Types a module spec may use","Method shapes","Constants","Events","Platform-only modules","Component specs","The shape the parser requires","Prop types a component spec may use","Events","Commands","The codegenConfig reference","What is generated, and where","Modules","Components","Provider files generated for the app","Running Codegen by hand","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"This is the reference page for spec files. Codegen explains why the pipeline exists; this page is the detail you come back to when the build says at four in the afternoon. Everything below is how React Native 0.87's Codegen behaves. Where a rule comes from a regular expression or a hard coded name inside the generator, that is said explicitly, because those are the rules that fail silently rather than loudly. Why the spec language is so restricted Codegen parses your spec. It never executes it. A parser reading a type annotation can only understand a fixed vocabulary, and everything outside that vocabulary has no native equivalent to emit. That is also why the failures split into two very different categories: Failure Symptom The file is not recognised as a spec Nothing is generated. No error. The module or component is simply missing at runtime. The file is a spec but uses an unsupported type A build error naming the file, the method and the offending annotation. The second is the good failure. The first is the one that costs an afternoon, and it is almost always a filename. How Codegen finds a spec Three conditions must all hold. 1. The file is inside . That directory is read from and scanned recursively. 2. The basename matches . That is the literal regular expression in the generator. So: Filename Recognised As yes module yes component yes module, iOS only no — no is excluded no paths containing are excluded A platform suffix works because the basename is split on dots: two segments means platform agnostic, three means the middle segment is the platform. 3. The contents match. A module file must contain the text . A component file must have a default export of the form . A file that satisfies the filename rule but not this one is skipped in silence. Module specs The"},{"u":"native-modules/writing-a-module-in-kotlin.html","t":"Writing a Module in Kotlin","s":"Native Modules","d":"The Android half of a TurboModule in depth — the generated class, type mapping, threading, context lifetime, permissions, Gradle and R8.","h":["Why Kotlin, and what version","The generated class you extend","How spec types land in Kotlin","Returning structured data","Promises and coroutines","Threading","Context, activity and lifetime","Requesting a permission from a module","Gradle for a library module","R8 and ProGuard","Testing the Android half","Common mistakes","Related topics"],"b":"Android is the easier half of a native module, because the artefact Codegen produces is a JVM class and Kotlin is a JVM language. You extend the generated abstract class and the compiler tells you when your implementation and the spec have drifted apart. This page assumes you have read TurboModules End to End, which walks the whole path once. Here we stay on Android and go deeper: what the generated class actually looks like, how each spec type lands in Kotlin, which thread your code runs on, how long you may hold the context, and what the build needs. Why Kotlin, and what version React Native 0.87 bundles Kotlin 2.2.0 and requires Kotlin 2.0 or newer . Java still works — the generated class is Java and a Java subclass compiles fine — but every generated signature carries nullability information that Kotlin turns into types and Java does not. That is the practical argument: the same mistake is a compile error in Kotlin and a in Java. The toolchain numbers you need: Thing Value Kotlin 2.2.0 bundled; 2.0 minimum Android Gradle Plugin 9.x Gradle 9.x / 37 for a published library 34 AGP 9 ships its own Kotlin support and a new DSL, and React Native 0.87 is not yet built against either. Opt out in the app's : The generated class you extend For a spec file , Codegen writes into the Java package you named in . Reduced to its shape: Three consequences worth internalising: It is a class, not an interface. A method you forget is a compile error naming the method. There is no runtime surprise. and are already on the generated members. You do not write them, and you must not add an export macro or annotation of your own — the generated class is the registration. The constructor takes . Your subclass must pass it up. Read the real generated file is regenerated on every build and is t"},{"u":"native-modules/writing-a-module-in-swift.html","t":"Writing a Module in Swift","s":"Native Modules","d":"The iOS half of a TurboModule in depth — why Swift needs an Objective-C++ layer, how much of one, type mapping, threading, the podspec and the Experimental SPM path.","h":["Why the Objective-C++ layer exists","The four files, in order","1. The Swift class","2. The bridging header","3. The Objective-C++ wrapper","4. Registration","How spec types land in Objective-C and Swift","Threading","Errors","Packaging a library for iOS","Xcode settings that matter","Common mistakes","Related topics"],"b":"You can write the logic of an iOS TurboModule entirely in Swift. What you cannot do is make Swift the thing React Native talks to. The generated interface is an Objective C protocol whose methods take and return C++ types, and Swift cannot adopt it. This page is about that boundary: exactly where it sits, how thin you can make the layer that crosses it, and what the rest of the iOS build needs. It assumes you have read TurboModules End to End, which walks both platforms once. Why the Objective C++ layer exists Codegen writes two things into : , with one Objective C method per spec method plus a requirement that returns and takes . , a C++ class in namespace that holds the JSI bindings. Swift has no way to express either of those signatures. Apple's Swift/C++ interoperability does not cover of a polymorphic C++ type reached through an Objective C protocol, and the header itself cannot be imported into Swift because it is C++ rather than Objective C. So the layering is fixed: File Language Job Swift All of your logic. Knows nothing about React Native. generated by Xcode Exposes the Objective C visible surface of your Swift / Objective C++ Adopts the generated protocol, forwards to Swift The file is small and mechanical: one , one , and one forwarding method per spec method. It is not where your logic goes, and it should not grow. You can skip Swift entirely If you are comfortable in Objective C++, put the logic straight in and delete the Swift file. That removes a file, a bridging header and a generated header from the build. The Swift split is worth it when the logic is substantial or when your team writes Swift; it is overhead when the module is four short methods. The four files, in order 1. The Swift class What crosses into Objective C, and what does not: Swift featur"},{"u":"native-modules/autolinking.html","t":"Autolinking and react-native.config.js","s":"Native Modules","d":"How a native dependency reaches your build without you editing Gradle or the Podfile, every key of react-native.config.js, and what to do when the defaults are wrong.","h":["Why it exists — and what it does not do","Basic example — what happens on install","How it works","Android","iOS","Codegen and autolinking are the same pass","react-native.config.js in a library","Every dependency.platforms.android key","Every dependency.platforms.ios key","react-native.config.js in an app","Inspecting what the CLI actually found","Platform differences","Common patterns","Common mistakes","Related topics"],"b":"Autolinking is the step that turns into a registered TurboModule on both platforms. The React Native Community CLI scans your dependencies, works out which of them ship native code, and hands that list to Gradle and to CocoaPods. When it works you never think about it. When it does not, the symptom is almost always the same — throws on one platform — and the fix is almost always in . This page is about both halves. Why it exists — and what it does not do Before autolinking, adding a native dependency meant editing , , , the and sometimes the Xcode project. Five files per library, each with its own way of going wrong, each conflicting on every merge. Autolinking replaces all five with a scan. What it does not do is equally important: Autolinking does Autolinking does not Find packages with native code in Install pods for you — you still run Add Android subprojects and register their Register a package you wrote inside your own app Feed each library's into Codegen Decide which native modules your JavaScript imports Wire the library's podspec into the Podfile Add permissions, capabilities or keys The second row is the one that catches people. Your own app's is not in , so you add it to by hand — see TurboModules End to End. Basic example — what happens on install Three separate mechanisms run: 1. The CLI produces a config. walks dependencies, applies each library's , and emits JSON describing every linkable dependency. 2. Gradle consumes it. The settings plugin writes , includes each library as a Gradle subproject, and generates so contains the library's . 3. CocoaPods consumes it. in the reads the same config and adds a entry for each library's podspec. Codegen runs inside 2 and 3, over every discovered library's as well as your app's. How it works Android Two pieces of G"},{"u":"native-modules/platform-folders.html","t":"Platform Folders","s":"Native Modules","d":"What android/ and ios/ actually contain in a 0.87 project, which files are yours to edit and which are build output, and how Metro resolves .ios.tsx / .android.tsx.","h":["Why the folders are checked in — and what it costs","What android/ contains","Which of those you actually edit","The generated folders inside android/","What ios/ contains","Which of those you actually edit","The same setting, on both platforms","Platform extensions on the JavaScript side","How Metro resolves them","Choosing between an extension and Platform.select","Pairing platform files with a native module","Platform differences","Common patterns","Common mistakes","Related topics"],"b":"A project created by the Community CLI checks the whole Android project and the whole Xcode project into your repository. That is the single biggest difference between this path and a managed one: nothing regenerates or for you, so every file in them is yours — including the ones you should never touch. This page is a map. It says what each file is, whether editing it is normal or a mistake, and where the equivalent knob lives on the other platform. It then covers the JavaScript side half of the same idea: / files and how Metro picks between them. Why the folders are checked in — and what it costs Checking in the native projects buys you three things: you can add a native dependency that needs a Gradle or Xcode change, you can write your own native code without ejecting from anything, and what you build locally is what CI builds. The cost is that upgrades touch files you have edited. React Native ships template changes in every minor version — a new AGP property, a changed , a different Podfile helper — and those land in files that also contain your own edits. That is what the Upgrade Helper exists to diff. The practical rule that follows: make the smallest native edit that works, and leave a comment saying why. A three line diff against the template is easy to reapply. A rewritten is not. What contains The tree below is the 0.87 Community CLI template, read from the template repository rather than from memory. Which of those you actually edit File Edit it? What you edit it for Often , / , flavours, signing configs, Often Permissions, intent filters for deep links, , service declarations Often Icons, splash theme, strings, colours Sometimes Keep rules for a library R8 stripped Sometimes JVM heap, , the AGP 9 opt outs Rarely Registering a you wrote yourself Rarely , a cu"},{"u":"native-modules/publishing-a-native-library.html","t":"Publishing a Native Library","s":"Native Modules","d":"The package layout, package.json fields, codegenConfig and peer dependency ranges a React Native 0.87 library needs, what its consumers have to do, and where Swift Package Manager fits.","h":["What a consumer's build actually needs from you","Package layout","package.json, field by field","The spec file, shipped as source","Peer dependency ranges: how to choose one honestly","The Android side","The iOS side","Swift Package Manager support","Shipping pre-generated native code","What your consumers have to do","Testing the package before you publish","Versioning","Tooling worth knowing about","Security considerations","Threat","Exploit","Fix","Verification","Common mistakes","Related topics"],"b":"A React Native library is an npm package that also carries an Android Gradle project and an iOS podspec, plus the spec files Codegen reads. Nothing about publishing it is special — is — but the package has to be laid out in the way the Community CLI's autolinking expects, or it installs cleanly and then does nothing. This page is about the package, not about writing the native code. The implementation is in Writing a Module in Kotlin and Writing a Module in Swift. What a consumer's build actually needs from you Four things, and every layout rule below exists to deliver one of them. The consumer's build needs You provide it as Spec files to run Codegen over in your , and the specs inside it An Android Gradle project to include An directory with a An iOS pod to install A single at the package root JavaScript the app can import / (or ) pointing at built files inside Autolinking discovers all four by convention. A library that follows the convention needs no at all. Package layout Two details in that tree are load bearing: The specs ship in the published tarball. Codegen runs in the consumer's build, over your . If is excluded from , Codegen finds nothing and the module is never registered. This surprises people who assume only needs publishing. One podspec, at the root. The CLI finds the iOS project by globbing for a single . Two podspecs, or one nested inside , means you now need a to say which is which. , field by field Field Why it is there / / What a bundler and resolve for a consumer. Point at built output, not . Metro specific entry. Pointing it at lets Metro transpile your TypeScript directly, which is what makes a library debuggable in a consuming app. Read by ; not used at runtime. The allow list npm publishes. Getting this wrong is the single most common publishi"},{"u":"native-modules/debugging-native-code.html","t":"Debugging Native Code","s":"Native Modules","d":"Attaching Android Studio and Xcode to a running React Native app, reading native logs and crashes, and using Codegen output to diagnose a spec mismatch.","h":["Deciding which layer is actually broken","Native logs, from the terminal","Attaching a debugger","Reading Codegen output when a spec mismatch bites","Where to look","Regenerating deliberately","The three mismatches that actually happen","Proving a module is linked at all","Platform differences","Common patterns","Common mistakes","Related topics"],"b":"React Native DevTools debugs JavaScript. The moment a problem lives in Kotlin, Swift, Objective C++ or the generated C++ between them, DevTools shows you a call into a module and nothing after it. This page is about the other side of that call. The tools are the platforms' own: Android Studio's debugger and on one side, Xcode and LLDB on the other. Nothing here is React Native specific except where to attach and which generated file to read . Deciding which layer is actually broken Before attaching anything, work out which side of the boundary the failure is on. The symptom usually tells you. Symptom Layer Where to look first Registration / linking Autolinking config, , The call returns, but the value is wrong or Your native implementation A breakpoint in the Kotlin or Swift method The call throws a type error inside the generated code Spec mismatch The generated spec header or The app crashes with no JavaScript stack Native crash / the iOS crash report, then a native debugger It works in debug and fails in release Shrinking, or a bundled asset difference R8 keep rules, release only build settings It works on one platform only One half was never linked or never written That first row is the most common of all, and it is worth reading literally: the message says the module is not registered in the native binary . That is a linking problem, not a code problem, and no amount of stepping through your Kotlin will show it to you. Start at Autolinking. Native logs, from the terminal The CLI ships two log commands. They are convenience wrappers, and knowing exactly what they wrap is the difference between using them and being confused by them. takes no options at all in 20.2.0 — verified by reading the command definition in the published package. It filters to two tags, which m"},{"u":"animation/animated-vs-reanimated.html","t":"Animated vs Reanimated","s":"Animation and Gestures","d":"What core Animated actually gives you, where useNativeDriver stops, and why Reanimated 4 needs a separate worklets package.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What useNativeDriver actually does","Why Reanimated is different in kind","Honest comparison","Common patterns","Start with core Animated, escalate deliberately","useAnimatedValue over useRef","Do not mix the two on one value","Performance considerations","Common mistakes","Related topics"],"b":"React Native ships an animation library in the box. , , , and are all real exports of in 0.87 — you can verify that against the export list in the API Reference. Nothing has to be installed to fade a view in. Reanimated is a third party library that solves a different problem. It is not \"Animated but faster\"; it runs your animation logic as JavaScript on a second runtime pinned to the UI thread, so an animation keeps running while the React thread is busy. That difference in kind is what this page is about. Reanimated 4 does not bundle its worklets runtime declares as a required peer dependency , and the Babel plugin you add to is . Reanimated 3 shipped its own runtime and its own ; almost every tutorial predates the split and gives you an install that fails at runtime. The full install is in The UI Thread and Worklets. Why it exists / when to use it — and when NOT to Core is enough more often than the internet suggests. Use it when: The animation is driven entirely by React state or a timer — a fade, a press scale, a spinner. You only animate and , which is what supports. You do not want another native dependency in a project you have to upgrade twice a year. Reach for Reanimated when: The animation follows a gesture frame by frame. A drag that goes through React state will stutter the moment a render takes longer than a frame. You need to animate layout properties ( , , , ) smoothly. You want entering, exiting and layout transitions on mount/unmount without hand rolling measurement. You need per frame logic — clamping, snapping, physics — that has to run on the UI thread. A one off 200 ms fade does not justify a native dependency, a Babel plugin and a peer package. Write it with and move on. Basic example The same fade, written both ways. The Reanimated version is not"},{"u":"animation/worklets.html","t":"The UI Thread and Worklets","s":"Animation and Gestures","d":"What a worklet really is, how the Babel plugin transforms it, what crosses the serialisation boundary, and why calling a normal function from a worklet breaks.","h":["Installing Reanimated 4 and worklets","Why it exists / when to use it — and when NOT to","Basic example","How it works","The 'worklet' directive and what the plugin does","The serialisation boundary — what can be captured","Crossing back: runOnJS and scheduleOnRN","Going the other way: runOnUI and scheduleOnUI","Why calling a normal function from a worklet misbehaves","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"A worklet is a JavaScript function that has been marked for extraction so it can be sent to and executed on a second JavaScript runtime that lives on the UI thread. It is not a thread, not a web worker, and not a native callback. It is your function, compiled into a form that a different runtime can rebuild and run. Getting this model right is the difference between Reanimated feeling obvious and Reanimated feeling haunted. Most confusing Reanimated bugs — a value that never updates, a callback that never fires, a crash that says something cannot be cloned — are the serialisation boundary telling you that your mental model has one runtime in it when there are two. Installing Reanimated 4 and worklets This is the single most misreported fact about Reanimated 4 Reanimated 3 bundled its own worklets runtime and its own Babel plugin. Reanimated 4 does not. is a separate, required peer dependency, and the Babel plugin lives in that package. Verified from the installed packages: declares in with no marking it optional, and contains nothing but . Then add the plugin. The name is : With plugin options: Restart Metro with a cleared cache after changing , or the old transform stays in the cache and the symptoms make no sense: Version Worklets runtime Babel plugin Reanimated 3.x Bundled Reanimated 4.6.0 Separate package still resolves in 4.6.0 — it re exports the worklets plugin — so a copied from a blog config may appear to work. Write the worklets path. It is the one the pair is built around, and the shim is a compatibility courtesy, not the contract. Why it exists / when to use it — and when NOT to React renders on the JS thread. If that thread is busy — a long list re render, JSON parsing, a state update cascade — anything driven from it stops. A gesture following drag driven "},{"u":"animation/shared-values.html","t":"Shared and Derived Values","s":"Animation and Gestures","d":"useSharedValue, what .value really means, useDerivedValue, the animation helpers, and why a shared value is a mutation rather than React state.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","useSharedValue","Mutation, not state","useDerivedValue","useAnimatedStyle","The animation helpers","Cancelling","Common patterns","One shared value per animated quantity","Keep a starting offset for gestures","Props that are not styles","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"A shared value is one memory cell that both JavaScript runtimes can read and write. It is the only thing in Reanimated that is genuinely shared across the thread boundary, and almost everything else in the library is built on it. Getting one idea right makes the rest of Reanimated straightforward: a shared value is a mutation, not state. Writing to it does not re render anything, and reading it during render tells you nothing useful. This page assumes Reanimated 4.6.0 and 0.12.2 are installed with the Babel plugin configured. If they are not, start at The UI Thread and Worklets. Why it exists / when to use it — and when NOT to React state exists to trigger renders. That is exactly the wrong mechanism for animation: a value that changes sixty or a hundred and twenty times a second must not re render a tree each time, and it must keep changing while the JS thread is busy with something else. A shared value fills that gap. Use one when the value changes per frame, is driven by a gesture, or has to be readable from a worklet. Do not use one for anything the UI has to render as text or structure through React. If a number appears in a and must be correct on screen, it is state. The bridge between the two worlds is plus , and it costs a JS thread task each time — which is the point at which you should ask whether you needed the animation to drive React at all. Basic example No , no re render, and the animation keeps running if the JS thread stalls. How it works The hook returns the same object for the life of the component. It is not recreated on re render, and the initial value is only used on mount — passing a different later does nothing. The initialiser can be a function, which is called once on mount. Use that form when computing the initial value is expensive. The retur"},{"u":"animation/layout-animations.html","t":"Layout Animations","s":"Animation and Gestures","d":"Entering, exiting and layout transitions with Reanimated 4 — the animations you get without writing a single frame of motion yourself.","h":["Why it exists / when to use it — and when NOT to","What core LayoutAnimation gives you, and why it is not enough","Basic example","How it works","The three props","The builder API","The named animations","Lists","Keyframe for something the named animations do not cover","LayoutAnimationConfig","Common patterns","Stagger a list by index","Respect the accessibility setting","Animate presence, not height","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Layout animations are the ones you declare rather than drive. You say \"this view fades in when it mounts, slides out when it unmounts, and moves smoothly when its position changes\", and Reanimated measures the before and after and interpolates between them. They are the highest value per line feature in the whole library, because the alternative — measuring a view, storing the old position, animating the delta, and cleaning up on unmount — is tedious and easy to get subtly wrong. This page assumes Reanimated 4.6.0 with 0.12.2 and the Babel plugin configured; see The UI Thread and Worklets. Why it exists / when to use it — and when NOT to Use a layout animation when a component appears , disappears or changes size or position because something else changed . Those three cases cover most of the motion in a normal app: list insertions, a disclosure expanding, a validation message appearing, a chip being removed. Do not use them for motion you control frame by frame. A drag that follows a finger is a shared value and a gesture, not a layout animation — see Gesture Handler. Do not use them on a screen that is already scrolling fast with hundreds of mounting rows. Every entering animation is a measurement plus a per frame worklet, and a burst of them at once is visible. What core gives you, and why it is not enough React Native ships in the box. It is one global switch that animates every view that moved in the next commit: Core Reanimated layout animations Scope The whole next commit Per component Control Three presets and a config object Duration, easing, spring, delay, callback, custom keyframes Entering / exiting Create and delete only, same config for all Sixty odd named animations, plus your own Composability None — one global config Each component declares its own Cost"},{"u":"animation/gesture-handler.html","t":"Gesture Handler","s":"Animation and Gestures","d":"react-native-gesture-handler 3.3.0 — the hook-based gesture API, GestureHandlerRootView, composition and relations, and how gesture callbacks become Reanimated worklets.","h":["Installing","GestureHandlerRootView is mandatory","Why it exists / when to use it — and when NOT to","Basic example","How it works","The hooks are the API in 3.x","The callback names changed","The callbacks are worklets, automatically","Composition","Relations: gestures in *different* detectors","The deprecated Gesture builder","Platform differences","Common patterns","Swipe to dismiss, with velocity","Continue the motion after the finger lifts","Enable and disable without remounting","Performance considerations","Common mistakes","Related topics"],"b":"React Native's built in touch system — the gesture responder system behind and the components — runs on the JavaScript thread. Every move event is a JS callback, so a drag is only as smooth as your slowest render. replaces that with platform gesture recognisers that run on the UI thread and report through Reanimated worklets. The finger keeps its grip on the object even when the JS thread is busy, which is the entire point. This page documents version 3.3.0 , which reshaped the API. If a snippet you found uses it is two major versions out of date; if it uses it is one, and every symbol on that path is marked in the installed type definitions. Installing 3.x requires React Native 0.82 or newer — stated in the package's own README, not in its , which are unconstrained. Since 0.82 is the first version that is New Architecture only, that is the same as saying 3.x is a New Architecture library. There is no Babel plugin for this package and no side effect import to add at the top of : calls its own at module scope, so importing anything from it starts the native event listener. What you do have to add is the root view. is mandatory Every must have a above it. This is not a recommendation you can skip and get degraded behaviour — in 3.x the detector reads a context and throws in development when it is missing: GestureDetector must be used as a descendant of GestureHandlerRootView. Put it at the true root, outside your navigation container. One per app. Nesting a second one is harmless but pointless, and wrapping only the screen that happens to need a gesture guarantees a crash the first time someone adds a gesture elsewhere. Why it exists / when to use it — and when NOT to Use gesture handler when the interaction is continuous : a drag, a swipe to dismiss, a pinch, a pull down"},{"u":"animation/scroll-driven-animation.html","t":"Scroll-Driven Animation","s":"Animation and Gestures","d":"useAnimatedScrollHandler, Animated.ScrollView and Animated.FlatList, collapsing headers and parallax, and what scrollEventThrottle actually does under Reanimated in 0.87.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","useAnimatedScrollHandler has two forms","Animated.ScrollView and Animated.FlatList","scrollEventThrottle, and why you should stop copying 16","useScrollOffset, when you do not need a handler","Common patterns","Parallax without animating layout","Hide on scroll down, show on scroll up","Telling React about a threshold","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"A scroll offset is the second great source of per frame input, after a finger. Collapsing headers, parallax banners, progress bars, fading toolbars and page indicators are all the same shape: read the offset every frame, map it through , apply it as a style. The reason to do this with Reanimated rather than is the same reason as everywhere else in this section. A scroll event that goes through React re renders the tree once per frame while the user is scrolling, which is the worst possible moment to be doing extra work. A scroll handler that is a worklet never touches the JS thread at all. This page assumes Reanimated 4.6.0 and 0.12.2 are installed with the Babel plugin configured — see The UI Thread and Worklets. Why it exists / when to use it — and when NOT to Use a scroll driven animation when the thing you are animating must stay locked to the scroll position — moving with it, frame for frame, in both directions, and stopping exactly where the finger stops. Do not use one when the animation is a reaction to scrolling rather than a function of it. \"Hide the tab bar once the user has scrolled past 200 points\" is a threshold, and a threshold is better served by firing a once than by recomputing a style sixty times a second for an animation that only has two states. Goal Approach Header height shrinks as you scroll Interpolate the offset Banner moves at half scroll speed Interpolate the offset Toolbar fades in past a threshold + \"Back to top\" button appears past a threshold + , or an animated style with a clamped interpolation Pull to refresh — it is a platform control, not an animation Sticky section headers on — the platform already does it Basic example A collapsing header. The header is a sibling of the scroll view, not a child of it, so it can shrink without the li"},{"u":"animation/animation-performance.html","t":"Animation Performance Rules","s":"Animation and Gestures","d":"Which properties are cheap and why, how to keep a worklet inside its frame budget, what runOnJS really costs per frame, and how to measure instead of guessing.","h":["The bridge story is over","Rule 1: animate transform and opacity, not layout","The exception, stated honestly","Rule 2: a worklet runs on the thread that draws","Rule 3: never call back into JavaScript per frame","Rule 4: shadows and blurs are the expensive pixels","Rule 5: cost multiplies by count","Rule 6: measure, then change one thing","PerformanceMonitor — two frame rates, not one","useFrameCallback — the actual frame deltas","Turn Reanimated's own warnings into errors","And the platform tools","Platform differences","Common mistakes","Related topics"],"b":"An animation has a hard deadline. At 60 Hz you have about 16.7 milliseconds to produce a frame; at 120 Hz, about 8.3. Miss it and the user sees a stutter — and they see it during the interaction they are paying the most attention to, which is why animation jank feels worse than equally slow work elsewhere. Every rule below is a consequence of that deadline plus one fact about how React Native draws. None of them are style preferences. The bridge story is over Most animation performance advice on the internet is built on a component that no longer exists. Ignore every \"minimise bridge crossings\" article you find The Bridge was removed in React Native 0.82. There is no message queue between JavaScript and native any more, no JSON serialisation of every call, and no batching schedule to fight. Advice framed around \"reduce bridge traffic\" is describing a component that is not in the version you are running, and the fixes it recommends are frequently the wrong ones now. See The New Architecture for what replaced it. The two boundaries that are real today are different, and knowing which one you are up against is most of the diagnosis: The old framing What it actually is in 0.87 \"The bridge is slow, batch your calls\" JSI calls are direct C++ calls. There is nothing to batch \" avoids the bridge\" It moves ownership of the frame loop to native, which still matters — the reason is scheduling, not serialisation \"Fewer native module calls means faster animation\" TurboModules are lazily loaded and called synchronously. Call count is not the animation's problem \"Keep payloads small across the boundary\" Still true, but for a different boundary: / serialise arguments between the two JavaScript runtimes. That is Rule 3 So the two constraints that actually bind are the frame deadline and"},{"u":"animation/reduce-motion.html","t":"Respecting Reduce Motion","s":"Animation and Gestures","d":"Reading AccessibilityInfo.isReduceMotionEnabled and its change event, configuring Reanimated's ReduceMotion behaviour, and designing so that removing the motion does not remove the meaning.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Reanimated already honours it, by default","What \"reduced\" actually does, exactly","useReducedMotion() is a snapshot, and says so","ReducedMotionConfig — changing the default globally","Platform differences","Common patterns","Swap the motion, do not just delete it","Design so the final frame is the whole story","Never gate content on an animation","Let gestures keep tracking","Auto-play needs its own check","Performance considerations","Common mistakes","Related topics"],"b":"Both platforms ship a system setting that says \"I do not want animation\". People turn it on for concrete medical reasons — vestibular disorders, where large or parallax motion causes genuine nausea and dizziness, and migraine and seizure triggers — and for ordinary ones like battery and attention. When it is on, your app is being asked a direct question, and shipping the full motion anyway is the wrong answer. This is an accessibility requirement, not a polish item. The WCAG success criterion \"Animation from Interactions\" exists precisely for motion triggered by scrolling and other interaction, and the platform setting is how a mobile user expresses it. The good news is that React Native and Reanimated make honouring it almost free. The work is designing so that removing the motion does not remove any information — which is the part this page spends most of its time on. Why it exists / when to use it — and when NOT to Honour the setting for anything that is decoration or emphasis: entrance animations, parallax, bouncing springs, spinning loaders, sliding panels, zooming transitions, anything with a large travel distance or a scale change. You do not have to remove motion that is the interaction. A view that follows the user's finger during a drag is direct manipulation: the user is causing the movement, frame by frame, and freezing it would break the control. Apple's and Google's own system UIs keep drag tracking under reduced motion and remove the flourishes around it. Motion Under reduce motion Screen transition slide Replace with a cross fade, or nothing Parallax header Turn off — this is the canonical trigger Entrance animation on a list Skip; render in final state Spring bounce on a button press Remove the overshoot; keep the state change An object following the fi"},{"u":"performance/hermes-and-bytecode.html","t":"Hermes and Bytecode","s":"Performance","d":"What ahead-of-time bytecode and the absence of a JIT mean when you are tuning a React Native app, and which of your instincts about JavaScript performance stop applying.","h":["Why it matters for performance work","Basic example","How it works","The build pipeline, and where its knobs are","Memory-mapped bytecode, and what that buys","Where \"no JIT\" actually shows up","Garbage collection","Bytecode is version-locked","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Hermes explains what the engine is. This page is the part you need when you are holding a profile and deciding what to change: how bytecode changes the cost model, where the absence of a JIT actually shows up, and which JavaScript micro optimisations are worth nothing in a React Native app. The short version: your JavaScript is compiled to bytecode at build time and interpreted at runtime, with no tier up to machine code. Startup and memory are cheap; sustained numeric throughput is not. Why it matters for performance work Most JavaScript performance intuition comes from V8, where a JIT observes hot code and compiles it. Under that model, \"make the function monomorphic so it optimises\" is real advice. Under Hermes it is not, because nothing is going to optimise it. That cuts both ways, and the practical consequences are narrow: Instinct from a JIT engine Under Hermes Warm up a hot loop before measuring There is no warm up. First run is steady state. Keep object shapes monomorphic for inline caches Hermes has caches, but there is no speculative optimisation to deoptimise. Shape discipline is worth far less. Prefer over for speed The difference is noise next to one avoidable re render. Big bundles cost parse time at startup There is no parse phase. Bytecode is memory mapped. A tight numeric loop will get fast It will not. Move it to native. The one instinct that survives intact: allocation still costs , and retention costs more. See Memory. Basic example Because there is no JIT to hoist work out of a hot function for you, hoisting it yourself is one of the few source level changes that reliably pays: Inside a list called for hundreds of rows, that is a real difference. Inside a once per screen header, it is not worth the diff. Which one you have is a question for Measurin"},{"u":"performance/measuring-first.html","t":"Measuring Before Optimising","s":"Performance","d":"How to get a real number out of a React Native app — React Native DevTools, the React Profiler, Perfetto and Instruments — and why an unmeasured optimisation is a guess.","h":["Why it exists — and what goes wrong without it","Basic example","How it works — the four tools that matter","1. React Native DevTools","2. The React Profiler","3. Platform tracing — Perfetto and Instruments","4. The clock in your own app","Common patterns","Build a baseline before you touch anything","Change one thing","Measure on the worst device you support","Prefer a reproducible script to a manual poke","Performance considerations","Common mistakes","Related topics"],"b":"Every other page in this section assumes you already know which part of your app is slow. This page is how you find out. An optimisation applied without a measurement is a guess, and guesses in React Native are expensive: most of the advice you will find online was written for a runtime that no longer exists, and applying it costs you complexity in exchange for nothing. The discipline is small. Reproduce the problem, measure it in a build that resembles what users run, form one hypothesis, change one thing, measure again. Why it exists — and what goes wrong without it The three failure modes are all common enough to have a shape. Optimising the wrong phase. \"The list is janky\" is a symptom, not a cause. It can be JS thread work during render, it can be mount volume on the main thread, it can be image decoding, it can be a native module blocking the UI thread. Each has a different fix and the fixes do not overlap. See The Render Pipeline for the phases. Optimising a cost that is not there. Wrapping every component in , or rewriting into a loop, is work you can do without any evidence. It adds code, it adds bugs, and in the memo case it can make things measurably slower — see Render Performance and Memoization. Measuring a build nobody ships. A debug build compiles JavaScript at load, runs unminified, keeps development only assertions and ships the whole DevTools surface. Its numbers are not related to release numbers by any fixed factor. Debug numbers are not release numbers Before you report, compare or act on any timing, check which build produced it. The single most common \"React Native is slow\" report is a debug build. Basic example The cheapest useful measurement is a named trace around a suspect block. is exported from and its markers show up in an Android system t"},{"u":"performance/render-performance.html","t":"Render Performance and Memoization","s":"Performance","d":"Cutting render-phase cost with React.memo, useMemo and useCallback — including the cases where they make your app slower.","h":["Why it exists — and when NOT to reach for it","Basic example","How it works","What memo actually compares","The memo that never hits","When useMemo earns its keep","Where memoization does nothing at all","Cheaper than memoization: move the state down","React 19 details worth knowing","Common patterns","Stabilise from the top of the chain","Keep style objects out of render","Split large components","Let the profiler, not a rule, decide","Performance considerations","Common mistakes","Related topics"],"b":"The render phase is the JavaScript React runs to work out what changed: your component functions, the reconciliation, and the shadow nodes Fabric creates from the result. It runs on the JS thread, so while it runs nothing else in your JavaScript does. Cutting it is the most common performance fix in a React Native app. Memoization is the usual tool, and it is the most over applied technique in React. It is not free, and applied wrongly it is a net loss. This page is about both halves. Why it exists — and when NOT to reach for it React re renders a component when its state changes, when its context changes, or when its parent re renders. That last one is the expensive default: a state update near the root calls every component function beneath it, even the ones whose props are identical. interrupts that. It compares the new props with the old ones, shallowly, and skips the component if they match. The reason not to reach for it automatically is that the comparison itself has a cost, and so do the and calls you will add to make the comparison succeed. Each one allocates a dependency array, stores it on the fiber, and compares it element by element on every render — including the renders you were trying to skip. The rule Memoize a component when it is expensive to render and its props are actually stable . A memo on a cheap component that receives a new prop object every render is strictly worse than no memo: you pay the comparison, it fails, and you render anyway. Before applying any of this, open the Profiler and find out which components render and how often. See The Profiler and React Native DevTools. Basic example The case where memoization pays: a list row that is not trivial, receiving a stable item and a stable callback. How it works What actually compares does an "},{"u":"performance/list-performance.html","t":"List Performance in Depth","s":"Performance","d":"How FlatList virtualization actually works, what every windowing prop does, why inline renderItem closures cost you renders, and where FlashList wins and where it does not.","h":["Why virtualization exists","Basic example","How it works","1. The render window","2. Measurement","3. Batched rendering","The props, with their real defaults","Why getItemLayout matters so much","The two mistakes that cause most list jank","extraData and the stale-row trap","keyExtractor and identity","List state does not survive the window","FlashList — when it wins and when it does not","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Lists are where React Native performance problems concentrate. A screen renders once; a list renders its row component for every visible item, re renders it on every data change, and mounts and unmounts views continuously while the user's thumb is moving. Every cost on every other page in this section gets multiplied by the row count here. This page is the long version: what does internally, what each windowing prop actually controls, the two mistakes that cause most list jank, and an honest account of when is the answer and when it is not. Why virtualization exists A mounts all of its children. A thousand rows is a thousand mounted view hierarchies, their images, their text, and their shadow nodes — all created before the first frame and all held in memory for the life of the screen. mounts a window instead. It keeps the visible rows plus a configurable buffer above and below, and replaces everything outside that window with a pair of blank spacer views sized to the content it is standing in for. Scrolling moves the window: rows entering it mount, rows leaving it unmount. That is the whole idea, and everything else on this page is a consequence of it. When a ScrollView is the right answer Virtualization has overhead: measurement bookkeeping, batched rendering, and mount/unmount churn. For a short, bounded list — a settings screen, a form, twenty items — a is simpler and faster. The crossover is roughly \"more than fits in two or three screens\", not a fixed number. Basic example A list configured the way a list should be: a memoized row, a stable , a that returns a real identity, and because the rows are a fixed height. How it works is a thin wrapper over . The interesting behaviour is all in , and it comes down to three mechanisms. 1. The render window The list tracks a"},{"u":"performance/image-performance.html","t":"Image Performance and Caching","s":"Performance","d":"Why decoded size — not file size — dominates image cost, how to request the right resolution, and how the two platform image caches actually behave.","h":["Why it matters — the decode arithmetic","Basic example","How it works","The pipeline for a remote image","The three caches","Controlling the cache from the source","Prefetching","Reserving space","Vector and static assets","Platform differences","Common patterns","Ask the server for the right size","Keep source identity stable","Placeholder, then image","Cap what is on screen","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Images are the largest single source of memory pressure in most React Native apps, and one of the most common causes of list jank. The reason is a number people rarely compute: a JPEG's file size tells you about the download, and nothing at all about what it costs once it is on screen. Image covers the component. This page is about the cost model and what to do with it. Why it matters — the decode arithmetic An image in memory is uncompressed pixels. Four bytes per pixel for RGBA: Source File size Decoded size 4000 × 3000 photo 2 MB JPEG 4000 × 3000 × 4 ≈ 48 MB 1080 × 1080 avatar 200 KB 1080 × 1080 × 4 ≈ 4.4 MB 96 × 96 avatar 6 KB 96 × 96 × 4 ≈ 37 KB Displaying that first photo in a 120 pt thumbnail costs the same 48 MB as displaying it full screen, because the decoder does not know or care how large your view is. Ten of those in a list is half a gigabyte, which is an out of memory kill on a mid range Android device. Compression ratio is irrelevant to this number. Resolution is the only thing that changes it. Basic example Ask the server for the size you are going to display, in device pixels: Three things are happening: the URL requests a correctly sized image, keeps the object identity stable so on the row above still works, and the placeholder background gives the layout something to show while the request is in flight. How it works The pipeline for a remote image 1. Request. The image loader fetches the URL, subject to the HTTP cache. 2. Decode. Compressed bytes become a bitmap. This is CPU work and it allocates the full decoded size. 3. Cache. The bitmap goes into a memory cache; the compressed bytes usually go into a disk cache. 4. Upload and draw. The bitmap is handed to the GPU and composited. Step 2 is where the time goes on a cold load and step 2's allocation "},{"u":"performance/startup-time.html","t":"Startup Time","s":"Performance","d":"What happens between the app icon being tapped and the first useful screen, which parts you can influence, and what actually moves the number.","h":["Why it matters — and what \"startup\" means","Why it exists / when to optimise it — and when not","Basic example","How it works — the cold start timeline","Bundle size is not startup time","Lazy requires are already on","Module evaluation is the thing to hunt","TurboModules are lazy; your SDK initialisation is not","Deferring work after first paint","Platform differences","Common patterns","Render a real first screen, not a spinner","Move navigation state restoration off the critical path","Keep the root component small","Budget it","Performance considerations","Common mistakes","Related topics"],"b":"Startup is the one performance number every user experiences and almost nobody profiles, because the tools that are easy to reach — the Performance panel, the React Profiler — are attached after the interesting part is over. The good news is that the cost model is simple and most of it is under your control. The bad news is that the largest single cost in a typical app is module evaluation at the entry point, and that is invisible unless you go looking for it. Why it matters — and what \"startup\" means Three launches with very different costs share the word: Launch Starting state What you pay Cold Process does not exist Process creation, native init, React Native host setup, bundle load and evaluation, first render Warm Process exists, activity/scene destroyed Some native init, full JavaScript re init in most setups Hot App in the background Essentially nothing — a resume Cold start is the number that matters. It is what a user gets after a reboot, after the system reclaims memory, and on first install. Time to Interactive (TTI) is the metric worth tracking: the moment the user can meaningfully act, not the moment something is painted. A splash screen paints immediately and interacts never. Why it exists / when to optimise it — and when not Startup work is worth doing when your cold start on a mid range device exceeds roughly two seconds, or when you have added a feature and want to know what it cost. It is not worth doing on a flagship device, because a flagship hides the problem entirely. It is also the one area where the improvement is permanent: unlike render tuning, which decays as the screen grows, removing a module from the startup path stays removed. Basic example React Native exposes its own startup markers on the global. They are not part of the public type sur"},{"u":"performance/bundle-size.html","t":"Bundle Size","s":"Performance","d":"Measuring the JavaScript bundle with a source map, what Metro will and will not eliminate, and why the APK or IPA the user downloads is a different number entirely.","h":["Why it matters — and when it does not","Basic example — measuring the JavaScript bundle","How it works","Reading the treemap","Metro's tree-shaking limits — this is the important part","The app package is a different measurement","Common patterns","Track it, do not audit it once","Check for duplicate dependencies","Look at assets before JavaScript","Prefer narrow entry points over configuration","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Two numbers get called \"bundle size\" and they behave differently. The JavaScript bundle is one asset inside your app. The APK, AAB or IPA is what the user downloads, and the JavaScript bundle is usually a small part of it. Neither one is startup time. Because Hermes bytecode is memory mapped and faulted in lazily, code that never executes costs disk rather than launch. See Startup Time for that distinction, which is the most useful thing to get straight before spending a day on this page. Why it matters — and when it does not Bundle size matters for: Download and install size , which affects conversion on store pages and matters a lot on metered connections and cheap devices. Disk footprint , which matters on 32 GB devices where the OS evicts apps. Review time and CI , indirectly. It does not meaningfully affect: Startup , except through the small cost of faulting in pages that execute. Runtime speed of code you never call. So: optimise it when the download is large enough that a user might notice, and do not optimise it as a proxy for performance. Basic example — measuring the JavaScript bundle Produce a production bundle and its source map, outside of any platform build: Every flag there is real: , , , and are all declared by the CLI's bundle command in 0.87. is what turns on minification. Then attribute the bytes back to your source files with the source map: is a general source map tool with no React Native coupling, which is why it works here at all. It opens a treemap: each rectangle is a source file, sized by how many bytes of the minified bundle it accounts for. What you are measuring This is Metro's minified JavaScript, before . The shipped artifact is bytecode compiled from this file, so the two sizes differ — but the proportions between your modules are what "},{"u":"performance/memory.html","t":"Memory","s":"Performance","d":"The two heaps a React Native app has, how Hermes collects the JavaScript one, the leaks that actually happen, and how to see each kind of memory.","h":["Why it matters","Basic example — the leak that actually happens","How it works","The Hermes heap","Native memory","The leaks worth knowing","Seeing each kind","Platform differences","Common patterns","Clean up in every effect that subscribes","Bound every cache","Keep list windows small","Release large data when you navigate away","Abort in-flight work on unmount","Performance considerations","Common mistakes","Related topics"],"b":"A React Native app has two separate pools of memory and almost every confusing memory investigation starts with mixing them up. The JavaScript heap holds your objects, closures and component state, and is collected by Hermes. Native memory holds views, decoded images, native module state and the engine itself, and is not visible from JavaScript at all. A heap snapshot that shows 12 MB while the process uses 600 MB is not a broken tool. It is the correct answer to a different question. Why it matters Running out of memory is not a slowdown, it is a kill. Android's low memory killer terminates your process; iOS's jetsam does the same. To the user both look like \"the app closed itself\", and neither produces a JavaScript stack trace. Before that point, memory pressure shows up as: Background termination, so every app switch becomes a cold start. More frequent and longer garbage collections. Images being evicted and re decoded while scrolling. Basic example — the leak that actually happens Subscriptions outliving their component is the most common JavaScript leak in React Native, and it is entirely preventable with a cleanup function: The leak is not the subscription object; it is the closure it holds, and whatever that closure captured. A handler that captures a component's whole props object pins that object, its children and any data hanging off it for the life of the subscription. How it works The Hermes heap Hermes uses a generational, mostly concurrent collector. Most collection work happens on its own thread rather than stopping JavaScript, and short lived allocations are cheap — the objects a render produces and discards cost very little. What costs: Promotion. An object that survives long enough is moved to the old generation, where collecting it is more expensive. "},{"u":"performance/profiling.html","t":"The Profiler and React Native DevTools","s":"Performance","d":"The recording workflow — JavaScript flame charts, React commits, heap snapshots and platform traces — and how to read what each one gives you.","h":["Why it exists — what each recording answers","Basic example — a recording worth reading","How it works","The Performance panel","The React Profiler","Heap snapshots","Platform traces","Common patterns","Profile one interaction, not one session","Use the Components highlighter before you record anything","Compare like with like","Keep the fix's measurement outside the profiler","Write down what you found","Performance considerations","Common mistakes","Related topics"],"b":"This is the practical companion to Measuring Before Optimising: not which tools exist, but how to drive them and what the output actually means. Four recordings answer four different questions, and picking the wrong one is how an afternoon disappears. React Native DevTools covers the tool itself — opening it, connecting, and the panels that are not about performance. Why it exists — what each recording answers Question Recording Where is JS thread time going? Performance panel — a sampled flame chart Which components re render, how often, and why? Profiler ⚛ panel — React's own commit data What is retaining memory? Memory panel — two heap snapshots, compared The JS thread is idle and the UI still stutters Platform trace — Perfetto or Instruments If you cannot say which of those four you are asking, stop and work that out first. Each recording is cheap; interpreting the wrong one is not. Basic example — a recording worth reading Every useful recording has the same shape. 1. Build Release. A debug build compiles JavaScript at load and keeps development assertions. 2. Reproduce the problem manually once , so you know exactly what to do. 3. Open DevTools — press <kbd j</kbd in the Metro terminal, or use the Dev Menu. 4. Start recording, perform the one interaction, stop. Not thirty seconds of everything. 5. Read the widest bar, not the tallest stack. Step 4 is where most recordings go wrong. A profile of \"using the app\" contains your problem and forty other things, and the forty other things are easier to see. How it works The Performance panel This is a sampling profiler: it interrupts the JavaScript thread at intervals and records the stack. It does not instrument every function call, which is why the overhead is tolerable and why very short functions can be invisible. Re"},{"u":"performance/common-performance-mistakes.html","t":"Common Performance Mistakes","s":"Performance","d":"The React Native performance advice that stopped being true, the APIs that no longer exist, and what replaced each of them in 0.87.","h":["Why this page exists","The removed APIs","InteractionManager — removed","Standalone react-devtools — removed","The architecture flags — ignored","FlashList's estimatedItemSize — not a v2 prop","The Bridge advice","The memoization folklore","The list folklore","The measurement folklore","The animation folklore","Common mistakes","Related topics"],"b":"React Native's runtime changed fundamentally between 0.76 and 0.87. Most performance advice online predates that change, and a lot of it is not merely outdated — it tells you to optimise a component that has been deleted. This page is the list. Each entry is a piece of advice you will find in tutorials, why it was correct once, and what the current answer is. Why this page exists Stale performance advice is worse than no advice. It costs you the time to implement it, the complexity it adds to your code, and the time you did not spend on the thing that was actually slow. It is also convincing: it was written by people who had measured something real, on a runtime that no longer ships. Before acting on anything you read — including this page — get a measurement. See Measuring Before Optimising. The removed APIs These do not exist in 0.87. Code using them does not run, and advice built on them is dead. — removed The old advice: wrap non urgent work in so it waits for animations and touches to finish. Why it existed: it was React Native's only deferral primitive, tied to the old touch and animation bookkeeping. What to use now: the global . It is installed by React Native's runtime setup and schedules a callback when the JavaScript thread has slack, with an optional timeout ceiling. fails the build on any mention of the removed API outside a page like this one that exists to warn about it. Standalone — removed The old advice: run in a second terminal to inspect the component tree. Why it existed: the component inspector lived outside the debugger. What to use now: React Native DevTools. The Components ⚛ and Profiler ⚛ panels are inside it. Open it with <kbd j</kbd in the Metro terminal or from the Dev Menu. See React Native DevTools. The architecture flags — ignored The old"},{"u":"debugging/react-native-devtools.html","t":"React Native DevTools","s":"Debugging and DevTools","d":"The built-in debugger — how to open it in 0.87, what each panel does, how it connects over the Chrome DevTools Protocol, and what it cannot do.","h":["Why it exists / when to use it — and when NOT to","Basic example — opening it","When several devices are connected","How it works","The connection path","Where the window opens","The panels","Sources and breakpoints","Inspecting components","Platform differences","Common patterns","Check /json/list when nothing connects","Use the Console against live state","Keep the debugger closed when measuring","Reconnect after a reload","Security considerations","Common mistakes","Related topics"],"b":"React Native DevTools is the debugger that ships with React Native. It is a Chrome DevTools frontend that talks to Hermes over the Chrome DevTools Protocol (CDP) , which Hermes implements directly. That means breakpoints, stepping, a console bound to the real runtime, and a profiler — in the engine your app actually ships with, rather than in a desktop browser engine pretending to be it. It replaced a scattering of older tools, and in 0.87 it is the only supported one. Why it exists / when to use it — and when NOT to Before it existed, debugging a React Native app meant choosing between tools that each saw part of the picture: a remote debugger that ran your JavaScript in Chrome's engine (so the numbers were wrong and native calls behaved differently), a standalone component inspector in another terminal, and a separate network inspector. React Native DevTools sees one runtime: the Hermes instance in your app, on the device. When not to use it: Release builds. The inspector is a development feature. A release build has no debugger to attach to — that is what Crash Reporting and Reading a Release Stack Trace are for. Performance measurement of startup. Attaching the debugger changes startup materially. Use the platform launch tools instead; see Startup Time. Native crashes. A JavaScript debugger cannot see a native signal. See Native Crash Logs. Standalone react devtools was removed Its WebSocket support was removed in 0.87. Do not run — the component inspector and the React profiler are panels inside React Native DevTools now. Basic example — opening it Start Metro, run the app, then either: Those three key bindings are exactly what the 0.87 CLI registers, printed by Metro itself when it starts in an interactive terminal. Or open the Dev Menu on the device and choose Op"},{"u":"debugging/console-and-logs.html","t":"Console and Logs","s":"Debugging and DevTools","d":"Where console output actually goes in 0.87 — the DevTools console, LogBox, adb logcat and the iOS device console — and what changed for unhandled promise rejections.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The four destinations","Metro no longer streams your logs","LogBox","Unhandled promise rejections raise console.error — since 0.82","What survives into a release build","Platform differences","Common patterns","Log an identifier, look the rest up","One logger, not scattered console calls","Clear, reproduce, read","Use console.error deliberately","Keep ignoreLogs patterns narrow and commented","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"is the first debugging tool anyone reaches for, and in React Native it is also the one with the most surprising plumbing. The same call can land in four different places depending on how the app was built and what is attached to it, and in 0.87 one of those places — the Metro terminal — is off by default. This page is about where output goes, which log surface answers which question, and the behavioural change in 0.82 that made rejected promises loud. Why it exists / when to use it — and when NOT to A log line is the cheapest possible instrument. It survives a reload, it works on a physical device with no cable attached, and it costs nothing to add. That is genuinely useful for questions of the form \"did this code run, and with what\". It is the wrong tool for three things: \"What is the value of this right now.\" The React Native DevTools console evaluates expressions against the live runtime. Reading state there beats adding a log and reloading. \"Why is this slow.\" Timing with logs measures the logging as well. See The Profiler and React Native DevTools. \"What did the app do before it crashed in the field.\" Device logs are not collected for you. That is Crash Reporting. And there is one hard rule that belongs at the top rather than the bottom: calls are not removed from a release build. Everything you log ships, runs, and writes to the platform log on a user's device. Safe Logging in Release Builds is the page that deals with that properly. Basic example The console methods React Native's polyfill provides are , , , , , , , , and . and are special: they also feed LogBox. is not part of the polyfill React Native's console does not declare / . Measure with deltas, a region, or the Performance panel — see The Profiler and React Native DevTools. How it works The four destina"},{"u":"debugging/source-maps.html","t":"Source Maps","s":"Debugging and DevTools","d":"How Metro and Hermes produce maps, how the two are composed into one, where each build system writes them, and why you must keep the map as a build artifact.","h":["Why it exists / when to use it — and when NOT to","Basic example — generate a map by hand","How it works","Two maps, because there are two compilers","Where each build system puts it","Hermes changes what \"minified\" means","Metro 0.87 makes this cheaper","The dev server symbolicates for you","Common patterns","Name the map after the build that produced it","Upload maps as part of the release job, not by hand","Keep maps private","Verify the map before you need it","Do not ship the map inside the app","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"A source map is a lookup table from a position in generated output back to a position in the code you wrote. Without one, a stack trace from a shipped app is a list of offsets into a bundle nobody can read. With one, it is a list of your files and line numbers. React Native builds two maps per release bundle and composes them into a single file. Understanding that two stage shape is what makes the difference between \"symbolication does not work\" and knowing exactly which of the two halves is missing. Why it exists / when to use it — and when NOT to You need a map in exactly three situations: 1. Reading a release stack trace from a crash reporter or a bug report. See Reading a Release Stack Trace. 2. Analysing bundle size , because the map is what attributes bytes to modules. See Bundle Size. 3. Debugging in the Sources panel , where the map is why you see your own files rather than the bundle. That one is handled for you by the dev server. You do not need to think about maps during normal development. Metro serves them to the debugger automatically, and LogBox resolves its stack traces through the dev server. The work starts at the moment you produce a build you cannot attach a debugger to. The map is only useful if you kept it A source map is generated during the build and then, by default, thrown away with the rest of the build directory. If you have not archived it alongside the artifact that produced it, the crash report you get next week is unreadable and there is no way to reconstruct it. Treat the map as a release artifact with the same care as the signing key's fingerprint or the . Basic example — generate a map by hand The CLI's command produces the JavaScript bundle and its map in one pass. These option names are declared by the 0.87 bundle command itself: is "},{"u":"debugging/network-inspection.html","t":"Network Inspection","s":"Debugging and DevTools","d":"The Network panel in React Native DevTools, exactly which requests it can and cannot see, and the proxy workflow for everything else.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The instrumentation is native, not a JavaScript patch","What the panel sees, and what it does not","Correlating with the rest of the tooling","Platform differences","Common patterns","Seeing native-side traffic: an intercepting proxy","Certificate pinning defeats proxying — on purpose","Add a request id and log it, not the payload","Reproduce with curl before blaming the client","Use a local mock server when you need to change responses","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"The Network panel in React Native DevTools lists the requests your app makes, with timing, headers and payloads, in the same frontend you use for breakpoints. For anything your JavaScript initiates, it is the fastest answer to \"did that request go out, what did it carry, and what came back\". It is also the tool whose boundaries are least obvious. A significant share of an app's traffic is initiated below JavaScript and never appears in the panel at all. Knowing where that line sits saves you from concluding that a request was never made when it was made and simply not reported. Why it exists / when to use it — and when NOT to Use the Network panel when the question is about a request you wrote: the URL, the headers, the body, the status, the ordering, the duration. Reach for something else when: The traffic comes from a native module or an SDK. Analytics, ads, push registration, a native HTTP client inside a third party library — none of it is visible here. Use a proxy. You need to modify a request or a response. The panel observes; it does not rewrite. Use a proxy with a rule, or a local mock server. The app is a release build. CDP network reporting is disabled in a production build by design. A proxy still works on a release build, subject to the trust caveats below. The question is really about caching or retries. That behaviour lives in your data layer. See Data Fetching and Caching. Basic example Open DevTools (press <kbd j</kbd in the Metro terminal), select Network , and reproduce the request. A from your code appears immediately. The panel is most useful with a request id header that your server also logs. Then a slow or failed request in the panel maps to a specific server side trace without guessing. How it works The instrumentation is native, not a JavaScript"},{"u":"debugging/error-boundaries.html","t":"Error Boundaries","s":"Debugging and DevTools","d":"What a React error boundary actually catches — and the much longer list of what it does not — plus the global JavaScript handler and what a production fallback should do.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Where to put boundaries","Resetting, and the retry loop trap","The global JavaScript error handler","Unhandled rejections, precisely","What a production fallback should do","Platform differences","Common patterns","One boundary per screen, plus one per optional region","Report from componentDidCatch, render from getDerivedStateFromError","Use the boundary name in the report","Pair every boundary with a state machine for async failure","Test the fallback","Performance considerations","Common mistakes","Related topics"],"b":"An error boundary is a React component that catches an error thrown while rendering its subtree and renders a fallback instead of unmounting the whole app. Without one, a single thrown error in one component tears down the entire React tree and leaves the user looking at a blank screen. That is a real and worthwhile protection. It is also much narrower than most people assume, and the gap between what boundaries catch and what people think they catch is where a lot of bad error handling lives. Why it exists / when to use it — and when NOT to React's recovery model is all or nothing by default: if rendering throws and nothing catches it, React unmounts the tree rather than leaving a half rendered UI on screen. On the web that shows a blank page; in a native app it shows a blank screen inside your chrome, which looks like a freeze. A boundary converts that into a bounded failure: this screen is broken, the rest of the app is not. What an error boundary catches: Errors thrown during rendering anywhere in its subtree. Errors thrown in lifecycle methods of class components below it. Errors thrown in constructors below it. Errors thrown synchronously in the body of an effect , because effects run as part of the commit React is already inside. What an error boundary does NOT catch: Not caught Why What to use instead Event handlers An runs outside React's rendering work / in the handler Asynchronous code , , a callback — the stack has left React / around the , or a rejection handler Rejected promises Same reason. Since 0.82 these raise , which is visibility, not handling Handle the rejection; put the failure in state Errors thrown by the boundary itself It cannot catch its own render A boundary above it, kept trivially simple Native crashes A signal in native code is not a Java"},{"u":"debugging/crash-reporting.html","t":"Crash Reporting","s":"Debugging and DevTools","d":"Getting errors from devices you do not own — what a crash reporter has to cover in a React Native app, how to choose one, and how to wire your app so the choice is reversible.","h":["Why it exists / when to use it — and when NOT to","The three failure classes","Basic example — wire a seam, not an SDK","How it works","What makes a report useful","Symbolication is a build-time obligation","Choosing a reporter","Do not build your own","Platform differences","Common patterns","Breadcrumbs beat stack traces","Report non-fatals from your boundaries","Attach a release identity you control","Do not report expected failures","Watch the trend, not the count","Alert on new groups, not on volume","Test the pipeline before you need it","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Everything else in this section is a tool you point at a device in front of you. Crash reporting is the one that works on the ten thousand devices you will never touch. It is also the only way to learn that a bug exists at all: users do not file tickets, they uninstall. A React Native app fails in three distinct ways, and a reporting setup that covers only one of them gives you a clean dashboard and a one star review page full of crashes you cannot see. Why it exists / when to use it — and when NOT to The platform stores already give you something. Google Play Console and App Store Connect both collect native crashes and ANRs from real installs, for free, with no SDK. That is a real baseline and you should be reading it. What the stores do not give you: JavaScript errors , unless they escalated into a process crash. Non fatal errors — the ones a boundary caught and recovered from, which are most of them. Context : what screen, what request, what the user tapped, which feature flag. Timeliness : store reporting is aggregated and delayed. You want to know within minutes of a bad release, not the next morning. Release health : what share of sessions on version 3.4.1 are crash free, compared with 3.4.0. That gap is what a crash reporting service fills. When not to reach for one: for a bug you can reproduce on your desk. Crash reporting is for failures you cannot reproduce, on builds you cannot attach to. Reproducible bugs belong in React Native DevTools, which is faster and tells you more. The three failure classes Any reporting setup should be evaluated against this table, because a tool that only covers the first row is common and insufficient. Failure Example What has to catch it Fatal JavaScript error An error escapes every boundary; the runtime cannot continue The glob"},{"u":"debugging/native-crash-logs.html","t":"Native Crash Logs","s":"Debugging and DevTools","d":"Reading the crash artefacts below JavaScript — logcat, tombstones and Play Console ANRs on Android, the device console, .ips reports and dSYM symbolication on iOS.","h":["Why it exists / when to use it — and when NOT to","How a JavaScript crash looks from down here","Platform differences","Common patterns","Establish the layer before you investigate","Reproduce with a debug build attached","Archive symbols as a build step","Suspect memory before you suspect a bug","Check the library before you debug it","Common mistakes","Related topics"],"b":"When the process dies, there is no JavaScript left to tell you about it. No error boundary runs, no global handler fires, and nothing you wrote gets a chance to log. What you get instead is a native crash artefact produced by the operating system, in a format that has nothing to do with React. This page is about reading those artefacts. It is the most platform specific page in this section, because Android and iOS share nothing here — not the tools, not the file formats, not the vocabulary. Why it exists / when to use it — and when NOT to Reach for native crash logs when: The app disappears rather than showing an error. A JavaScript error in a release build ends up here too, but as a native crash with the JavaScript detail buried inside it. The crash happens before JavaScript starts — during native initialisation, or while loading the bundle. A native module is involved, yours or a dependency's. The app is killed under memory pressure , which is not an exception at all. The app freezes — an ANR on Android, a hang on iOS. Nothing crashed; the main thread stopped responding. Do not start here for an ordinary JavaScript bug. A red box in development, or a symbolicated stack from your crash reporter, is a far more direct route. Start here when the JavaScript tools show you nothing because there was no JavaScript left to run. How a JavaScript crash looks from down here Worth knowing before you read a report, because it saves you from investigating the wrong layer. An unrecovered fatal JavaScript error in a release build is escalated into a native failure: Android: it surfaces as a . The JavaScript stack is inside the exception's message — readable, but unsymbolicated unless you apply your source map. iOS: React Native's fatal path ( / ) raises an exception whose name identif"},{"u":"debugging/release-stack-traces.html","t":"Reading a Release Stack Trace","s":"Debugging and DevTools","d":"What a Hermes release trace actually looks like, why it is full of bytecode offsets rather than minified names, and the exact commands that turn it back into your source.","h":["Why it exists / when to use it — and when NOT to","What a release trace looks like","Why \"address at\" and not minified names","Basic example — symbolicate a whole trace","How it works","The full procedure","Verifying the map before you trust the output","Other input formats metro-symbolicate accepts","When the trace is inside a native crash report","When you do not have the map","Platform differences","Common patterns","Symbolicate on the commit, not on the branch","Keep a symbolicate script in the repository","Test the pipeline on purpose, once per release process change","Grep by function name when the map is gone","Read the whole stack, not the top frame","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"A stack trace from a release build looks like nothing you wrote. It names functions you half recognise and points them at a single line of a file called , with a number in the hundreds of thousands where a column should be. That number is not a column. Understanding what it actually is turns symbolication from a ritual into a two command procedure — and tells you immediately when the trace cannot be symbolicated at all, so you stop trying. Why it exists / when to use it — and when NOT to You are reading a release trace because there is no debugger to attach. It arrived from a crash reporter, a store console, a tester's screenshot, or your own release build on your own desk. Do not do this by hand when your crash reporter already symbolicates for you — that is what you uploaded the source map for. Do it by hand when: The reporter failed to symbolicate and you need to know why. The trace came from somewhere with no reporter — a QA bug report, a support ticket. You are verifying that your source map pipeline actually works, before you need it. You want to check one frame rather than read a whole dashboard. What a release trace looks like React Native parses Hermes stacks with a known grammar, so the shapes below are exact rather than approximate. A frame is one of three things: Frame shape Meaning A bytecode frame. is 1 based; the last number is a 0 based virtual offset into the bytecode , not a column. A source frame — the bundle still had a source location. is 1 based here. A frame inside the engine or a host function. Nothing to symbolicate. Hermes truncated the middle of a deep stack. Two frames you can ignore on sight: anything in is Hermes' own internal code, and frames have no JavaScript source at all. Why \"address at\" and not minified names This is where web intuit"},{"u":"testing/jest-setup.html","t":"Jest Setup","s":"Testing","d":"Configure Jest for a React Native 0.87 project with @react-native/jest-preset, and understand what the preset actually does.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Common patterns","Adding your own setup file","Letting an untransformed dependency through","Choosing the platform under test","Code that is worth unit-testing","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Jest is the test runner a Community CLI project ships with. React Native 0.87 provides , a preset that teaches Jest how to resolve platform extensions, transform JSX and Flow typed core files, stub out assets, and install the globals the runtime expects. Without it, importing in a test throws on the first syntax it does not understand. This page covers the unit test layer only: fast, no device, no simulator. Behaviour that needs a real app on a real device belongs in End to End with Detox or Maestro. Why it exists / when to use it — and when NOT to React Native's JavaScript is not plain ES modules. Core files use Flow syntax, components resolve through / extensions, and is a Metro concept that Node knows nothing about. Jest needs all of that explained to it, and the preset is the explanation. Use Jest for logic you can assert without a screen: reducers, selectors, formatting, hooks, navigation param handling, and component behaviour through React Native Testing Library. Do not use Jest to prove that your app launches, that a native module is correctly linked, or that a Fabric component draws anything. Jest runs in Node with every native call mocked, so those tests pass whether or not the native side exists. That is a job for an end to end run. Basic example A project created with ships exactly this file: That is the whole default configuration. The matching script is: And the relevant dev dependencies the 0.87 template installs: Run it: How it works is a small object. Reading it is the fastest way to understand why your tests behave the way they do: Key What it sets Why it matters to you A bare import resolves before . Your tests run the iOS branch unless you say otherwise. , , The platform suffixes the resolver will consider at all. maps and to the package directory, p"},{"u":"testing/testing-library.html","t":"React Native Testing Library","s":"Testing","d":"Render and query components with @testing-library/react-native 14, including the renderer peer dependency that changed and trips up most upgrades.","h":["Why it exists / when to use it — and when NOT to","Installation — the part that changed","Basic example","How it works","Common patterns","Waiting for something asynchronous","Rendering inside the providers the component needs","Asserting on a list without asserting on every row","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"React Native Testing Library (RNTL) renders a component tree in Jest and lets you query it the way a user perceives it — by visible text, by accessibility label, by role — rather than by internal structure. Tests written that way survive refactors, because they assert what the screen offers rather than which component produced it. The version this handbook targets is . Its peer dependencies changed in a way that is easy to get wrong, so read the installation section carefully even if you have used RNTL before. Why it exists / when to use it — and when NOT to A component test is worth writing when the component makes a decision: it shows an error when a field is empty, it disables a button while a request is in flight, it renders an empty state when the list is empty. Those are behaviours a refactor should not change, and a query by text assertion pins them without pinning the markup. It is not worth writing when the component only forwards props to a . There is no decision to assert, and the test becomes a second copy of the implementation that has to be edited every time the implementation is. RNTL cannot tell you whether something is visible on a real screen. Layout runs in native code that does not exist under Jest, so a view that is 0 pixels tall, behind a modal, or off screen still counts as rendered. Anything that depends on actual pixels belongs in an end to end run. Installation — the part that changed RNTL 14 declares these peer dependencies, read from the npm registry: Peer Range The last row is the trap. is not . It is a separate package (current version ) described on npm as \"a lightweight test renderer for React and a modern replacement for the deprecated React Test Renderer\". It depends on and peers on . React Test Renderer was deprecated by React, and RNT"},{"u":"testing/mocking-native-modules.html","t":"Mocking Native Modules","s":"Testing","d":"Mock TurboModules and Fabric components in Jest under the New Architecture, and avoid mocking module shapes that no longer exist.","h":["Why it exists / when to use it — and when NOT to","What the preset already mocks","Basic example — mocking your own TurboModule","How it works","Mocking a Fabric native component","The failure everyone hits: mocking a module that no longer exists","Platform differences","Security considerations","Common mistakes","Related topics"],"b":"Jest runs in Node. There is no app process, no JSI runtime and no native code, so every call that would cross into Kotlin or Swift has to be replaced with a JavaScript stand in. React Native's Jest preset does that for the core modules. Anything you wrote, and anything you installed, is your responsibility. The shape you mock matters. Under the New Architecture a native module is obtained through from a generated spec, not looked up on a global object, so the mocking recipes in most existing articles patch something the code under test never reads. Why it exists / when to use it — and when NOT to Mock a native module when the test is about your logic and the native call is incidental: a hook that persists a token, a screen that asks for a permission before opening the camera, a retry policy around a network reachability check. Do not mock a native module to test the native module. A mock proves that your JavaScript calls a function you defined in the same file; it proves nothing about the Kotlin or Swift on the other side, about Codegen wiring, or about whether the library is linked at all. That is what end to end tests are for. Also resist mocking the whole of . It is a large module with interdependent parts, and replacing it wholesale produces failures far from their cause. What the preset already mocks installs mocks through its entry for the core surface. Among them: Mocked Behaviour under Jest / A fixed 750x1334 device at scale 2 , etc. are spies Screen reader reported as disabled is , , Host components render as inert leaf nodes , , , , , , , , , Component mocks that render without native views Returns a fixed value rather than reading the system Nothing from outside React Native's own packages is mocked. If a library touches native code, you supply the mock. Basi"},{"u":"testing/snapshot-testing.html","t":"Snapshot Testing","s":"Testing","d":"Where snapshots genuinely help, why large component snapshots rot, and how to keep the few you keep worth reviewing.","h":["Why it exists / when to use it — and when NOT to","Basic example — a snapshot that earns its place","How it works","Common patterns","Snapshotting one element, not the screen","Snapshotting the call, not the render","Making an accidental new snapshot fail CI","Deleting snapshots that outlived their test","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"A snapshot test serialises a value — usually a rendered component tree — writes it to a file on first run, and on later runs fails if the serialisation differs. It is cheap to write and it catches unintended change. It is also the most over used tool in a React Native test suite. This page argues for using it narrowly and gives the rules that make the narrow use survive. Why it exists / when to use it — and when NOT to A snapshot is a good fit when three things are true: 1. The output is small — tens of lines, not hundreds. 2. The output is meaningful to a human — a reviewer can look at the diff and say whether the change was intended. 3. There is no better assertion — the value has no single property that captures the intent. That describes serialised data far more often than it describes a screen: a normalised API response, a generated deep link URL, a theme token object, the arguments a native module was called with, an error message intended for a user. It is a poor fit when the output is a full component tree. The failure mode is specific and predictable: Snapshots rot. A 600 line snapshot changes whenever any descendant changes. The diff is large and mostly irrelevant. Nobody reviews them. Once a snapshot diff is routinely large, becomes reflex. A test that is always updated without reading is not a test. They assert nothing about behaviour. A snapshot records that the tree was serialised the same way. It cannot tell you the button is reachable, the error appears when the field is empty, or the request was sent. Those are the things that break. They pass for broken UI. Layout does not run under Jest, so a snapshot is identical whether the view is on screen or collapsed to zero height. The rule of thumb If you cannot read the whole snapshot in a code review and say"},{"u":"testing/end-to-end.html","t":"End-to-End with Detox or Maestro","s":"Testing","d":"Choosing between Detox 20 and Maestro for React Native end-to-end tests, with an honest comparison of setup cost, speed, flakiness and CI fit.","h":["Why it exists / when to use it — and when NOT to","Detox versus Maestro — the honest comparison","Basic example","Maestro","Detox","How it works","Common patterns","Give the flows stable handles","Control the backend","Reset state between runs","Keep the suite small and named after the business flow","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"An end to end test drives a real build of your app on a real simulator, emulator or device. It is the only layer that proves the things Jest cannot: that the app launches, that native modules are linked, that Fabric components draw, that a permission dialog can be dismissed, that a deep link opens the right screen. Two tools dominate the React Native ecosystem: Detox ( on npm) and Maestro (a JVM CLI distributed by mobile.dev, not on npm). They make opposite trade offs, and the right answer depends on your team more than on your app. iOS end to end testing requires macOS Building and booting an iOS app needs Xcode, which runs only on macOS. Android end to end tests run on macOS, Linux and Windows. If your CI has no macOS runner, you can test Android end to end and nothing else. Plan for that before you choose a tool, not after. Why it exists / when to use it — and when NOT to Write end to end tests for the handful of flows whose failure means the app is unusable: launch, sign in, the core task, checkout or submit. Those are the flows where a broken native dependency, a mis signed build or a missing permission shows up, and where every other layer of testing is blind. Do not use end to end tests for logic, validation rules, edge cases or error copy. Each such test costs a full app build, a device boot and tens of seconds of runtime, and each one is a chance for a flake. That work belongs in unit and component tests, which run in milliseconds and fail for exactly one reason. A useful ratio in practice: hundreds of component tests, a dozen end to end flows. If your end to end suite is growing faster than your unit suite, something is being tested at the wrong level. Detox versus Maestro — the honest comparison Detox 20.51.4 Maestro Distribution npm ( , with a binary) Standa"},{"u":"testing/ci-for-mobile.html","t":"CI for Mobile","s":"Testing","d":"Running a React Native 0.87 test suite on hosted runners — Node version, caching, the macOS constraint, and what to run on every push versus nightly.","h":["Why it exists / when to use it — and when NOT to","The constraints to design around","Basic example","How it works — what to run where","Common patterns","Verify the toolchain before blaming the code","Cache the things that dominate","Fail on a dependency drift you did not intend","Upload something you can debug from","Keep the runner's Node pinned in one place","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Mobile CI differs from web CI in three ways that decide the shape of your pipeline: builds are slow and native, iOS needs macOS, and the expensive checks are the ones that catch real regressions. Getting the split right is more valuable than any single optimisation. This page is about running checks . Producing signed artefacts for a store is a different job; see CI Pipelines and Release Checklist. Why it exists / when to use it — and when NOT to CI exists to make a regression visible before it reaches a device you cannot patch remotely. A mobile release takes days to review and cannot be rolled back for users who already updated, so a bug that ships is expensive in a way a web bug is not. That does not mean running everything on every push. A full iOS build on every commit burns the scarcest resource you have — macOS runner minutes — to re prove something that rarely breaks. Run the fast, high signal checks on every push and the slow ones on a schedule or before a release. The constraints to design around Constraint Consequence React Native 0.87.1 is Pin Node to at least 22.13.0 . A runner defaulting to an older Node fails in ways that do not name the Node version. iOS builds need Xcode Any iOS job must run on macOS . There is no Linux path, no container, and no flag. macOS runners cost several times a Linux runner Put everything that can run on Linux on Linux. Android needs the SDK and, for emulators, hardware acceleration Emulator jobs need a runner that exposes nested virtualisation; without it, boot times make the job unusable. Native builds are minutes, JS checks are seconds Cache aggressively and split jobs so a lint failure does not wait for a Gradle build. pins to Use a lockfile and . A floating install can resolve a React that does not satisfy the peer range. "},{"u":"build-and-release/build-variants.html","t":"Build Variants and Flavours","s":"Build and Release","d":"Build one codebase into dev, staging and production apps that install side by side, using Android product flavours and iOS schemes and configurations.","h":["Why it exists, and when not to bother","The vocabulary, on both platforms","Android: build types","Android: a dev/staging/prod split","Per-flavour source sets","Building and running a variant","iOS: configurations and schemes","Step 1 — duplicate the configurations","Step 2 — give each configuration its own identifier and name","Step 3 — tell CocoaPods about the new configurations","Step 4 — add a scheme per environment","Building and running an iOS scheme","Reading the variant from JavaScript","Common patterns","Keep the variant visible in the app","One flavour dimension until you actually need two","Gate release-only tooling by variant, not by __DEV__","Give each variant its own crash-reporting project","Performance considerations","Common mistakes","Related topics"],"b":"A real app is not one app. It is at least three: the one you run against a local API, the one QA runs against a staging API, and the one that reaches the store. They need different API endpoints, different app icons, different names, and — this is the part people discover late — different application identifiers, so all three can sit on the same device at once. Android and iOS solve this with different machinery. Android has build types and product flavours , combined into variants . iOS has build configurations and schemes . This page builds the same dev/staging/prod split on both, end to end. Why it exists, and when not to bother The alternative to variants is editing a constant before each build, or keeping three branches. Both fail the same way: someone ships a production build pointing at staging, and nobody notices until the support tickets arrive. Use variants when any of these is true: You have more than one backend environment. You want testers to keep a staging build installed alongside the store build. You need different signing, different crash reporting projects, or different analytics keys per environment. Do not reach for variants when the only difference is a feature flag that can be flipped at runtime. A flag fetched from your server is cheaper to change than a build, and it does not double your CI matrix. Variants are for things that must be baked into the binary: the application ID, the icon, the entitlements, the signing identity. Variants multiply your build matrix Three flavours times two build types is six Android variants, and CI has to build the ones you ship. Add a second dimension and it is twelve. Start with one dimension and three flavours. The vocabulary, on both platforms Concept Android iOS Debuggable vs optimised Build type ( , ) Build c"},{"u":"build-and-release/environment-configuration.html","t":"Environment Configuration","s":"Build and Release","d":"Wire per-environment values into Android and iOS builds with react-native-config 1.7.2 — and understand why everything you put there is public.","h":["Why it exists, and when not to use it","Installing it","Android needs one manual line","Reading values from JavaScript","Typing your own keys","The env files","Selecting a file per build","The portable way: ENVFILE","Android: map flavours to env files","iOS: map build configurations to env files","Exposing values to Info.plist and build settings","Using values from Gradle and the manifest","Platform differences","Common patterns","A single typed config module","Fail fast on a missing key","Keep .env.example in sync","Show the environment in non-production builds","Security considerations","Threat","Exploit","Fix","Verification","Common mistakes","Related topics"],"b":"Your app needs to know which API to call, which crash reporting project to report to, and which environment it is running in. Those values differ per build and have to be decided at build time, because the app cannot ask a server which server to ask. is the standard answer on the Community CLI path. It reads a file at build time and compiles the values into the native app, where a TurboModule hands them back to JavaScript. This page sets it up for a dev/staging/prod split on both platforms. These values are not secrets. They are public data with an extra step Everything in your is compiled into the binary that you hand to strangers. On Android each key becomes a field and a string resource in . On iOS each key is written into a generated Objective C file that ends up in the app binary. Anyone with the app can read every one of them in under a minute, with no rooted device and no reverse engineering — , then . There is no setting that changes this. The library's own README says it plainly: it does not obfuscate or encrypt anything. If a value would hurt you when published on a web page, it does not belong in a mobile app at all. See Why Secrets in JS Are Readable for the extraction, demonstrated on a real release build. Why it exists, and when not to use it Use it for configuration : values that vary per build, that you would happily print in a log, and that grant no access on their own. Belongs in Never belongs in Payment provider secret keys ( / / ) Cloud provider access keys A crash reporter DSN (a write only ingest endpoint) Database credentials A publishable / client side API key the vendor documents as public Anything a vendor calls a \"secret key\" Feature flag defaults Signing keystore passwords A CDN or asset host Admin or service tokens Do not use it when the val"},{"u":"build-and-release/icons-and-splash-screens.html","t":"App Icons and Splash Screens","s":"Build and Release","d":"Produce the real native assets both stores require — Android mipmap densities and adaptive icons, the iOS AppIcon catalog, and the Android 12+ system splash screen.","h":["Android icons","The two icon systems, and why you need both","Legacy density buckets","Adaptive icons","A different icon per flavour","The Play Store listing icon","iOS icons","One image, every size","Appearance variants and Icon Composer","A different icon per configuration","Splash screens","What Android 12 changed, and why your old splash is wrong","iOS launch screens","Doing both with react-native-bootsplash","Platform differences","Common patterns","Badge non-production icons","Keep the source art, not just the exports","Regenerate rather than hand-edit","Match the splash background to your first screen","Performance considerations","Common mistakes","Related topics"],"b":"Icons and splash screens are pure native configuration. There is no React Native API for either: they are resources compiled into the app, read by the launcher and the window manager before any JavaScript exists. That is also why they are the last thing anyone does and the first thing that fails review. Two things changed materially and invalidate most older guidance. Android 12 (API 31) made the system splash screen mandatory — your app gets one whether you configure it or not, which is why old custom splash implementations now produce two splashes in a row. And Xcode generates every iOS icon size from a single 1024×1024 image , so the twenty file icon sets in older tutorials are unnecessary. Android icons The two icon systems, and why you need both Legacy launcher icon Adaptive icon Since Always API 26 (Android 8.0) Files One PNG per density bucket Two or three layers plus an XML descriptor Shape Whatever you draw Masked by the launcher into a circle, squircle, teardrop… Lives in for React Native 0.87 is 24, so devices below API 26 still exist in your install base and still need the legacy PNGs. Ship both. Android picks on API 26 and above and falls back to the density buckets below it. , not Launcher icons belong in . resources are stripped by density when you build a density split APK, and the launcher may ask for a density your APK does not carry. directories are exempt from that stripping. This is the reason the directory exists. Legacy density buckets The base launcher icon is 48 dp, scaled by the density multiplier for each bucket: Directory Density multiplier Pixel size 1x 48 × 48 1.5x 72 × 72 2x 96 × 96 3x 144 × 144 4x 192 × 192 A generated project ships and in each. Replace both. is what circular icon launchers use on API 25 and below; above that the adaptive"},{"u":"build-and-release/android-signing.html","t":"Android Signing","s":"Build and Release","d":"Generate an upload keystore, wire it into Gradle without committing a secret, and understand what Play App Signing does and does not protect you from.","h":["Why it exists, and what it actually proves","Two keys, once you are on Play App Signing","Generating the keystore","Wiring it into Gradle without leaking it","The credentials","The signing config","CI: environment variables, not files in the repository","Building and verifying a signed release","Fingerprints for third-party services","Security considerations","Threat","Exploit","Fix","Verification","Common mistakes","Related topics"],"b":"Every Android app is signed. A debug build is signed with a throwaway key that Android Studio generated for you; a release build must be signed with a key you control, and Android uses that signature to decide whether an update really came from you. The consequences of getting this wrong are unusually permanent. A lost signing key used to mean you could never update your app again — you had to publish a new listing and ask every user to reinstall. Play App Signing removed most of that risk, but only for apps enrolled in it, and only for the upload key. Why it exists, and what it actually proves An Android signature proves continuity , not identity. It says \"the thing that signed this update also signed the version you have installed\". Nothing about the certificate is verified by a certificate authority — it is self signed by design, and the you type into is whatever you felt like typing. So the security property is: an attacker who does not have your key cannot push an update to your users. An attacker who does have your key can. That is the whole threat model, and it is why the key handling below matters more than the cryptography. Two keys, once you are on Play App Signing This is the concept that makes the rest of the page make sense. Upload key App signing key Who holds it You Google, in Play's key management infrastructure What it signs The AAB you upload to the Play Console The APKs Play generates and delivers to devices What devices verify Nothing — it never reaches a device This one If you lose it Request a reset through Play Console support Unrecoverable, if you are not enrolled Rotatable Yes Effectively no When you upload an AAB, Play verifies your upload key, strips that signature, and re signs the generated APKs with the app signing key it holds. Losing the "},{"u":"build-and-release/play-store-submission.html","t":"AAB and Play Store Submission","s":"Build and Release","d":"Build an Android App Bundle, test it locally with bundletool, and move it through Play Console tracks and a staged rollout without breaking production.","h":["Why the AAB exists","Building the bundle","Testing the bundle before you upload it","Size limits","Target API level","First submission: what Play asks for","Tracks","Staged rollout","Release notes","Automating the upload","Common patterns","Internal track on every merge to main","Promote, do not rebuild","Keep versionCode monotonic and automatic","Upload the mapping file with every release","Common mistakes","Related topics"],"b":"Google Play does not accept an APK for a new app. You upload an Android App Bundle ( ), Play generates the actual APKs for each device configuration, signs them with the app signing key it holds, and delivers only the parts a given device needs. That indirection is the source of most of the surprises on this page: the artefact you build is not the artefact your users install, and you cannot test the delivered APK by installing the bundle. Why the AAB exists A traditional APK carries every density of every drawable, every ABI of every native library, and every localisation, because it has to run on any device. A React Native app is a heavy example: Hermes and the React Native native libraries ship for four ABIs. Play splits the bundle into a base APK plus configuration APKs and sends the device only the ones it can use. The typical saving on a React Native app is substantial, and it costs you nothing but the inability to sideload the build directly. APK AAB Gradle task Installable with Yes No Contains every ABI and density Yes Yes, but they are not all delivered Signed by You You for upload; Play re signs for delivery Accepted for a new Play app No Yes Useful for Direct distribution, device farms, QA The Play Store You still build APKs — for testers, for device farms, for anyone outside Play. You just do not upload them. Building the bundle The output is . If you have no product flavours it is and . This task depends on a working release signing config. If it fails on the signature, fix that first — see Android Signing. and are different tasks produces an APK and does not produce an AAB. Running it and then hunting for a under is a common five minute detour. The bundle is under . Testing the bundle before you upload it An AAB cannot be installed. To test what Play will a"},{"u":"build-and-release/proguard-and-r8.html","t":"ProGuard and R8","s":"Build and Release","d":"What R8 shrinks in an Android release build, the keep rules React Native and native modules rely on, and why over-aggressive shrinking breaks reflection.","h":["Why it exists, and what it actually does","It is off by default in a generated project","The rules you do not have to write","The rules you do have to write","Your own reflection","JSON model classes","Libraries without consumer rules","Enums used by name","What R8 does not touch","Diagnosing a build R8 broke","Step 1 — confirm it is R8","Step 2 — find out what was removed or renamed","Step 3 — write the narrowest rule that fixes it","Release stack traces and mapping.txt","Platform differences","Performance considerations","Security considerations","Threat","Exploit","Fix","Verification","Common mistakes","Related topics"],"b":"R8 is the Android shrinker, optimiser and obfuscator that runs on your Java and Kotlin bytecode when you build a release variant with . It removes code nothing references, rewrites what is left to be smaller and faster, and renames classes and methods to short strings. The name \"ProGuard\" survives in and because R8 consumes ProGuard's rule syntax. ProGuard itself is not what runs. Every rule on this page is a rule R8 reads. Two things this page is careful about, because both are widely misstated. R8 does nothing to your JavaScript bundle. And R8 is not a security control — it raises the effort of reading your native code and shrinks your app, and that is all. Why it exists, and what it actually does Four separate jobs, each independently controllable: Job What it does Turned on by Shrinking Removes classes, methods and fields nothing reaches Optimisation Inlines, merges classes, removes dead branches Obfuscation Renames everything not kept to , , Resource shrinking Removes unreferenced resources from (requires ) The win on a React Native app comes mostly from shrinking your Java/Kotlin dependencies — networking libraries, JSON parsers, analytics SDKs, AndroidX. React Native's own native code is already fairly tight, and the JavaScript bundle is not affected at all. It is off by default in a generated project React Native 0.87.1's template ships this in : So a stock release build is not shrunk, not optimised and not obfuscated. Turning it on is a deliberate decision, and it is the right one for a shipped app — but make it early in the project, not the week you release. R8 failures surface at runtime in code paths you have not walked. The generated is empty apart from comments. That is correct: the rules React Native needs arrive automatically, as explained next. , not Th"},{"u":"build-and-release/ios-signing.html","t":"iOS Signing and Provisioning","s":"Build and Release","d":"Certificates, identifiers, devices and provisioning profiles — what each one is, how they combine, and how to sign an iOS build on a CI runner.","h":["The four pieces","Certificate types","Profile types","Automatic signing","Manual signing","Entitlements must match the profile","Inspecting what you actually have","Archiving and exporting","Signing on CI","Authenticate with an App Store Connect API key","Getting certificates onto the runner","Expiry, and planning for it","Common mistakes","Related topics"],"b":"iOS code signing is four separate things that must agree with each other: a certificate that proves who you are, an identifier that names your app, a list of devices that may run a non store build, and a provisioning profile that binds the three together. Every signing error you will ever see is one of those four disagreeing with another. It is not conceptually hard. It is unforgiving, the error messages describe the symptom rather than the cause, and the state lives in three places at once — your Keychain, the Apple Developer portal, and your Xcode project. This page requires macOS Code signing an iOS app requires Xcode's toolchain and the macOS Keychain. There is no supported path on Windows or Linux: not a VM you can license, not a cross compiler, not a container. If your team is on Windows or Linux you can build the entire Android app and all the shared JavaScript, but signing, archiving and uploading an iOS build needs a Mac — your own, a colleague's, or a hosted macOS CI runner. See Environment Setup. The four pieces Piece What it is Where it lives Expires Certificate A public/private key pair asserting your identity as a developer or as a distributor The private key in your Keychain; the certificate in the Developer portal Yes Identifier (App ID) Your bundle identifier, plus the capabilities it is allowed to use Developer portal No Device A UDID registered to your team Developer portal Renewed annually Provisioning profile A signed file binding a certificate, an App ID and (for non store builds) a device list Downloaded to your Mac; embedded in the app Yes, typically one year The profile is the part that gets embedded in your as . iOS checks it at install time: the app's bundle identifier must match the profile, the signature must come from a certificate the prof"},{"u":"build-and-release/app-store-submission.html","t":"TestFlight and App Store Submission","s":"Build and Release","d":"Upload a signed archive, distribute it through TestFlight, and get through App Review — including the privacy manifest and permission strings that reject React Native apps.","h":["The path a build takes","Building the archive","Uploading","Export compliance","The privacy manifest","Permission usage strings","TestFlight","App Review","Releasing","Common patterns","Upload to TestFlight from CI on every merge","Keep release metadata in the repository","Archive the .dSYM with every build","Submit the version bump before you need it","Common mistakes","Related topics"],"b":"Once you have a signed archive, everything else is App Store Connect: upload the build, let Apple process it, hand it to testers through TestFlight, and eventually submit it for review. The mechanics are straightforward. What catches React Native teams is the metadata — the privacy manifest, the permission usage strings, the export compliance answer, and the App Privacy questionnaire. Those reject builds far more often than code does. This page requires macOS Archiving and uploading an iOS build require Xcode's toolchain, which runs only on macOS. There is no supported route from Windows or Linux. Everything from \"build the archive\" onwards needs a Mac or a hosted macOS CI runner. Managing metadata and releases in App Store Connect is a web interface and works from anywhere; producing the binary does not. The path a build takes Each arrow is a place things stop. Processing can reject an upload outright for a missing icon or an invalid entitlement. Beta App Review gates external TestFlight testers, not internal ones. App Review gates the store. Building the archive Signing details, including , are on iOS Signing and Provisioning. One React Native specific point: the JavaScript bundle is produced by a build phase inside the Xcode project , so a Release archive contains a bundled, Hermes compiled JavaScript payload rather than a Metro connection. If the archive builds but the app shows a red screen about a missing bundle, that build phase is what to look at — not the archive settings. Uploading Route Good for Notes Xcode Organizer → Distribute App The first upload, and one offs Shows validation errors in a readable form Transporter (Mac App Store app) Uploading an someone else built No Xcode project required Fastlane Everything repeatable Uses the App Store Connect API key"},{"u":"build-and-release/versioning.html","t":"Versioning Strategy","s":"Build and Release","d":"Keep versionCode, versionName, CFBundleShortVersionString, CFBundleVersion and your JavaScript release in agreement, automatically.","h":["The five numbers","Choosing a scheme","The user-facing version","The build number","Keeping shallow clones honest","One source of truth","Android reads it directly","iOS takes them on the command line","Verify what actually shipped","Reading the version at runtime","Platform differences","Common patterns","Bump with npm version, let CI do the rest","Never reuse a build number, even for a failed upload","Tag the commit that produced each store build","Keep the build number the same on both platforms","Tie the JavaScript version to the native one","Common mistakes","Related topics"],"b":"A React Native app has five version numbers that can disagree: two on Android, two on iOS, and the one in that your JavaScript reports. Each store enforces different rules about which may repeat and which must increase. The goal is not a clever scheme. It is that any crash report, any support ticket and any store listing can be traced back to one commit, without anyone having to remember to edit four files. The five numbers Where Name Type Seen by users Store rule Integer No Must be strictly greater than every code previously uploaded to any track String Yes No rule; display only → Dotted string Yes Must increase for each new App Store version → Dotted string No Must be unique and increasing within a given short version string Semver Only if you show it None The pairing is the same on both platforms: one number humans read, one number the store uses to order builds. Android calls them and ; iOS calls them the short version string and the bundle version. They map onto each other exactly. React Native 0.87.1's template ships and in , and an that already delegates both iOS values to build settings: That indirection is useful: you never edit for a version bump. You change two build settings, which can be set per configuration, overridden on the command line, or written by a script. Choosing a scheme The user facing version Use semantic versioning for and , and keep them identical to each other and to . There is no benefit to Android and iOS showing different numbers, and there is a real cost when a support ticket says \"version 2.4\" and you have two of them. Resist encoding build metadata here. is legal semver and is not what a user wants to read in the App Store. The build number and need to be monotonic and automatic. Four common sources: Source Monotonic? Reproducible fro"},{"u":"build-and-release/fastlane.html","t":"Fastlane","s":"Build and Release","d":"Automate signing, building, and uploading to both stores with a Fastfile you can run locally and from CI.","h":["Installing it through Bundler","The Appfile","The Android lanes","The Play service account","The iOS lanes","match is the point","Secrets","Store metadata in the repository","Common patterns","A lane per destination, not per action","Fail early on a dirty tree","Wrap Gradle rather than replacing it","Use the same lanes locally and on CI","setup_ci at the top of every iOS lane","Common mistakes","Related topics"],"b":"Fastlane is a Ruby tool that wraps the commands this section has been running by hand — , , the upload to each store — into named lanes you invoke as or . The value is not that it types for you. It is that the release process becomes a file in your repository: reviewed, versioned, identical on your machine and on CI, and runnable by someone who has never released this app before. iOS lanes need macOS Any lane that calls , or runs Xcode and the macOS Keychain, so it runs only on macOS. Android lanes run on macOS and Linux. Fastlane's Windows support is limited and not what Android CI runners use — if your team is on Windows, expect to run lanes on a Linux container for Android and a hosted macOS runner for iOS. Installing it through Bundler A generated React Native project already has a , because CocoaPods is a gem. Add Fastlane to it rather than installing globally, so everyone and every CI job runs the same version: Run every lane through . A globally installed is whatever version you last happened to install, and a release process that behaves differently per machine is the problem you are trying to remove. Fastlane's version is not verifiable from the npm registry Every other version in this handbook was read from . Fastlane is a RubyGem, so it was not verified that way. Pin it in your (which writes and you commit), and check on your own machine before relying on any action parameter below. creates and . A React Native project has two native projects, and you can either keep one at the root with platform blocks — which is what this page does — or one inside and one inside . The root layout keeps shared configuration in one place. The Appfile holds identifiers, not secrets. Everything sensitive comes from the environment. The is committed; the values are not. The Andr"},{"u":"build-and-release/ci-pipelines.html","t":"CI Pipelines","s":"Build and Release","d":"A working GitHub Actions pipeline for a React Native 0.87 app — checks, an Android build on Linux, an iOS build on a macOS runner, and secrets handled safely.","h":["The shape","Checks","The Android job","Making Gradle fast enough","The iOS job","Secrets: the rules that matter","Other providers, same shape","Performance considerations","Common patterns","Gate builds on checks","One workflow per purpose","Upload the debug symbols with every build","Run the release lane from a tag","Post the build to a chat channel","Common mistakes","Related topics"],"b":"Mobile CI has one structural difference from web CI: you cannot build both platforms on one runner . iOS needs macOS; Android does not and should not pay for it. Every pipeline in this section is therefore at least two jobs, on two operating systems, with different caches and different secrets. This page builds that pipeline for GitHub Actions, because it is the most common starting point. The shape transfers to GitLab CI, Bitrise, CircleCI and the rest — the jobs, the caches and the secrets are the same; only the syntax moves. The shape Run checks once, on the cheap runner, and gate both builds on them. There is no reason to spend macOS minutes discovering a lint error. macOS runners are the expensive part Hosted macOS minutes bill at a multiple of Linux minutes on every provider. That multiplier is the single biggest lever on your CI bill, and it is why the iOS job below runs only where it has to: on release branches and tags, not on every pull request. Checks matters more than it looks. React Native 0.87.1 declares , and those are three disjoint ranges — a runner defaulting to Node 22.11 satisfies \"Node 22\" and not the engine constraint. Commit an (see Environment Setup) and point CI at it. The Android job Points worth calling out: . The Gradle daemon is a long lived process that helps on a machine you keep. A CI runner is discarded, and the daemon costs startup time and memory for nothing. as . Monotonic, never reused, and requires no git history. See Versioning Strategy. The cleanup step runs . A failed build must not leave a keystore in the workspace where a cache or an artefact upload could pick it up. The mapping file is uploaded with the bundle. It is regenerated on every build; once the runner is gone it cannot be recovered. Making Gradle fast enough Lever Eff"},{"u":"build-and-release/over-the-air-updates.html","t":"Over-the-Air Updates","s":"Build and Release","d":"What an OTA update can and cannot change in a React Native app, what Apple and Google actually permit, the state of the tooling after CodePush shut down, and how to evaluate an option yourself.","h":["What is actually in scope","Why teams want it, and when it is the wrong answer","What the stores actually permit","Apple","Google","The state of the tooling","CodePush and App Center are gone","There is no first-party replacement on the CLI path","How to evaluate an OTA option","What building the bundle looks like","Security considerations","Threat","Exploit","Fix","Verification","Performance considerations","What to do instead, or as well","Common mistakes","Related topics"],"b":"An over the air update replaces the JavaScript bundle and its assets in an installed app, without going through App Review or a Play release. It is the most over promised capability in the React Native ecosystem, so this page starts with the boundary rather than the benefits. An OTA update can change your JavaScript bundle and your bundled assets. It can change nothing else. Not your native code, not a native dependency, not a permission, not an key, not the Hermes version. Every honest discussion of OTA starts there, and a surprising amount of advice online does not. What is actually in scope Changes without a store release Requires a new store build Your JavaScript and TypeScript Any Kotlin, Swift, Objective C++ or C++ Images and fonts bundled through Metro Adding or upgrading a native dependency Layout, copy, business logic, styling A new permission in A JavaScript only bug fix A new usage description string in A feature built from components already linked into the binary Upgrading React Native itself Remote configuration your JavaScript reads Changing the app icon, name or bundle id The second column is not a limitation of any particular tool. The native binary is signed and installed by the platform; nothing your app downloads at runtime can change it. There is a subtler version of the same rule that catches teams out: a JavaScript update that calls a native module which is not in the shipped binary will install perfectly and then crash on the first call. OTA changes what code runs, not what code exists. The bundle must match the binary it lands in A JavaScript bundle built against React Native 0.87.1 is not interchangeable with one built against 0.86, and a Hermes bytecode bundle is tied to the bytecode version the shipped Hermes understands. Every OTA mechanism "},{"u":"build-and-release/release-checklist.html","t":"Release Checklist","s":"Build and Release","d":"An ordered, runnable checklist for shipping a React Native 0.87 release — versions, signing, store compliance, security and rollout — with links to the page that explains each step.","h":["How to use it","1. Code and dependency health","2. Version numbers","3. Configuration and environment","4. Security review","5. Android build","6. iOS build — macOS only","7. Store metadata and compliance","8. Test the artefact you are actually shipping","9. Observability, before the rollout rather than after","10. Rollout","11. After it is out","A single pre-flight script","Common mistakes","Related topics"],"b":"This page is a checklist, not an explanation. Every item says what to do, why it bites when you skip it, and links to the page that covers it properly. Work top to bottom; the order is roughly the order in which a mistake becomes expensive to fix. Nothing here is React Native folklore. Each item is either a store rule, a signing property, or a failure this handbook documents elsewhere with a reproduction. How to use it Run it twice. Once a few days before the release, when there is still time to fix a signing or compliance problem, and once immediately before you upload, when the only question is whether the artefact in front of you is the one you tested. The iOS half needs macOS with Xcode . There is no cross platform path to an iOS archive, so a team shipping to both stores needs a Mac or a hosted macOS runner before any of section 6 is possible. 1. Code and dependency health [ ] The branch builds and the tests pass locally , not only on CI. A red pipeline on release day is a bad time to discover a flaky native test — see CI for Mobile. [ ] No left in a code path that runs in release. Logs leak user data and survive into the shipped bundle. Safe Logging in Release Builds [ ] Dependencies audited. , and a look at anything that changed since the last release. Dependency Auditing [ ] Every native dependency is New Architecture compatible. \"It installed\" proves nothing on 0.87. Native Dependency Compatibility [ ] Companion peer packages are installed , not merely warned about. Reanimated 4 needs ; 4 needs . Both fail at runtime, not at install. Troubleshooting [ ] Lock files are committed and unchanged by the build. and decide what CI installs. Autolinking 2. Version numbers [ ] is strictly greater than every code ever uploaded to any Play track — including internal testi"},{"u":"security/threat-model.html","t":"Threat Model","s":"Security","d":"Who attacks a React Native app, what they control, and which defences are real rather than decorative.","h":["The one rule that governs everything","Who is actually attacking you","The assets you are actually protecting","Trust boundaries in a React Native app","Threat: the binary is not private","Exploit","Fix","Verification","What each defence in this section actually buys you","How to use this section","Common mistakes","Related topics"],"b":"Before you pick a defence you need to know what you are defending against. A mobile app is not a server. It runs on hardware the attacker owns, in a process the attacker can inspect, on an operating system the attacker can modify. Every line of code you ship, every string you embed and every request you make is visible to someone who cares enough to look. This page sets the ground rules for the rest of the section. Read it first, because almost every \"security feature\" sold for mobile apps only makes sense once you accept that the client is hostile territory. The one rule that governs everything The attacker controls the device. That is not a worst case, it is the normal case. Android emulators and rooted phones cost nothing. Jailbroken iPhones are obtainable. Frida, , and are free and well documented. There is no configuration you can ship in your app that changes who owns the CPU it runs on. The practical consequence: a client side control never stops a determined attacker. It raises the cost of an attack. Raising cost is worth doing — most attackers are not determined, and cost is what turns a five minute copy paste attack into a two day reverse engineering project. But if your security depends on the client behaving, you do not have security, you have a convention. Everything that must actually be enforced is enforced on your server. Who is actually attacking you Not all of these people care about you, and the defences differ sharply. Actor What they want Effort they will spend What actually stops them Curious user Free features, cheats Minutes Nothing client side, reliably Script following attacker Hardcoded API keys found by automated scanners Minutes Not shipping the key at all Network attacker on shared Wi Fi Session tokens, request/response contents Hours TLS, "},{"u":"security/secrets-in-the-bundle.html","t":"Why Secrets in JS Are Readable","s":"Security","d":"Extract string literals from a real release build, including a Hermes bytecode bundle, and see why no API key belongs in an app.","h":["Threat","Exploit","Step 1 — build a real release APK","Step 2 — unzip it","Step 3 — confirm it is Hermes bytecode, not JavaScript","Step 4 — read the strings out of the bytecode","Step 5 — check the Android resources too","Step 6 — the iOS equivalent","What Hermes does and does not hide","Fix","1. The secret does not go in the app","2. Put a backend-for-frontend in front of third-party services","3. Use short-lived, revocable tokens","react-native-config is configuration, not secrets","Verification","Manual check on your own release build","Automated check in CI","The rotation check","Common mistakes","Related topics"],"b":"Every string literal in your JavaScript ships inside the app binary. Hermes compiles your source to bytecode, which makes the code hard to read — but string constants are stored in a string table, verbatim, and a ten second command prints them all. This page shows you the extraction on a release build so the rule stops being something you were told and becomes something you have seen. The conclusion up front: there is no way to put a secret in a React Native app and keep it secret. Not with Hermes, not with obfuscation, not with native code, not with encryption whose key also ships. The only fix is to not ship the secret. Threat An attacker wants a credential your app uses: a third party API key, a signing secret, a service token. They install your app from the store — no rooted device needed on Android — pull the package, and search it for anything that looks like a key. This is automated. Scanners crawl store binaries looking for known key formats. A leaked key is usually found by a bot before a human ever thinks about your app. Exploit Step 1 — build a real release APK Do this against a release build, not a debug one. A debug build loads the bundle from Metro and proves nothing. Step 2 — unzip it An APK is a zip file. No special tooling is required to open it. You are looking for . That name is the React Native Gradle plugin's default ( ), so unless you changed it, that is your app's JavaScript. Step 3 — confirm it is Hermes bytecode, not JavaScript The file keeps the extension whether it contains source or bytecode, so check the content rather than the name. A Hermes bytecode bundle begins with an eight byte magic number. React Native's own loader checks for it in , where the constant is — little endian, so on disk the first eight bytes are: Check yours: If the firs"},{"u":"security/secure-storage-keychain-keystore.html","t":"Keychain and Keystore","s":"Security","d":"Where credentials actually belong on iOS and Android, what AsyncStorage leaves in plaintext, and how far hardware-backed storage really goes.","h":["Threat","Exploit: AsyncStorage is a plaintext database","Where the data actually sits","Step 1 — write a token the wrong way","Step 2 — read it back without the app","Step 3 — check the backup path","What AsyncStorage does not protect","Fix: react-native-keychain","The shape of the API","iOS accessibility constants","Biometric-gated access","Android: Keystore, security levels and StrongBox","Platform differences","Verification","1. Confirm nothing sensitive remains in AsyncStorage","2. Confirm the Keychain item is not in the same place","3. Confirm the biometric gate is real","4. Confirm the backup behaviour","What this still does not buy you","Common mistakes","Related topics"],"b":"A refresh token has to live somewhere on the device. The question is not whether an attacker with full control of that device can eventually reach it — the threat model already answered that — but how much of the device they have to control first. Plaintext in a file needs nothing. A Keychain item gated on biometrics needs a live, unlocked device and a successful face or fingerprint match. That difference is the whole value of this page. Platform secure storage does not make a credential safe; it narrows the set of attackers who can read it and the set of situations in which it leaks. Threat Three concrete situations, in rough order of how often they actually happen: 1. A lost or stolen device. Someone picks up a phone. Whether they can read your token depends entirely on where you put it. 2. A backup. Android's auto backup and iOS's unencrypted local backups can copy app files to somewhere the user never thought about — a laptop, a cloud account with a weaker password than the phone. 3. A rooted or jailbroken device. The user did this themselves, or malware did. Every file your app owns is readable. In all three the attacker is reading files, not attacking cryptography. Storage choice is what decides whether there is anything worth reading. Exploit: AsyncStorage is a plaintext database (3.1.1) is not encrypted and has never claimed to be. Its README says so; people install it anyway and put tokens in it. Here is what that means in practice. Where the data actually sits Read from the installed 3.1.1 package rather than from memory: Platform Location Format Android (migrated from the older ) Room/SQLite database, table , columns and iOS plus one file per large entry Step 1 — write a token the wrong way Step 2 — read it back without the app On an emulator, or any device w"},{"u":"security/certificate-pinning.html","t":"Certificate Pinning","s":"Security","d":"What pinning actually defends against, how to configure it on both platforms, and the outage it causes when a certificate rotates.","h":["Threat","The cost, stated first","Fix: pin to an SPKI hash, with backups","1. Pin the public key, not the certificate","2. Pin the intermediate, not the leaf","3. Always ship a backup pin","Configuration","The cross-platform library option","Rotation planning","Verification","1. Prove an intercepting proxy is refused","2. Prove a wrong pin fails closed","3. Prove the backup pin works on its own","4. Check the pins in CI against the live host","5. Demonstrate the bypass, once","Should you pin at all","Common mistakes","Related topics"],"b":"Certificate pinning tells your app to accept only a specific key for a specific host, on top of normal TLS validation. It defends against one thing: an attacker who can get a certificate your device already trusts. That is a narrower threat than most teams assume, and pinning carries an operational cost that no other control on this list has — a pinned app that outlives its pins stops working, for everyone, until they install an update. Read the cost section before the configuration sections. Pinning is the one security control in this handbook that has caused more outages than it has prevented breaches, and that is a statement about how it is deployed rather than about the technique. Threat Normal TLS already stops a passive eavesdropper and an active attacker without a trusted certificate. Pinning addresses what is left: 1. A user installed CA. The classic case: an attacker (or an analyst, or a curious user) installs their own root certificate and runs an intercepting proxy. On Android 7 and later, apps do not trust user CAs by default, so this is mostly an iOS and older Android concern — but it is also exactly what your own QA team does. 2. A compromised or coerced public CA. Rare, historically real. 3. A misissued certificate for your domain. Certificate Transparency makes this detectable after the fact; pinning makes it unusable in the moment. What pinning does not stop, and this needs saying every time: A rooted or jailbroken device. has a one command pinning bypass. Frida scripts that hook the platform trust evaluation are maintained and public. If the attacker controls the device, pinning is a speed bump measured in minutes. Anything your API does with a valid request. Pinning is about the transport, not about authorisation. So pinning raises the cost of casual "},{"u":"security/network-security-config.html","t":"Network Security Config and ATS","s":"Security","d":"Android's network security config and iOS App Transport Security — what the defaults give you, and how a debug exception ends up in production.","h":["Threat","Exploit","Step 1 — find out what your build actually permits","Step 2 — intercept the traffic that the exception allows","Step 3 — the analytics endpoint nobody audited","Fix","The classic mistake: shipping the development exception","Enforcing HTTPS in the app as well","Verification","1. Prove cleartext is actually refused","2. Prove the proxy cannot see HTTPS in a release build","3. Read the merged manifest, not the source","4. Enumerate the hosts the app talks to","Common mistakes","Related topics"],"b":"Both platforms ship with a transport policy that is stricter than the network defaults of a decade ago: cleartext HTTP is refused, and TLS below a minimum version is refused. Both also provide a way to weaken that policy, because some app somewhere genuinely needs it. Almost every transport security incident in a React Native app comes from someone using that escape hatch to get past a development problem and never removing it. This page covers both mechanisms, what the defaults actually are, and the two lines of configuration that most often get shipped by mistake. Threat An attacker on the network path — shared Wi Fi, a hostile ISP, a compromised router, a rogue access point with your café's name — wants to read or alter your traffic. They win outright if any of the following is true: 1. Any request goes over cleartext HTTP. Reading it takes no skill; altering the response is only slightly harder. 2. Your app accepts a downgrade. An HTTPS request that silently falls back, or an HTTPS page that loads HTTP subresources. 3. Your app trusts certificates it should not. A build that trusts user installed CAs turns every device with a proxy CA into an open window. The defence is not code. It is two configuration files, and the discipline to keep the development versions of them out of the release build. Exploit Step 1 — find out what your build actually permits Read the shipped configuration rather than the one you remember writing. The authoritative answer is in the built artifact, not the source: Step 2 — intercept the traffic that the exception allows With anywhere that applies to your API host, the requests show up in the proxy with no certificate involved at all. With user CA trust enabled, the HTTPS requests show up too. Step 3 — the analytics endpoint nobody audited T"},{"u":"security/deep-link-validation.html","t":"Deep Link Validation","s":"Security","d":"A deep link is input from an arbitrary other app. Treat it that way, with an allow-list rather than a parser.","h":["Threat","Exploit","Step 1 — find the schemes and hosts your app claims","Step 2 — fire a link at the running app","Step 3 — the vulnerable handler","Step 4 — the trap specific to React Native","Fix","1. Allow-list the destination, not the string","2. Wire it to Linking correctly","3. The React Navigation linking config","4. There is no safe version of next=","Verification","1. Fire hostile links at your own build","2. Assert the resolver in a unit test","3. Watch what actually leaves the device","4. Confirm your App Links are verified","Common mistakes","Related topics"],"b":"A deep link is the one input your app accepts from a party you have never met. Any app on the device, any web page, any QR code, any message can hand you a URL and your app will act on it. If your handler trusts a parameter in that URL, you have given every app on the device a remote control for your navigation stack. This is the cheapest high value fix in the section. It costs an afternoon, it needs no native code, and it closes a class of bug that is trivially exploitable from a command line. Threat The attacker sends your app a URL. What they want from it, in rough order of payoff: 1. A token. Your app is handed and helpfully forwards the session token to after sign in. This is an open redirect with a credential attached. 2. A navigation primitive. Your app reads a route name out of the URL and navigates to it. The attacker now reaches screens the UI never links to — a debug screen, an internal admin view, a \"confirm payment\" screen with prefilled params. 3. A WebView load. Your app opens whatever URL it was given inside a WebView that has a bridge. See WebView Hardening. 4. State corruption. Your app writes a parameter straight into persisted state or into a request to your backend. None of this requires the attacker to have any privilege. A link in a message, a redirect on a web page, or another installed app is enough. Exploit You can run the whole attack from a terminal against your own build. No root, no instrumentation. Step 1 — find the schemes and hosts your app claims Custom schemes are listed under in , and universal link domains in the entitlement. Custom schemes and App Links are both entries with a element. An filter is an App Link; without it, the scheme can be claimed by any other app too. Step 2 — fire a link at the running app Anything your handler d"},{"u":"security/webview-hardening.html","t":"WebView Hardening","s":"Security","d":"A WebView puts remote HTML inside your app's process. Lock down origins, file access, injected script and the postMessage boundary.","h":["Threat","Exploit","Step 1 — the default-configured WebView","Step 2 — reach the bridge from the page","Step 3 — read the filesystem","Step 4 — the originWhitelist trap","Fix","1. Lock the origin properly, with two layers","2. Decide about javaScriptEnabled honestly","3. Treat injectedJavaScript as code you are writing into someone else's page","4. The postMessage trust boundary","Platform differences","Verification","1. Prove the origin restriction actually restricts","2. Prove the bridge rejects unknown messages","3. Prove file access is off","4. Grep the release build for debugging flags","What a hardened WebView still does not stop","Common mistakes","Related topics"],"b":"A is the largest trust boundary most React Native apps have. You are rendering code you did not write, fetched at runtime, inside your process, often with a message channel back into your JavaScript. Every other page in this section is about data you control; this one is about code you do not. Everything here is verified against 14.0.1 as installed, including the parts of its own source that decide what the props actually do. Several of the defaults are more permissive than people assume, and one of them — — behaves differently from how its name reads. Threat The attacker's goal is to get script running inside your WebView and then use it to reach something native. Ways in, from most to least common: 1. A page you allow listed loads third party content. An ad network, an analytics tag, a chat widget, an embedded iframe. You trusted the origin; the origin trusted someone else. 2. A cross site scripting bug on your own site. Your web team's XSS becomes your mobile app's native bridge exposure. 3. A navigation you did not intend. A link, a redirect chain, or moves the WebView to an origin you never considered. 4. A URL supplied from outside the app. A deep link carrying a parameter that ends up in . See Deep Link Validation. Once script runs, what it can reach depends entirely on the props you set. Exploit Step 1 — the default configured WebView Step 2 — reach the bridge from the page Any script running in the document can call the injected global. There is no origin check on it, no sender identity, and nothing distinguishing your own script from an advertiser's: Step 3 — read the filesystem With , and enabled, a document loaded from a URL can read other local files and send them anywhere: is the one that turns a local page into a universal reader: it removes the same orig"},{"u":"security/obfuscation.html","t":"Obfuscation and Its Limits","s":"Security","d":"What R8 and JavaScript minification actually do to a React Native app, what they cost, and why neither is a security control.","h":["Threat","What actually happens to your code","Android: R8","JavaScript: minification is off by default","Fix: turn R8 on, deliberately, and know what it buys","Over-aggressive rules break reflection, and only in release","Keep the mapping file","What obfuscation does not do","Verification","1. Confirm R8 actually ran","2. Decompile your own release build","3. Confirm the strings are untouched","4. Test the release build as a real build","Common mistakes","Related topics"],"b":"Obfuscation makes code harder to read. That is the whole claim, and it is a real one — harder to read means more hours for an attacker, and most attackers do not spend hours. What it is not is encryption, protection, or a reason to put something in the binary that does not belong there. This page separates the two halves people conflate: R8 on the Android side, which shrinks and renames your Java and Kotlin, and JavaScript minification on the Metro side, which in a default React Native 0.87 build does not run at all . Both facts are verifiable from the installed toolchain, and the second one surprises nearly everyone. Threat Someone has your app and wants to understand it. Their goals, in increasing order of effort: 1. Find a credential. Automated, seconds. Covered in Why Secrets in JS Are Readable — obfuscation does not help here, because string constants survive every transformation on this page. 2. Understand your API. Endpoint paths, parameter names, request signing. A day with a decompiler. 3. Modify and repackage. Remove a paywall, disable a check, republish. Days. 4. Extract an algorithm. Pricing logic, a matching algorithm, anything proprietary. Days to weeks. Obfuscation moves each of these along the effort axis. It removes none of them. What actually happens to your code Android: R8 R8 is the default shrinker in the Android Gradle Plugin. When enabled for a variant it does four things, and only the third is \"obfuscation\": Pass What it does Security value Shrinking Removes unreachable classes and members None directly; a smaller attack surface in the weakest sense Optimisation Inlines, merges, removes dead branches Makes decompiled output less like your source Obfuscation Renames classes, methods and fields to , , Raises reading effort Resource shrinking Drops "},{"u":"security/root-and-jailbreak-detection.html","t":"Root and Jailbreak Detection","s":"Security","d":"A check that runs on the attacker's hardware and is routinely defeated — useful as a risk signal, useless as enforcement.","h":["Threat","Exploit: the check is defeated by changing its answer","Hiding the evidence","Changing the answer","The JavaScript layer is even softer","What it is actually good for","1. As a risk signal sent to your server","2. As telemetry that tells you something about your user base","3. As a warning, not a wall","Where enforcement actually belongs","Platform attestation is the stronger version","Costs you are taking on","Verification","1. Bypass your own build","2. Confirm nothing breaks when the signal is suppressed","3. Confirm the server is doing the enforcing","4. Confirm a missing native module is not silently \"clean\"","5. Measure your false-positive rate before you act on it","Common mistakes","Related topics"],"b":"Root and jailbreak detection asks the device whether the device has been compromised. Stated that way, the problem is obvious: the code doing the asking, the code doing the answering and the result all live on hardware the attacker owns. Every detection technique is a check the attacker can find, and a check the attacker can find is a check the attacker can change. That does not make it worthless. It makes it a signal , not a control . This page is about keeping those two words apart, because most of the damage done by this feature comes from teams treating the second as if it were the first. Threat A rooted or jailbroken device gives an attacker capabilities your app cannot take away: Read and write every file your app owns, including anything in AsyncStorage. Attach a debugger or a dynamic instrumentation toolkit to your running process. Hook any function — yours, the platform's, or a library's — and change its return value. Install a CA and intercept traffic, defeating certificate pinning. Modify and repackage your app. This is not a hypothetical set. It is the standard toolkit, it is free, and it is documented in tutorials aimed at beginners. Exploit: the check is defeated by changing its answer Detection libraries look for the usual traces — the binary, known root manager package names, writable system paths, Cydia and Substrate artefacts on iOS, test keys build tags. All of those are findable. All of them are also hideable, and the detection function's return value is directly modifiable. Hiding the evidence Modern root solutions ship a deny list feature specifically to hide root from selected apps. The user adds your package to a list in a settings screen; your checks then look at a device that presents as stock. This requires no reverse engineering of your app a"},{"u":"security/permissions-hygiene.html","t":"Permissions Hygiene","s":"Security","d":"Every permission you hold is a capability an attacker inherits — and a reason a store reviewer rejects your build.","h":["Threat","Exploit","Step 1 — enumerate what you actually ask for","Step 2 — find out who added them","Step 3 — see what a granted permission gives an attacker","Fix: least privilege, stated concretely","1. Do not request what you do not use today","2. Prefer the narrower variant every time","3. Ask at the moment of use, with a reason","4. Trim what the build compiles in","5. Remove what a dependency added","6. Revisit grants you no longer need","Store review: over-requesting gets you rejected","Platform differences","Verification","1. Diff the permission list every release","2. Prove each permission is used","3. Test every denial path","4. Check the iOS build only contains the handlers you listed","Common mistakes","Related topics"],"b":"A permission is not a feature flag. It is a standing grant of access to something the user owns: their camera, their contacts, their location history, their photo library. Your app holds that grant for as long as it is installed, and anything that gains control of your app — a compromised dependency, an exploited WebView, a malicious native module — holds it too. That is the security framing, and it lines up exactly with the product framing and the store framing. Asking for less is better on all three counts, which makes permissions one of the rare places where doing the secure thing costs nothing. Threat Two distinct threats, with different attackers. The app is compromised. A supply chain attack lands code inside your bundle or your native build (Dependency Auditing). That code runs with your app's identity and therefore with every permission you have been granted. It does not need to ask the user for anything, because you already did. An app with granted is one malicious dependency away from an exfiltrated address book, and the user sees no prompt. The data is over collected. No attacker needed. Location, contacts or photo metadata flows to your servers or to an SDK's servers, where it becomes a breach waiting to happen and a regulatory question you have to answer. The safest data is the data you never held. The second threat is far more common than the first, and it is entirely self inflicted. Exploit Step 1 — enumerate what you actually ask for Do this against a release build. Manifest merging means your dependencies can add permissions you never wrote. Compare the output against the list of features your app actually has. On almost every real project the first list is longer, and the extra entries came from dependencies. Step 2 — find out who added them This is th"},{"u":"security/dependency-auditing.html","t":"Dependency Auditing","s":"Security","d":"Third-party code runs with your app's full privileges and, in a native package, on your build machine too. Audit it like you mean it.","h":["Threat","Exploit: what postinstall can do","Fix","1. Lockfile discipline","2. Run npm audit, and make it mean something","3. Check a package before you add it","4. Reduce the number of dependencies","5. Know what the native side pulls in","6. Automate the recurring part","Verification","1. Prove your CI install is reproducible","2. Prove the audit gate actually fails","3. Inventory what ships","4. Check the install scripts you are actually running","5. Confirm what a new dependency added to the shipped app","Common mistakes","Related topics"],"b":"A React Native app is mostly other people's code. A modest project pulls in several hundred npm packages, dozens of native Android and iOS dependencies, and a build toolchain that downloads more at install time. Every one of those runs with your app's full privileges on the device — every permission you hold, every credential in your Keystore, every request your app is authorised to make. And the native ones run somewhere else too: on your build machine, as your build user, during . That is a second attack surface with a different blast radius, and it is the one this page spends the most time on. Threat Four distinct ways a dependency hurts you. 1. A known vulnerability, unpatched. The boring one. A transitive package has a published advisory and nobody looked. 2. A compromised release. A maintainer's account is taken over, or a maintainer hands the package to someone else, and a new version ships with additional behaviour. Historically this has meant credential harvesting from CI environments and cryptocurrency address swapping — both of which target the build machine , not the end user. 3. A malicious install script. runs arbitrary code with your shell's privileges, before you have run a single test. Native React Native packages routinely have legitimate install scripts, which is what makes a malicious one plausible. 4. A package that is abandoned. No advisory, no attack, only a native module that no longer builds against React Native 0.87 and whose maintainer stopped answering. This is the most common failure by far and it is a security problem too, because the workaround is usually pinning to an old version of something else. Exploit: what can do The demonstration is short because the attack is short. A package's can carry: runs that script as you, in your repositor"},{"u":"security/safe-logging.html","t":"Safe Logging in Release Builds","s":"Security","d":"Where console output actually goes on a shipped app, what it leaks, and how to strip or redact it without losing your crash diagnostics.","h":["Threat","Exploit","Android: adb logcat","iOS: the device console","The realistic source: a helpful debugging line","Fix","1. __DEV__ guards, which genuinely remove code","2. Strip console.* at build time as a backstop","3. Redact what you deliberately keep","4. Never log the whole request or response","5. Audit your crash reporter and analytics","Verification","1. Watch the log during a real sign-in on a release build","2. Confirm the strip actually ran","3. Lint console out of the codebase","4. Test the redactor","5. Grep the release bundle for credential shapes","Common mistakes","Related topics"],"b":"does not disappear in a release build. It writes to the platform log, where anyone with a USB cable, a bug report, or in some cases another process can read it. A single on the wrong screen turns a token into something recoverable from a device that was plugged into a laptop once. This is the cheapest leak in the section to create and the cheapest to fix. It is also the one that most often defeats everything else on these pages: there is no point putting a refresh token in the Keystore and then printing it. Threat Who reads a device log, realistically: Reader How Effort Anyone with physical access and a laptop , macOS Console.app Minutes A support ticket The user sends a bug report or sysdiagnose containing system logs None — you asked them to A crash reporting service Breadcrumbs that capture output, shipped to a third party None — you configured it A colleague's screen recording A demo with the log window open None Malware on a rooted device Reads everything Already lost Note the second and third rows. The most common way logged secrets escape a device is not an attacker at all — it is your own diagnostics pipeline faithfully carrying them to a server where they persist, get indexed, and outlive the session they belonged to. Exploit Android: Run this against your own release build while signing in. Anything that scrolls past is readable by anyone who can attach to the device — which includes the repair shop, the second hand buyer before a factory reset, and whoever the user hands the phone to. iOS: the device console macOS Console.app shows the same stream with a search box. A sysdiagnose, which Apple support and many enterprise MDM tools routinely ask users to produce, contains it. The realistic source: a helpful debugging line Nobody writes . They write this: And th"},{"u":"migration/upgrade-helper-workflow.html","t":"The Upgrade Helper Workflow","s":"Upgrading and Migration","d":"A repeatable process for moving a Community CLI project between React Native versions using the Upgrade Helper, one minor at a time.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Common patterns","One minor at a time","Pin the version you land on","Re-init when the diff is too large","Verify the toolchain moved too","Clear the caches that hold stale output","Read the release notes, not only the diff","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Upgrading React Native is not . The JavaScript dependency is the small part; the and folders you own contain template files that the React Native team also changes every release, and nothing updates them for you. The React Native Upgrade Helper solves exactly that problem. It is a web tool that diffs the project template between any two versions, so you can see every change the template made and apply each one deliberately. <https://react native community.github.io/upgrade helper/ Why it exists / when to use it — and when NOT to When you run , the CLI copies a template into your repository. From that moment the template and your copy diverge: you add permissions to , you edit , you change the Gradle config, and upstream changes the same files for its own reasons. Nothing reconciles the two. The Upgrade Helper shows you the upstream half of that divergence. You apply the parts that matter, skip the parts you have deliberately changed, and end up with a project that matches the new version without losing your own work. Use it for every upgrade that crosses a minor version. Do not use it for a patch bump within the same minor — to almost never touches the template, and the diff will be empty. There is one case where it is the wrong tool: a project several minors behind with heavy native customisation. Past roughly four or five minors the accumulated diff is large enough that re initialising a fresh project at the target version and moving your source into it is faster and produces a cleaner result. Use to compare afterwards rather than trusting your memory of what was custom. Basic example The whole loop, for a single minor step: Commit after each successful step. When something breaks two hours later you want to bisect your own upgrade, not re derive it. How it works The "},{"u":"migration/breaking-changes-087.html","t":"0.87 Breaking Changes","s":"Upgrading and Migration","d":"The complete list of what React Native 0.87 removed and deprecated, each with its replacement and a before/after pair — usable as an upgrade checklist.","h":["How to use this page","1. Removed — type-level and import-level","1.1 Deep imports into react-native/Libraries/ and react-native/src/private/","1.2 *Properties type aliases","1.3 NativeMethods and NativeMethodsMixin","2. Removed — JavaScript APIs","2.1 InteractionManager","2.2 Modal's animated prop","2.3 StatusBar props backgroundColor, translucent, networkActivityIndicatorVisible","2.4 Boolean keyboardShouldPersistTaps on ScrollView","2.5 useColorScheme() no longer returns an \"unspecified\" string","2.6 The useTurboModules feature flag","2.7 NativeDialogManagerAndroid export, and the undocumented Touchable root export","2.8 react-native/rn-get-polyfills","2.9 @react-native/core-cli-utils","2.10 Standalone react-devtools WebSocket support","2.11 Metro: YAML config files and .es6 extensions","3. Removed — Android native","3.1 UIBlock and UIManagerModule.addUIBlock / prependUIBlock","3.2 New-architecture-flag constructors on DefaultReactActivityDelegate","4. Removed — iOS native","4.1 TimingModule, RCTTurboModuleEnabled(), RCTEnableTurboModule()","4.2 The RCTAppDelegate.h import path","5. Deprecated — still present, do not build on them","5.1 react-native/Libraries/Core/InitializeCore","5.2 @react-native/assets-registry","5.3 ImageBackground","5.4 The NativeMethods interface","5.5 Appearance.setColorScheme with an \"unspecified\" argument","5.6 DrawerLayoutAndroid","5.7 iOS: TimingModule, RCTTurboModuleEnabled(), RCTEnableTurboModule()","6. Behaviour changes that do not break the build","7. The upgrade checklist","Common mistakes","Related topics"],"b":"This is the definitive list for React Native 0.87.1. Every removal and every deprecation is below, with the replacement and, where code is involved, a before/after pair. Work through it top to bottom as a checklist. Two of the changes here are large enough to have their own pages: Migrating to the Strict TypeScript API for the deep import and ref type changes, and CocoaPods to Swift Package Manager for the experimental iOS build path. This page gives the short form of both and links out. Legacy code appears on this page on purpose Every \"before\" block below shows code that no longer works. It is here so you can recognise it in your own project. None of it is a pattern to copy, and none of it compiles against 0.87. How to use this page 1. Run . Under the Strict TypeScript API this catches the type level removals — deep imports, aliases, ref types — as hard errors, which is most of section 1. 2. Grep for the runtime removals, which the compiler cannot always see. 3. Build both platforms. The native removals in sections 4 and 5 surface there. 4. Run the app. The behavioural changes in section 3 do not break the build. A grep pass that finds most of it: 1. Removed — type level and import level 1.1 Deep imports into and Replacement: import from the package root. This is enforced by the map in itself, which sets for . It is a type error, not a lint rule. Imports from were removed , not merely untyped. Everything that used to be reached that way and is still public is re exported from the root — , , , and the family all come from now. Full treatment, including the temporary opt out: Migrating to the Strict TypeScript API. 1.2 type aliases Replacement: the name. , , , , , and the rest were long standing aliases. They are gone. The compiler names the missing type, so finds every"},{"u":"migration/strict-typescript-api.html","t":"Migrating to the Strict TypeScript API","s":"Upgrading and Migration","d":"How the react-native exports map makes deep imports a type error in 0.87, the ref and prop type renames that came with it, and a migration order for a large codebase.","h":["Why it exists / when to use it — and when NOT to","How it works — the mechanism","Basic example","The three migrations, in order of how many files they touch","1. Deep imports become root imports","2. *Properties becomes *Props","3. Ref types: NativeMethods out, instance types in","The temporary opt-out","A migration order for a large codebase","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"The Strict TypeScript API became the default in React Native 0.87. It was opt in from 0.80. The visible symptom is that imports which compiled yesterday are errors today; the cause is a change to the map inside the package, and understanding that mechanism is what lets you fix the errors instead of suppressing them. Introduction states the change. This page explains why it happens, what else moved with it, and the order to fix a large codebase in. Why it exists / when to use it — and when NOT to For most of React Native's history, the public API and the file layout were the same thing. A type you could reach, you could import — so people imported directly, and the React Native team could not move a file without breaking somebody. The Strict API draws a line. What the package root exports is public and supported. Everything inside and is an implementation detail, and the type system now says so. That is what makes internal refactors — including the ones that deleted the legacy architecture — possible at all. There is no \"when not to\". It is the default and there is no supported way to stay on the old surface past 0.88. The opt out below exists to unblock a build for a few weeks, not to be adopted. How it works — the mechanism Modern TypeScript resolves types through the field in a package's , the same way a bundler resolves runtime code. Read straight out of at version 0.87.1: Three lines in that block explain everything: on — when TypeScript asks for types, it gets the generated, curated surface in . That file is machine generated from React Native's Flow definitions and contains exactly the supported public API. on — when TypeScript asks for the types of , the answer is literally \"there are none\". That is what makes a deep import a type error. It is not a lint rule, a "},{"u":"migration/new-architecture-migration.html","t":"New Architecture Migration","s":"Upgrading and Migration","d":"For apps still on 0.81 or earlier — what the old Bridge patterns were, why the architecture flags no longer do anything, and what \"migration\" actually means now.","h":["The single most important thing to understand","The legacy pieces, so you can recognise them","The architecture flags","A legacy native module","The legacy host objects","What replaces each piece","The interop layers — a shim, not a destination","The migration, in the order that works","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"If your app is on React Native 0.81 or earlier and still running the legacy architecture, this page is the map. If you are already on 0.82 or later, there is nothing to migrate: you are on the New Architecture whether you asked for it or not, and the work is described in 0.87 Breaking Changes instead. This page is full of code that no longer works The Bridge, , , and the architecture flags all appear below. They are here so you can recognise them in your own project and in the tutorials you will find while searching. None of it is a pattern to write today. The single most important thing to understand There is no migration switch, because as of React Native 0.82 there is nothing to switch. Version What happened 0.76 (Oct 2024) New Architecture became the default 0.81 (Aug 2025) Last version supporting both architectures 0.82 (Oct 2025) Runs entirely on the New Architecture. The flags are ignored . Bridgeless by default; the Bridge is gone 0.83+ Legacy architecture classes are being removed to cut install size So \"migrating to the New Architecture\" is not a project you plan and execute. It is a consequence of upgrading past 0.82. The real project is: upgrade, then fix everything that assumed a Bridge . That reframing matters because it changes the order of work. You do not enable a flag, test, and roll back if something breaks — there is no rollback. You audit your dependencies and your native code first , fix what you can, and only then upgrade. The legacy pieces, so you can recognise them The architecture flags If a blog post, a Stack Overflow answer or a library's README tells you to set either of these, it was written for 0.81 or earlier. Treat the rest of that source as stale too. Setting them today changes nothing — including, importantly, nothing about the bug you"},{"u":"migration/cocoapods-to-spm.html","t":"CocoaPods to Swift Package Manager","s":"Upgrading and Migration","d":"The experimental Swift Package Manager path for iOS in React Native 0.87 — the commands, the header change, the xcframeworks, and an honest account of what is not settled yet.","h":["Why it exists / when to use it — and when NOT to","The commands","Basic example — migrating an existing project","The header change","The new xcframeworks","Libraries must ship a Package.swift","How it works","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"React Native 0.87 ships an experimental opt in alternative to CocoaPods for the iOS build: Swift Package Manager. It removes Ruby, Bundler and CocoaPods from the toolchain entirely, leaving Xcode as the only requirement. Experimental — commands and layout may still change This is labelled experimental by React Native and this page labels it experimental everywhere. CocoaPods remains the default for and for the ecosystem at large. Do not migrate a project you are about to ship, and do not assume the commands below will be spelled the same way in 0.88. macOS only Every command on this page requires Xcode, which runs only on macOS. There is no Linux or Windows path for any iOS build, CocoaPods or SPM. Why it exists / when to use it — and when NOT to CocoaPods is a Ruby program. That means every machine and every CI runner that builds your iOS app needs a Ruby, a , a , Bundler, and the discipline to use so everyone runs the same CocoaPods version. A surprising share of \"it builds on my machine\" comes from that stack rather than from your code. CocoaPods is also in maintenance, while Swift Package Manager is Apple's own dependency manager and is built into Xcode. Consider the SPM path if you are starting a greenfield project, if you want to try it on a branch, or if Ruby specifically is causing you pain in CI. Do not adopt it if you are close to a release, if your team is not comfortable debugging an Xcode project by hand, or — most importantly — if any of your native dependencies do not ship a . That last one is usually the deciding factor, and it is a question about your dependency list rather than about your app. The commands All of these run from the directory of your project. Command What it does Remove CocoaPods and switch the project to SPM — the migration command Set"},{"u":"migration/native-dependency-compatibility.html","t":"Native Dependency Compatibility","s":"Upgrading and Migration","d":"How to determine whether a library actually supports React Native 0.87 and the New Architecture, before you depend on it.","h":["Why it exists / when to use it — and when NOT to","The four-step check","Step 1 — read the peer dependencies","Step 2 — look for a Codegen config","Step 3 — find the spec files","Step 4 — check the repository","Basic example — auditing before an upgrade","Verified library versions","Two traps almost every tutorial gets wrong","How it works — why \"it installs\" proves nothing","Platform differences","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"succeeding tells you nothing about whether a native library works. npm resolves JavaScript; it does not compile Kotlin, it does not run Codegen, and it does not check that a view manager can be mounted by Fabric. A package with no New Architecture support installs perfectly and then fails when a screen mounts or a method is called. This page is the method for finding that out in advance. It takes about two minutes per package and it is the highest value check you can run before an upgrade. A library without Fabric / TurboModule support is not a valid choice Since 0.82 there is no legacy architecture to fall back to. Interop layers may carry an unmigrated library for a while, but they are a shim over classes that are being deleted. Treat \"has not migrated\" as \"do not adopt\", not as \"adopt carefully\". Why it exists / when to use it — and when NOT to Run this check in three situations: before adding a new native dependency, before starting an upgrade, and when something that installed cleanly behaves strangely at runtime. You do not need it for pure JavaScript packages. A date library, a validation library, a state manager — anything with no , or in its published files — has no native side to be incompatible. and are in this category; they are listed in this handbook's verified table because their versions were checked, not because they carry native code. The four step check Step 1 — read the peer dependencies A well maintained native library declares a peer range, and that range is the maintainer telling you which versions they test. Real output shape, for two packages verified for this handbook: Package Version peer 4.6.0 0.12.2 A range that stops below 0.87 is a direct statement that 0.87 is untested. A package with no peer at all is weaker evidence, not stronger — it u"},{"u":"reference/component-reference.html","t":"Component Reference","s":"Reference","d":"Every component React Native 0.87.1 exports, with its purpose, platform and status — read from the installed type definitions.","h":["How to read the tables","Layout and content","Scrolling and lists","Input and interaction","Overlays and status","Android-specific","iOS-specific","Feedback","Unstable and internal components","Components people expect and core does not have","Basic example","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"This is the complete list of components exported from under the Strict TypeScript API. It was read from in the installed package, not from memory. If a component is not on this page, it is not in React Native core. It is either from a third party package, or it was removed, or it never existed. That is a useful thing to be able to check, because a large share of React Native advice online names components that core does not have. The non component exports — APIs, hooks, utilities — are in API Reference. The two pages together cover the 92 values exports. How to read the tables Platform is , or . An iOS only component renders nothing useful on Android and vice versa. Status is or . Deprecated means the export still exists in 0.87.1 and your code still runs, and also that the replacement exists today and this one will be removed. Every deprecation below is quoted from the annotation in the installed type definitions, not inferred. Layout and content Component Purpose Platform Status The base layout primitive. A flex container that maps to / both current The only component that renders text. Text cannot be a direct child of both current Displays a local or remote image both current An image with children drawn over it both deprecated — \"Use a with an absolutely positioned instead\" Pads content inside the device's safe area iOS deprecated — \"Use instead\" Opts a subtree into a specific layout conformance mode both current (experimental) Replace it with a containing an styled . Remember on the container — the deprecated component clipped for you. The before/after pair is in 0.87 Breaking Changes. It was always iOS only, and it cannot report inset values to your own layout code. (5.9.1) works on both platforms and exposes the insets as numbers, which is what most layouts actua"},{"u":"reference/api-reference.html","t":"API Reference","s":"Reference","d":"Every non-component value React Native 0.87.1 exports — modules, hooks, utilities and Codegen helpers — read from the installed type definitions.","h":["How to read the tables","Device and environment","Styling and colour","Layout measurement","Interaction and gestures","Animation","System UI and user feedback","App lifecycle and registration","Events","Native module access and Codegen","Development and debugging","Text and miscellaneous","The virtual-collection family","Type-only exports worth knowing","Basic example","APIs people expect and core does not have","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"This page lists the 60 non component values exported from , plus the namespace exports and the one function declared directly in the entry point. It was read from in the installed package. Components are on Component Reference. Together the two pages cover the 92 named value exports of the package root. If an API is not on this page or that one, it is not in React Native core. Check before you assume; a great deal of React Native advice online names APIs that were extracted into community packages years ago. How to read the tables Platform is , or . Status is or , and every deprecation is quoted from the annotation in the installed types rather than inferred. Types (as opposed to values) are listed separately at the end — , , and so on are type only exports and cannot be imported as values. Device and environment Export Purpose Platform Status , , , both current Window and screen size, scale and font scale. Prefer in components both current Device pixel density and conversions between dp and physical pixels both current Raw device constants, including the dimensions payload both current Right to left layout state and forcing both current Read and write native user preferences iOS (documented as iOS only; the 0.87.1 types carry no platform annotation) current The running React Native version as both current Foreground/background state and change events both current Hardware back button handling Android current Styling and colour Export Purpose Platform Status , , , , , both current Converts a colour value to the platform's internal representation both current References a named system colour by platform both current A colour that resolves differently in light and dark mode iOS current Hook returning . only when the native Appearance module is unavailable both current Nam"},{"u":"reference/cli-reference.html","t":"CLI Command Reference","s":"Reference","d":"Every command the React Native Community CLI 20.2.0 registers in a 0.87 project, with the flags read from the published packages rather than from memory.","h":["How the CLI is actually wired","Global behaviour","The commands","init","start","bundle","run-android","Shared with build-android","run-android only","build-android","log-android","run-ios","run-ios only","Shared with build-ios","build-ios","log-ios","config","doctor","info","clean","codegen","spm — Experimental","What is not verified on this page","Platform differences","Common mistakes","Related topics"],"b":"This is the command surface of a React Native 0.87.1 project using 20.2.0 . Every command name, flag and default below was read from the published packages — the CLI itself, its platform packages, and 's own — not from a help screen someone pasted into a blog post. Where something could not be verified, this page says so rather than guessing. Those gaps are collected in What is not verified on this page. How the CLI is actually wired This trips people up before they get to any flag, so it comes first. The binary comes from the package. Its does nothing except find and hand over to it. If the CLI is not installed it prints a warning telling you to add it to and exits. must be an explicit devDependency. does not depend on it. The 0.87 template lists , and , all at 20.2.0. The CLI's own bin is named , not . Commands arrive from three places: the CLI package ( , , , , ), 's ( , , , ), and the two platform packages ( , , ). That last point explains an otherwise baffling behaviour: does not exist outside a project, because it comes from a package the project depends on. Remove any globally installed CLI first A stale global or shadows the project's version and produces errors that make no sense against the documentation. Global behaviour Flag Applies to Meaning Every command Registered on each command by the CLI itself, and raises the logger's verbosity , The CLI Prints 's version — not React Native's Any command Commander's generated help for that command To read React Native's own version, ask the package: The commands Command Comes from What it does CLI Creates a new project. Works outside a project → community cli plugin Starts the Metro development server → community cli plugin Builds a JavaScript bundle and copies assets cli platform android Builds and launches on an em"},{"u":"reference/cheat-sheet.html","t":"Cheat Sheet","s":"Reference","d":"The commands, components, style props, native config file locations and removed APIs of React Native 0.87.1, on one page.","h":["Versions","Setup","Everyday commands","Dev menu and reload","Core components","Core APIs worth memorising","Common style props","Layout","Appearance","Text","Flexbox defaults that differ from the web","Where native configuration lives","android/gradle.properties — the 0.87 lines that matter","Info.plist keys you will actually add","AndroidManifest.xml shape","Removed in 0.87 — never write these","Deprecated in 0.87 — still exported, do not build on them","The Strict TypeScript API in three lines","Verified ecosystem versions","The two peer traps","Repository validation commands","Common mistakes","Related topics"],"b":"One page, tables only. Everything here is verified against the installed type definitions, the published packages, and the npm registry on 2026 09 12. Longer explanations are one link away in every section. Versions Thing Value 0.87.1 peer — pin peer 20.2.0 Node engine — minimum 22.13.0 Kotlin = 2.0 (bundled 2.2.0) Android 34 Android / 37 Android Gradle Plugin 9 Unsupported 0.84.x and older Setup Everyday commands Command What it does Metro Build and launch on Android Build and launch on iOS ( macOS only ) Metro with a clean cache Build native libs for one architecture — much faster A specific simulator The autolinking configuration, as JSON OS, toolchain and library versions, for bug reports Diagnose the toolchain; attempts repairs Clear caches logcat, filtered to / Simulator syslog, pick which simulator Run Codegen by hand Which key each variant signs with The AAB you upload to Play Full flag tables: CLI Command Reference. Dev menu and reload Action Android iOS simulator Reload <kbd R</kbd twice <kbd R</kbd twice Dev menu <kbd Ctrl</kbd +<kbd M</kbd <kbd Cmd</kbd +<kbd D</kbd Core components The complete component export surface of 0.87.1. If it is not here, it is not in core. Group Components Layout and content Scrolling and lists Input and interaction Overlays and status Feedback Android only iOS only deprecated in 0.87. Full table with replacements: Component Reference. Core APIs worth memorising API Use / Branch on platform / Styles / Screen size; prefer the hook Returns Read and set the colour scheme; use , not the removed third value Density aware sizing Open and receive URLs Foreground / background Keyboard events and dismissal / / System UI Android runtime permissions Registers the root component in / Reach a native module; returns , throws / Fabric component "},{"u":"reference/troubleshooting.html","t":"Troubleshooting","s":"Reference","d":"Specific React Native 0.87 failures, what actually causes them, and how to fix them.","h":["Environment and setup","error react-native@0.87.1: The engine \"node\" is incompatible","Commands behave strangely, or a flag the docs describe does not exist","npx react-native doctor reports a missing Android SDK","Metro","error listen EADDRINUSE: address already in use :::8081","The app cannot reach Metro on a physical Android device","Changes do not appear, or a module resolves to an old version","Attempted to import the module ... which is not listed in the \"exports\"","0.87 TypeScript errors","Deep imports into react-native/Libraries/... suddenly fail","Cannot find name 'NativeMethods'","Cannot find name 'ViewProperties'","Cannot find name 'InteractionManager'","A Modal or StatusBar prop is rejected","ScrollView's keyboardShouldPersistTaps rejects true","ImageBackground is flagged as deprecated","Dependencies that install cleanly and then fail","Reanimated animations do nothing, or a value \"is not a worklet\"","MMKV throws at startup","Gestures do nothing at all","A library installs, builds, and then crashes at runtime","Native builds","A tutorial tells you to set newArchEnabled=false or RCT_NEW_ARCH_ENABLED=0","Android build fails after upgrading, with AGP or Kotlin DSL errors","iOS fails with 'RCTAppDelegate.h' file not found","iOS builds fail after adding a dependency","A C++ backward-compatibility header is missing","Debugging","npx react-devtools cannot connect","An unhandled promise rejection now floods the console","Common mistakes","Related topics"],"b":"Real failures, with the cause rather than a list of things to try at random. Several of these are specific to 0.87 and did not exist a version earlier. If you are upgrading, read 0.87 Breaking Changes first — most upgrade time errors are listed there with their replacement. Environment and setup Cause. React Native 0.87 requires Node . The minimum is 22.13.0 , and Node 20 is no longer supported. Fix. Check what you are actually running, which is often not what you think inside a CI container or an IDE terminal: Install and select a supported version with your version manager, then confirm again. Note that declares a lower floor than itself — satisfying the CLI is not enough. Commands behave strangely, or a flag the docs describe does not exist Cause. A globally installed CLI is shadowing the local one. This is the single most confusing class of error in the CLI path, because the symptoms look like documentation being wrong. Fix. Remove the globals. There is no supported global CLI any more: Then always invoke it through or an npm script, so you get the version pinned in your . reports a missing Android SDK Cause. is unset, or points at an SDK without the platform React Native 0.87 needs. Fix. 0.87 requires compileSdk 37 and buildToolsVersion 37 , with an aar metadata of 34 . Install those through the Android Studio SDK Manager and confirm is exported in the shell that actually runs the build. See Environment Setup. Metro Cause. A Metro instance is already running — commonly one orphaned from a previous session. Fix. Stop the old one, or run on another port: If you change the port, the app must be told where to look; otherwise it will sit on a red screen saying it cannot connect to the development server. The app cannot reach Metro on a physical Android device Cause. The"},{"u":"expo-getting-started/introduction.html","t":"What Expo Actually Is","s":"Expo: Getting Started","d":"Expo is an SDK, a CLI and a set of cloud services built on React Native — not a fork of it. What that means, and which React Native version SDK 57 gives you.","h":["Which React Native do I actually have?","SDK and React Native version pairing","Why it exists / when to use it — and when NOT to","Basic example","How it works","Continuous Native Generation","Always install SDK packages with expo install","Expo Go vs development builds","Common mistakes","Related topics"],"b":"Expo is three things that ship together: An SDK — a set of well maintained native modules ( , , and about 120 others) with a consistent API and a single version line. A CLI and build system — , , and Continuous Native Generation, which produces the and directories from your config rather than having you hand maintain them. Optional cloud services — EAS Build, Submit and Update. These are paid, with a free tier, and none of them is required to build or ship an Expo app. Expo is not a fork of React Native. An Expo app is a React Native app. The same , , , Fabric renderer and Hermes engine, with an SDK and tooling layered on top. Anything you know about React Native still applies. Which React Native do I actually have? This matters more than anything else on this page, because this site documents two toolchains and they are not on the same React Native version. This half (Expo) The CLI half Toolchain Expo SDK 57 React Native Community CLI React Native 0.86.3 0.87.1 Every page in the Expo sections is written and type checked against React Native 0.86.3 , because that is what SDK 57 ships. Every page in the CLI sections targets 0.87.1. Do not copy code between the two halves without checking The differences are real and they bite. React Native 0.87 made the Strict TypeScript API the default and introduced per component ref types like . Those types do not exist in 0.86 at all , so a snippet copied from a CLI page into an Expo project will not compile. Going the other way, deep imports into still resolve on 0.86 but are hard type errors on 0.87. Type a ref by the component itself on this half: . SDK and React Native version pairing Read out of each release's own : Expo SDK React Native SDK 55 0.83.10 SDK 56 0.85.3 SDK 57 0.86.3 Upgrading React Native independently of the SDK i"},{"u":"expo-getting-started/choosing-expo-or-cli.html","t":"Choosing Expo or the Bare CLI","s":"Expo: Getting Started","d":"A decision framework for picking between Expo SDK 57 and the React Native Community CLI, including what each choice costs you later.","h":["Why it exists / when to use it — and when NOT to","Start from the native surface you need, not from preference","Decision table","What each choice actually costs","What is NOT a reason to avoid Expo","Basic example","How it works","The two toolchains are not far apart at runtime","The escape hatches, in order of cost","Common patterns","Decide once, write it in the README","Prototype in Expo even when you suspect you need bare","Do not choose on ecosystem fear","Common mistakes","Related topics"],"b":"What Expo Actually Is explains what Expo is. This page is about the decision: which of the two toolchains this site documents should you start a project with, and what does each choice cost you six months in. The decision is less permanent than people assume — you can move in either direction — but the moves are not free, and the cost is asymmetric. Adding Expo to an existing native codebase is harder than starting on Expo and taking the native directories over later. Why it exists / when to use it — and when NOT to Start from the native surface you need, not from preference Almost every real answer falls out of one question: can every native change your app needs be expressed in configuration? Expo generates and from your app config and installed packages. If the native changes you need are covered by SDK packages, by an existing config plugin, or by a plugin you can write, Expo handles the native build pipeline and you never open Xcode or Android Studio for build reasons. If they are not, you are fighting the toolchain. The question is narrower than it sounds, because config plugins can edit , entitlements, , Gradle files and Xcode project settings. What they cannot do well is maintain a large body of hand written native code that lives inside the app target itself. Decision table Your situation Choose Why New app, no existing native code Expo The native projects are generated and upgraded for you. You need a library with custom native code that has a config plugin, or needs no native config Expo A development build covers this. Common. You want file based routing, typed routes and deep links out of the box Expo Expo Router ships in the default template. You need over the air updates with a first party path Expo EAS Update. The CLI half has no first party equivalent. "},{"u":"expo-getting-started/prerequisites.html","t":"Prerequisites","s":"Expo: Getting Started","d":"What you need installed before creating an Expo SDK 57 project, which parts are optional, and what genuinely requires a Mac.","h":["Why it exists / when to use it — and when NOT to","The baseline: what everyone needs","What each platform target adds","Android","iOS","Web","Platform differences","How it works","What npx is doing","Checking an existing machine","Common patterns","Use a Node version manager","Do not install a global Expo CLI","Install platform tooling when you first need it","Common mistakes","Related topics"],"b":"Expo SDK 57 needs less installed up front than a bare React Native project does, because the native build tooling only becomes necessary when you build a native binary. This page separates what you need on day one from what you need when you build. Why it exists / when to use it — and when NOT to The honest framing: you need Node and a package manager to start. You need platform SDKs to build. Many people install Xcode and Android Studio before writing a line of code, discover the download takes an afternoon, and conclude the toolchain is heavy. It is not, yet. You will need the platform tooling soon — the point where a project outgrows Expo Go and needs a development build arrives early — but \"soon\" is not \"before \". The baseline: what everyone needs Thing Requirement Node.js A current LTS release. React Native 0.86.3 declares as . A package manager npm, yarn, pnpm or bun. All four are supported by the Expo CLI. Git Not strictly required, but checks git status before it deletes native directories, and that check is worth having. An OS macOS, Windows (PowerShell or WSL 2), or Linux. That is the whole list for creating a project and running it in Expo Go or on the web. Watchman is no longer required Expo documents Watchman as required only for SDK 55 and earlier . On SDK 57 you do not need to install it. Check your Node version before anything else: What each platform target adds Android Needed to run or to build a development build locally. Android Studio , which brings the Android SDK, platform tools and the emulator. JDK 17. Expo documents the Azul Zulu distribution ( on macOS, on Windows). Android SDK Platform 36 (Android 16, \"Baklava\"), plus its Sources. Android Studio installs the newest platform by default, which is not necessarily the one required — select Platfo"},{"u":"expo-getting-started/creating-a-project.html","t":"Creating a Project","s":"Expo: Getting Started","d":"create-expo-app, its templates and examples, what the default template actually contains, and how to strip it back to an empty app.","h":["Basic example","Why it exists / when to use it — and when NOT to","Options","Templates","Examples","How it works","What the default template contains","What else gets generated","Resetting the demo content","Naming","Common patterns","Add the packages you need with expo install","Verify the project before you build on it","Choose blank-typescript when you are learning the mechanics","Common mistakes","Related topics"],"b":"One command creates an Expo SDK 57 project. The interesting decisions are which starting point you pick and what you do with the demo content that comes with the default one. Basic example The package manager you use for creation is the one the project is set up for — the lockfile it writes is what later commands, including and , detect and reuse. Why it exists / when to use it — and when NOT to does three things you would otherwise do by hand: it downloads a template at the version matching the current SDK, installs dependencies with the package manager you invoked it with, and renames the app inside the config. The version matching matters. Templates are published per SDK — the SDK 57 default template is — so the dependency set you get is one that was tested together. Assembling the same project from and a list of packages is how you end up with an SDK 56 library in an SDK 57 app. Options Verified against : Flag Effect Which template to use: , , , , . Default: . Also accepts an npm package name. Clone an example from github.com/expo/examples instead of a template. Accept the defaults without prompting. Create the files, skip installing dependencies. Skip generating , and . Print the version. Usage. Templates What they actually give you on SDK 57: Template Router Language and Dependencies Expo Router 57 TypeScript Generated (git ignored) 20, including , , , Reanimated, Gesture Handler, none JavaScript Generated 4: , , , none TypeScript Generated the same 4, plus and Expo Router 57 TypeScript Generated between and none TypeScript Committed minimal, with the native projects in the repo is a different maintenance model, not just a different template It ships and in the repo. That is the committed native directories strategy, and it changes how you upgrade and how you make"},{"u":"expo-getting-started/project-structure.html","t":"Project Structure","s":"Expo: Getting Started","d":"What every file and directory in a fresh Expo SDK 57 project is for, which ones are generated, and which ones belong in git.","h":["Basic example","How it works","There is no ios/ or android/ — that is deliberate","src/app/ is routing, not a components folder","The entry point depends on the template","app.json is the source of native configuration","tsconfig.json extends the SDK's base",".expo/ and expo-env.d.ts are generated on every start","Common patterns","What belongs in git","Where to put things that are not routes","Colocating tests and platform variants","Resetting the template's demo content","Common mistakes","Related topics"],"b":"A fresh Expo project has fewer directories than a bare React Native one, and the missing ones are the point: there is no and no because those are generated. This page walks the tree the SDK 57 default template produces and says, for each entry, whether it is yours, generated, or cache. Basic example What leaves on disk, before you run anything: also runs , so the project is already a repository with one commit. The / / trio is generated by default; pass at creation time if you do not want them. After the first , three more entries appear, all generated and all git ignored: How it works There is no or — that is deliberate The default template's ends with: The native projects are build output . They are produced from plus your installed packages when you run , or , and they are regenerated whenever that runs again. Treating them as source is the single most expensive mistake available in an Expo project — see Continuous Native Generation for the mental model and expo prebuild for the destructive part. If a directory appears after a build, do not edit it. Edit the config that produced it. is routing, not a components folder On SDK 57's default template, everything under is a route. A file at becomes the route with no registration step anywhere. A file at wraps the routes beside and below it. This is why exists separately: a component that is not a screen must not live under , or the router will treat it as a route. The distinction trips people up once and then never again. Older tutorials put routes in a top level directory. Both work — Expo Router looks for at the project root and inside — but SDK 57's default template uses , and mixing the two in one project is ambiguous. Pick one. The app Directory covers the routing rules properly. The entry point depends on the templa"},{"u":"expo-getting-started/running-on-a-simulator.html","t":"Running on a Simulator","s":"Expo: Getting Started","d":"Opening an Expo SDK 57 app in the Android emulator and the iOS Simulator, the difference between expo start and expo run, and what each one needs installed.","h":["Basic example","Why it exists / when to use it — and when NOT to","Platform differences","How it works","What actually runs on the simulator","The dev server auto-detects a development build","When a native rebuild is actually required","Common patterns","Keep Metro running across native rebuilds","Clear the bundler cache before blaming your code","Measure on a release build, never a debug one","Common mistakes","Related topics"],"b":"There are two ways to get your app onto a simulator, and they are not variations of the same thing. attaches a JavaScript bundle to an app that already exists. / compiles a native app first. Knowing which one you are doing explains most of the confusing errors in this area. Basic example Start the dev server and open a simulator from it: Then press a key in the terminal: Key Action open Android open iOS simulator (macOS only) open web reload app open debugger toggle the dev menu on the device more tools open the project in your editor switch between Expo Go and a development build show all commands Or skip the key press: and let you choose Pressing or opens whichever device the CLI picks. prompts you to select an Android device or emulator; prompts you to select an iOS simulator. Press to see the full list of commands, which includes these. Why it exists / when to use it — and when NOT to is what you run all day. It starts Metro, serves your JavaScript, and connects to an app already installed on the simulator — either Expo Go or your development build. and do a full native build: generate the native project if it is missing, compile it, install it on the device, and start Metro. You run these when the native side changed — a new native dependency, an app config change that affects native files, an SDK upgrade — and not otherwise. A native build takes minutes; a JS reload takes under a second. and invoke prebuild If or does not exist, these commands generate it. If it does exist and is malformed, the CLI offers to clear and reinitialise it. Any hand edits you made inside those directories can be destroyed. Read expo prebuild before running either on a project with hand edited native code. Platform differences Works on macOS, Windows and Linux. You need Android Studio, J"},{"u":"expo-getting-started/running-on-a-device.html","t":"Running on a Device","s":"Expo: Getting Started","d":"Getting an Expo SDK 57 app onto a physical phone — QR codes, LAN and tunnel hosting, and what a real iPhone needs that a simulator does not.","h":["Basic example","Why it exists / when to use it — and when NOT to","How it works","Getting the dev server onto the phone's network","What the phone has to have installed","Platform differences","Common patterns","Fix \"the QR code scans but nothing loads\" in order","Set a scheme before you need it","Open the dev menu on hardware","Test notifications and permissions on hardware only","Common mistakes","Related topics"],"b":"Physical devices are the only place some things are observable: real touch latency, real thermal behaviour, real camera and sensor hardware, real push notifications. They are also the only iOS target available if you do not have a Mac. The mechanics are the same as a simulator — a dev server plus an installed app — with one extra problem to solve: the phone has to be able to reach the dev server. Basic example The terminal prints a QR code and a URL. What you do with it depends on which app is on the phone: Installed app How to connect Expo Go Scan the QR code with the Expo Go app on Android, or the Camera app on iOS. The terminal says \"Scan the QR code above to open in Expo Go.\" A development build Scan the QR code with the system camera, or open the dev client's launcher screen and pick the server. The terminal says \"Scan the QR code above to open in a development build.\" The CLI also prints the Metro URL, which you can type into the dev client's launcher by hand when scanning is inconvenient. The QR code is not the same thing in both cases Under Expo Go the QR encodes a URL Expo Go knows how to open. Under a development build it encodes a link in your app's own URL scheme , which is why has to be set in the app config for a dev build to be reachable this way. If the scheme cannot be resolved the CLI prints \"Linking is disabled because the client scheme cannot be resolved.\" and only shows the Metro URL. Why it exists / when to use it — and when NOT to Use a device when the thing you are testing is a property of hardware: camera, biometrics, GPS, push notifications, haptics, Bluetooth, thermals, or real world performance. Use a simulator for everything else, because the iteration loop is faster and the tooling is better integrated. One exception makes devices mandatory"},{"u":"expo-getting-started/running-on-web.html","t":"Running on the Web","s":"Expo: Getting Started","d":"What Expo SDK 57 actually supports in the browser — react-native-web, the three web output modes, static rendering and where the parity ends.","h":["Basic example","Why it exists / when to use it — and when NOT to","How it works","Required packages","The three output modes","What static rendering does and does not do","server output and API Routes","The bundler","DOM components: the escape hatch for web-only libraries","Platform differences","Common patterns","Check module support before you rely on it","Keep the web build honest with a CI export","Do not treat the browser as a secure environment","Common mistakes","Related topics"],"b":"Expo bundles for the web with the same Metro pipeline it uses for native, mapping imports to . The result is a real web app: your and components render to DOM elements, Fast Refresh works, and produces static files you can host anywhere. What it is not is a promise of parity. Plenty of the SDK has no web implementation, and the honest way to use the web target is to know in advance which parts of your app will work. Basic example The default template already has everything web needs. Start the dev server and press , or: Build for production: That writes to by default. changes it. Why it exists / when to use it — and when NOT to The web target earns its keep in three situations: A marketing or content surface that shares code with the app — shared design system, shared data layer, one repository. A fast layout and logic loop. Browser devtools, no simulator boot time, instant reload. Useful even for an app that will never ship to the web. Sharing a preview with someone who cannot install a build. A URL is easier than distributing a binary. It is the wrong tool when your app's value is in native capability — camera, background tasks, biometrics, notifications, high frequency gestures. Those either do not exist on web or behave differently enough that maintaining the web path costs more than it returns. How it works Required packages The default template includes and already. is genuinely required — the CLI refuses to start web without it. is technically optional but not in practice: without it the CLI warns that \"Some React Native components may not work on web without it\", which is another way of saying your app will not render. If you never want a web target, remove it from the config rather than leaving it half configured: The CLI then refuses with a clear message inste"},{"u":"expo-getting-started/dev-server-and-fast-refresh.html","t":"The Dev Server and Fast Refresh","s":"Expo: Getting Started","d":"What npx expo start actually runs, the terminal commands it accepts, how Fast Refresh decides between patching and reloading, and when a cache clear is the real fix.","h":["Basic example","Why it exists / when to use it — and when NOT to","The terminal interface","Start flags worth knowing","How it works","The dev server picks its target from your dependencies","Fast Refresh, and when it gives up","Reload versus refresh versus rebuild","Clearing the Metro cache","Environment variables","The dev menu and the debugger","Common patterns","Keep one dev server, not three","Separate components from constants for better refresh behaviour","Restart after dependency changes","Common mistakes","Related topics"],"b":"runs Metro, serves your JavaScript over HTTP, and keeps a WebSocket open to every connected client. Everything else on this page — Fast Refresh, the terminal key commands, the dev menu, the debugger — hangs off that one process. Understanding where the boundary sits between \"the dev server can fix this\" and \"you need a native rebuild\" saves more time than any other piece of Expo knowledge. Basic example Edit a file, save, and the change appears on every connected device in well under a second. No rebuild, no reinstall. Why it exists / when to use it — and when NOT to The dev server exists so the JavaScript half of your app can iterate without touching the native half. Native code is compiled and installed; JavaScript is fetched at runtime. That split is what makes React Native development fast, and it is also the source of the most common confusion in the ecosystem: a change the dev server cannot serve needs a rebuild, and nothing tells you which is which until something fails. The boundary: Change Dev server handles it Needs Any / / file Yes Styles, images, JSON, fonts already bundled Yes Adding a JavaScript only package Yes (restart the server) Adding a package with native code Yes Changing native keys, plugins, permissions, scheme Yes Adding or editing a config plugin Yes Upgrading the SDK Yes Editing or Restart with The terminal interface With the server running, the terminal accepts single key commands. Verified against 57.0.24: Key Action switch between Expo Go and a development build open Android select an Android device or emulator open the iOS simulator (macOS only) select an iOS simulator open web reload the app open the debugger toggle the dev menu on the device more tools open the project in your editor show the project QR code show all commands The line abo"},{"u":"expo-core-concepts/expo-go-vs-development-builds.html","t":"Expo Go vs Development Builds","s":"Expo Core Concepts","d":"Expo Go is a prebuilt sandbox app with a fixed set of native modules. A development build is your own binary. Why a library with custom native code fails at runtime in Expo Go, and when to switch.","h":["Why it exists / when to use it — and when NOT to","The failure: a runtime error, not a build error","Why it happens at runtime","Decision table","Basic example — creating a development build","How it works","Detecting Expo Go at runtime","Common patterns","Switch early and write it down","Keep Expo Go for experiments only","Rebuild on native change, not on every change","Common mistakes","Related topics"],"b":"There are two different apps that can load your project's JavaScript during development, and confusing them is the most common beginner mistake in Expo. Expo Go is an app Expo publishes to the App Store and Play Store. It was compiled by Expo, ahead of time, with a fixed set of native modules inside it. Your project contributes only JavaScript. You cannot add native code to it. A development build is your app, compiled from your project's native dependencies and app config, with added so it connects to Metro and fast refreshes the same way. Expo's own documentation calls Expo Go a playground that is not useful for production grade projects. Treat it that way. A development build is the normal way to develop a real app, and most projects need one early — often the first week. Why it exists / when to use it — and when NOT to Expo Go exists so that you can run SDK code on a phone without installing Xcode or Android Studio and without waiting for a native build. That is genuinely valuable for: Trying an SDK package before you commit to it. Teaching, workshops and first day exploration. Sharing a small reproduction with someone who has no native toolchain. Do not use Expo Go as your development environment once any of these is true: You installed a library with custom native code that Expo Go does not contain. You need a config plugin to take effect (permissions strings, entitlements, Gradle settings). You need your own bundle identifier, app name, icon or URL scheme — for deep links, OAuth redirects or push notifications with your credentials. You want the thing you test to resemble the thing you ship. For a real app, one of those arrives almost immediately. The failure: a runtime error, not a build error This is the part that catches people, because nothing fails where you"},{"u":"expo-core-concepts/continuous-native-generation.html","t":"Continuous Native Generation","s":"Expo Core Concepts","d":"In an Expo project the ios/ and android/ directories are build output generated from the app config and installed packages, not source you maintain. What that buys you, what it limits, and how it works.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Config plugins are how you extend it","What CNG cannot express well","Common patterns","Inspect what gets generated","Review the resolved config instead of native files","Regenerate after native-affecting changes","Common mistakes","Related topics"],"b":"Continuous Native Generation (CNG) is the idea that the and directories of an app are build output , produced on demand from inputs you do control: your app config ( or ); the packages in , and the config plugins they register; a native project template matched to your SDK version. A new project from has no or directory at all. The default template's ends with: That is the whole model in two lines. The native projects are regenerated whenever they are needed, the same way is regenerated in a web project, and nobody commits them. Why it exists / when to use it — and when NOT to The alternative — committing native projects and editing them by hand — is what the React Native Community CLI does. It works, and it has one recurring cost: every React Native upgrade is a merge. The new template's native files change, your hand edited copies have changed too, and someone reconciles the two by hand. CNG removes that merge. When the SDK changes, the template changes, and the native projects are regenerated from it with your config applied on top. Nothing to reconcile, because nothing was hand edited. Use CNG when: every native change your app needs can be expressed in the app config, an SDK package, a community config plugin, or a plugin you write; you want SDK upgrades to be a version bump plus a regenerate; you want reviewers to read a 3 line diff rather than a 300 line diff. Do not rely on CNG when: you have a large body of hand written native code in the app target that no plugin can sensibly reproduce; your organisation requires the native projects to be committed and audited as source; you are adding React Native to an existing native app. In those cases, commit the native directories and stop regenerating them. That is a supported choice with real costs, compared in CNG vs "},{"u":"expo-core-concepts/prebuild.html","t":"expo prebuild","s":"Expo Core Concepts","d":"npx expo prebuild generates ios/ and android/ from your app config. In SDK 57 it clears and regenerates them by default, destroying hand edits. The model, the flags, and the two strategies you must choose between.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Choosing a strategy","Strategy 1: Stay fully CNG","Strategy 2: Commit the native directories and stop running prebuild","The failure mode: mixing them","Common patterns","Guard the CNG choice in CI","Guard the committed choice in CI","Preview a plugin's output before committing to it","Common mistakes","Related topics"],"b":"generates the native and projects for an Expo app from the app config, installed packages and config plugins. It is the command that implements Continuous Native Generation. SDK 57 prebuild deletes the native directories first By default, clears and and then regenerates them. Any change made by hand inside those directories — a Gradle tweak, an edit, a hand added build phase, a custom key — is gone . Verified in the installed : the option is computed as , and when it is true the CLI calls with on each platform directory before copying the template. Before running prebuild on a project that already has native directories, decide which of the two strategies below you are following. Running prebuild over hand edited native code is the single most common way to lose work in an Expo project. Why it exists / when to use it — and when NOT to Use prebuild when the native directories are build output : you want them generated from config, and you are prepared to regenerate them whenever the SDK, a native dependency or the app config changes. Do not run prebuild on a project whose or contain hand edits you want to keep. On such a project, prebuild is not a sync — it is a reset. You often do not need to call it directly. and call prebuild only when the platform directory does not exist , and EAS Build runs it for projects that do not commit native directories. Basic example Every flag above is listed in for SDK 57. The complete set: Flag Effect Apply changes to the existing native folders instead of recreating them Skip installing npm packages and CocoaPods Platforms to sync. Default Native project template: a local tar file, npm package or GitHub repo Preserve the listed packages' versions in / / / Package manager for installs is still accepted for compatibility, but it no longer"},{"u":"expo-core-concepts/app-config.html","t":"The App Config","s":"Expo Core Concepts","d":"app.json, app.config.ts and app.config.js — the file that describes your Expo app's name, identifiers, icons, plugins and native settings. Which form to use, how they combine, and when dynamic config earns its keep.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Resolution order","The dynamic form, typed","Reading config values at runtime","Seeing the resolved result","Common patterns","Per-environment builds without duplicating the file","Native settings the config has no key for","Config changes need a rebuild","Common mistakes","Related topics"],"b":"The app config is the file that describes your app to the Expo toolchain: its name, version, bundle identifier and package name, icons and splash screen, URL scheme, permissions, config plugins and experiments. Under Continuous Native Generation it is the main input from which and are generated, so for most native settings it is the source of truth rather than or . It comes in three forms: File Kind Language Static JSON, config under a top level key Dynamic TypeScript, exports an object or a function Dynamic JavaScript, exports an object or a function also resolves , , and , and a static — verified from in the installed — but the three above are what you will meet in practice. Why it exists / when to use it — and when NOT to Start with . Both SDK 57 templates ship it, and it has a practical advantage the dynamic forms lack: tools can edit it. adds a package's config plugin to for you — but only in a static config. With a dynamic config the CLI cannot safely rewrite your code, so it tells you what to add and you add it by hand. Move to when the config has to compute something: different bundle identifiers, names or icons per environment (development, preview, production); values read from environment variables at build time; sharing values with other build scripts; conditional plugins. Do not move to a dynamic config just to get comments or because TypeScript feels more serious. If every value is a constant, is simpler, and tooling can maintain it. The app config is not a place for secrets Values under , and much of the rest of the config, are embedded in the app and readable at runtime through . Anything in the config ships to every user. Never put an API secret there. See What Ships Inside the Bundle. Basic example This is the generated by with the SDK 57 default templ"},{"u":"expo-core-concepts/expo-install-and-sdk-alignment.html","t":"expo install and SDK Alignment","s":"Expo Core Concepts","d":"Why SDK packages must be installed with npx expo install rather than a bare package-manager install, how it picks versions for your SDK, and how --check and --fix find and repair mismatches.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Checking and fixing alignment","Common patterns","Gate CI on alignment","Upgrading the SDK","Deliberately leaving the SDK's version","Do not try to upgrade React Native on its own","Common mistakes","Related topics"],"b":"An Expo SDK release is a set of native packages built and tested against one React Native version. SDK 57 pairs with React Native 0.86.3 , and every package in it — plus the community native libraries the SDK tracks — has a version range chosen for that pairing. is the command that keeps your project inside that set. It is a thin wrapper over your package manager that rewrites the version before installing. The second line is named here only to warn against it. Why it exists / when to use it — and when NOT to A bare resolves the dist tag on npm. follows the newest SDK, not yours. If the newest SDK is ahead of your project, you get a package compiled against a different and a different React Native. That mismatch rarely fails at install time. It fails at runtime , as a crash or a missing native method, sometimes only on one platform, sometimes only in a release build. It is one of the most common causes of a broken Expo app, and it is miserable to diagnose because nothing about the install looked wrong. Use for: every package; community native libraries the SDK tracks — , , , , , and the rest; , and itself during upgrades. For a package the SDK does not track — a pure JavaScript library, or a native library outside the map — passes the name through to your package manager unchanged, so using it everywhere is harmless and a good habit. Basic example On SDK 57 that installs and — the values in — not the newer versions the CLI half of this site documents for React Native 0.87. The versions are genuinely different — Gesture Handler is a whole major version apart — so never copy a version number from a CLI page. See Choosing Expo or the Bare CLI. A few SDK 57 values, read from the installed map: Package SDK 57 range You can read any entry yourself: How it works Read from the "},{"u":"expo-core-concepts/cng-vs-committed-native.html","t":"CNG vs Committed Native Directories","s":"Expo Core Concepts","d":"The one structural decision inside an Expo project — generate ios/ and android/ from config every time, or commit them and maintain them by hand. The trade-offs, how to decide, and how to move in either direction.","h":["Why it exists / when to use it — and when NOT to","Choose CNG when","Choose committed native directories when","Basic example","How it works — the trade-offs in full","Common patterns","Moving from CNG to committed","Moving from committed back to CNG","A middle ground that is not a mixture","Common mistakes","Related topics"],"b":"Every Expo project is on one of two strategies for its native projects, whether or not the team has noticed: 1. Continuous Native Generation (CNG). and are ignored by git and regenerated from the app config by whenever needed. 2. Committed native directories. and are in git, edited by hand, and prebuild is never run again. Both are supported. Both ship real apps. What is not supported is a mixture, because in SDK 57 prebuild clears and regenerates the native directories by default — and a mixture means someone's hand edits are sitting in a directory the tool believes it owns. Mixing the strategies destroys work Committed native directories plus a single equals lost hand edits. Ignored native directories plus a hand edit equals the same loss on the next generate, usually on another machine. Choose one, document it, and enforce it in CI. Why it exists / when to use it — and when NOT to The decision exists because there are two sources of truth for native configuration — the app config and the native files — and only one can win. Choose CNG when Your native needs are covered by SDK packages, the app config, , community config plugins, or plugins you are willing to write. You want SDK upgrades to be a version bump and a regenerate. Your team is mostly JavaScript and TypeScript engineers. You build on several machines or services and want them to produce identical native projects. Choose committed native directories when You have substantial hand written native code inside the app target that cannot reasonably be expressed as plugins or moved into modules. Policy requires native project files to be reviewed and audited as source. You are integrating with native tooling that edits the Xcode or Gradle projects directly and cannot be driven through config. You have tried to exp"},{"u":"expo-router/fundamentals.html","t":"Expo Router Fundamentals","s":"Expo Router","d":"File-based routing for React Native, using React Navigation code that expo-router vendors internally. What Expo Router v6 in SDK 57 gives you, what it costs, and the mental model that makes the rest of this section make sense.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The entry point","Filenames are the routing language","Layouts are navigators","Navigation has two forms","Platform differences","Common patterns","Group your app by layout, not by feature","Keep route files thin","Put shared state above the navigator","Performance considerations","Common mistakes","Related topics"],"b":"Expo Router turns the files in your directory into the routes of your app. A file at is the route. A file at matches . There is no central route table to keep in sync with your screens, because the file system is the route table. , not A project generated by on SDK 57 puts the routes in , alongside , and , with a path alias pointing at . A root level directory still works and is still found automatically — the router looks in both. This section writes because that is what the template produces. It is not a new navigation engine. Expo Router ships its own copy of React Navigation — the installed expo router 57.0.21 has no dependency and instead vendors core, native, native stack, bottom tabs, drawer and elements under — and it exposes React Navigation's stack, tabs and drawer navigators behind a file based convention. Everything you know about React Navigation's object, screen options and lifecycle still applies — see Migrating from React Navigation. This section documents 57.0.21 , the version Expo SDK 57 pins. That is the v6 generation. Tutorials written for Expo Router v2 or v3 will be wrong about typed routes, about where is imported from, and about , all of which are covered here. Why it exists / when to use it — and when NOT to The problem it solves is drift. In a hand registered navigator, adding a screen means touching at least three places: the screen file, the navigator, and the param list type. Those three drift apart, and the symptom is a route that exists in the type but not at runtime, or vice versa. File based routing removes the drift by removing the duplication. It also gives you two things that are difficult to retrofit: Every screen has a URL. Deep links, universal links and web routes all fall out of the same route tree rather than needing a separate "},{"u":"expo-router/app-directory.html","t":"The app Directory","s":"Expo Router","d":"Every filename convention Expo Router understands in SDK 57 — index, dynamic segments, catch-alls, groups, layouts, the plus-prefixed special files, and platform-specific routes.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The full filename vocabulary","index versus a named file","Generated routes you did not write","Route ordering and specificity","Platform differences","Platform-specific route files","Web-only special files","Common patterns","Colocate nothing; export from outside","Use a group for the authenticated area","Name dynamic segments after what you read","Common mistakes","Related topics"],"b":"The directory is the route table. Expo Router reads it through a bundler context module, converts each filename into a route, and hands the result to React Navigation. Learning the filename vocabulary is most of learning Expo Router. Where the directory lives on SDK 57 generates , with , and beside it and a alias pointing at . A root level also works — the router finds either. The conventions on this page are identical in both layouts; only the prefix differs. This page is the reference for that vocabulary. The concepts each get a fuller treatment on their own pages, linked below. Why it exists / when to use it — and when NOT to The directory is a convention, not configuration, which means there is no second place for a route to be defined and therefore no way for the two to disagree. The cost is that the directory is load bearing : a file you drop in to \"keep it near the screen that uses it\" becomes a route, appears in the sitemap, and gets bundled as a screen. So the rule is narrow: holds routes and layouts, and nothing else. Components, hooks, utilities and tests live beside it — in the generated template, , and . The config plugin exposes a option that points the router at some other directory. The option's own documentation says to avoid it, and the two standard locations ( and root level ) are found without it. Leave it alone. Basic example How it works The full filename vocabulary Filename Route Notes the directory itself is ; is the ordinary case one dynamic segment — see Dynamic Routes catch all, matches the remaining path no segment organisational only — see Groups duplicated an array group generates one route tree per name not a route the navigator for its directory — see Layouts unmatched URLs one per directory; the root one is the global fallback not a rout"},{"u":"expo-router/layouts.html","t":"Layouts","s":"Expo Router","d":"What _layout.tsx does, how Slot differs from a navigator, how nesting works, and where providers, auth gates and shared chrome belong.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","A layout is rendered above its routes, and stays mounted","Slot renders the child route without a navigator","Stack.Screen in a layout configures; in a screen it overrides","Layouts nest, and so do navigators","unstable_settings picks the anchor route","Error boundaries are a layout-adjacent export","Platform differences","Common patterns","Root layout that mounts providers and nothing else","A group layout that adds chrome without a navigator","Hiding a screen's header only when it has one","Performance considerations","Common mistakes","Related topics"],"b":"A file wraps every route in its directory. It is where the navigator lives, where providers are mounted, and where anything that must survive navigation between sibling routes belongs. Layouts are the piece that turns a flat list of files into a navigation tree. A directory with a exporting is a stack; the same directory exporting is a tab bar. Nothing else about the files changes. Why it exists / when to use it — and when NOT to Without layouts, every screen would have to render its own chrome and its own providers, and state would be destroyed on every navigation because each screen is a separate React tree. A layout stays mounted while its children come and go. Write a when you need: A navigator — , , , or a custom one. Persistent state or providers — a session context, a query client, a theme. Shared chrome — a header, a background, a safe area wrapper. A guard — see Redirects and Auth Gated Routes. Do not write one just to add padding to a group of screens. A layout that only renders inside a styled is a component; put it in the screens or in a shared wrapper. Each layout is a real React Navigation navigator boundary, and unnecessary boundaries make and behave in ways nobody predicted. Basic example Every route file in renders inside . How it works A layout is rendered above its routes, and stays mounted When you navigate from to , does not unmount. Its state survives; only the screen below it changes. That is the entire reason providers go here. renders the child route without a navigator is the layout equivalent of . It renders whichever child route is active and adds no navigator, no header and no animation. Use when the directory needs a wrapper but not a navigator — most commonly at the root of a web first layout, or in a group that exists only to attach a pro"},{"u":"expo-router/stack.html","t":"Stack","s":"Expo Router","d":"The native stack navigator in Expo Router — screen options, headers, the SDK 57 Stack.Toolbar API including Stack.Toolbar.Badge, and Stack.Protected.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Options come from three places","The options worth knowing","Declarative header components","Stack.Toolbar — buttons, menus and badges in the header","Stack.Protected gates screens declaratively","Platform differences","Common patterns","Hide the header globally, opt in per screen","A title that depends on fetched data","Per-platform animation without duplicating the layout","Performance considerations","Common mistakes","Related topics"],"b":"is the default navigator and the one most screens live in. It renders a native stack — on iOS, native fragments on Android — through , so push animations, the back gesture and the header are the platform's, not a JavaScript imitation. That is a complete root layout. Every route file in becomes a screen of it. Why it exists / when to use it — and when NOT to A stack models \"I went somewhere, and I can come back\". It is the right default because it matches what the platform back gesture and the Android back button already do. Use something else when: The destinations are peers, not a history — a tab bar or drawer. See Tabs and Drawer. You need no chrome and no history at all — use (see Layouts). There is also a JavaScript stack exports a JavaScript implemented stack with the same file conventions. Use it when you need a transition the native stack cannot express, or when you are running on web and want identical animations everywhere. It costs you the native feel; prefer the native . Basic example Note how a nested route is named: , the path relative to this layout, not . How it works Options come from three places In increasing order of precedence: 1. on — the default for every screen. 2. on in the layout — per route, known statically. 3. rendered inside the screen file — per route, at render time, so it can depend on data. The options worth knowing Option What it does header title, and the default back label on iOS show or hide the native header header floats above content , , , , , , , , , , , , , , , , and the variants allow the interactive back gesture detent stops for the drag handle on a form sheet is how you get modals; that has its own page, Modals. Declarative header components SDK 57's exposes header pieces as components you render from inside a screen, which i"},{"u":"expo-router/tabs.html","t":"Tabs","s":"Expo Router","d":"The three tab implementations in Expo Router 57 — the JavaScript Tabs navigator, the alpha native tabs, and the headless UI primitives — and how to pick one.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The tab bar is driven by screen options","Each tab keeps its own state","Native tabs use the platform's real tab bar","Headless tabs give you routing without a bar","Platform differences","Common patterns","A stack inside each tab","A badge driven by state","Hiding the tab bar on a detail screen","Performance considerations","Common mistakes","Related topics"],"b":"A tab layout is a directory whose exports a tabs navigator. Each route file in that directory becomes a tab. Expo Router 57 ships three tab implementations, which is confusing until you know why: the original JavaScript one, an alpha native one that uses the platform's real tab bar, and a headless set of primitives for when you want to draw the bar yourself. Why it exists / when to use it — and when NOT to Tabs are for peer destinations : places the user switches between, where each keeps its own state and its own history. Feed, Search, Profile. They are not for a flow. If step two only makes sense after step one, that is a stack. And a tab bar with seven items is a menu that has been drawn badly — both platforms degrade past five. Which implementation: Import Use when JavaScript tabs the default; full styling control, works on web Native tabs you want the genuine platform tab bar (iOS liquid glass, Android Material) and can accept an alpha API Headless tabs you are drawing a completely custom bar and want only the routing Where is imported from changed In the installed 57.0.21 types, is marked deprecated in favour of . The old import still works and still appears in much of the published documentation. This page uses the non deprecated path. Basic example The group keeps out of the URLs — see Groups. How it works The tab bar is driven by screen options Option What it does the tab label and the header title a badge value on the tab colour of the focused tab hide labels and show icons only whether each tab gets its own header Expo Router addition — set to to hide a route from the bar is the Expo Router specific one, and it is how you keep a route inside the tabs directory without giving it a tab: Each tab keeps its own state Switching tabs does not unmount the tab you le"},{"u":"expo-router/drawer.html","t":"Drawer","s":"Expo Router","d":"The drawer navigator in Expo Router 57 — the expo-router/drawer entry point, the peer dependencies it needs, custom drawer content, and why it needs a development build.","h":["Why it exists / when to use it — and when NOT to","Installation","Basic example","How it works","The options that matter","Opening and closing it","DrawerToggleButton for the header","Custom drawer content","Hiding a route from the drawer","Platform differences","Common patterns","Permanent on large screens, overlay on small","A drawer wrapping tabs","Performance considerations","Common mistakes","Related topics"],"b":"A drawer is a panel that slides in from the edge of the screen. In Expo Router it is a navigator like any other: a directory whose exports , with each route file in that directory becoming a drawer item. Unlike and , comes from a separate entry point and needs peer packages installed. That is the part people get wrong. Why it exists / when to use it — and when NOT to A drawer is for a long, flat list of destinations that do not deserve permanent screen space: settings, archived items, account switching, an admin section. Do not use it for primary navigation on a phone. It hides the destinations behind a gesture or a hamburger button, which costs discoverability, and on Android it competes with the system back gesture. Tabs are the better default for the three to five things people actually use. A drawer earns its place when: there are more destinations than a tab bar can hold, and most of them are visited rarely, and you are on a tablet or the web, where the drawer can stay permanently open. Installation is bundled inside — it uses internally, so there is no package to install. What you do need are the gesture and animation packages: On SDK 57 that resolves , and — the versions the SDK pins. Reanimated 4 does not bundle the worklets runtime, which is why is a separate install. Expo Go vs development build All three of these are native modules. , and are included in Expo Go, so a drawer will run there — but the moment you add any other library with native code, Expo Go stops being an option. Plan on a development build. Reanimated also needs its Babel plugin. In an SDK 57 project wires this up, so check before adding it by hand. Basic example is the text in the panel; is the header title. They are separate because they usually differ — the drawer entry is a noun, the hea"},{"u":"expo-router/dynamic-routes.html","t":"Dynamic and Catch-All Routes","s":"Expo Router","d":"Square-bracket filenames in Expo Router — [id] for one segment, [...rest] for the remainder, how they are matched, and how to read them safely.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","One segment versus the remainder","Matching order","Multiple dynamic segments","Route parameters and query parameters arrive together","Every parameter is a string, and may be missing","Platform differences","Common patterns","A catch-all that inspects the path","A typed helper instead of repeating the assertion","Optional-looking segments are two files, not one","Security considerations","Common mistakes","Related topics"],"b":"A filename in square brackets captures part of the URL as a parameter. matches one path segment; matches everything left. This is how a list screen and a detail screen share one file, and how an app answers URLs it has never seen before. Why it exists / when to use it — and when NOT to Use a dynamic segment when the URL identifies which of a set of things to show, and the set is open ended: posts, users, products, documents. Use a catch all when the shape of the remainder is genuinely unknown: a file browser, a documentation site, a proxy for server defined paths. Also use one for a global when you want to inspect the unmatched path rather than just show a message. Do not use a dynamic segment for a small, known set. for exactly three sections gives you a runtime check where you could have had three files and a compile time one. Do not put anything in a URL parameter that the user should not be able to change. A parameter is attacker controlled input on native, because deep links carry it. See Deep Links and Universal Links. Basic example Linking to it: Prefer the object form. It survives a rename of the parameter, it is what typed routes check against, and it handles escaping for you — an id containing or breaks the string form. How it works One segment versus the remainder File Matches Does not match — A dynamic segment yields a string ; a catch all yields a string array . This trips people up because both come out of the same hook. Matching order More specific wins, regardless of the order the files happen to be read in: So a route literally named is not shadowed by . You cannot configure this ordering; it is the algorithm. Multiple dynamic segments Each bracketed directory or file contributes one key. Two segments in the same path must not use the same name. Route p"},{"u":"expo-router/groups.html","t":"Groups","s":"Expo Router","d":"Parenthesised directories in Expo Router — organising routes and attaching layouts without adding a URL segment, plus array groups and shared routes.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Groups are invisible to URLs, visible to layouts","Two groups can both claim the root","unstable_settings and the anchor route","Array groups share one set of files across several trees","Targeting a group explicitly","Platform differences","Common patterns","Split the app into signed-out and signed-in trees","Attach a provider to part of the app only","Group by chrome, not by feature","Performance considerations","Common mistakes","Related topics"],"b":"A directory whose name is wrapped in parentheses is a group . It organises files and gives them a layout, but it contributes nothing to the URL. That is the whole feature. Its usefulness comes from what a directory implies: a directory can have its own , so a group is how you give a set of routes their own navigator without pushing a segment into every URL. Why it exists / when to use it — and when NOT to Without groups, every layout boundary would show up in the URL. An app with a tab bar would have and , and a signed in area would be . Those segments carry no meaning for the user and would be baked into every link, bookmark and deep link. Use a group when: A set of routes needs a shared layout — a tab bar, a drawer, an auth only stack. A set of routes needs a shared provider — attach it in the group's . You want to separate concerns in the file tree — and at the root, with nothing in the URL to show for it. Do not use a group when you actually want the segment. needs a real directory, not . And do not add a group whose is only wrapped in a — that is a component, and an extra navigator boundary changes back behaviour for no gain. Basic example The URLs are , and . never appears. How it works Groups are invisible to URLs, visible to layouts Expo Router strips group segments when building the URL, but keeps them when building the navigator tree. So a group is simultaneously: absent from s, from , and from the address bar on web; present in , which reports the file path, not the URL. That asymmetry is the basis of the auth pattern: tells you which part of the file tree the user is in, even though the URL does not. Two groups can both claim the root Each group may have its own , and both are . That is a collision, and the router cannot pick for you: Only one route may answ"},{"u":"expo-router/typed-routes.html","t":"Typed Routes","s":"Expo Router","d":"How experiments.typedRoutes turns the app directory into TypeScript types — what Expo CLI generates into .expo/types, why the tsconfig include is what makes it work, and what is and is not checked.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What npx expo start generates","What the generated file contains","What is checked","Dynamic routes want the object form","Reading params by route","Platform differences","Common patterns","Typing your own navigation helpers with Href","Generating types in CI without starting the dev server","Restarting generation after a large move","Common mistakes","Related topics"],"b":"Typed routes make an that points at a route that does not exist a compile error . With the feature on, fails in your editor instead of opening a blank screen at runtime. The types are not written by hand. Expo CLI reads the directory and generates a declaration file into , and TypeScript picks it up through your . If either half is missing, everything still compiles — it just stops checking anything. Why it exists / when to use it — and when NOT to Routes in Expo Router are strings. Strings do not refactor: rename to and every in the codebase is now wrong, silently. Typed routes close that gap by generating a union of every valid path from the file tree. Use them in every TypeScript Expo Router project — on SDK 57 turns them on by default. They are not runtime validation. The types describe which paths the app can build; they say nothing about URLs arriving from outside through a deep link, and they do not check that a parameter value is well formed. See Deep Links and Universal Links for that half. Skip them only if the project is JavaScript, where there is nothing to check against. Basic example The SDK 57 template already contains both halves. The flag lives in the app config: And includes the generated files: Start the dev server once, and the types exist: From then on, , , and the search param hooks are checked against your routes: How it works What generates When the dev server starts and is , Expo CLI does four things (read from 's type generation source in SDK 57): Effect File Writes a reference to Expo's global types in the project root Adds that file to Adds and to if they are missing Generates the route declarations, and regenerates them as files in change is one line and says it should not be edited: When you set to , the next does the reverse: it deletes an"},{"u":"expo-router/navigation-and-params.html","t":"Navigation and Params","s":"Expo Router","d":"Link, router and useRouter in Expo Router 57 — push versus navigate versus replace, dismissing, passing and reading URL parameters, and focus-scoped work.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The router methods","push versus navigate versus replace","Link takes the same instructions as props","Passing params","Reading params: local versus global","Updating params without navigating","Where you are: usePathname and useSegments","Running work only while a screen is focused","Platform differences","Common patterns","Close a flow and land somewhere specific","Only show a back control when there is something to go back to","Encode, don't concatenate","Performance considerations","Common mistakes","Related topics"],"b":"Expo Router gives you two ways to move between routes and a small set of hooks for reading where you are. Every navigation target is an — a path string or a object — because every screen has a URL. Declarative: . Imperative: the object, or the identical object returned by . Reading state: , , , . Everything on this page was checked against the installed 57.0.21 type definitions. Why it exists / when to use it — and when NOT to In React Navigation you navigate to a screen name and pass params as a JavaScript object. In Expo Router you navigate to a URL , and params are URL parameters. That has consequences: A param is a string (or an array of strings) when you read it — never an object, function or class instance. Anything you pass must survive being written into a URL, because on web it is, and on native it can arrive the same way from a deep link. So do not use params as a transport for data. Pass an identifier and load the data on the destination screen, or share it through a store or context mounted in a layout. Use when the thing on screen is a link. Use when navigation is a consequence of something else — a form submitting, a sign in completing, a timer firing. Basic example How it works The methods Method What it does pushes a new screen, even if that route is already in the stack pushes, or unwinds to an existing instance of the route if one is in the stack swaps the current screen out; the old one leaves the history goes back one entry in the closest navigator that can whether has somewhere to go pops screens (default 1) from the closest stack pops until is reached; if it is not in the stack, replaces the current screen with it returns to the first screen of the closest stack — React Navigation's whether the closest stack has more than one screen updates the cur"},{"u":"expo-router/modals.html","t":"Modals","s":"Expo Router","d":"Presenting routes as modals and form sheets in Expo Router 57 — presentation options, sheet detents, dismissing, deep-linked modals, web behaviour, and guarding unsaved changes.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","presentation values","Form sheets and detents","Dismissing","A modal with its own stack","Deep-linking straight into a modal","Platform differences","Common patterns","A dismiss control that works on every platform","Guarding unsaved changes","Returning a result from a modal","Performance considerations","Common mistakes","Related topics"],"b":"A modal in Expo Router is an ordinary route presented differently. You create a file, and in the stack that owns it you set in its screen options. There is no separate modal API, which means a modal has a URL, can be deep linked, and is dismissed with the same methods as any other stacked screen. Why it exists / when to use it — and when NOT to A modal says \"finish or abandon this, then return to exactly where you were\". Composing a message, picking a filter, confirming a purchase. Use a route based modal when the content is a destination: it has its own state, may be linked to, and should survive a reload on web. Do not use one for: A transient confirmation. An is lighter and does not add a history entry. A tooltip, popover or in screen menu. Those are views, not routes. Content that must stay on top while the user navigates elsewhere. A modal is part of the stack; navigating away from it dismisses or covers it. React Native's own component still exists and is appropriate for view level overlays that do not deserve a URL. The route based modal is the right choice when they do. Basic example How it works values is a native stack option. The installed types allow (the default push) and the presentations: Value Behaviour the platform's standard modal modal whose background shows the previous screen / presented within the current context rather than full window covers the whole screen; no swipe to dismiss on iOS a sheet with detents iOS page sheet Form sheets and detents A form sheet stops at heights you choose. takes fractions of the screen height, or : The Expo documentation lists two constraints worth designing around: Android supports at most three detents, and headers and nested navigators are not supported inside a form sheet. Dismissing Call Effect in a modal pops t"},{"u":"expo-router/nested-navigators.html","t":"Nested Navigators","s":"Expo Router","d":"How nested _layout.tsx files become nested navigators in Expo Router 57, and what that does to back, dismiss, headers, anchors and reaching a parent navigator.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","A child navigator is a screen of its parent","Where a navigation lands","back() bubbles up","dismiss acts on the closest stack","Anchors and cold starts","Reaching a parent navigator","Platform differences","Common patterns","Full-screen routes at the root, browsing inside tabs","Switching tab and resetting its stack","Performance considerations","Common mistakes","Related topics"],"b":"Every that exports a navigator creates a navigator, and a directory inside another directory creates a navigator inside another navigator. The file tree is the nesting. You never register a child navigator as a screen of its parent, the way you do in React Navigation; Expo Router does that from the directories. The navigation itself is path based: works from anywhere. What nesting decides is which navigator the new screen lands in , and therefore what is on screen around it and what back does afterwards. Why it exists / when to use it — and when NOT to Nesting is how you get both kinds of navigation at once: peers (tabs, a drawer) and history (a stack). The two most common shapes each answer a real design question: Shape Result Stack around Tabs detail screens pushed on the outer stack cover the tab bar Stack inside each tab detail screens stay inside the tab; the bar stays visible, and each tab keeps its own history Many apps use both: a stack inside each tab for browsing, and routes at the root for things that should cover everything (settings, a full screen player, modals). Do not nest to organise files. Every navigator is a boundary that changes what and do, allocates native views, and adds a header. If a directory only needs grouping, use a group without a . If it only needs a wrapper, use a component. Basic example A stack inside a tab, so opening a post keeps the tab bar on screen: Two header decisions keep this from showing doubled headers: the outer stack hides its header for , and the tabs hide theirs, so the only visible header is the inner feed stack's. How it works A child navigator is a screen of its parent In the tree above, the outer has two screens: and . The has two screens: and . The inner has and . That is why and name a directory — from the parent's"},{"u":"expo-router/deep-linking.html","t":"Deep Links and Universal Links","s":"Expo Router","d":"How URLs reach an Expo Router 57 app — the custom scheme, Android App Links with assetlinks.json, iOS Universal Links with apple-app-site-association, rewriting incoming paths, and treating every link as untrusted input.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","From URL to screen","Building URLs to hand out","Rewriting incoming paths with +native-intent","Platform differences","Common patterns","Read the URL the app was opened with","One link format for web and native","Security considerations","Threat","Exploit","Fix","Verification","Common mistakes","Related topics"],"b":"Because every Expo Router screen has a URL, deep linking needs no routing code. A link to or opens with set to , the same way an in app would. What does need work is getting the operating system to hand those URLs to your app. There are two kinds of link, and they are set up differently: Custom scheme App Links / Universal Links Looks like Setup one app config key app config plus a file hosted on your domain Proves you own it no — any app can claim the scheme yes — the OS checks your domain Falls back to a website no yes, when the app is not installed Why it exists / when to use it — and when NOT to Use a custom scheme for links that only ever come from your own surfaces — an OAuth redirect, a link inside a notification you send, a QR code in your own app. It is quick and works offline. Use App Links and Universal Links for anything shared publicly: emails, marketing pages, messages between users. They open the app when it is installed and the website when it is not, and the domain verification means another app cannot intercept them. Do not treat either as authenticated. A deep link is a URL anyone can construct and any app or web page can fire at your app. Verification proves the domain is yours; it says nothing about who built the URL. See Security considerations. Basic example The SDK 57 template sets a scheme already: With that, resolves to . Rebuild the native app after changing — it is written into the native projects, so a JavaScript reload does not pick it up. Test it on a development build: (or ) does the same. Expo Go vs development build Expo Go has its own scheme ( ), so your and your verified domains do not apply there. Test deep links in a development build, where the app config you wrote is the one compiled into the app. How it works From URL to screen 1"},{"u":"expo-router/redirects-and-auth.html","t":"Redirects and Auth-Gated Routes","s":"Expo Router","d":"Keeping signed-out users out of signed-in routes in Expo Router 57 — Stack.Protected as the recommended mechanism, the Redirect component, why the gate belongs in a layout, loading the session behind the splash screen, and what client-side gating does not protect.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","A failing guard unregisters the screens","Flipping the guard redirects and clears history","Two groups, one URL","Guards nest","Loading the session without a flash","The older pattern: Redirect in a layout","Platform differences","Common patterns","Static redirects for moved URLs","A one-off redirect from a screen","Security considerations","Common mistakes","Related topics"],"b":"An auth gated app has two route trees — one for signed out users and one for signed in users — and a rule that decides which one is reachable. Expo Router 57 gives you two mechanisms for the rule: Mechanism What it does Use for (and , ) removes screens from the navigator while a is the auth gate — recommended navigates as soon as it renders one off redirects, and projects still on the older pattern Both are verified in the installed 57.0.21 types: takes and is attached to , , , the JS stack, top tabs and any navigator built with ; takes , and . The Expo authentication guide for current SDKs uses , and keeps the redirect based version as the guide for SDK 52 and earlier. This is navigation, not security Protected routes and redirects run in the app . They decide what the user is shown; they do not stop anyone reading data. Anything a signed out user must not see has to be refused by your server , on every request, based on a credential it verifies. The Expo documentation says the same: protected screens are evaluated on the client only. Why it exists / when to use it — and when NOT to Without a gate, a signed out user can reach a signed in screen three ways: through a stale , through the back button after signing out, and through a deep link. The gate has to close all three, at every screen in the tree, including screens added next year. Put the gate in a layout, not in the screens. A layout wraps every route below it, so one check covers the whole subtree: Check in each screen Check in the layout New screens unprotected until someone remembers protected by default Deep link to a nested route the screen mounts, renders, then redirects never registered, or redirected before children render Flash of protected content yes — first render happens before the redirect no Back a"},{"u":"expo-router/api-routes.html","t":"API Routes","s":"Expo Router","d":"Server endpoints defined as +api.ts files in an Expo Router 57 project — what web.output server requires, the handler signature, calling routes from native, and the honest hosting story.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Filenames","Handlers","Server-side helpers from expo-server","Calling routes from the app","Deploying","Platform differences","Common patterns","Keeping a secret server-side","Returning an error from deep inside a handler","Security considerations","Common mistakes","Related topics"],"b":"An API route is a file in whose name ends in . Instead of a screen, it exports functions named after HTTP methods, and those functions run on a server when a request arrives at that path. That is a working endpoint at — once there is a server to run it. Nothing in your app binary executes this code. API routes are supported in SDK 57, but they are a web server feature : they need , an export step, and hosting that can run server side JavaScript. Why it exists / when to use it — and when NOT to API routes let you keep server code in the same repository, next to the routes that call it, using the same file conventions. Typical uses: Holding a secret — calling a third party API with a key that must never ship in the app bundle. Aggregating or shaping data for the app, so the client makes one request instead of five. Webhooks from a payment provider or other service. Do not choose them expecting free hosting, or expecting them to run \"inside\" the native app. They are server code, and you are operating a server: You need somewhere to deploy it. EAS Hosting is Expo's paid hosted service with a free tier; the alternatives are adapters for platforms such as Express, Bun, Netlify and Vercel, each with its own costs and limits. Check the current terms of whichever you pick before committing. The native app talks to it over the network, so it is subject to latency, outages and versioning like any backend. If you already have a backend, or your app has no server side needs, you do not need API routes. They are also a poor fit for long running work, WebSockets, or code that needs native binaries. Basic example 1. Switch the web output to in the app config. The SDK 57 template defaults to , which pre renders HTML and cannot run request time code: 2. Create the route: 3. Run the dev s"},{"u":"expo-router/error-boundaries.html","t":"Error Boundaries","s":"Expo Router","d":"Catching render errors per route in Expo Router 57 — the ErrorBoundary export, its error and retry props, how boundaries nest through layouts, the navigator-level fallback, and what a boundary does not catch.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The props","Boundaries nest through the React tree","A default for every screen in a navigator","The built-in fallback","Platform differences","Common patterns","Report the error once","Give the user a way out, not only retry","Surfacing an async failure to the boundary","Security considerations","Common mistakes","Related topics"],"b":"Any route or layout file can export a component named . If rendering that route throws, Expo Router renders your in its place instead of taking down the whole app. It is a React error boundary with the class boilerplate removed: the router wraps the route in one for you and passes your component the error and a way to recover. Why it exists / when to use it — and when NOT to An uncaught render error unmounts the React tree above it up to the nearest boundary. With no boundary, that is the whole app: in a release build the user sees a blank screen. A per route boundary limits the damage to one screen, keeps the navigator and tab bar working, and gives the user a way out. Add one where a screen renders data you do not control — server responses, user content, parsed params — and at the root as a last resort. It is not a general error handling mechanism: Errors in event handlers ( , ) do not reach it. React boundaries only catch errors thrown while rendering, in lifecycle methods and in constructors. Rejected promises in or a chain do not reach it unless you turn them into render state and throw during render. A URL that matches no route is not an error. That is , covered in The app Directory. Basic example A root level boundary catches anything no closer boundary handled: How it works The props Verified from the installed 57.0.21 types: clears the error and renders the route again. If the cause is still there — the same bad data — it throws again and the boundary shows again. Retry is for transient failures; for anything else, pair it with a way to leave the screen. Boundaries nest through the React tree A route file's wraps that route. A layout's wraps the layout, and everything the layout renders — including its child routes. An error propagates upward to the nearest bo"},{"u":"expo-router/migrating-from-react-navigation.html","t":"Migrating from React Navigation","s":"Expo Router","d":"Moving an app from hand-registered React Navigation navigators to Expo Router 57 — what stays the same because expo-router vendors React Navigation's code internally, what maps to a file, and where the two genuinely differ.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The concept map","What you keep, imported from expo-router","Params are strings now","Nested navigation becomes a path","Auth: conditional screens become Protected","Platform differences","Common patterns","Migrate in order","Keeping an old URL working","Performance considerations","Common mistakes","Related topics"],"b":"Expo Router does not bring a different navigation engine. The installed 57.0.21 package vendors React Navigation's code internally : it has no dependency, and instead ships its own copy under ( , , , , , , and more), with some files marked in their source as forked from React Navigation. Its is documented as mirroring React Navigation's object. So the navigation object, screen options, focus events and themes follow the React Navigation API you already know. What Expo Router replaces is the registration — the , the calls, the screen list and the config — with the file system. So most of a migration is moving code, not rewriting it. The part that needs thought is params, because Expo Router's params are URL parameters. Two React Navigation versions on this site The CLI half of this site documents React Navigation 7 ( 7.3.18) on React Native 0.87 . This page is about Expo SDK 57 , which ships React Native 0.86.3 , and 57.0.21. In that version carries its own copy of the React Navigation code, exposed at , and does not depend on the packages. Do not copy version numbers or install commands from the CLI navigation pages into an Expo project. Why it exists / when to use it — and when NOT to Migrate when the costs of hand registration are the ones you are paying: screens registered in one file, typed in another and linked in a third; a config that drifts from the navigator; web URLs that do not exist. Do not migrate when: The project is on the React Native Community CLI without Expo. Expo Router is an Expo SDK package. Stay on React Navigation, which is documented in the CLI half of this site. Navigation is owned by native code in a brownfield app. Expo Router expects to own the root. Your navigation depends heavily on passing objects or callbacks as params. That pattern does"},{"u":"expo-sdk/images.html","t":"Images with expo-image","s":"Expo SDK Libraries","d":"expo-image replaces the core Image component with caching, placeholders, transitions and a shared-reference API — plus the SDK 57 cache read/write functions.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","source accepts more than a URL","Caching is a policy, not a boolean","Static methods on Image","New in SDK 57: writeToCacheAsync and readFromCacheAsync","useImage for a decoded reference","Native configuration","Platform differences","Common patterns","Authenticated images","Prefetching the next screen's images","Stable identity in a recycling list","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"is a drop in replacement for React Native's core that adds a real disk and memory cache, blurhash and thumbhash placeholders, cross fade transitions, and a native image reference type you can pass around without re decoding. On SDK 57 it is . resolves the version that matches your installed SDK from . A plain package manager install fetches , which is routinely built for a different SDK — and native module mismatches fail at runtime, not at install time. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to React Native's core has no meaningful disk cache you can control, no placeholder story, and no way to hold a decoded image in memory and hand it to another view. gives you all three, backed by SDWebImage on iOS and Glide on Android. Use it for essentially every remote image, and for any local image you show in a list. Do not reach for it when: You need the image as pixel data for processing. hands you an opaque (a on Android, a on iOS), not a buffer. Decode it yourself. You are rendering SVG. Use ( on SDK 57 — a different version from the CLI half of this site). You only ever show one bundled d asset. Core is fine and costs you no extra native code. Expo Go vs development build Works in Expo Go. ships in the Expo Go module set, and none of its features require a config plugin change on a default project. The one exception is the config plugin option below ( ). That changes how the native iOS target links, so it only takes effect in a build you produce yourself — a development build or a release build. Setting it changes nothing about Expo Go. Basic example How it works accepts more than a URL takes a URL string, a d number, an object, an array of sources, an , or an prefixed SF Symbol name on iOS. When you pass an array, the source that"},{"u":"expo-sdk/fonts-and-splash-screens.html","t":"Fonts and Splash Screens","s":"Expo SDK Libraries","d":"Loading custom fonts with expo-font at build time or runtime, and controlling the native splash screen with expo-splash-screen so the two do not fight each other.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","Fonts at build time (recommended)","Fonts at runtime","How it works","The expo-font API surface","The expo-splash-screen API surface","Native configuration","Platform differences","Common patterns","Holding the splash for more than fonts","Loading a font you only learn about at runtime","Checking what actually loaded","Performance considerations","Common mistakes","Related topics"],"b":"Fonts and splash screens are one topic because they share one problem: the first frame. A custom font that is not ready yet renders as the system font and then reflows; a splash screen hidden too early shows that reflow to the user. and are the two halves of getting the first frame right. On SDK 57 that resolves and . Always use — it reads and picks the SDK matched version, which a plain package manager install does not. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to React Native has no built in way to register a font family at runtime, and no API for the native launch screen. covers both build time linking (the config plugin) and runtime loading ( / ). gives you and so you decide when the splash disappears. Prefer the config plugin for fonts you ship with the app. It links the font into the native project, so the family is available on the very first frame with no loading state at all. Reach for runtime loading only when the font is not known at build time — a font downloaded per tenant in a white label app, or one chosen from a remote config. Do not use to build a branded animated intro. It controls the native launch screen, which is a static image by design on both platforms. An animated intro is a normal React component you render after the splash is hidden. Expo Go vs development build Package Expo Go Notes runtime loading ( , ) Works Nothing native changes. config plugin ( array) Needs a development build The plugin copies font files into the native project at prebuild. Expo Go's binary has no idea your fonts exist. JS API ( , , ) Works Expo Go shows its own splash image, not yours. config plugin (your image, background colour) Needs a development build Same reason: the image is baked into the native project. The pattern is co"},{"u":"expo-sdk/file-system-and-storage.html","t":"File System and Storage","s":"Expo SDK Libraries","d":"Choosing between expo-file-system, AsyncStorage, SQLite and SecureStore, and using the File / Directory / Paths API that expo-file-system exposes in SDK 57.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Paths, File and Directory","Synchronous by default","Downloads and uploads","The legacy API still exists","Native configuration","Platform differences","Common patterns","Cache versus document, decided once","Reading JSON without a manual parse","Checking free space before a big download","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"An Expo app has four distinct places to put data, and picking the wrong one is the most common storage mistake. handles files on disk. AsyncStorage handles small unencrypted key–value pairs. handles structured, queryable data. handles secrets. They are not interchangeable. That resolves on SDK 57. reads and installs the version built for your SDK; a plain package manager install fetches , which fails at runtime rather than at install time. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Pick by the shape of the data, not by convenience: You have Use Why Binary or large text data: photos, downloads, exports, caches It is the only one that gives you a real file path and streaming. A handful of small settings: theme, onboarding seen, last tab AsyncStorage ( , on SDK 57) Simple, synchronous feeling, no schema. Rows you need to query, sort or join Reading a 5 MB JSON blob to find one record is not storage, it is a linear scan. Tokens, keys, anything an attacker wants Keychain / Keystore backed. AsyncStorage is plaintext. Do not use as a key–value store by writing one JSON file per key. You get no atomicity, no query, and a directory listing that grows without bound. Do not use AsyncStorage for anything above a few hundred kilobytes. On Android it is backed by a single SQLite table with a per row size limit that varies by device; on iOS it is a file that is fully read into memory. Expo Go vs development build Works in Expo Go. is in the Expo Go module set, and all of the , and APIs work there. The config plugin options ( , ) set iOS keys, so they only take effect in a build you produce yourself. In Expo Go, the documents directory is Expo Go's, not your app's — paths you record in development will not exist in your production build. Basic "},{"u":"expo-sdk/secure-store.html","t":"Secure Store","s":"Expo SDK Libraries","d":"expo-secure-store stores small values in the iOS Keychain and the Android Keystore — what that actually protects, what AsyncStorage does not, and the biometric gating option.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","API surface","SecureStoreOptions","keychainAccessible constants","Biometric gating","Native configuration","Platform differences","Common patterns","A typed wrapper that fails closed","Storing an object","Treating null as \"re-authenticate\"","Security considerations","What the Keychain and Keystore actually give you","What AsyncStorage does not protect","Common mistakes","Related topics"],"b":"stores string key–value pairs in the iOS Keychain and in Android's backed by the hardware Keystore. It is where refresh tokens, session cookies and any client side key belong. It is not a general storage API: values are strings, and the store is sized for secrets, not data. That resolves on SDK 57. See expo install and SDK Alignment for why the command matters. Why it exists / when to use it — and when NOT to Use it for anything whose disclosure is a security incident: refresh tokens, OAuth access tokens, API keys scoped to a user, a device binding secret, a PIN hash. Do not use it for: Bulk data. Values are strings, and each read and write crosses into the Keychain / Keystore. It is not a cache. Anything over a couple of kilobytes belongs in or . A secret the server should hold. No client side store makes a shared API key safe. If the secret must never be seen by the user, it must never reach the device. See What Ships in the Bundle. Non sensitive preferences. AsyncStorage is faster and simpler for . Expo Go vs development build Works in Expo Go — with one real exception. Basic / / work in Expo Go. does not reliably work in Expo Go. The package's own documentation says so plainly: the option is unsupported in Expo Go when biometric authentication is available, because Expo Go's binary has no string. You need a development build with the config plugin applied. Two further caveats that are easy to lose a day to: Simulators and emulators do not enforce biometrics the way a real device does. Testing on a simulator can succeed when it would prompt on hardware. Test on a real device. Values written in Expo Go live under Expo Go's Keychain / Keystore identity , not your app's. They are not visible to your development build, and vice versa. Basic example How it works API surfa"},{"u":"expo-sdk/sqlite.html","t":"SQLite","s":"Expo SDK Libraries","d":"expo-sqlite gives an Expo app a real on-device SQLite database — opening it, parameterised queries, migrations with user_version, transactions, the React provider, and the SQLCipher option.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Opening a database","Query methods","Tagged template queries","Migrations with PRAGMA user_version","The React provider","Native configuration","Platform differences","Common patterns","Atomic batch writes","Reusing a prepared statement","Streaming a large table","Performance considerations","Security considerations","SQL injection","Data at rest","Common mistakes","Related topics"],"b":"embeds SQLite in your app and exposes it through an async (and a synchronous) JavaScript API. It is the right home for structured, queryable data that has to survive an app restart: offline caches, drafts, message history, anything you would otherwise fake with a large JSON blob. That resolves on SDK 57. See expo install and SDK Alignment for why the command matters. Why it exists / when to use it — and when NOT to Use it when you need to query data: filter, sort, join, paginate, or update one record out of thousands without rewriting the rest. Do not use it for: Secrets. A plain SQLite file is readable plaintext on a rooted device or from a debuggable build. Tokens belong in . If the whole database is sensitive, use the SQLCipher option described below. A handful of preferences. AsyncStorage is simpler for . Large binary blobs. Store images and video as files with and keep the path in a row. Expo Go vs development build Works in Expo Go — except for SQLCipher. The core API is included in Expo Go. The config plugin options ( , , , , ) change how SQLite is compiled , and Expo Go's binary is already built. The Expo documentation states plainly that SQLCipher is not supported on Expo Go. Any of those options means a development build. Basic example How it works Opening a database resolves a . The file lives in inside the app sandbox unless you pass a directory. Connections are cached by name; pass in the options when you deliberately need a second one. Meaning Turns on SQLite's update hook so fires. Default . Open a new connection even if one with that name is cached. Default . for the libSQL integration; requires the plugin option. Query methods Method Returns Use it for Schema and multi statement scripts. Binds no parameters. / / ; exposes and . One row. All rows, loaded"},{"u":"expo-sdk/notifications.html","t":"Notifications","s":"Expo SDK Libraries","d":"expo-notifications for local and push notifications in an Expo SDK 57 app — permissions, Android channels, foreground handling, scheduling triggers, push tokens, and what Expo Go can no longer do.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Permissions","Android channels","Foreground behaviour","Triggers","Responding to taps","Native configuration","Platform differences","Common patterns","Registering for push","Cancelling what you scheduled","Security considerations","Push tokens are send credentials","Notification payloads are untrusted input","Lock-screen exposure","Common mistakes","Related topics"],"b":"covers both halves of notifications: local notifications your app schedules on the device, and push notifications a server sends through APNs (iOS) or Firebase Cloud Messaging (Android). It also handles permission prompts, Android notification channels, badges, categories with action buttons, and responding when the user taps a notification. That resolves on SDK 57. and are used below to detect a real device and read your EAS project ID. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Use local notifications for reminders and timers the device can fire on its own. Use push notifications when the event happens on your server: a message arrived, an order shipped. Do not use them for: Guaranteed delivery. Push is best effort. The OS may throttle, delay or drop notifications, and the user can revoke permission at any time. Anything that must arrive belongs in your app's own sync. Running code on a schedule. A notification shows something to the user. For work, see Background Tasks. Sensitive content in the payload. Notification text appears on the lock screen and passes through Apple, Google and (if you use it) Expo's push service. Expo Go vs development build Local notifications work in Expo Go. Push notifications need a development build. The Expo documentation is explicit: remote (push) notification functionality is unavailable in Expo Go on Android from SDK 53 onward, and local notifications remain available in Expo Go. In practice you should treat push as development build only on both platforms, because push credentials (APNs, FCM), the entitlement and your notification icon all belong to your app binary, not Expo Go's. See Why You Need a Development Build. Basic example A local notification, shown in five seconds, that is also dis"},{"u":"expo-sdk/camera-and-media.html","t":"Camera and Media","s":"Expo SDK Libraries","d":"Taking photos and scanning barcodes with expo-camera, letting users pick images with expo-image-picker, and saving to the photo library with expo-media-library — with the usage strings that stop iOS from crashing.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","expo-image-picker","expo-camera","expo-media-library","Native configuration","Platform differences","Recovering a result Android threw away","Common patterns","A QR code scanner that fires once","Performance considerations","Security considerations","Photos leak location","Ask for the least access","Common mistakes","Related topics"],"b":"Three packages cover the camera and the photo library, and picking the right one saves a lot of code: Package SDK 57 Use it when The user picks an existing photo, or takes one with the system camera UI. You need an in app camera preview: custom UI, barcode scanning, video recording. You read the library directly or save a file into it. Install only the ones you use; each adds permissions to your app. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Reach for first. The system picker is familiar, handles cropping, and on iOS needs no photo library permission at all to let the user choose images. Most \"upload an avatar\" features need nothing else. Use only when the system UI does not fit: a QR scanner, a document capture overlay, a camera embedded in a screen. Do not use to let a user choose one photo — that requests broad library access for a job the picker does without it. Users notice, and app review asks why. Expo Go vs development build All three are included in Expo Go , so you can prototype there. Two caveats: Your usage strings do not apply in Expo Go. The permission prompts show Expo Go's text, not yours. The first time your own strings are exercised is in a development build — test there before release. Simulators have no camera. and need a physical device; the iOS Simulator has no camera to open. Basic example Pick one image with the system picker: How it works Function Behaviour Opens the system photo picker. Opens the system camera. Requires camera permission first. / Camera permission. / Library permission, when you need it. Recovers a result lost because Android killed the activity. Both launchers resolve an : either or . Always check before touching . Option Meaning , , , or an array of them. Show the crop UI. crop ratio"},{"u":"expo-sdk/location.html","t":"Location","s":"Expo SDK Libraries","d":"expo-location in an Expo SDK 57 app — foreground position and watching, geocoding, and background location and geofencing through TaskManager, with the permission escalation and store review it triggers.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Permissions come in two tiers","Reading position","Watching position","Geocoding","Background location and geofencing","Native configuration","Platform differences","Common patterns","Asking for background only when the feature starts","Stop what you start","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"reads the device's position, watches it as the user moves, converts between addresses and coordinates, and — with — keeps receiving locations and geofence events while the app is in the background. That resolves on SDK 57. Background location also needs ( ). See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Use foreground location for \"near me\" search, filling in an address, or showing the user on a map. Think hard before using background location. It is the most scrutinised permission on both platforms: it needs a second, separate user grant, a visible indicator or foreground service notification, and an explicit justification in both App Store and Google Play review. If your feature works with \"while using the app\", ship that. Do not use location as: An identity or anti fraud signal. The user controls the device, and positions exist. A precise trigger for time critical work. Background updates and geofence events are delivered when the OS decides, not instantly. Expo Go vs development build Foreground location works in Expo Go. Background location and geofencing need a development build. The Expo documentation states that background location is not available in Expo Go for Android and is not supported in the Expo Go app on iOS. Background location depends on , Android foreground service permissions and your own usage strings — all properties of your binary, not Expo Go's. See Why You Need a Development Build. Basic example How it works Permissions come in two tiers Function Grants / Location while the app is in use. / Location while the app is backgrounded. Requires foreground first. The foreground response carries platform detail: ( , , ) and ( , , ). A user can grant approximate location only, so check — or the size of — before ass"},{"u":"expo-sdk/sensors.html","t":"Sensors","s":"Expo SDK Libraries","d":"expo-sensors in an Expo SDK 57 app — accelerometer, gyroscope, magnetometer, barometer, light sensor, device motion and pedometer, with update intervals, units, and the platform limits that decide what you can build.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","The shared sensor API","What each sensor measures","DeviceMotion","Pedometer","Native configuration","Platform differences","Common patterns","Detecting a shake","Throttling what reaches React state","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"exposes the device's motion and environment sensors as event streams: accelerometer, gyroscope, magnetometer, barometer, light sensor, a fused stream, and a pedometer. You subscribe with a listener, set an update interval, and remove the subscription when you are done. That resolves on SDK 57. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Use it for shake gestures, tilt controls, a spirit level, a compass needle, step counting while a screen is open, or orientation aware UI. Do not use it for: Screen orientation. Use the hook or ; they report what the UI actually did, which is what layout needs. Location or heading on a map. 's gives a calibrated compass heading; raw magnetometer values need calibration you would have to build. Background step tracking. Pedometer updates are not delivered while the app is in the background. On Android the package points to Health Connect instead; on iOS, query history with . Expo Go vs development build Works in Expo Go. is included in Expo Go, so you can prototype there. Your own string, and turning the motion permission off with , only take effect in a development build. Sensors also need real hardware: simulators report no or synthetic data, so test on a device. Basic example How it works The shared sensor API Every sensor export except is an instance of with the same methods: Method Behaviour Whether this device has the sensor. Always check first. Starts updates; returns a subscription with . Requested interval between updates, in milliseconds. Removes every listener for that sensor. / Inspect active listeners. / Motion permission, where the platform has one. The interval is a request . The OS may deliver faster or slower, and on Android 12+ the system caps each sensor at 200 Hz (a 5 ms interva"},{"u":"expo-sdk/audio-and-video.html","t":"Audio and Video","s":"Expo SDK Libraries","d":"Playing and recording audio with expo-audio and playing video with expo-video in Expo SDK 57 — players, recording, the audio session, background playback and Picture in Picture. expo-av is legacy and not in SDK 57.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Players are objects, not components","expo-audio playback","The audio session","Recording","expo-video","Native configuration","Platform differences","Common patterns","Background audio with lock-screen controls","Hiding a poster until the first frame","Performance considerations","Security considerations","Microphone access","Authenticated media URLs","Common mistakes","Related topics"],"b":"Expo SDK 57 splits media into two packages built on the same pattern — a player object you create with a hook, and state you observe through events: Package SDK 57 Covers Audio playback, playlists, recording, the audio session, lock screen controls. Video playback, native controls, fullscreen, Picture in Picture, background playback. Install only what you use. See expo install and SDK Alignment. expo av — with , and the component — is not in the SDK 57 bundled module map . has no SDK 57 version to resolve for it. Its successors are and , whose APIs are different: there is no object and no component. Tutorials that call are describing the old package. Migrate rather than pinning an old version. Why it exists / when to use it — and when NOT to Use for sound effects, music and podcasts, voice notes and recording. Use for any video, from a looping background clip to a full player with subtitles. Do not use them for: Real time calls. Voice and video calling need a WebRTC style stack with echo cancellation and network jitter handling; neither package is that. Low latency audio synthesis or games that need sample accurate timing. Playback is buffered through the platform media stack. Showing a video thumbnail in a list. Render a poster image with ; mounting dozens of players is expensive. Expo Go vs development build Both packages are included in Expo Go for basic playback and recording. A development build is required for anything driven by the config plugins: your own text, background audio ( and the Android foreground services), and Picture in Picture. Expo's video documentation also notes that some native features, such as asset interception, are unavailable in Expo Go. If background playback or PiP \"does nothing\", check that you are not in Expo Go before debugging anythin"},{"u":"expo-sdk/authentication.html","t":"Authentication and OAuth","s":"Expo SDK Libraries","d":"Signing users in with expo-auth-session in Expo SDK 57 — the authorization code flow with PKCE, redirect URIs, why the implicit flow and client secrets do not belong in a mobile app, and where tokens go afterwards.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","The flow, step by step","useAuthRequest","Discovery","Redirect URIs","Exchanging the code","Native configuration","Platform differences","Common patterns","Store tokens, then refresh","Signing out","Security considerations","1. Use the authorization code flow with PKCE","2. Do not use the implicit flow","3. No client secret in the app","4. Register exact redirect URIs","Common mistakes","Related topics"],"b":"implements OAuth 2.0 and OpenID Connect sign in for an Expo app. It builds the authorization request, generates the PKCE verifier and challenge, opens the provider's login page in a secure system browser session (through ), and hands the redirect back to your code. That resolves , (used for PKCE) and on SDK 57. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Use it to sign in with an identity provider that speaks OAuth 2.0 / OpenID Connect: Google, Microsoft Entra ID, Okta, Auth0, Keycloak, Cognito, or your own authorization server. Do not use it: To collect a username and password in your own UI and post them to a provider. That defeats the point of OAuth — the user's password should only ever be typed on the provider's page. With the implicit flow ( ). It is still in the API for legacy code; the Expo documentation recommends moving away from it. See below. As a place to keep a client secret. There is no such place in a mobile app. For \"Sign in with Apple\" native UI. That is a separate native API ( ), not covered here. Expo Go vs development build Use a development build. The package itself is included in Expo Go, but Expo's authentication guide states that Expo Go cannot be used for local development and testing of OAuth or OpenID Connect apps, because you cannot customise Expo Go's URL scheme. In Expo Go, redirect URIs use Expo Go's scheme, which you cannot register with a provider as belonging to your app. A development build carries your own , so the redirect URI you register with the provider is the one the app really receives. Basic example Authorization code with PKCE, exchanging the code on your server: How it works The flow, step by step 1. builds an : a random , a random PKCE , and its SHA 256 . 2. opens the provider's aut"},{"u":"expo-sdk/biometrics.html","t":"Biometrics","s":"Expo SDK Libraries","d":"Prompting for Face ID, Touch ID and fingerprint with expo-local-authentication in Expo SDK 57 — checking hardware and enrolment, the authenticate options, error codes, and why a local biometric check is a UI gate rather than authentication.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Capability checks","authenticateAsync(options?)","Error codes","Native configuration","Platform differences","Common patterns","Lock the app when it returns from the background","Requiring strong biometrics for a sensitive action","Security considerations","A JavaScript true is not a cryptographic check","Server-side actions need server-side proof","Common mistakes","Related topics"],"b":"shows the system biometric prompt — Face ID or Touch ID on iOS, the fingerprint / face / iris sheet on Android — and tells you whether the user passed. It also reports what hardware exists and what the user has enrolled. That resolves on SDK 57. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Use it for re verifying a user who is already signed in : unlocking the app after it was backgrounded, confirming a sensitive action, revealing a stored password. Do not use it: As sign in. A biometric check proves someone who can unlock this device is holding it. It does not identify the user to your server. As the only protection for a secret. resolves in JavaScript; code that can be hooked or patched can skip the check. To make a secret genuinely unreadable without biometrics, store it with and , which enforces the check in the Keychain / Keystore. To replace server side authorisation. A \"confirm payment with Face ID\" step still needs your server to authorise the payment. Expo Go vs development build Fingerprint and Touch ID work in Expo Go. Face ID needs a development build. The Expo documentation states that Face ID authentication for iOS is not supported in Expo Go, and that you need a development build to test it: Face ID requires , and that string must be in your app's . Test on a real device. Simulators and emulators can simulate enrolment and matches, but they do not enforce the checks the way hardware does. Basic example How it works Capability checks Function Returns Whether a face or fingerprint sensor exists. Whether the user has enrolled biometrics. : (1), (2), (3, Android only). Empty if none. The strongest enrolled . values: Value Meaning (0) Nothing enrolled. (1) Only a PIN, pattern or password. (2) Weak biometrics, such as 2D "},{"u":"expo-sdk/linking.html","t":"Linking","s":"Expo SDK Libraries","d":"expo-linking in Expo SDK 57 — URL schemes, building and parsing deep links, reading the URL that opened the app, opening other apps and Settings, universal links and App Links, and validating incoming links.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Incoming links","Building and parsing","Outgoing links","Native configuration","URL scheme","Testing a link","Platform differences","Common patterns","Handling links without Expo Router","Sending the user to Settings after a denied permission","Security considerations","Common mistakes","Related topics"],"b":"handles URLs in both directions. Into your app: registering a URL scheme, reading the link that launched or resumed the app, and building links that point back at it. Out of your app: opening web pages, the phone dialer, maps, other apps and your app's page in Settings. That resolves on SDK 57. It is already a dependency of , so projects using Expo Router usually have it. See expo install and SDK Alignment. Why it exists / when to use it — and when NOT to Use it to: Build a URL that opens a specific screen ( ), for sharing or as an OAuth redirect. React to the URL that opened the app when you are not using Expo Router. Open , , and other apps' URLs, and send the user to Settings. Do not use it: To route inside an Expo Router app. Expo Router already maps incoming URLs to routes; handling them again with a listener causes double navigation. See Deep Links and Universal Links. To show web content inside the app. Use 's for an in app browser sheet, or a WebView. As a trusted input. Any app, web page or QR code can open your scheme with any parameters. Expo Go vs development build The API works in Expo Go, but your own URL scheme does not. In Expo Go, links into the app use Expo Go's scheme — returns something like . The installed package documents that in Expo Go for published updates the created URL is \"neither stable nor predictable\", and that when a stable URL is needed (for example, authorization callbacks) you should use a build with your scheme. Expo's linking guide says that after adding a custom scheme you need to create a new development build to test it. Universal links and App Links always need a build, because they depend on your bundle identifier and signing certificate. Basic example How it works Incoming links API Behaviour Hook: the initial URL, then subseq"},{"u":"expo-sdk/clipboard-and-haptics.html","t":"Clipboard and Haptics","s":"Expo SDK Libraries","d":"Copying and pasting with expo-clipboard and giving tactile feedback with expo-haptics in Expo SDK 57 — the iOS paste prompt and ClipboardPasteButton, Android's clipboard toast, haptic styles, and when haptics silently do nothing.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","expo-clipboard","The iOS paste prompt and ClipboardPasteButton","expo-haptics","Native configuration","Platform differences","Common patterns","Cross-platform haptics helper","Copy, then clear a sensitive value","Security considerations","The clipboard is shared, and it is visible","Clipboard content is untrusted input","Reading on launch looks like surveillance","Common mistakes","Related topics"],"b":"Two small packages that make an app feel native: Package SDK 57 Covers Reading and writing text, URLs and images on the system clipboard; the iOS paste button. Impact, notification and selection feedback through the Taptic Engine or Android haptics. See expo install and SDK Alignment. Not React Native's React Native 0.86 still exports a core API. is the maintained alternative with images, URLs, HTML, change listeners and the iOS paste button. Use . Why it exists / when to use it — and when NOT to Use the clipboard for explicit user actions: a \"Copy\" button next to a code, a share link, a one time password field that offers to paste. Use haptics to confirm a physical feeling action: a toggle flipping, a successful payment, a drag snapping into place, a picker changing value. Do not : Read the clipboard on launch \"to be helpful\". iOS shows a paste banner and Android 13+ shows a toast every time you read it. Users read that as spying. Copy secrets to the clipboard casually. Other apps, keyboards and cloud clipboard sync can see it. Use haptics as the only feedback. Haptics can be disabled by the user or the system; always pair them with a visual change. Fire haptics continuously. Constant vibration is noise, and it drains the battery. Expo Go vs development build works in Expo Go. The Expo documentation lists it as included in Expo Go. It has no config plugin and needs no permissions. has no config plugin and needs no native configuration from you , so there is nothing a development build adds for it. The documentation pages consulted for this guide did not state its Expo Go status explicitly; verify on your target Expo Go version before relying on it there. Test haptics on a real device . Simulators have no Taptic Engine. Basic example How it works Function Behaviour Writ"},{"u":"expo-sdk/background-tasks.html","t":"Background Tasks","s":"Expo SDK Libraries","d":"Running deferrable JavaScript work in the background with expo-background-task and expo-task-manager in Expo SDK 57 — defining and registering tasks, what WorkManager and BGTaskScheduler actually promise, testing, and why a scheduled task is a request, not a guarantee.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Defining vs registering","expo-background-task API","expo-task-manager API","What each OS does","Native configuration","Platform differences","Common patterns","A testing button in debug builds","Record when the task last ran","Keep the work small and idempotent","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"asks the operating system to run a piece of your JavaScript periodically while the app is in the background — on Android through WorkManager, on iOS through . The task itself is defined with , which is also the mechanism behind background location and geofencing. That resolves and on SDK 57. See expo install and SDK Alignment. A scheduled task is a request, not a promise Registering a background task tells the OS \"please run this at some point, no more often than every N minutes\". The OS decides whether and when. It weighs battery level, network availability, whether the device is charging and how often the user opens your app. A task can run hours late, run rarely, or not run at all — and on both platforms it stops entirely if the user force quits the app. Design every feature so that it still works when the task never ran. Why it exists / when to use it — and when NOT to Use it for opportunistic, deferrable work where \"sometime in the next few hours\" is fine: Pre fetching content so the next launch is fast. Uploading queued analytics or logs. Syncing a local SQLite database with the server. Cleaning up caches. Do not use it for: Anything time critical or exact. Reminders at 9:00 belong in a scheduled local notification. Server side events belong in a push notification. Continuous work. Tracking a run or playing music needs a foreground service / background mode of its own: see Location and Audio and Video. Guaranteeing data reaches the server. Also sync on app launch and on foreground; the background task is a bonus. Long computation. The OS gives the task a limited window and can stop it at any time. expo background fetch still appears in the SDK 57 bundled module map ( ), but Expo's documentation marks it as being replaced by , not receiving patches, and due to be r"},{"u":"expo-sdk/permissions-patterns.html","t":"Permissions Patterns","s":"Expo SDK Libraries","d":"How runtime permissions work across Expo SDK 57 libraries — the shared PermissionResponse shape and hooks, when to ask, handling denial and the Settings screen, Info.plist usage strings, Android permissions and blockedPermissions, and testing each state.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","PermissionResponse","The two functions and the hook","What the prompt looks like, and when it disappears","Native configuration","Platform differences","Common patterns","Ask in context, once, with a primer","A reusable permission flow","Re-check when the app returns to the foreground","Handling partial grants","Security considerations","Testing permission flows","Common mistakes","Related topics"],"b":"Every Expo SDK library that touches something private — camera, microphone, location, photos, notifications, motion — follows the same permission model. They return the same object, most ship a hook built from the same factory, and they are configured the same way: a usage string on iOS and manifest permissions on Android, both written by config plugins. This page is the pattern. Each library page covers its specific keys. Why it exists / when to use it — and when NOT to Both platforms require explicit user consent for sensitive capabilities, and both let the user revoke it at any time. Your app has to handle every state — not yet asked, granted, denied, permanently denied, partially granted — or it breaks for real users. Do not : Ask for everything at launch. Users refuse permissions they do not yet understand, and on iOS you usually get only one chance. Request a permission to do something that does not need it. The image picker on iOS needs no photo permission; see Camera and Media. Treat a denial as an error. It is a normal outcome. Degrade the feature. Expo Go vs development build Permission prompts work in Expo Go, but they are not your app's prompts. The Expo permissions guide notes that permissions in development and standalone builds require native build time configuration before JavaScript can request them, and that this is not required in Expo Go. Expo Go's binary already declares every usage string and permission, with Expo Go's own wording. That has three consequences: You never see your own usage strings in Expo Go. Verify them in a development build. A missing config plugin does not crash in Expo Go — and then crashes in your build. Test permission flows in a development build before release. Permission state is Expo Go's. Granting camera access to Expo G"},{"u":"expo-development-builds/why-you-need-one.html","t":"Why You Need a Development Build","s":"Development Builds","d":"Expo Go ships a fixed set of native modules, so it breaks at runtime the moment your project adds one it does not contain. A development build is your own binary with your own native code.","h":["Why it exists / when to use it — and when NOT to","The failure you actually hit","Why it is a runtime error and not a build error","Decision table","How it works","Common patterns","Start the dev server for a development build","Keep Expo Go working for as long as it is useful","Tell the team which one to use","Performance considerations","Common mistakes","Related topics"],"b":"A development build is your own app binary, compiled from your own native dependencies, with installed so it still connects to a Metro dev server and still hot reloads. It is the normal way to develop a real Expo app. Expo Go is not that. Expo Go is a pre compiled app from the App Store and Play Store containing a fixed set of native modules chosen by Expo. You cannot add to it. The moment your project needs a native module Expo Go was not compiled with, Expo Go cannot run your project — and it tells you so at runtime , after the bundle has already loaded. Expo Go vs Development Builds introduces that distinction and when each one applies. This page is about the build itself: what forces you into one, what the failure looks like, and what you get in exchange. Why it exists / when to use it — and when NOT to Use a development build when any of the following is true, which for a real app is almost immediately: You installed a library with custom native code that Expo Go does not bundle. You need a config plugin to change something in , , an entitlements file, Gradle or the Podfile. You need a custom URL scheme, an app specific bundle identifier, push notification entitlements, or a background mode. You are testing anything that depends on your real app identity — deep links, OAuth redirects, App Clips, widgets, share extensions. You want the app your testers install to resemble the app you will ship. Expo Go is still genuinely useful for: trying an SDK package before committing to it, reproducing a bug on someone else's machine without a build, and teaching. Treat it as a sandbox with a fixed module list, not as a development environment. A development build is a debug build, not a release build is a development tool. It belongs in your debug and internal distribution bui"},{"u":"expo-development-builds/creating-locally.html","t":"Creating One Locally","s":"Development Builds","d":"Build a development build on your own machine with npx expo run:android, npx expo run:ios, or eas build --local — no hosted service and no queue.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Useful flags","Building with eas build --local","Platform differences","Common patterns","Rebuild only when the native surface changes","Keep local and cloud builds honest about each other","Clear the native cache before blaming your code","Common mistakes","Related topics"],"b":"You do not need a hosted build service to produce a development build. Two local routes exist, and both are fully supported: / — a plain native build through Gradle or Xcode, which then installs and launches the app and starts Metro. — the EAS build pipeline executed on your machine instead of Expo's servers. EAS CLI still reads , still resolves credentials, and produces the same kind of artifact as a cloud build. Local builds are a real alternative to paid cloud builds, not a fallback. They cost you machine time and a working native toolchain instead of money and queue time. Creating One with EAS covers the hosted route and where it earns its place. Why it exists / when to use it — and when NOT to Build locally when: You already have Android Studio, or a Mac with Xcode, set up. You are iterating on native configuration and want the turnaround measured in minutes rather than queue position. You cannot or do not want to upload source to a hosted service. You want to avoid a paid plan. Do not build locally when: You need an iOS build and you do not have a Mac. This is absolute. Building for iOS requires macOS with Xcode. No amount of configuration makes work on Windows or Linux. You need the same artifact reproduced consistently across a team with different machines. You need signed builds for testers and do not want to manage signing identities yourself. Your CI does not have the toolchain, and you do not want to install it there. Basic example Install the dev client first, so the build that comes out is a development build rather than a plain debug build: That resolves to on SDK 57. Then build and run: Requires the Android SDK and a connected device or running emulator. Works on macOS, Windows and Linux. Requires macOS with Xcode. There is no cross platform substitute. "},{"u":"expo-development-builds/creating-with-eas.html","t":"Creating One with EAS","s":"Development Builds","d":"eas build --profile development produces a development build on Expo's servers. EAS is a paid hosted service with a free tier, it requires an Expo account, and queue time depends on your plan.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","eas.json and the development profile","Profile fields you will reach for","Command flags worth knowing","Platform differences","Common patterns","Separate the development profile from everything else","Download and run a finished build","Use it in CI without an interactive prompt","Common mistakes","Related topics"],"b":"EAS Build compiles your app on Expo's infrastructure and hands you back an artifact. For development builds the command is: Before deciding to use it, three facts matter more than any feature list: EAS is a paid hosted service with a free tier. You can produce builds without paying. You can also exhaust the free tier. Build queues and concurrency depend on your plan. A free tier build waits in a shared queue; paid plans buy priority and the ability to run more than one build at a time. How long you wait is therefore not a property of your project. An Expo account is required. EAS builds are tied to an account and a project on Expo's servers. There is no anonymous mode. None of that makes EAS the wrong choice. It makes it a choice with a price. Creating One Locally is the alternative that costs machine time instead, and EAS Costs and Limits is where the plan comparison belongs. Why it exists / when to use it — and when NOT to Use EAS Build for a development build when: You need an iOS build and have no Mac. This is the single strongest reason. Local iOS builds require macOS with Xcode; EAS runs them on macOS workers. You want signed, installable builds for testers without managing signing identities by hand. You want every team member and your CI to get the same artifact from the same inputs. Your machine is slow, or you would rather not install Android Studio and Xcode on it. Do not use it when: You already have the toolchain and are iterating fast on native config — a local build's turnaround is shorter than a queued one. Your source cannot leave your network. The free tier does not cover your volume and the budget is not there. Build locally. Basic example Install EAS CLI and sign in. The verified current version is 24.5.0 . Add the dev client, link the project, and g"},{"u":"expo-development-builds/installing-on-a-device.html","t":"Installing It on a Device","s":"Development Builds","d":"Getting a development build onto an emulator, a simulator or real hardware — adb, eas build:run, internal distribution links, and the iOS device registration rule.","h":["Why it exists / when to use it — and when NOT to","Basic example","From a local build","From an EAS build","From a file you already have","How it works","Platform differences","Common patterns","Give testers a link, not a file","Keep one development build per project on the device","Reinstall rather than debug a stale binary","Security considerations","Common mistakes","Related topics"],"b":"A development build is a binary. Once it exists, installing it is a separate problem from building it, and the route depends on where the binary came from and what hardware you are targeting. This page covers the install step only. Creating One Locally and Creating One with EAS cover producing the artifact. Why it exists / when to use it — and when NOT to You need this page when the build finished somewhere other than the device you want to run it on — a cloud build, a colleague's machine, a CI artifact. If you built with or on the machine holding the device, installation already happened as part of the command and there is nothing more to do. Basic example From a local build and build, install and launch in one step. lets you pick which connected device or emulator to target. From an EAS build runs simulator and emulator builds from EAS CLI. takes the most recent build for that platform; , and select a specific build, a build archive URL, or a local archive. narrows the search to builds made with a given profile. From a file you already have installs an existing or instead of building one. The iOS command accepts too, for an app bundle. How it works Once the app is installed, you start the dev server and connect: The dev launcher screen inside the app looks for bundlers on your local network and lists them. If it finds none, scan the QR code Expo CLI prints — with the system camera on iOS, or from the launcher's own scanner on Android. While the server runs, the interactive command table offers: Key Action <kbd a</kbd open Android <kbd shift</kbd +<kbd a</kbd select an Android device or emulator <kbd i</kbd open iOS simulator (macOS only) <kbd shift</kbd +<kbd i</kbd select an iOS simulator (macOS only) <kbd r</kbd reload app <kbd j</kbd open debugger <kbd m</kbd toggl"},{"u":"expo-development-builds/dev-client-features.html","t":"expo-dev-client Features","s":"Development Builds","d":"The dev launcher, the dev menu, custom menu entries, and the launchMode option that decides whether a development build opens the launcher or reconnects to the last project.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The dev launcher","Configuring it in the app config","The dev menu","Controlling the menu from JavaScript","Platform differences","Common patterns","Set launchMode: 'launcher' when you switch projects constantly","Turn the menu off for end-to-end tests","Put your debug affordances in the dev menu, not in the UI","Performance considerations","Common mistakes","Related topics"],"b":"is what turns a plain debug build into a development build. On SDK 57 the version is : It brings two user facing pieces — the dev launcher (a screen for choosing what to load) and the dev menu (an in app menu of development tools) — plus a config plugin that wires both into the native project during generation. Why it exists / when to use it — and when NOT to Without it, a debug build loads one bundle from one hard coded location. That is fine for a single developer on a single machine and painful for everyone else: switching between a colleague's dev server, a branch preview and an embedded bundle each means a rebuild. The launcher removes the rebuild from that loop. You rebuild when native code changes, not when the bundle source changes. Leave it out of release builds. It is development tooling, and it carries a launcher UI that has no business in a store submission. Keep it to the build profile — see Creating One with EAS. Basic example Install the package and rebuild once. Everything below is then available. Start the dev server in development build mode: How it works The dev launcher On launch, the app either reconnects to a project or shows the launcher screen. The launcher lists dev servers it discovers on the local network, accepts a URL typed by hand, can scan a QR code, and — when the build is configured for updates — can load a published update such as a pull request preview. That last capability is the reason internal testers can review several branches from one installed binary. Configuring it in the app config is a config plugin. Its options are the dev launcher's options, with a small addition of its own. Read from the installed plugin type definitions for and . Option Type Default What it does \\ Launch straight into the previously opened project, or alw"},{"u":"expo-development-builds/adding-native-dependencies.html","t":"Adding Native Dependencies","s":"Development Builds","d":"Installing a library with native code into a development build — npx expo install, the rebuild rule, autolinking, and how to tell whether a package needs a new binary at all.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The rebuild rule","Is this package native?","Autolinking","Checking alignment","Common patterns","Install, regenerate, rebuild — in that order","Decide about Expo Go once, up front","Tell the team a rebuild is needed","Prefer a library that has shipped New Architecture support","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"Adding a dependency to an Expo project is two questions, not one: 1. Which version? answers it against your installed SDK. 2. Does the binary need rebuilding? Only if the package contains native code, or changes the native project through a config plugin. Getting the first wrong produces a runtime crash that looks like a code bug. Getting the second wrong produces hours of debugging a change that was never in the binary. Why it exists / when to use it — and when NOT to Every development build is compiled against a specific set of native modules. The JavaScript in and the native code in the installed binary are two separate things that only agree because you built them together. The moment you add a package with native code, they disagree until you rebuild. That is the whole subject of this page. Basic example Install, then rebuild. resolves the version that matches your installed SDK from — for SDK 57 that is . Never install an SDK package with a bare package manager command A bare install fetches whatever is newest on npm, which is routinely built against a different SDK. The mismatch surfaces at runtime, not install time, which is what makes it expensive. This page names the wrong command in order to warn against it. That requires the opt in in its front matter. How it works The rebuild rule What you changed Rebuild the binary? JavaScript or TypeScript in your app No — fast refresh handles it A JavaScript only dependency No — Metro picks it up, restart the server if it does not A dependency with native code Yes A config plugin, or a plugin's options Yes , , app name, icon, URL scheme Yes A permission string in the app config Yes An Expo SDK upgrade Yes An environment variable read at runtime by JavaScript No Everything in the \"yes\" column ends in native files being re"},{"u":"expo-development-builds/debugging.html","t":"Debugging a Development Build","s":"Development Builds","d":"React Native DevTools, the dev menu, the network inspector, native logs and the failures specific to a development build rather than to your JavaScript.","h":["Basic example","How it works","React Native DevTools","The dev menu","The network inspector","Native logs","Uncaught promise rejections are loud now","Common patterns","Rule out the launcher before debugging your code","Clear caches in the right order","Read the error's vocabulary to find its layer","Debug a release-like build when the bug only happens there","Common mistakes","Related topics"],"b":"A development build debugs like any React Native app, with one addition and one subtraction. The addition is the dev launcher, which introduces a class of failure that has nothing to do with your code. The subtraction is that you can no longer blame Expo Go's fixed module set, because the binary is yours. This page covers both: the tools, and the development build specific failures. Basic example Start the dev server, then press <kbd j</kbd : <kbd j</kbd opens React Native DevTools against the connected app. The interactive command table also offers: Key Action <kbd r</kbd reload app <kbd j</kbd open debugger <kbd m</kbd toggle menu <kbd shift</kbd +<kbd m</kbd more tools <kbd a</kbd / <kbd i</kbd open Android / iOS simulator <kbd o</kbd open project code in your editor <kbd s</kbd switch between Expo Go and development build DevTools requires Hermes If no Hermes backed app is connected, Expo CLI declines to open the debugger and says so: React Native DevTools can only be used with Hermes. Hermes is the engine SDK 57 uses, so in practice this message means nothing is connected rather than that your engine is wrong. How it works React Native DevTools The debugger gives you the console, sources with breakpoints, a memory profiler and the React component tree. It attaches over the same connection Metro uses, which is why the app has to be running and connected before <kbd j</kbd does anything. React Native 0.86, which SDK 57 ships, adds light and dark mode emulation to React Native DevTools. That is useful precisely because theme bugs are tedious to reproduce on a device. The standalone package is not the tool Standalone WebSocket support was removed from React Native. React Native DevTools is the supported debugger. If a tutorial tells you to run a separate process, it pr"},{"u":"expo-config-plugins/what-they-are.html","t":"What a Config Plugin Is","s":"Config Plugins","d":"A config plugin is a function that takes your app config and returns a modified one. It runs during prebuild, and it is how native changes stay expressible when you never hand-edit ios/ or android/.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The plugins array","What a plugin can and cannot see","Where a plugin lives","The composition helpers","How it fits Continuous Native Generation","Common patterns","Reach for a config key before writing a plugin","Keep local plugins in one directory","Write a comment saying why the plugin exists","Common mistakes","Related topics"],"b":"A config plugin is a function that receives your app config and returns a modified app config . That is the entire idea. Its type, read from the installed 57.0.9, is: Plugins run during prebuild , the step that generates and from your config. They are the reason Continuous Native Generation is workable: if the native directories are build output, every native change has to be expressible as an input, and a config plugin is that input. Why it exists / when to use it — and when NOT to Without plugins, Continuous Native Generation would only support whatever the app config schema happens to cover. The moment you needed a element in , or an extra entry, or a line in the Podfile, you would be forced to commit the native directories and maintain them by hand. Plugins close that gap. Write the change once as code, and it is reapplied every time the native project is regenerated — after an SDK upgrade, on a fresh clone, in CI. Do not reach for a plugin when: The app config already has the field. , , , and many more are first class config keys. A plugin that sets something the schema already sets is a maintenance liability. covers it. Gradle and Podfile knobs — SDK versions, Proguard rules, , extra Maven repositories — are already exposed as options. See expo build properties. The library already ships one. Most libraries that need native configuration include a plugin. See Using Community Plugins. You have decided to commit and . Then edit them directly and stop running prebuild. What you must not do is both. Basic example A plugin is a module that default exports the function. This one adds an Android permission. Reference it from the app config by path, with its options: Then regenerate and rebuild. A plugin has no effect on an already built binary. How it works The array Eac"},{"u":"expo-config-plugins/using-community-plugins.html","t":"Using Community Plugins","s":"Config Plugins","d":"Most libraries that need native configuration ship a config plugin. How to find one, add it to the plugins array, pass its options, and check what it actually changed.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Finding out whether a library has a plugin","Entry shapes","Ordering","Version alignment","Common patterns","Prefer a library's plugin over your own","Use false to suppress a permission you do not need","Group your plugin options where you can read them","Verify rather than assume","Security considerations","Common mistakes","Related topics"],"b":"Most of the config plugins you use are ones you did not write. A library that needs a permission string, a URL scheme, a Gradle property or an entitlement ships an , and using it is one line in your app config. This page is about consuming those plugins well: finding them, passing options, ordering them, and confirming they did what you expected. Why it exists / when to use it — and when NOT to A library that needs native configuration has two options: ask you to edit by hand, or ship a plugin. The second survives a regeneration; the first does not. That is why a library's plugin is not optional decoration. In a generated project, a hand edit to a native file lives until the next prebuild and then disappears, taking the feature with it. Do not add a plugin when the library does not ship one. Adding a name that is not a plugin to the array is a resolution error, not a no op. Basic example Install the library with , then add its plugin: Those four option names — , , and — are the plugin's real props, read from 's own type definitions. The two permission strings also accept , which omits the key instead of setting it. Then regenerate and rebuild. A plugin has no effect on a binary that already exists. How it works Finding out whether a library has a plugin Three checks, cheapest first: 1. Look for at the package root. If it exists, the package name is a valid entry. 2. Read its props type. The file is conventionally a one line re export: That is the authoritative option list. A library's README goes stale; the type does not. 3. Check the library's documentation for which options are platform specific and what the defaults are. The beats the README Every option name and default in this section was read from an installed package's type definitions rather than from prose. Whe"},{"u":"expo-config-plugins/build-properties.html","t":"expo-build-properties","s":"Config Plugins","d":"The common Gradle and Podfile knobs — SDK versions, R8 and Proguard, use_frameworks, extra repositories and pods — exposed as config options so you do not have to write a plugin.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Shared options","Android options","iOS options","Common patterns","Raise the Android minimum SDK for a dependency","Turn on R8 and keep what reflection needs","Add a private Maven repository","Add a pod a library forgot to declare","Performance considerations","Common mistakes","Related topics"],"b":"is a config plugin that exposes the native build settings people reach for most often. Instead of writing a plugin that edits or the Podfile with a regular expression, you set an option in your app config. That resolves to on SDK 57. Every option name, type and default in this page was read from that installed package's own . If a name here disagrees with something you read elsewhere, the type definitions are what run. Why it exists / when to use it — and when NOT to Most native build changes are one of a small set: raise , turn on R8, add Proguard rules, switch CocoaPods to frameworks, add a Maven repository, add a pod. Writing a plugin for each of those means writing string manipulation against a Gradle file whose shape changes between SDKs. does that manipulation for you and keeps it working across upgrades. Use it for anything in the tables below. Write your own plugin only when the setting is not there — and check the tables before concluding that it is not, because the list is longer than people expect. It is a build time setting, so it needs a rebuild Changing an option here changes the generated native project. Regenerate and rebuild; nothing picks it up at runtime. Basic example How it works The plugin reads its options and applies them to the generated Gradle files, , the Podfile and . Because it runs during generation, it composes with everything else in the array rather than fighting it. Shared options These three can be set at the top level or overridden per platform. A platform specific value wins. Option Type Default What it does boolean Build React Native from source instead of using precompiled binaries. Significantly increases build times. On iOS it also disables the precompiled and \\ \\ Which set of internal React Native feature flags to enable boolean"},{"u":"expo-config-plugins/writing-your-own.html","t":"Writing Your Own Plugin","s":"Config Plugins","d":"The full authoring path for a local config plugin — typed props, one mod per platform, idempotency, composition, and shipping it inside a published package.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The anatomy","Registering is not running","What the callback receives","Idempotency","Composition","Errors and warnings","Platform differences","Common patterns","Ship a plugin inside a package","Keep the logic testable","Read the app config, do not duplicate it","Security considerations","Common mistakes","Related topics"],"b":"You write your own config plugin when a native change is needed, no app config key covers it, does not expose it, and no library you use ships a plugin for it. That is rarer than people expect — check What a Config Plugin Is for the list of things to rule out first. Everything on this page was checked against the installed 57.0.9 , the version depends on, imported through as an app should. Expo Go vs development build A config plugin changes the generated native project. Expo Go's native project is not yours, so plugins do nothing there. You need a development build, rebuilt after every plugin change. Why it exists / when to use it — and when NOT to The situation that justifies a plugin: your app needs a specific element in , a key in , an entitlement, or a line in a Gradle file, and you have chosen to keep and generated rather than committed. The plugin is the only way that change survives the next prebuild. Do not write one when: The app config already has the key. , , , and cover a lot. has the option. See expo build properties. You have committed the native directories. Edit them directly and stop running prebuild. You want runtime behaviour. A plugin runs in Node at generation time and never again. If you need code that runs in the app, you want a module. Basic example A plugin that writes an analytics key into both native projects: a element on Android and an key on iOS. Reference it by path, with its options: A path entry resolves with the extensions , , , , and , read from the plugin resolver in 57.0.9, and files are transformed to CommonJS by when loaded. You can therefore write a local plugin in TypeScript without a build step. If you prefer plain JavaScript, the same plugin works as a file using . Then check the output before you trust it — Verifying Generate"},{"u":"expo-config-plugins/mods.html","t":"Mods and the Dangerous Mods","s":"Config Plugins","d":"How a config plugin actually edits a native file — every mod plugin in @expo/config-plugins 57.0.9, the order they run in, and why withDangerousMod is a last resort.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The mod plugins, verified","Registration versus evaluation","The order your callbacks run in","String mods and mergeContents","The dangerous mods","Why they are a last resort","Platform differences","Common patterns","Security considerations","Common mistakes","Related topics"],"b":"A mod is the part of a config plugin that touches a native file. , and the rest are mod plugins : they register a callback against one file, and prebuild later reads that file, runs every registered callback over its parsed contents, and writes it back. Every mod name on this page was read from the installed 57.0.9 — its exports and its type. The ordering behaviour was read from its mod compiler and then confirmed by running against a scratch copy of an SDK 57 project. Expo Go vs development build Mods change the generated native project, which Expo Go does not use. Anything here needs a development build. Why it exists / when to use it — and when NOT to Mods exist so that plugins never have to find, open, parse or save native files themselves. The base mods own the file I/O; your callback only sees a parsed object and returns it. That is what lets twenty plugins edit the same without trampling each other. Use a structured mod (manifest, plist, entitlements, Gradle properties) whenever one reaches the file you need. Use a string mod (Gradle files, Podfile, ) when the file has no structured form. Use a dangerous mod only when nothing else reaches the file at all. Basic example How it works The mod plugins, verified Exported from (re exporting 57.0.9): Mod plugin key File Parsed XML (xml2js) Parsed XML Parsed XML Parsed XML Parsed XML Array of property items string string string (Kotlin or Java) string (Kotlin or Java) string Mod plugin key File Plist object Plist object Plist object Key value object An project object string string Plus the platform agnostic ones: Export What it does Registers on — no file is read or written for you Registers on — same, but runs after everything else Registers an action on any mod key by name Creates or intercepts a base mod (a provider)."},{"u":"expo-config-plugins/verifying-output.html","t":"Verifying Generated Native Output","s":"Config Plugins","d":"Proving a config plugin did what you meant — run prebuild in a scratch copy, diff the generated android/ and ios/ directories, and use introspection for a no-write preview.","h":["Why it exists / when to use it — and when NOT to","Basic example","Real output","How it works","What prebuild does, in order","Flags that matter here","--no-clean as an idempotency test","Introspection: a preview that writes nothing","Platform differences","Common patterns","Verify in CI","Snapshot the generated files you care about","Diff before and after an SDK upgrade","Security considerations","Common mistakes","Related topics"],"b":"A config plugin edits files you normally never open. \"The plugin should have added that permission\" is a hope; a diff of the generated native project is a fact. This page is the procedure for getting that diff without risking your real working tree . Everything below was run against a scratch copy of an SDK 57 project ( , ) on Windows, for the Android platform. The output shown is the real output of those runs. iOS generation was not run on this machine — see Platform differences. clears and regenerates the native directories In SDK 57, deletes and and recreates them by default. The installed CLI's own help text describes the opt out, , as \"Apply changes to the existing native folders instead of recreating them\". Any hand edit in those directories is gone after a normal prebuild, and there is no undo. That is why every command on this page runs in a scratch copy . Never verify a plugin by running prebuild in a checkout that contains native edits you have not committed. Why it exists / when to use it — and when NOT to Verify after: Adding or upgrading a library that ships a plugin. Writing or changing your own plugin. Upgrading the SDK — the template changed, and string mods may no longer match. Reordering the array. You do not need a scratch prebuild to check a single or manifest value. Introspection is faster and writes nothing. Use the full diff when you need to see Gradle files, the Podfile, or everything a plugin touched. Basic example The procedure: copy the project, commit a baseline native tree without the change, apply the change, prebuild again, diff. gives you an independent working directory on the same repository, so the scratch commits never land on your branch. Remove it with when you are done. A plain directory copy works equally well. skips installing np"},{"u":"expo-config-plugins/testing-and-failures.html","t":"Testing Plugins and Common Failures","s":"Config Plugins","d":"Unit-testing a config plugin without prebuild, testing the registered mod, and the real error messages and silent failures you meet when a plugin goes wrong.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Three layers of testing","Why fixtures, not real files","Common failures","Prebuild stops: the plugin cannot be found","Prebuild stops: the export is not a function","Prebuild stops: a mod returned nothing","Prebuild stops: mod ordering errors","Prebuild succeeds, change missing: anchor no longer matches","Prebuild succeeds, change missing: another plugin overwrote it","Prebuild succeeds, change missing: a dangerous mod was overwritten","Works on a clean prebuild, duplicates on --no-clean","Works locally, fails on a build server","The binary has the old behaviour","Platform differences","Common patterns","Common mistakes","Related topics"],"b":"A config plugin fails in one of two ways. Either prebuild stops with an error — annoying, but honest — or prebuild succeeds and the change is not in the binary. The second kind is the one that costs days, because nothing tells you it happened. This page covers testing that catches both, and the failures you will actually meet. The tests below were run with Node's built in test runner against the installed 57.0.9; the error messages are quoted from its source. Expo Go vs development build Plugins only matter for a development build or release build. Nothing on this page can be observed in Expo Go. Why it exists / when to use it — and when NOT to Test a plugin when it has logic: conditionals on props, idempotency checks, string edits with anchors. Those break quietly on SDK upgrades. A plugin that only sets one key through a helper does not need unit tests. A single verification diff after each SDK upgrade is enough. Basic example Structure the plugin so the transformation is a plain exported function, and the mod callback only adapts it: Then test three things: the transformation, its idempotency, and that the plugin registers a mod which applies it. All three tests passed against 57.0.9. Pass the file path, not the directory: tries to load the directory as a module and fails. is equally valid if your project already uses Jest; the test bodies are the same. How it works Three layers of testing Layer Tests Speed Catches Pure function The transformation on a fixture Milliseconds Logic, idempotency Registered mod The plugin registers the right key and applies the function Milliseconds Wrong mod, forgotten Scratch prebuild The real template, every plugin, real ordering Tens of seconds Anchors that no longer match, plugin order, library conflicts The first two run on every co"},{"u":"expo-native-code/expo-modules-api.html","t":"The Expo Modules API","s":"Native Code with Expo","d":"Expo's own native module system — a Kotlin and Swift DSL that replaces a Codegen spec with a declarative module definition, verified against expo-modules-core 57.0.18.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The definition is data, not generated code","The definition components","Function versus AsyncFunction","Events","Where the module lives, and how it is found","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"The Expo Modules API is how you write native code for an Expo app. It is not React Native's TurboModule system. Instead of a TypeScript spec file that Codegen turns into abstract native classes, you write a module definition in Kotlin or Swift using a small domain specific language, and reads that definition at runtime to build the JavaScript object your app imports. The package that implements it is , at 57.0.18 in SDK 57. It is already installed in every Expo project — depends on it — so there is nothing to add before you start. Expo Go vs development build Anything on this page adds native code to your app. Expo Go cannot load it , because Expo Go ships a fixed set of prebuilt native modules. You need a development build before your module will run. Why it exists / when to use it — and when NOT to The Expo Modules API exists because the SDK itself needed a way to write 120 native modules that stay consistent across two platforms, two languages and many SDK versions. Everything in , and is written with it. Reach for it when: You need a platform capability no SDK package covers — a vendor SDK, a hardware integration, a piece of platform API nobody has wrapped. You want the native surface typed from the native side . The DSL infers argument and return conversion from the Kotlin or Swift types you write, so there is no second place to keep in sync. You want the same module to work on both platforms without maintaining an Objective C++ bridge layer by hand. Do not reach for it when: A config plugin is enough. Adding an key or a Gradle dependency is not a module. See Writing Your Own Plugin. You are publishing a library for the whole React Native ecosystem, Expo or not. A module written with the Expo Modules API requires in the consuming app. That is installable in a bare "},{"u":"expo-native-code/module-in-kotlin.html","t":"Writing a Module in Kotlin","s":"Native Code with Expo","d":"The Android half of an Expo module end to end — Gradle setup, the ModuleDefinition DSL, argument conversion, coroutines, errors, events and the TypeScript call site.","h":["Why it exists / when to use it — and when NOT to","Basic example","The file layout","Gradle","The module class","Telling autolinking about it","The TypeScript side","How it works","Argument and return conversion","Enums","Coroutines and queues","Errors","Activity results and intents","Platform differences","Common patterns","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"This page builds the Android side of one real Expo module from an empty directory to a typed call site. The example is a battery module: it reports a level, exposes a charging flag, runs a slow read off the main thread, and emits an event when the charging state changes. Everything here is verified against , the version Expo SDK 57 pins. Expo Go vs development build A module you wrote is native code, and Expo Go cannot load it. Create a development build and rebuild it whenever the native code changes — Fast Refresh only reloads JavaScript. This page was not compiled The Kotlin here was written against the installed sources and the SDK 57 module template, but it has not been run through Gradle as part of authoring this site. Treat it as correct in shape and check it against your own build. Why it exists / when to use it — and when NOT to Write Kotlin when the capability you need lives in the Android platform or in an Android only vendor SDK. If the same capability exists on iOS you should write both halves together, under one , so the JavaScript side has no platform branch in it. Do not write a module when the change is a manifest permission, a Gradle dependency or a resource. Those are config plugin territory, and a plugin survives while a hand edited directory does not. Basic example The file layout A module — local or published — has the same Android layout: Gradle The SDK 57 template's is short, because supplies the Android and Kotlin configuration that every Expo module shares: Do not add , a Kotlin plugin, or an dependency by hand. The Gradle plugin sets those from the app's Expo version, which is what keeps a module working across SDK upgrades. The module class Telling autolinking about it The Android entry is the fully qualified class name. A bare here produces "},{"u":"expo-native-code/module-in-swift.html","t":"Writing a Module in Swift","s":"Native Code with Expo","d":"The iOS half of an Expo module end to end — podspec, the ModuleDefinition result builder, argument conversion, queues, records, errors and the TypeScript call site.","h":["Why it exists / when to use it — and when NOT to","Basic example","The file layout","The podspec","The module class","Telling autolinking about it","The TypeScript side","How it works","The definition is a result builder","Argument and return conversion","Records — structured arguments","Enums","Queues","Errors","Platform differences","Common patterns","Performance considerations","Security considerations","Common mistakes","Related topics"],"b":"This page builds the iOS side of the same battery module the Kotlin page builds for Android: a level property, a synchronous charging flag, an asynchronous read, and an event when the charging state changes. The thing to notice, if you have written a TurboModule in Swift before, is what is missing : there is no Objective C++ wrapper, no generated protocol to conform to, and no implementation. reads your Swift definition directly. Everything here is verified against , the version Expo SDK 57 pins. Expo Go vs development build Expo Go cannot load a module you wrote. Build a development build, and rebuild it after every Swift change — Fast Refresh only reloads JavaScript. This page was not compiled The Swift here was written against the installed sources and the SDK 57 module template. It has not been built with Xcode as part of authoring this site. Treat it as correct in shape and verify against your own build. Why it exists / when to use it — and when NOT to Write Swift when the capability lives in an Apple framework or an iOS only vendor SDK. Write it alongside the Kotlin half under one whenever the capability exists on both platforms, so that the JavaScript call site has no platform branch. Do not write a module for something an entry or a Podfile change would do. That is config plugin work, and a plugin survives while a hand edited directory does not. Basic example The file layout The podspec This is the SDK 57 module template's podspec for a local module, with the parts that matter called out: The iOS deployment target in the SDK 57 template is 16.4 . If your app targets something lower, the pod will not install. The module class Telling autolinking about it The entry is the bare Swift class name — no module prefix, no package. is accepted as a deprecated fallback fo"},{"u":"expo-native-code/view-components.html","t":"View Components","s":"Native Code with Expo","d":"Exporting a native view with the Expo Modules API — the View definition, props, view events, imperative view functions, and the React component that wraps it.","h":["Why it exists / when to use it — and when NOT to","Basic example","The native view","The view definition inside the module","The React component","How it works","Props","View events","Imperative view functions","Multiple views in one module","Why ExpoView, not View / UIView","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"A module can export a view as well as functions. The native view is an ordinary subclass on iOS and an ordinary Android subclass, declared inside the module definition with a block, and consumed from React with . This is Expo's equivalent of a Fabric native component, and the same trade off applies as for modules: there is no Codegen spec, so the prop list is declared once in native code and mirrored by hand in TypeScript. Expo Go vs development build A custom view is native code. Expo Go cannot render it; you need a development build, rebuilt whenever the native view changes. The native code here was not compiled The Kotlin and Swift below were written against the installed sources and the SDK 57 module template, not built on this machine. The TypeScript is type checked against expo 57 and React Native 0.86.3. Why it exists / when to use it — and when NOT to Export a view when the thing you need is a piece of platform UI: a map, a camera preview, a chart rendered by a native library, a video surface, a platform control with behaviour React Native cannot reproduce. Do not export a view when: A React Native composition would do. A with and a from an existing library is cheaper to build, cheaper to maintain, and works on web. You only need to call into native code. Functions do not need a view. The \"view\" is really a modal or an alert. Those are presented imperatively; a module function that presents them is simpler than a zero size view that renders nothing. A native view carries a real cost: it participates in layout, it has a lifecycle, and every prop is a conversion on every update. Basic example The example is a tappable gradient: it takes a list of colours, and emits an event carrying the index of the band that was tapped. The native view The view definition inside "},{"u":"expo-native-code/vs-turbomodules.html","t":"Expo Modules vs TurboModules","s":"Native Code with Expo","d":"An honest comparison of the Expo Modules API (Kotlin and Swift DSL, no Codegen spec) and React Native TurboModules (TypeScript spec plus Codegen), and how to choose between them in an SDK 57 app.","h":["Why it exists / when to use it — and when NOT to","Basic example","Expo Modules API","TurboModule","How it works","The comparison","The DSL names, verified","What \"no Codegen\" really costs","Using TurboModule libraries in an Expo app","Platform differences","Common patterns","Performance considerations","Common mistakes","Related topics"],"b":"An Expo app can call native code through two mechanisms, and both run on JSI under React Native's New Architecture: The Expo Modules API — you describe the module in a Kotlin or Swift DSL, and builds the JavaScript object from that description at runtime. No spec file, no Codegen. TurboModules — you describe the module in a TypeScript spec, Codegen generates native interfaces from it, and you implement those interfaces. Neither is \"the right one\". They make different trade offs, and the choice mostly comes down to who will consume the module and whether you need C++. The TurboModule pages on this site target React Native 0.87, not 0.86 The CLI half of this site documents TurboModules on React Native 0.87 . Expo SDK 57 ships React Native 0.86.3 . The TurboModule mechanism is the same, but the surrounding details — the TypeScript type surface, template files, library versions — are not. When you follow TurboModules End to End from an Expo project, treat its version numbers and any 0.87 only API as not applying to you, and install packages with . Expo Go vs development build Both mechanisms add native code. Expo Go cannot load either; you need a development build. Why it exists / when to use it — and when NOT to The Expo Modules API exists because writing a TurboModule means maintaining the same contract in three places — a TypeScript spec, a Kotlin implementation, and a Swift implementation behind an Objective C++ wrapper — plus the Codegen configuration that connects them. Expo's own documentation frames the choice this way: use the Expo Modules API for a better developer experience if you are willing to depend on the package, and use TurboModules if you intend to use C++, because they give easier access to lower level mechanisms. Choose the Expo Modules API when: The mo"},{"u":"expo-native-code/local-modules.html","t":"Local Modules","s":"Native Code with Expo","d":"An Expo module that lives inside your app's modules/ directory — generating one with create-expo-module --local, what autolinking does with it, and when to move it into a package.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The flags, verified","What gets generated","How autolinking finds it","Calling it","Platform differences","Common patterns","Security considerations","Common mistakes","Related topics"],"b":"A local module is an Expo module that lives inside your app repository, in a directory next to your app code, rather than in its own npm package. It uses exactly the same Expo Modules API as a published module; the only difference is where autolinking finds it. It is the right default for native code that only your app needs. You get Kotlin and Swift that are versioned with the JavaScript that calls them, and no package to publish. Expo Go vs development build A local module is native code. Expo Go cannot load it. You need a development build, rebuilt after every native change. The native code on this page was not compiled Flags and file layout were read from and its template . The Kotlin and Swift were not built with Gradle or Xcode as part of writing this page. Why it exists / when to use it — and when NOT to Use a local module when: The native code is specific to this app — a vendor SDK wrapper, a platform API only you need. You want native changes reviewed in the same pull request as the JavaScript that uses them. You are keeping and generated. Module code in is not in the directories prebuild clears, so it survives regeneration, unlike a hand edit to the app's . Do not use a local module when: Several apps need it. Copying between repositories diverges quickly. Publish it — see Publishing an Expo Module — or share it through a monorepo package. The change is configuration, not code. A permission, a plist key or a Gradle property is a config plugin, not a module. Basic example From the root of an existing Expo project: The command prompts for a name and the native module name, then writes the module under . Everything it asks can also be passed as flags, which is how you run it in a script: Then rebuild the native app, because a new module is new native code: Fast R"},{"u":"expo-native-code/publishing.html","t":"Publishing an Expo Module","s":"Native Code with Expo","d":"Turning an Expo module into an npm package — the create-expo-module standalone layout, package.json fields, building, shipping a config plugin, testing with npm pack, and what consumers must do.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The package layout","package.json, as the template writes it","What ships","Shipping a config plugin with the module","Testing the package before publishing","Publishing","What consumers do","Platform differences","Common patterns","Security considerations","Common mistakes","Related topics"],"b":"A standalone Expo module is an npm package containing Kotlin, Swift and TypeScript that any Expo app can install. Structurally it is the same as a local module plus a , a build step and an example app. The layout and scripts on this page were read from and the template it downloads, . The native code was not compiled as part of writing it, and nothing was published. Expo Go vs development build A published Expo module contains native code. Apps that install it need a development build; Expo Go cannot load it. Say so in your README. Why it exists / when to use it — and when NOT to Publish when more than one app — yours or other people's — needs the same native code, and copying a directory between repositories has started to drift. Do not publish when: Only one app uses it. A local module has no versioning, build or release overhead. Your consumers include React Native apps that do not use Expo, and adding to them is not acceptable. An Expo module needs the package in the consuming app. A TurboModule does not — see Expo Modules vs TurboModules and Publishing a Native Library, which covers the TurboModule path on React Native 0.87. It is a monorepo with one team. A workspace package gives you the same sharing without a registry. Basic example Without , the command creates a standalone package in a new directory, installs its dependencies, and creates an app that depends on the module. Pass if the module will live in a monorepo that already has an app to test it in. Useful flags for a publishable package, all verified in 57.0.1: Flag Sets The native module name in The Android package name , , , and Initial Any of , , , Add / Skip the example app Develop against the example app: How it works The package layout , as the template writes it The fields that matter, from the tem"},{"u":"expo-native-code/community-interop.html","t":"Interoperating with Community Libraries","s":"Native Code with Expo","d":"How an SDK 57 app links React Native community libraries alongside Expo modules — Expo Autolinking, checking compatibility, libraries without config plugins, excluding and inspecting what gets linked.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Expo Autolinking links both kinds","Inspecting what gets linked","Autolinking options","Opting back into community CLI autolinking","Libraries that need native configuration","Checking compatibility before you install","Platform differences","Common patterns","Security considerations","Common mistakes","Related topics"],"b":"Most Expo apps use native libraries that were not written with the Expo Modules API — style TurboModules, Fabric components, vendor SDK wrappers. They work in an Expo app. The rules for making them work are the subject of this page. The short version: any library compatible with React Native can be used in an Expo project once you have a development build , installed with , configured through a config plugin if it needs native configuration, and linked automatically by Expo Autolinking. Expo Go vs development build A community library with native code cannot run in Expo Go unless Expo Go happens to bundle it. Expo's own guidance is that any React Native compatible library works once you create a development build, but may not work in Expo Go. See Why You Need a Development Build. Why it exists / when to use it — and when NOT to An Expo project is a React Native project. The Expo SDK covers many capabilities, but not all of them, and the community library ecosystem is where the rest lives. Interop matters every time you add a library that is not an package. Do not reach for a community library when an SDK package covers the same capability for your needs. SDK packages are versioned with the SDK, which removes one class of upgrade problem. Where both exist and the SDK package is sufficient, prefer it. Basic example picks the version the SDK expects for packages listed in the SDK's — is for SDK 57 — and warns about known incompatibilities. For a library the SDK does not list, it installs normally; you are then responsible for checking compatibility. Then rebuild: No linking step. Expo Autolinking finds the library. How it works Expo Autolinking links both kinds Expo's autolinking documentation states that from SDK 52, Expo Autolinking replaces React Native community CLI au"},{"u":"expo-eas/overview.html","t":"EAS Overview","s":"EAS","d":"EAS is Expo's paid hosted build, submit and update service with a free tier. What each product does, what it costs you, and what you can do instead.","h":["What is in EAS","Why it exists / when to use it — and when NOT to","Basic example","How it works","EAS Build","EAS Update","EAS Submit","EAS Workflows","Common patterns","Use EAS for iOS only","Use EAS Update without EAS Build","Keep a local build path that works","Security considerations","Common mistakes","Related topics"],"b":"EAS (Expo Application Services) is a set of hosted, paid services with a free tier , run by Expo, that build your app on their machines, upload it to the stores, and deliver JavaScript updates to installed apps. It is a separate product from the Expo SDK and the Expo CLI. EAS is optional. An Expo app is a React Native app; you can build it on your own machine or your own CI with , , or , and ship the resulting binary yourself. Nothing in the SDK requires an EAS account. This section documents EAS because plenty of teams use it, not because you have to. What is in EAS Product What it does The alternative EAS Build Compiles Android and iOS binaries on Expo's machines Build locally, or on your own CI EAS Submit Uploads a binary to App Store Connect and Google Play Transporter, Xcode, Play Console, fastlane EAS Update Serves JavaScript and asset updates to installed apps A self hosted server, or shipping a new binary EAS Workflows Runs those jobs from a YAML file on triggers GitHub Actions, GitLab CI, Bitrise, Jenkins EAS Metadata Pushes store listing text from a config file (beta, iOS only) Editing the store dashboards All of them are driven by one command line tool, , and one config file, . Why it exists / when to use it — and when NOT to The concrete problem EAS Build solves is machine ownership . iOS binaries can only be produced on macOS with Xcode installed. If your team is on Windows or Linux, or your CI has no Mac runners, you either rent a Mac somewhere or you use a hosted service. EAS is one hosted service among several. The second problem is credential handling . Signing an iOS app needs a distribution certificate and a provisioning profile; signing an Android app needs a keystore. EAS can generate and store these for you, which removes a genuinely awkward setup "},{"u":"expo-eas/eas-json.html","t":"eas.json and Build Profiles","s":"EAS","d":"The real eas.json schema as eas-cli 24.5.0 validates it — cli options, build profiles, platform overrides, submit profiles and profile inheritance.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The cli block","Build profile fields","Android-specific options","iOS-specific options","Profile inheritance with extends","Submit profiles","Common patterns","One profile per environment, one channel per profile","Validate before you push","Common mistakes","Related topics"],"b":"sits at the root of your project next to and configures every command. It has exactly three top level keys: , and . Everything else is a profile name you chose. The field names on this page were read from the 24.5.0 validation schema — the same code runs against your file. Fields not listed here either do not exist or could not be verified; rejects unknown keys, so a guessed field name fails the build rather than being ignored. Why it exists / when to use it — and when NOT to One file describes every way you build the app, so \"the preview build\" means the same thing on your machine and in CI. Without it, build variants live in people's shell history. You do not need at all if you never run . and read the app config, not this file. Basic example generates this and nothing else: Three profiles that correspond to three questions: Profile What it produces A development build that loads JS from a dev server absent ( ) A release mode build installable by testers without the stores absent absent ( ) A store ready / being empty is not an oversight. defaults to and defaults to , so an empty profile already means \"signed store build using credentials EAS holds\". How it works The block Key Type What it does semver range Refuses to run if the installed is outside the range. Pin this. boolean Refuses to build with uncommitted changes. Makes a build traceable to a commit. or Whether build numbers live in your app config or on EAS servers. See Versioning and Runtime Versions. boolean Suppresses the push credentials prompt. Two further keys, and , exist in the schema for self hosted update infrastructure. They are not documented here because we could not verify their correct use. Build profile fields These apply to a whole profile, and can also be set per platform: Field Values Notes a"},{"u":"expo-eas/credentials.html","t":"Credentials Management","s":"EAS","d":"iOS distribution certificates and provisioning profiles, Android keystores, who holds them, and how to move between EAS-managed and your own.","h":["Why it exists / when to use it — and when NOT to","What each platform actually needs","Basic example","Let EAS generate and hold them","Bring your own","How it works","The remote path","The local path","Moving between them","Freezing credentials in CI","Platform differences","iOS device registration","Android key rotation","Security considerations","Common mistakes","Related topics"],"b":"Every release build is signed. On iOS that means a distribution certificate and a provisioning profile ; on Android it means a keystore containing a private key. EAS can generate and hold these for you, or you can keep them yourself and hand them to the build. This is the part of shipping where a mistake is expensive rather than annoying. Lose an Android upload key and you cannot update your own app on Google Play without a key reset request. Commit a keystore to a public repository and anyone can sign a package that claims to be you. Never commit signing material A keystore, a , a , an App Store Connect key, a Google Play service account JSON and a are all secrets. Add them to before you create them, not after. Deleting a committed secret does not remove it from git history — the only fix is to rotate it. Why it exists / when to use it — and when NOT to You need managed credentials when more than one person, or any CI machine, has to produce a signed build. Passing a keystore around on Slack does not survive a team of three. You do not need EAS to hold them. keeps everything on your machine and in your secret store, and / sign with whatever Xcode and Gradle are configured to use, with no EAS involvement at all. What each platform actually needs Credential Scope Notes Distribution certificate One per Apple Developer account, shared across apps Represents your team's identity Provisioning profile Per app, per distribution method Expires after 12 months; ties the app ID, certificate and (for ad hoc) a device allow list Push notification key ( ) Per Apple Developer account Limited to 2 per account — do not let tooling regenerate them casually All iOS signing requires a paid Apple Developer Program membership . EAS supplies build machines, not membership. Credential Scope N"},{"u":"expo-eas/building.html","t":"Building on EAS","s":"EAS","d":"Running eas build — the verified flags, what the hosted worker does with your project, and how to read a failure.","h":["Why it exists / when to use it — and when NOT to","Basic example","Verified flags","How it works","What the worker does","Queues and concurrency","Builds in CI","Platform differences","Common patterns","Build, then submit, in one step","Install a finished build on a simulator or emulator","Reproduce a failure locally before paying for another build","Reading a failure","Common mistakes","Related topics"],"b":"uploads your project to Expo's infrastructure, compiles it on a hosted worker, and gives you a download URL for the resulting , or . It is the paid product most people mean when they say \"EAS\". If you want the same binary without a hosted worker, Building Locally covers and the plain path. Neither is a fallback — both are supported. Why it exists / when to use it — and when NOT to The honest case for it is narrow and real: iOS binaries require macOS with Xcode . If nobody on the team has a Mac, or your CI has no Mac runners, a hosted builder is the practical answer. The secondary case is that the worker also resolves signing credentials, which is the step teams most often get stuck on. Skip it when your builds already run somewhere — an existing CI pipeline that produces signed binaries is not improved by moving to EAS — or when uploading your source to a third party is not acceptable at your organisation. Basic example Follow the build in the terminal, or pass to return immediately and check later: Build both platforms from one command: Verified flags Read from the 24.5.0 command manifest. Anything not listed here was not verified. Flag Meaning / , or / Build profile name from . Note the short form is , not . Compile on this machine instead of a hosted worker Where to write the artifact (local builds) / Block until the build finishes, or return immediately Ignore the build cache / Submit the finished binary with the matching submit profile Submit using a named submit profile TestFlight \"what to test\" notes , , , , , / A note attached to the build Fail rather than create or modify credentials Regenerate the iOS ad hoc profile with the current device list Skip the pre flight credential validation Skip the project configuration step , , , , , More output from the build it"},{"u":"expo-eas/local-builds.html","t":"Building Locally","s":"EAS","d":"Producing Android and iOS binaries on your own machine — npx expo run:*, eas build --local, and the host-OS facts that decide what is possible.","h":["Why it exists / when to use it — and when NOT to","Basic example","A plain native build, no EAS","eas build --local","How it works","Host OS rules, stated plainly","What --local gives up","Debugging a local build","Platform differences","Common patterns","Keep the local path in the test matrix","Local for Android, hosted for iOS","Reproduce a hosted failure","Common mistakes","Related topics"],"b":"You do not need a hosted service to build an Expo app. There are two local paths, and they solve different problems: Path Needs an Expo account Uses What it produces / No No A native build, installed on a connected device or simulator Yes — it still authenticates Yes The same artifact the hosted worker would produce Both are supported. Keeping one of them working is worth doing even if you use EAS Build day to day, because a release you cannot cut without a third party is a single point of failure. Why it exists / when to use it — and when NOT to Use a local build when: You want a build with no account and no upload — is the only path that involves no Expo service at all. You are iterating on native code or a config plugin and a hosted round trip is too slow. Your source cannot leave your network. You are on the free tier and have run out of included builds. Do not use a local build when: You need an iOS binary and have no Mac. This is not a preference; iOS binaries require macOS with Xcode. No flag works around it. You want the build to be reproducible across a team. Local builds use whatever toolchain is on the machine — the , , , , and pins in are hosted worker settings and are ignored locally. You need EAS secret environment variables. They are not available to . Basic example A plain native build, no EAS This generates the native projects if they do not exist, compiles them, and installs the app. It is the Expo equivalent of running Gradle or Xcode by hand, and it is the answer to \"can I build this without an account\". For a release configuration build: A release build needs real signing configuration, which is the point at which Credentials Management stops being optional. This runs the same build pipeline the hosted worker runs — prebuild, credential resolution, "},{"u":"expo-eas/internal-distribution.html","t":"Internal Distribution","s":"EAS","d":"Getting builds onto testers' devices without the stores — distribution internal, iOS ad hoc device registration, Android APKs, and the TestFlight alternative.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What distribution: \"internal\" changes","Registering iOS devices","Enterprise provisioning","Re-signing an existing build","Platform differences","Common patterns","One preview profile, one channel","Simulator builds for people with Macs","Make the build identifiable","Security considerations","Common mistakes","Related topics"],"b":"Internal distribution means handing a build to people who are not using the app stores: QA, a designer, a client, the person who filed the bug. In it is one field: That single setting changes what gets built on both platforms, and the two platforms behave very differently. Android is nearly friction free. iOS requires registering every device before the build. Why it exists / when to use it — and when NOT to Use internal distribution when you need a build in someone's hands today and a store review would take days, or when the build is not something you would submit at all — a staging build pointed at test infrastructure, for example. Do not use it as your main tester channel on iOS if you have more than a handful of testers. The ad hoc device limit and the rebuild per device rule make it painful at scale; TestFlight exists for that and is covered in EAS Submit. Basic example When the build finishes, EAS gives you a shareable install page and a QR code. Opening it on a registered device installs the app. How it works What changes The default Gradle task changes so the build produces an APK instead of an AAB . That matters because an AAB cannot be installed on a device — Google Play processes it into device specific APKs. An APK installs directly. Testers need to allow installation from the browser or file manager they use. That prompt is an OS security feature, not a bug in your build, and it is worth telling testers about in advance. No device registration, no per tester limit, no Apple account involved. The build is signed with either an ad hoc or an enterprise provisioning profile. An ad hoc profile contains an allow list of device UDIDs. A device not on that list cannot install the build — it fails at install time with an unhelpful message. The allow list is fixed w"},{"u":"expo-eas/submit.html","t":"EAS Submit","s":"EAS","d":"Uploading a finished binary to App Store Connect and Google Play — submit profiles, the credentials each store needs, and what submission does not do.","h":["Why it exists / when to use it — and when NOT to","Basic example","Verified flags","How it works","Submit profile fields","What each store needs from you","releaseStatus and track","Build and submit together","Common patterns","CI submission with an API key from secrets","Upload to a draft, release by hand","Check status without watching the terminal","Security considerations","Common mistakes","Related topics"],"b":"takes a binary you already have and uploads it to App Store Connect or Google Play. That is the whole job. It does not build, it does not review, and it does not release your app to users — it puts the file where the store expects it. It is optional in the same way the rest of EAS is optional: Xcode's Organizer, Apple's Transporter, the Play Console web upload and fastlane all do the same thing. Why it exists / when to use it — and when NOT to Use it when uploads should be scriptable and identical every time, especially from CI, and when you want to close the loop between building and uploading. Do not use it when you already have a working setup, when your release process needs a human to make choices in the store dashboard anyway, or when a submission requires store features does not expose. Basic example Note that the credential paths point outside the repository. Neither a Play service account JSON nor an App Store Connect key belongs in git. Verified flags Read from the 24.5.0 command manifest: Flag Meaning / , or / Submit profile name from Submit the most recent finished build Submit a specific EAS build by id Submit a local binary file Submit a binary from a URL TestFlight \"what to test\" notes / TestFlight groups to distribute to Set up TestFlight automatically Block until the submission finishes , More output Never prompt. Required in CI. Related commands: , , , , . How it works Submit profile fields Verified against the 24.5.0 schema. Android Field Notes Path to the Google Play service account JSON Play track name. Defaults to , , or A number — the staged rollout fraction Submit the change without sending it for review Override the application id iOS Field Notes , , App Store Connect API key — the recommended authentication , Apple ID authentication, as an alte"},{"u":"expo-eas/update.html","t":"EAS Update","s":"EAS","d":"Shipping JavaScript and asset changes to installed apps with expo-updates — setup, the publish commands, the client API, and the compatibility rule that keeps it safe.","h":["Why it exists / when to use it — and when NOT to","Basic example","Verified flags","How it works","What the app does at launch","updates config keys","Controlling the update in JavaScript","Expo Go vs development build","Common patterns","One channel per environment, one branch per channel","Check what is actually out there","Publish from CI on a merge","Verify what a device is running","Security considerations","Common mistakes","Related topics"],"b":"EAS Update delivers a new JavaScript bundle and assets to an app that is already installed, so a fix can reach users without a new binary and a store review. The client half is (SDK 57 ships ); the server half is EAS Update, which is a paid hosted service with a free tier. The single most important fact on this page: an update is matched to a build by runtime version . Publishing an update to a runtime that does not match the installed binary is how production apps get broken. That mechanism has its own page — Runtime Versions, Channels and Branches — and you should read it before you publish anything to users. Why it exists / when to use it — and when NOT to Use it for JavaScript level fixes with a short fuse: a crash on a specific screen, a wrong string, a broken API call, a feature flag you need off. Store review takes days; an update takes minutes. Do not use it for: Anything native. New native dependencies, permission changes, SDK upgrades and app config changes that touch the native projects all need a new binary. What OTA Updates May Not Change is the honest list. Routine releases. If every change ships as an update, your installed binaries drift further from your source with every week, and the first time you need a native change the upgrade is enormous. Working around store review. Both stores have rules about this. See the policy page. You also do not need EAS Update specifically. reads its server from in the app config and can point at any server implementing the protocol, including a self hosted one. Basic example writes and into your app config, adds , and sets on your build profiles. Then build once — the channel is baked into the binary — and publish updates against a branch: infers the branch from your current git branch and the message from your last co"},{"u":"expo-eas/runtime-versions.html","t":"Runtime Versions, Channels and Branches","s":"EAS","d":"The runtime version identifies the native binary an update is compatible with; channels are fixed into builds and branches hold updates. How the four runtime version policies work, and how channels point at branches.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The runtime version","The four policies","What the fingerprint hashes","Seeing the fingerprint","Channels","Branches","Linking a channel to a branch","Reading them in the app","Platform differences","Common patterns","Name channels after environments, branches after streams","Promote tested updates between branches","Check compatibility in CI before publishing","Common mistakes","Related topics"],"b":"Three names decide which update an installed app receives: Name Where it lives What it answers Runtime version Fixed into the binary at build time, and attached to every update Is this update compatible with this binary's native code? Channel Fixed into the binary at build time Which stream of updates does this build listen to? Branch On the update server Which ordered list of updates is this? A channel points at a branch A build receives the newest update on the branch its channel points at, among updates whose runtime version matches the build's own . Nothing else is served to it. The runtime version is the safety mechanism. It is the only thing that stops an update built against one set of native code from running on a binary that has different native code. Config names were read from the installed for SDK 57 and ; commands from the 24.5.0 command manifest. Expo Go vs development build is native, and the channel logic applies to builds that have a channel. The installed types say is always in Expo Go and development builds, which can run any update compatible with their native runtime. Test channel behaviour on a or build. Why it exists / when to use it — and when NOT to Every app that uses EAS Update needs a runtime version; there is no opting out. The decision you make is which policy produces it, and that decision determines how likely you are to ship an incompatible update. Channels and branches exist so that one binary can be pointed at different streams of updates without rebuilding — staging builds at staging updates, a production channel moved to a new branch during a rollout, or back to an old branch in an incident. Basic example If the native code has not changed between the build and the update, the two runtime versions match and the build receives the upd"},{"u":"expo-eas/rollouts-and-rollbacks.html","t":"Rollouts and Rollbacks","s":"EAS","d":"Sending an EAS update to a percentage of users, growing or reverting that rollout, rolling out a whole branch on a channel, and rolling back to a previous update or the embedded bundle.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Two rollout mechanisms","Per-update rollouts","Branch rollouts on a channel","How users are assigned","Rollbacks","eas update:rollback","eas update:republish","eas update:roll-back-to-embedded","Platform differences","Common patterns","Always start production updates as rollouts","Define \"healthy\" before you start","Rehearse a rollback","Sign rollbacks too","Security considerations","Common mistakes","Related topics"],"b":"A rollout limits how many users receive an update until you have seen it behave in production. A rollback replaces a bad update with a known good one. EAS Update has two rollout mechanisms and three rollback commands, and choosing the right one during an incident is much easier if you have read this page before the incident. Every command and flag here was read from the eas cli 24.5.0 command manifest. The behaviour and limitations come from Expo's rollouts documentation. None of these commands were run against a real project while writing this page, because they publish to live channels. Expo Go vs development build Rollouts target builds that have a channel. Expo Go and development builds are not set to a specific channel — the installed types say is always there — so test rollout behaviour on a or build. Why it exists / when to use it — and when NOT to An update reaches every matching user as they relaunch the app, with no store review in between. That is the point of updates and also their risk: a bad update is a bad release for everyone on that runtime version. A rollout turns \"everyone\" into \"10% first\". Use a rollout for any update to a production channel that changes behaviour, not only for risky ones — the risky ones are the ones you did not recognise as risky. A rollout does not help with: Native changes. Those need a binary; see What OTA Updates May Not Change. Users who have not relaunched. Exposure grows as users open the app, not as you raise the percentage. Basic example Publish an update to 10% of users on the branch: Watch your crash and error reporting. Then raise it: Or, if it misbehaves, revert it: must be an integer between 1 and 100. takes an update group ID as an argument, or prompts you to choose one on the branch you pass with . How it works Two"},{"u":"expo-eas/ota-limits-and-policy.html","t":"What OTA Updates May Not Change","s":"EAS","d":"The technical boundary of an over-the-air update — JavaScript and assets only, never native code — and the App Store and Google Play rules that constrain how you use updates, stated without overclaiming.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The technical boundary","When an update goes wrong anyway","Apple's rules","Google Play's rules","What Expo says","Platform differences","Common patterns","Security considerations","Common mistakes","Related topics"],"b":"An over the air (OTA) update delivered by replaces the JavaScript bundle and the assets an installed app runs. It never replaces native code. That is a technical fact about how the update client works, and it is the first of two boundaries on this page. The second boundary is policy. Apple and Google both have rules about apps that download code, and an update has to satisfy them whether or not it is technically possible. This page states what those rules say and where the uncertainty is. It is not legal advice, and it cannot tell you how a reviewer will read your specific app. Why it exists / when to use it — and when NOT to Read this page before you rely on updates for anything beyond bug fixes. Teams get into trouble in two ways: publishing an update that needs native code the binary does not have, which crashes the app, and using updates to change what an app is , which is what store rules prohibit. Use OTA updates for fixes and changes that stay inside the existing native binary and the app's reviewed purpose. Ship a new binary for everything else. Basic example A change that can ship as an update: A change that cannot : After that install you need a new build, a new runtime version, and — for store users — a store release. How it works The technical boundary Expo's EAS Update FAQ lists what an update can and cannot change. Summarised: Can change in an update Needs a new binary JavaScript code Native code (Kotlin, Swift, Objective C, C++) UI, styling and layout implemented in JavaScript Adding, removing or upgrading a library with native code Copy and translations App permissions (camera, location, and so on) Images and other assets The Expo SDK version Anything else that requires a new app binary The reason is structural. The binary contains the native code and a "},{"u":"expo-eas/costs-and-limits.html","t":"Costs and Limits","s":"EAS","d":"What EAS actually meters — builds, update users and bandwidth, workflow minutes, concurrency and queue priority — what could and could not be verified about current prices, and what you can do without paying.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","EAS Build","Queues and concurrency","EAS Update","EAS Workflows","What costs nothing extra","Platform differences","Common patterns","Build Android locally, iOS on EAS","Keep development builds off the hosted queue","Watch update size before MAUs become the problem","Put a number on it before you commit","Common mistakes","Related topics"],"b":"EAS is a paid hosted service with a free tier . Build, Update and Workflows are metered, and what you get on the free plan is enough to evaluate them and ship small apps — not an unlimited service. This page explains what is metered, how that affects decisions, and what you can do without EAS at all. Prices change. This page is not the price list Expo publishes its plans and usage rates at expo.dev/pricing , with billing details in the Expo documentation under Billing. The figures below were read from that page on 2026 09 15 and are included only so you can see the shape of the plans. Check the pricing page before making any decision that depends on a number. Per unit usage rates are deliberately not reproduced here; read them on the pricing page. Why it exists / when to use it — and when NOT to Read this page when you are deciding whether to adopt EAS, choosing a plan, or wondering why a build is waiting in a queue on release day. Do not use it as a reason to assume EAS is required. Every product in EAS has an alternative: Product What is metered Alternative with no EAS bill EAS Build Builds, by platform and worker size; queue priority; concurrency / , or your own CI. still needs an Expo account but runs on your machine EAS Update Monthly active users receiving updates; bandwidth A self hosted server implementing the protocol, or shipping store releases EAS Workflows Workflow run time (CI/CD minutes) GitHub Actions, GitLab CI, Bitrise, Jenkins EAS Submit Included in plans as listed on the pricing page Transporter, Xcode, the Play Console, fastlane Basic example The plan cards on expo.dev/pricing, as read on 2026 09 15. Two separate reads of the page agreed on these values: Plan Price shown Builds Updates Other items listed Free $0/month 15 Android and 15 iOS builds, lo"},{"u":"expo-eas/workflows.html","t":"EAS Workflows","s":"EAS","d":"What EAS Workflows is — YAML files in .eas/workflows/ that run build, submit, update and custom jobs on Expo's infrastructure — with the verified triggers, job types, parameters and eas workflow commands, and how it compares to your own CI.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","File location and size","Top-level keys","Triggers (on)","Job keys","Pre-packaged job types","Custom jobs and sequencing","The commands","Platform differences","Common patterns","Start with workflow:run, add triggers later","Use --wait in scripts","Keep the same steps runnable outside EAS","Security considerations","Common mistakes","Related topics"],"b":"EAS Workflows is Expo's hosted CI/CD. You describe a workflow in a YAML file under , and Expo runs its jobs on EAS infrastructure — on a trigger such as a push to a branch, on a schedule, or when you run it from the command line. Jobs can be pre packaged (build, submit, update, and others) or custom steps you write. It is a metered part of EAS, and it is an alternative to — not an extension of — GitHub Actions, GitLab CI or any other CI you already run. What is on this page was checked against Expo's Workflows documentation (syntax and pre packaged jobs pages) and the commands in the eas cli 24.5.0 command manifest. No workflow was created, validated or run while writing it — that requires an Expo account and uploads the project. Treat the YAML below as illustrating documented keys, and run on your own file. Why it exists / when to use it — and when NOT to Workflows exists to chain EAS operations — build, then submit, or fingerprint then build or update — without you writing the CI glue, maintaining macOS runners, or managing an in a third party CI. Consider it when: You have no CI yet and already use EAS Build and Update. You need macOS for iOS builds and would otherwise pay for Mac CI runners elsewhere. You want pre packaged jobs that understand EAS concepts such as fingerprints and channels. Do not adopt it when: Your CI already works. Calling from existing CI gets you the same builds and updates. Your pipeline is mostly not mobile. Backend tests and deployment belong where the rest of your pipeline lives. You need to stay off third party infrastructure. uploads your project directory to EAS servers unless you use . Cost matters and your volume is high. Workflow run time is metered, and build jobs also consume build usage. See Costs and Limits. Basic example Validate"},{"u":"expo-performance/measuring-first.html","t":"Measuring Before Optimising","s":"Expo Performance","d":"An unmeasured optimisation is a guess. How to get a baseline number on an Expo SDK 57 app before you change anything, and which build to measure.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Measure the build your users run","Measure on the slowest device you support","Record the baseline somewhere durable","Common patterns","Know the four numbers that matter most","Account for React Compiler before hand-memoising","Performance considerations","Common mistakes","Related topics"],"b":"An unmeasured optimisation is a guess. Sometimes the guess is right; often it moves a cost somewhere you are not looking, and sometimes it makes the app slower while making the code harder to read. This page is about getting a number before you change anything, so that every other page in this section has something to be compared against. The rule for the whole section: write down the number, change one thing, measure again. If the number did not move, revert the change. Why it exists / when to use it — and when NOT to Performance work on React Native attracts folklore. \"Wrap everything in \", \"never use inline styles\", \" is slow\" — each was true for somebody, on some version, in some build. None of it is a substitute for a trace of your app on your target device. Measure first when: A user or tester reports something specific: slow launch, janky scroll, a screen that takes a beat to appear. You are about to add a dependency or architectural change justified by \"performance\". You are preparing a release and want a baseline to catch regressions later. Do not start a performance project when nothing is slow. Code that is fast enough is done. Time spent shaving milliseconds off a screen nobody complains about is time not spent on the screen they do complain about. Basic example The smallest useful measurement is a timestamp at two points you care about. This logs how long a screen takes from mount to having its data: That is crude, and it is still better than no number. For anything more precise, use the Performance panel in React Native DevTools — see Profiling. How it works Measure the build your users run A development build running against Metro is not representative. In development: JavaScript is served unminified from the dev server rather than loaded as precompiled H"},{"u":"expo-performance/startup-time.html","t":"Startup Time and the Splash Screen","s":"Expo Performance","d":"What happens between tapping the icon and the first real frame in an Expo SDK 57 app, how to measure it, and how to use expo-splash-screen without hiding a slow start.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","The phases of a cold start","The API surface","Configure the appearance in app config","Platform differences","Common patterns","Defer non-critical work until after the first frame","Keep the root layout's imports light","Always have a timeout on anything that gates hide()","Performance considerations","Common mistakes","Related topics"],"b":"Startup time is the gap between the user tapping your icon and seeing a usable screen. It is the first performance number anyone experiences, and the one most often made worse by well meaning work at launch. ( on SDK 57) controls when the native splash screen goes away. Used well, it removes a flash of empty UI. Used badly, it hides a slow start behind a logo and makes the start feel slower still. Why it exists / when to use it — and when NOT to Hold the splash screen when the first screen genuinely cannot render without something async: custom fonts, a restored auth session, a cached theme. Showing a half styled screen for 100ms and then snapping to the real one looks broken. Do not hold the splash screen while you: Fetch data from the network. The network can take seconds, or fail. Render the screen with a loading state instead. Run migrations, warm caches or prefetch images for later screens. Defer that until after the first frame. Wait for anything with no timeout. A splash screen that never hides is indistinguishable from a crash. Expo Go vs development build The splash screen API ( , ) works in Expo Go. The splash screen appearance — image, background colour, size — is configured through the config plugin and baked into the native project at prebuild, so it only changes in a build you produce yourself. Startup timing must be measured in a release build, never in Expo Go. Basic example How it works The phases of a cold start 1. Native process start. The OS launches the process and loads the app binary and native libraries. You influence this mostly by how many native modules you link. 2. JavaScript load. Hermes loads the bundle. On a release build this is precompiled bytecode embedded in the binary, so there is no parse and compile step at launch — see Hermes. Bund"},{"u":"expo-performance/bundle-size.html","t":"Bundle Size and Tree Shaking","s":"Expo Performance","d":"Measuring what npx expo export actually ships for Expo SDK 57, inspecting it with Expo Atlas, and what Metro tree shaking can and cannot remove.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What expo export flags help with measurement","Expo Atlas: see what is in the bundle","Tree shaking in SDK 57","Common patterns","Import the function, not the library","Measure a dependency before you adopt it","Keep secrets out of the bundle","Performance considerations","Common mistakes","Related topics"],"b":"The JavaScript bundle is every module your app imports, compiled into one file per platform. On a release build it ships as Hermes bytecode inside the binary (and in every OTA update you publish). A smaller bundle means less to download, less to load at startup, and fewer modules to initialise. This page shows a real measurement first, then the tools to find out what is in your own bundle. Why it exists / when to use it — and when NOT to Look at bundle size when: Startup time regressed and you suspect a new dependency. An OTA update is larger than you expect. You are choosing between two libraries and want to know what each costs. Do not chase bundle size for its own sake. A few hundred kilobytes of bytecode is rarely the reason an app is slow; images, network waterfalls and heavy first renders usually matter more. Measure startup first — see Startup Time. Basic example Export a production bundle and look at the file: A real run of that command on the minimal SDK 57 app in on this handbook's build machine bundled 580 modules into a single 1.4 MB Hermes bytecode file at: That is the floor: React, React Native 0.86.3, the Expo runtime and one screen. Everything you add is measured against it. Run the same command on your project and compare the size. The extension is because compiles to Hermes bytecode by default. The flag (present in SDK 57's ) skips that step if you need to read the plain JavaScript output. How it works What flags help with measurement Read from the installed argument parser: Flag What it does Export only the platform you are measuring. Emit source maps alongside the bundle, which source map explorers read. Write the asset map, so you can see which assets are referenced. Skip minification — useful for reading output, wrong for measuring size. Emit JavaS"},{"u":"expo-performance/hermes.html","t":"Hermes","s":"Expo Performance","d":"Hermes is the JavaScript engine Expo SDK 57 apps run on. What ahead-of-time bytecode means for startup and debugging, and the constraints it puts on you.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Ahead-of-time bytecode","Development builds are different","Bytecode is versioned","Bytecode is not obfuscation","Platform differences","Common patterns","Debug with React Native DevTools","Keep hot loops out of JavaScript","Performance considerations","Common mistakes","Related topics"],"b":"Hermes is the JavaScript engine that runs your app's code on Android and iOS. It is bundled with React Native 0.86.3, which Expo SDK 57 ships, and it is the engine Expo's tooling is built around. The key design choice: Hermes compiles JavaScript to bytecode ahead of time , at build or export time, and it has no JIT compiler . Your release app does not parse or compile JavaScript on the device at all. It loads bytecode and interprets it. Why it exists / when to use it — and when NOT to Hermes is optimised for what mobile apps care about: fast startup, low memory, and small binary size, rather than peak throughput in long running computation. You do not choose Hermes on SDK 57 so much as work with it: React Native DevTools requires Hermes. The SDK 57 CLI prints \"React Native DevTools can only be used with Hermes\" when no compatible app is connected. The field is not in SDK 57's app config type. It does not appear in the installed definition, so this handbook does not document switching engines on SDK 57. When Hermes is the wrong tool: sustained CPU heavy JavaScript (image processing, cryptography, large data transforms). With no JIT, hot loops do not get faster the longer they run. Move that work into a native module — see Expo Modules API. Basic example Confirm at runtime that you are on Hermes. Hermes installs a global: And see the bytecode it produces: A real run on the minimal SDK 57 app in produced a 1.4 MB Hermes bytecode file from 580 modules , at . How it works Ahead of time bytecode Step Where it happens Metro bundles your modules into one JavaScript file Your machine or CI, at export/build time Hermes compiles that file to bytecode Your machine or CI, at export/build time Bytecode is embedded in the binary or served as an OTA update Build / The device loads and "},{"u":"expo-performance/image-performance.html","t":"Image Performance with expo-image","s":"Expo Performance","d":"Finding and fixing slow image screens in Expo SDK 57 — decode size, cache policy, recycling in lists, prefetching, and the SDK 57 cache read/write functions.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Where the time goes","Cache policy is a trade-off","Constrain decoded size when you load into memory","Prefetch the next screen, not the whole catalogue","New in SDK 57: seed the cache from a local file","Platform differences","Common patterns","Stable cache keys for signed URLs","Placeholders instead of spinners","Performance considerations","Common mistakes","Related topics"],"b":"Images are the most common cause of a screen that feels slow, uses too much memory, or flickers while scrolling. On SDK 57 the tool is . This page is about using it fast : the API reference lives in Images with expo image. Why it exists / when to use it — and when NOT to Reach for this page when a profile or a user report points at images: A screen of photos janks while scrolling. Memory climbs as the user browses and the app is eventually killed. Images visibly pop in or show the wrong picture briefly in a list. The same image downloads again every time a screen opens. If none of those is happening, image tuning is not your bottleneck. Measure first — see Measuring Before Optimising. Expo Go vs development build ships in Expo Go, so everything here runs there. Measure in a release build, though: decoding and caching behaviour in a development build is real, but the rest of the app around it is not representative. Basic example The single most effective fix is decoding images at the size they are displayed, not the size they were uploaded: And request a thumbnail sized file from your image server if it supports resizing. A 4000px photo decoded for a 96pt thumbnail wastes memory no matter how well the view is configured. How it works Where the time goes Cost Symptom Fix Network download Images appear late , , smaller files Decode Jank when images appear Smaller source files, on / Memory App killed after browsing Smaller decodes, avoid for huge images Recycling Wrong image flashes in list rows Layout shift Content jumps as images load Fixed dimensions, a hash Cache policy is a trade off is one of , (the default), or . gives the fastest repeat render and costs memory. Use it for small images seen repeatedly — avatars, thumbnails. Leave large hero images on . Constrain deco"},{"u":"expo-performance/list-performance.html","t":"List Performance","s":"Expo Performance","d":"How FlatList virtualisation works on React Native 0.86, the props that actually change scroll performance, and when FlashList (2.0.2 on Expo SDK 57) is worth adding.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Virtualisation","The props that matter","React Compiler and row memoisation","FlashList","Recycling changes how you write rows","Common patterns","Images in rows","Choosing between them","Performance considerations","Common mistakes","Related topics"],"b":"Long lists are where React Native performance problems become visible: blank areas while scrolling fast, dropped frames, and rows that take a moment to appear. There are two tools on SDK 57: React Native's own , and Shopify's . Neither is fast by default if each row is expensive. The row component is almost always the thing to fix first. Why it exists / when to use it — and when NOT to Use a virtualised list ( or ) for any list whose length you do not control — anything from an API, a database or user content. Do not use one for a short, fixed set of items like a settings screen with eight rows. A renders them all at once, which is simpler and fine. Do not add before you have measured with a cheap row component. Swapping list libraries does not fix a row that re renders on every scroll event. Expo Go vs development build Both work in Expo Go. is core React Native. v2 is a JavaScript only library built for the New Architecture (its README states it moved to a JS only implementation), so it needs no custom native code. Measure scroll performance in a release build on a low end device. Basic example A with a memoisable row, a stable key and fixed row height: How it works Virtualisation does not render every item. It renders a window of items around the visible area and replaces the rest with empty space of the right size. As you scroll, it renders new items in batches and unmounts ones that leave the window. Blank areas appear when scrolling outruns rendering: the JS thread cannot produce rows fast enough. The fix is almost always cheaper rows, not bigger windows. The props that matter Verified from React Native 0.86.3's and types: Prop Default behaviour When to change it Uses , then index Always provide a stable id. Index keys break state and recycling. Measures each row "},{"u":"expo-performance/asset-strategy.html","t":"Asset Strategy","s":"Expo Performance","d":"Deciding which images, fonts and data ship inside an Expo SDK 57 app, which are embedded at build time, and which are fetched at runtime — and what each choice costs.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","How it works","Three ways an asset reaches the device","Loading bundled assets before use","Embedding at build time with config plugins","Common patterns","A decision rule","Right-size images before they ship","Watch update size","Platform differences","Performance considerations","Common mistakes","Related topics"],"b":"Every image, font, sound and data file in your app is either bundled with it, embedded in the native binary at build time, or fetched at runtime. Each choice moves a cost — app size, startup time, update size, offline behaviour — somewhere different. The relevant SDK 57 packages are and . Why it exists / when to use it — and when NOT to Think about asset strategy when: The app download is large and you do not know why. OTA updates are large, or slow to apply. The first screen shows a flash of missing images or system fonts. The app must work offline. It is not worth a strategy for a handful of small icons. Bundle them and move on. Expo Go vs development build d assets and the / hooks work in Expo Go. Embedding assets through the or config plugins changes the native project at prebuild, so it only takes effect in a development or release build you create yourself. Basic example A bundled image referenced with : How it works Three ways an asset reaches the device Strategy How Available offline Cost paid in Bundled with JS Yes App size, and OTA update size when it changes Embedded at build time / config plugin Yes, immediately App size; needs a new native build to change Fetched at runtime A URL rendered with Only once cached Network on first view Metro recognises asset extensions including images ( , , , , ), and Expo's Metro config adds , and — the same list 's preset mirrors so tests can import them. Loading bundled assets before use downloads (in development) or locates (in release) a set of bundled assets and tells you when they are ready. Signature from the installed types: . stores files in the caches directory. The installed type docs state there is no guarantee they persist between sessions — the OS may clear that folder. Embedding at build time with config plugin"},{"u":"expo-performance/profiling.html","t":"Profiling","s":"Expo Performance","d":"Profiling an Expo SDK 57 app with React Native DevTools — the Performance panel, React Profiler and Memory panel — plus when to reach for native profilers.","h":["Why it exists / when to use it — and when NOT to","Basic example","Expo Go vs development build","How it works","Which panel answers which question","Performance panel","React Profiler","Adding your own marks","When the problem is native","Common patterns","Profile, fix, then measure in release","Memory leaks","Performance considerations","Common mistakes","Related topics"],"b":"Profiling answers the question measurement raises: where is the time going? On Expo SDK 57 (React Native 0.86.3) the primary tool is React Native DevTools , the debugger and profiler that ships with React Native and connects to the Hermes engine. This page covers opening it from the Expo dev server, which panel answers which question, and when the answer lies outside JavaScript. Why it exists / when to use it — and when NOT to Profile when you have a number that is too slow and do not know why — a screen that takes 800ms to appear, a list that drops frames. Profiling without a specific question produces a large trace and no conclusion. Profiling is not measurement. A profiler adds overhead, and a development build is slower than release. Use the profiler to find the cause; use a release build to measure the fix. See Measuring Before Optimising. Basic example Start the dev server, open the app on a device or emulator, and press <kbd j</kbd : The SDK 57 dev server's key commands include <kbd j</kbd \"open debugger\", <kbd m</kbd \"toggle menu\" and <kbd Shift</kbd +<kbd M</kbd \"more tools\" (read from the installed ). The debugger is React Native DevTools, opened in Chrome or Edge. If no app is connected, or the app is not running Hermes, the CLI warns that React Native DevTools can only be used with Hermes. Expo Go vs development build React Native DevTools connects to Expo Go and to development builds. Profile in a development build of your own app: it contains your native dependencies and matches what you ship. Expo Go's native layer is not yours. How it works Which panel answers which question React Native DevTools provides these panels. Performance and Network have been available since React Native 0.83, so they are present on SDK 57's 0.86: Question Panel What is the JS "},{"u":"expo-security/what-ships-in-the-bundle.html","t":"What Ships Inside the Bundle","s":"Expo Security","d":"Everything your JavaScript touches is shipped to the device and can be read there. What an Expo build actually contains, how to look inside it, and what that means for secrets.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The pieces of a shipped app","Hermes bytecode is a compilation, not an encryption","Inspecting a real build","Platform differences","Common patterns","Move the secret behind an endpoint you control","Accept that some identifiers must ship","Security considerations","What obfuscation and minification really buy","Common mistakes","Related topics"],"b":"An Expo app ships as a native binary containing your JavaScript, your assets and your configuration. All of it lands on hardware the user controls. Anyone with the app file — which is anyone who can install it — can take it apart. This page is the foundation for the rest of this section. Once you accept that the bundle is readable, most security questions in a mobile app answer themselves: the client cannot keep a secret, so the secret has to live somewhere else. Why it exists / when to use it — and when NOT to Read this before you decide where a credential lives. The common failure is not malice or incompetence — it is an assumption that a compiled, minified Hermes bytecode bundle is opaque. It is not. It is a file format with a published structure, and the strings inside it are recoverable with a text search. You do not need this page to justify skipping ordinary hygiene. Minification, R8 and bytecode compilation are all worth having. They just buy you time and size, not secrecy. Basic example You can see the whole shipped JavaScript payload without building a binary at all. runs the same production bundling pipeline a real build runs and writes the result to . On a project with a single screen, that produces: That file is Hermes bytecode — your entire application logic, every module you imported, and every string literal any of them contain. It is 1.4 MB for a hello world screen because it also contains React, React Native and the Expo runtime. How it works The pieces of a shipped app Piece Where it ends up Readable by the user? Your JavaScript Hermes bytecode inside the app binary Yes — strings recover with a text search Images, fonts, JSON assets Files inside the app archive Yes — unzip / values that reach the client Baked into the bundle and the manifest Yes envir"},{"u":"expo-security/expo-public-env-vars.html","t":"EXPO_PUBLIC_ Variables and the Leak They Cause","s":"Expo Security","d":"EXPO_PUBLIC_ variables are inlined into the bundle as string literals at build time. Extract one yourself in two commands, then use the pattern that actually keeps a secret.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The substitution happens in Babel, at build time","Demonstrate the leak yourself","This is not an Expo flaw","Common patterns","The correct shape: server holds the secret, client holds a short-lived token","Keep secret-shaped names out of .env entirely","Security considerations","Common mistakes","Related topics"],"b":"environment variables are a build time text substitution . Metro replaces every in your code with the literal value before the bundle is written. There is no runtime lookup, no environment, and no indirection — by the time your app runs, the variable does not exist, only the string it was replaced with. The prefix is a warning label, not a feature. means public. This page shows you how to extract one from a bundle on your own machine in two commands, so you do not have to take anyone's word for it. Why it exists / when to use it — and when NOT to exists so you can vary non secret configuration between builds without editing source: an API base URL, a feature flag, an analytics environment name, a public OAuth client ID. Use it for anything you would be willing to print on the app's About screen. Do not use it for: Value Why not API keys with billing or write access Extractable; an attacker spends your money Third party secret keys ( , etc.) Extractable; usually a full account compromise Private keys, signing keys Extractable Database credentials Extractable, and they should never be reachable from a client anyway A \"shared secret\" used to authenticate the app to your backend Extractable, so it authenticates nothing This is not a workaround you can engineer around There is no Expo setting that makes an value private, because the value is substituted into code that must run on the user's device. The only fix is to not put the secret in the client. Basic example You must write in full. The substitution is a static text replacement performed by a Babel plugin, so it cannot see through a variable: That limitation is the clearest evidence of what is happening: it is not an environment, it is find and replace. Where gets its TypeScript types Expo CLI generates in your project "},{"u":"expo-security/eas-secrets.html","t":"EAS Secrets and Build-Time Variables","s":"Expo Security","d":"EAS environment variables keep values out of your repository and out of build logs. They do not keep values out of your app. Where that line falls, exactly.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Visibility levels","Environments","The line, drawn sharply","Verify which side of the line a variable is on","Common patterns","A server-side proxy, with the key held by EAS only for deploys","Keep local and remote configuration consistent","Local builds read your shell, not EAS","Security considerations","Common mistakes","Related topics"],"b":"EAS environment variables let you store configuration on Expo's servers and have it injected into a build job. Values marked secret are never readable back — not on the website, not through . That sounds like a place to put an API key. It is not, and the reason is the single most misunderstood thing about EAS secrets: A secret protects the value on its way to the build. It does nothing about what the build does with it. If the build inlines the value into your JavaScript bundle, the value is in the shipped app and anyone can read it. Why it exists / when to use it — and when NOT to EAS environment variables exist to solve a real problem: a build machine needs values that should not be committed to git. A private npm registry token, a Sentry auth token used to upload source maps, a Google Services file, a signing credential for a custom step. They are the right tool when the value is consumed by the build process itself and never reaches the bundle. They are the wrong tool when you were hoping they would make a client side credential safe. For that, see EXPO PUBLIC Variables and the Leak They Cause and What Ships Inside the Bundle. Basic example Create or update a variable: List what a project has, and pull the readable ones into a local file: Remove one: Verify the flags against your installed CLI moves quickly. Run and against the version you have (this site documents eas cli 24.5.0 ) rather than trusting a flag list copied from a blog post. The four subcommands above — , , , — are the documented set; individual flags change more often. writes values to disk It creates a file in your project. Make sure that filename is in before you run it, and remember that a visibility variable cannot be pulled at all — that is the point of the visibility level. How it works Visibili"},{"u":"expo-security/secure-store-vs-asyncstorage.html","t":"expo-secure-store vs AsyncStorage","s":"Expo Security","d":"expo-secure-store uses the iOS Keychain and the Android Keystore. AsyncStorage is unencrypted key-value storage. Which values belong in which, and exactly what neither of them protects.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What each one actually does","What AsyncStorage does NOT protect","What SecureStore does NOT protect either","Accessibility constants","Platform differences","Common patterns","Config plugin setup","Split the record: secret in SecureStore, the rest in AsyncStorage","Handle an unreadable entry as a normal state","Security considerations","Common mistakes","Related topics"],"b":"Expo SDK 57 gives you two obvious places to put a value on the device, and they are not interchangeable: ( ) — small string values in the iOS Keychain and, on Android, in SharedPreferences encrypted with the Android Keystore . (SDK 57 pins ) — an unencrypted key value store backed by a plain file or database in your app's sandbox. The short rule: credentials and tokens go in SecureStore; everything else goes in AsyncStorage. The longer version, including what SecureStore does not do, is below. Version note SDK 57 pins at 2.2.0 . The React Native CLI half of this site documents 3.1.1 for React Native 0.87. Do not copy that number into an Expo project — resolves 2.2.0, and a mismatched native module fails at runtime, not at install time. Why it exists / when to use it — and when NOT to Use for session tokens, refresh tokens, API tokens issued to this user, and anything whose disclosure would let someone act as the user. Use AsyncStorage for cache, the last viewed screen, a theme preference, a feature flag snapshot, onboarding state — data whose disclosure is uninteresting. Do not use SecureStore as a general database. Values are strings, they go through the platform's keychain machinery, and large payloads can be rejected by the platform. Expo's documentation notes that some historical iOS releases refused values above roughly 2048 bytes and that Expo itself does not enforce a limit, so you must handle native errors if you store large strings. For structured local data, use expo sqlite. Basic example Expo Go vs development build Both packages contain native code and are included in Expo Go, so simple usage runs there. is not supported in Expo Go when biometric authentication is available, because Expo Go lacks an . Use a development build with the config plugin for that o"},{"u":"expo-security/oauth-and-pkce.html","t":"OAuth with expo-auth-session and PKCE","s":"Expo Security","d":"Signing users in with the authorization code flow and PKCE using expo-auth-session ~57.0.12 — why the implicit flow and client secrets do not belong in a mobile app, and how to validate the redirect.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The authorization code flow with PKCE, step by step","state is checked for you","Why the implicit flow is wrong on mobile","Why a client secret cannot be kept in an app","Platform differences","Common patterns","Build the redirect URI once and register exactly that","Refresh without prompting","Security considerations","Threat 1: redirect interception with the implicit flow","Threat 2: a client secret in the bundle","Threat 3: loose redirect URI validation at the provider","Common mistakes","Related topics"],"b":"An Expo app that signs users in through an identity provider (your own OAuth server, Auth0, Okta, Keycloak, Google and so on) is an OAuth public client . It runs on hardware the user controls, so it cannot hold a secret. The protocol has a specific answer for that situation: the authorization code flow with PKCE (Proof Key for Code Exchange, RFC 7636). (SDK 57 resolves ) implements that flow. It opens the provider's login page in the system browser, receives the redirect back into your app, and gives you an authorization code to exchange for tokens. PKCE is on by default . Why it exists / when to use it — and when NOT to Use it when your app delegates sign in to a standards compliant OAuth 2.0 / OpenID Connect provider and you want the user to type their password into the provider's page, not yours. Do not use it: For a username/password form posting to your own API. That is not OAuth; it is a login endpoint. Store the resulting session token with expo secure store. To obtain a token that needs a client secret (the \"confidential client\" flows). The app cannot keep that secret — see Security considerations. As a replacement for a platform native sign in SDK where the provider requires one. Check the provider's own mobile guidance first. Basic example uses to generate the PKCE verifier and to open the login page. Give the app a URL scheme so the provider can redirect back into it: A complete sign in screen, verified against the installed 57.0.12 types: Expo Go vs development build , and are all in Expo Go, so the flow can run there. But Expo Go does not use your — it has its own — so the redirect URI differs from the one your release build uses, and a provider configured with an exact redirect URI will reject one of them. Test sign in on a development build, where the sch"},{"u":"expo-security/deep-link-validation.html","t":"Deep Link Validation","s":"Expo Security","d":"Every deep link is attacker-controlled input. A vulnerable Expo Router handler that trusts a next= parameter, the open redirect and token leak it causes, and the strict allow-list that fixes it.","h":["Why it exists / when to use it — and when NOT to","Basic example","The vulnerable handler","The attack","The fixed handler","How it works","Where the parameter comes from","Who can send your app a link","Validating before routing with +native-intent","Platform differences","Common patterns","Unit-test the allow-list with the attacks, not the happy path","Opening an external URL on purpose","Confirm destructive actions","Security considerations","Common mistakes","Related topics"],"b":"A deep link is a URL that opens a screen in your app: , or as a universal link or app link. Expo Router turns every file in into a route that can be opened this way, and hands you the query string as values. Those values come from whoever wrote the link . That is a web page, an email, a QR code, an SMS, a push notification payload, or another app on the device. Treat every parameter exactly as you would treat a query string arriving at a public web server: untrusted input. Why it exists / when to use it — and when NOT to Read this page for any route that does something with a parameter beyond displaying it: navigating to it, opening it, fetching it, attaching a credential to it, or using it to decide what the user is allowed to do. A route that only reads and fetches from your own API with the user's session is not the risk here — your server still checks that the order belongs to the user. The risk is a parameter that controls where the app sends the user or where it sends data. Basic example The vulnerable handler A common shape: after sign in, a callback route sends the user on to wherever they were going, using a parameter. It works in every test anyone writes, because every test passes a legitimate . The attack The attacker sends a link: The app opens, reads the session token from SecureStore — the secure storage did its job; the code handed the value away — and opens . The attacker's server logs the token. No exploit, no malware, one tap. Even without the token, is an open redirect : your app launches a convincing phishing page, and the user trusts it because your app opened it. The fixed handler The fix is an allow list: may only be a path inside this app , from a known set, and nothing is ever appended to it. Three things changed, and all three matter: 1. The de"},{"u":"expo-security/certificate-pinning.html","t":"Certificate Pinning Options","s":"Expo Security","d":"What certificate pinning protects against, the platform mechanisms an Expo SDK 57 app can reach through config plugins, and the operational cost — a pinned app with no backup pin stops working when your certificate rotates.","h":["Why it exists / when to use it — and when NOT to","Basic example","Android: a local config plugin","iOS: NSPinnedDomains","How it works","What is pinned: the public key, not the certificate","Leaf, intermediate or CA","Which traffic is covered","Operational cost","A pinned app bricks itself on rotation without a backup pin","The rotation plan that works","Other costs","Platform differences","Common patterns","Third-party pinning libraries","Pin only what you own","Security considerations","Common mistakes","Related topics"],"b":"Certificate pinning makes your app refuse a TLS connection unless the server presents a specific public key (or one issued by a specific certificate authority), in addition to the normal certificate validation the operating system already performs. It closes one gap: a certificate that the device trusts but that is not yours. It opens another: if your server's key changes and the app does not know the new one, every installed copy of your app stops talking to your server until users install an update. Read the operational cost section before you ship this. Most teams that regret pinning did not plan for rotation. Why it exists / when to use it — and when NOT to Ordinary TLS already protects you from a passive network attacker and from a random attacker run server. Pinning adds protection against a certificate the device trusts but you did not issue: A user installed or MDM installed root CA on a corporate or compromised device, used to intercept traffic. A mis issued certificate from a public CA. An attacker who has persuaded the user to install a proxy's CA certificate. Consider pinning when a successful interception is severe and specific: banking, health data, high value authentication endpoints. Do not pin when: You do not control the certificate lifecycle for the domain (a SaaS API, a CDN that rotates keys on its own schedule, a third party auth provider). You will be broken by someone else's change. You cannot guarantee a release process that ships new pins before old keys are retired. The goal is to stop users from inspecting your app's traffic. On a device the user controls, pinning is bypassable with instrumentation tools. It raises effort; it does not hide your API. Basic example An Expo SDK 57 app has no pinning API in JavaScript. goes through the platform ne"},{"u":"expo-security/update-signing.html","t":"EAS Update Signing","s":"Expo Security","d":"Code signing for expo-updates — why an unsigned update channel lets whoever can publish run code on every install, how to configure updates.codeSigningCertificate, and what key management costs you.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The trust chain","Why an unsigned channel is a supply-chain risk","Signing does not replace account security","Operational cost","You now own a private key","Losing the key","Key compromise","Certificate validity and rotation","Common patterns","Keep the certificate in the repo, the key in the release job only","Check the repository never gained the key","Security considerations","Common mistakes","Related topics"],"b":"An EAS update is executable JavaScript delivered to apps that are already installed. Once an app accepts it, that code runs with everything your app has: the user's session, SecureStore entries, camera and location permissions, your API. Without code signing, an installed app accepts any update your update server serves for its channel and runtime version. The practical meaning: whoever can publish to your channel can run code on every install . Code signing adds a second requirement: the update must also be signed by a private key whose certificate is baked into the binary. Why it exists / when to use it — and when NOT to Use code signing when a malicious update would be severe — which is true for most apps that handle accounts, payments or personal data — and when your team can manage a private key properly. It protects you against: A compromised Expo account, leaked , or over broad CI access publishing an update. An insider or a compromised CI job publishing code that did not go through your release process. A self hosted update server being compromised, if you use one. It does not protect you against: Malicious code that reaches the update through your own build : a compromised dependency is signed along with everything else. See Dependency Auditing. Theft of the signing private key itself. Anything in the store binary. The binary is protected by store signing, a separate system. Plan requirement Expo's code signing documentation states that EAS Update Code Signing is only available to accounts subscribed to the EAS Production or Enterprise plans. Check the current plan terms on docs.expo.dev before you design around it — see Costs and Limits. Basic example Everything here uses the CLI shipped inside 57.0.22 (SDK 57 resolves ). The commands and flags below were read"},{"u":"expo-security/permissions-hygiene.html","t":"Permissions Hygiene","s":"Expo Security","d":"Requesting only the permissions your Expo app uses, writing honest usage descriptions, removing permissions libraries add behind your back, and proving what the shipped binary actually declares.","h":["Why it exists / when to use it — and when NOT to","Basic example","Configure permission strings through the module's config plugin","Remove permissions you did not ask for","Ask at the moment of use","How it works","Where permissions come from","Why a vague usage string is a real problem","Platform differences","Common patterns","Audit what an SDK package brings before you add it","Keep a permissions inventory in the repository","Foreground before background, and background only with a reason","Security considerations","Common mistakes","Related topics"],"b":"Every permission your app declares is a capability an attacker inherits if they ever run code inside it. That can happen through a compromised dependency, an unsigned update, or a WebView bug. It is also a line on a store review checklist, and a reason for a user to decline an install. Permissions hygiene is three habits: 1. Declare only what a shipped feature uses. 2. Request at the moment of use, with an explanation that is true. 3. Check the built binary , because libraries add permissions you never wrote. Why it exists / when to use it — and when NOT to Do this for every release build. It matters most when you add an SDK package with native code: , , , and third party SDKs all bring permissions with them. This page is about declared permissions and when you ask for them. For the request API of each module, see Permissions Patterns. Basic example Configure permission strings through the module's config plugin SDK modules with sensitive permissions accept their usage strings as plugin options. Verified against the installed SDK 57 plugin types: What each option does here: Option Package Effect iOS camera usage description Type is ; opts out of the microphone description for an app that never records audio Do not add Android's audio recording permission iOS when in use usage description / Leave unless a shipped feature tracks location in the background Remove permissions you did not ask for removes a permission from the final merged , even when a library's own manifest adds it. The SDK 57 config types document that it uses internally and is not available in Expo Go. adds permissions during prebuild. removes them from the merged result. Use the second one to fix a library that over declares. Ask at the moment of use The permission is requested when the user opens the sc"},{"u":"expo-security/dependency-auditing.html","t":"Dependency Auditing","s":"Expo Security","d":"Your Expo app runs every package in its dependency tree with the user's session in the same process. npm audit, lockfiles, npx expo install --check, and the install-script risk on build machines.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","1. The lockfile is the audit target","2. npm audit — known vulnerabilities","3. npx expo install --check — SDK alignment","4. Install scripts — the build-machine risk","5. Registry signatures","Platform differences","Common patterns","Review a new dependency before adding it","Keep dependencies few","Scan the output, not just the input","Security considerations","Common mistakes","Related topics"],"b":"Every package in your dependency tree ends up in one of two places: In the bundle , running inside your app with access to everything your app can read — including tokens you correctly stored in SecureStore, the moment your code reads them. On the build machine , where install scripts run with your environment variables in scope — including EAS secrets and CI tokens. You wrote a few thousand lines. You ship a few hundred packages. Auditing is how you keep track of what those packages are and whether they changed. Why it exists / when to use it — and when NOT to Do this on every pull request that touches or the lockfile, and on a schedule for advisories published against packages you already have. Do not treat any single tool as the audit. knows about reported vulnerabilities in known versions. It says nothing about a package that was malicious from its first release, or a maintainer account taken over yesterday. The habits below cover different gaps. Basic example A CI step that covers the four checks this page describes: How it works 1. The lockfile is the audit target says . The lockfile says exactly , with an integrity hash. Without a committed lockfile, two installs a day apart can resolve different code, and you have audited neither. Commit the lockfile ( , , or ). Use in CI and on build machines , not . installs exactly the lockfile and fails if it does not match , instead of silently rewriting it. Review lockfile diffs. A one line change that adds forty lockfile entries is forty new packages. That is the thing to look at. 2. — known vulnerabilities limits the report to dependencies that are not dev dependencies, which is closer to what ships. sets the minimum severity that makes the command exit non zero. Read the output rather than reaching for . is allowed to i"},{"u":"expo-testing/jest-expo.html","t":"Jest with jest-expo","s":"Expo Testing","d":"Setting up Jest on Expo SDK 57 with the jest-expo preset — the install that actually works, the missing @react-native/jest-preset peer, platform presets and transformIgnorePatterns.","h":["Why it exists / when to use it — and when NOT to","Basic example","1. Install","2. Install the React Native Jest preset — the step everyone misses","3. Configure","4. Write a test","How it works","What the preset does","transformIgnorePatterns","Platform presets","Platform differences","Common patterns","A setup file for shared mocks","Coverage","Performance considerations","Common mistakes","Related topics"],"b":"is a Jest preset for Expo projects. It wraps React Native's Jest preset, adds Expo's Babel configuration, mocks the native side of Expo modules, and understands the same asset extensions Metro does. On SDK 57 it is , read from , and also reports . Jest runs your code in Node, not on a device. It is the right tool for logic, hooks and component behaviour — and the wrong tool for anything that depends on real native code. Why it exists / when to use it — and when NOT to Use Jest with for: Pure logic: formatters, reducers, validation, API clients with a mocked . Hooks and components, with React Native Testing Library. Code that calls Expo modules, with the native side mocked. Do not use it to prove that: A native module works on a device. The native side is mocked; your test proves nothing about it. Navigation, gestures and animations feel right. Use end to end tests. A config plugin produced the right native project. See Testing and Failure Modes. Basic example 1. Install SDK 57 resolves these to , and — the and pins come from SDK 57's on the Expo versions API. That is Jest 29 , even though Jest 30 is on npm. 57 is built on Jest 29 packages. The flag is 's own option in SDK 57's ( ). Forwarding npm's flag instead — — has been seen to put the packages under . Whichever form you use, check afterwards and move them to if they landed in the wrong place. 2. Install the React Native Jest preset — the step everyone misses declares a peer dependency on , and does not install it. Without it, the first run fails immediately: Install the version that matches SDK 57's React Native, 0.86.3 : An explicit is honoured by as written. Do not install 0.87.x — that is the React Native version of the CLI half of this site, not of Expo SDK 57. 3. Configure 4. Write a test Importing , and from "},{"u":"expo-testing/testing-library.html","t":"React Native Testing Library","s":"Expo Testing","d":"Testing Expo SDK 57 components with @testing-library/react-native 14 — the async render API, queries, userEvent, built-in matchers, and the test-renderer peer.","h":["Why it exists / when to use it — and when NOT to","Basic example","Install","A test","How it works","The v14 async API","Queries","userEvent vs fireEvent","Built-in Jest matchers","Common patterns","Waiting for async UI","Testing a hook","Expo Router screens","Performance considerations","Common mistakes","Related topics"],"b":"React Native Testing Library (RNTL) renders components in Jest and lets you query them the way a user finds things — by role, label and text — rather than by component internals. The current version is . Version 14 changed the API in a way most online examples have not caught up with: , , and friends are async and return promises. Why it exists / when to use it — and when NOT to Use RNTL for component behaviour: \"when the user types a wrong password and presses Sign in, an error appears\". Tests written this way survive refactors because they do not depend on state names or component structure. Do not use it for: Pixel level appearance. There is no real rendering; there is no layout. Real navigation transitions, gestures and animations. Use end to end tests. Pure functions. Test those directly with Jest. Basic example Install RNTL 14's peer dependencies, from : , , , . SDK 57 satisfies the first three (Jest 29.7, React 19.2.3, React Native 0.86.3). must be installed explicitly — it is a separate package, not . Neither package is in the SDK 57 version map, so resolves them from npm. You need jest expo set up first. A test How it works The v14 async API Verified from the installed type definitions: API Returns / (on the result) / / Promises then , Promises Code from RNTL 13 and earlier that calls without compiles, but the tree may not be ready when you query it. RNTL publishes migration codemods for the v14 async change; see its documentation. Queries holds the result of the most recent . Queries come in families: Prefix Not found Found many Async Throws Throws No Returns Throws No Rejects Rejects Yes — waits / / As above, for arrays — — Prefer them in this order: (with ), , , , , and only as a last resort. Role and label queries also check that your UI is accessible. vs s"},{"u":"expo-testing/mocking-expo-modules.html","t":"Mocking Expo Modules","s":"Expo Testing","d":"How jest-expo mocks the native side of Expo modules in SDK 57, which packages ship automatic mocks, and how to mock the rest — expo-secure-store, expo-haptics, expo-constants and friends — with correct types.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","What jest-expo mocks, read from its setup file","The automatic mock lookup — and where it stops","Three ways to mock, and when to use each","Common patterns","Overriding a property on a real module: mockProperty","Mocking a module that only some tests should mock","Wrap native modules behind your own interface","Expo Go vs development build","Common mistakes","Related topics"],"b":"Every Expo module has a JavaScript API backed by native code. Jest runs in Node, where that native code does not exist, so any test that reaches an Expo module needs the native side replaced. This is the page to read when a test fails with . ( ) mocks some of this for you. The rest you mock yourself, and doing it well is mostly about mocking at the right level : the package's JavaScript API, not its internals. Why it exists / when to use it — and when NOT to Mock an Expo module when the code under test calls it and you want to test your logic: what happens when returns a token, returns , or throws. Do not mock a module to \"prove\" it works. A test where is a proves only that you called it. Whether the Keychain or Keystore actually stored the value is an end to end question. And do not mock what you can run: plain JavaScript utilities, your own hooks and components should run for real. Basic example Code that reads a token from : A test that mocks the package's JavaScript API with a factory. The function under test is inlined so this block stands alone: How it works What jest expo mocks, read from its setup file runs before your tests. It: 1. Defines mocks for React Native's legacy table, including a set of third party native modules (react native screens, safe area context, gesture handler, reanimated, svg, webview and others). 2. Mocks , so and return a mock object instead of touching native code. 3. Installs web implementations of , and . 4. Mocks with resolved promise functions. The automatic mock lookup — and where it stops When an Expo package calls , the mocked looks for a file at inside that package. If it exists, every exported function becomes a wrapping the mock implementation. If there is no mock file and no other registered module, throws . So what matters is"},{"u":"expo-testing/end-to-end.html","t":"End-to-End on Development Builds","s":"Expo Testing","d":"End-to-end testing an Expo SDK 57 app against a real build of your own binary — not Expo Go — with Maestro locally or on EAS Workflows, and an honest status note on Detox.","h":["Why it exists / when to use it — and when NOT to","Expo Go vs development build","Basic example","1. Give elements stable identifiers","2. Write a flow","3. Run it locally","How it works","Building an app to test","Running on EAS Workflows","Platform differences","Common patterns","Detox — status on SDK 57","Keep flows independent","Performance considerations","Common mistakes","Related topics"],"b":"End to end (E2E) tests drive your real app on an emulator or simulator: launch it, tap, type, and assert what appears on screen. They are the only automated tests that exercise your actual native modules, navigation and permissions. They run against a build of your own app — never Expo Go. Expo Go is a different binary with a fixed set of native modules; a passing E2E run in Expo Go tells you nothing about what you ship. Why it exists / when to use it — and when NOT to Write E2E tests for the handful of flows that must never break: sign in, the core purchase or creation flow, onboarding, and anything gated by a native permission. Do not try to cover everything with E2E tests. They are slow (a build plus a device boot), and they fail for reasons unrelated to your change: timing, emulator state, network. Keep logic and component behaviour in Jest, and a small number of E2E flows on top. Expo Go vs development build Requires a build of your own app. Two kinds are useful: Build Contains Good for Development build ( ) Your native code, loads JS from Metro Writing and debugging flows locally Test build with embedded JS (for example the profile below) Your native code and bundled JS CI — no Metro, no dev launcher screen A development build opens the dev launcher first, which your flow would have to navigate. For CI, a build with the JavaScript embedded is simpler and closer to release. Basic example This follows the Maestro setup in Expo's own documentation (docs.expo.dev, \"Run E2E tests on EAS Workflows with Maestro\"). 1. Give elements stable identifiers 2. Write a flow is your / from app config. 3. Run it locally Install the Maestro CLI following the official instructions at docs.maestro.dev, install your build on a running emulator or simulator, then: How it works Building "},{"u":"expo-testing/ci.html","t":"Testing in CI","s":"Expo Testing","d":"Running an Expo SDK 57 test suite on every pull request — Jest on a Linux runner, dependency and type checks, and an honest account of iOS needing macOS and EAS needing an account.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","The jest-expo peer dependency must be in the lockfile","Why Linux is enough for the fast layer","iOS needs macOS — there is no way around it","EAS in CI needs an account and a token","Platform differences","Common patterns","Layer the pipeline by cost","Cache what is safe to cache","Keep snapshots honest","Security considerations","Common mistakes","Related topics"],"b":"A test suite that only runs on a developer's laptop stops being run. CI makes it run on every pull request, on a clean machine, with the exact dependency versions in the lockfile. For an Expo SDK 57 app the pipeline has layers with very different costs. Most of it runs on any Linux runner for free; iOS builds need macOS; hosted builds need an Expo account. Why it exists / when to use it — and when NOT to Run in CI, on every pull request: — catches a dependency installed at a version built for another SDK. TypeScript — catches a React Native 0.87 only API copied into a 0.86 project. Jest — logic, hooks and components. Run less often (on merge to main, nightly, or before release): Native builds and end to end tests. They take many minutes and cost runner time. Do not put E2E tests on every commit of every branch unless you have measured that the runner time and flakiness are acceptable. Basic example A GitHub Actions workflow for the fast layer. It needs no Expo account and runs on Linux: Use rather than if your script is , which is what Expo's setup instructions put in — watch mode waits for input. How it works The jest expo peer dependency must be in the lockfile needs , and does not install it. If it is missing, CI fails on the first test with: Fix it once locally, commit the lockfile, and CI follows: Use to match SDK 57's React Native — not 0.87. See Jest with jest expo. Why Linux is enough for the fast layer Jest runs in Node. and presets change module resolution, not the operating system, so the entire unit layer runs on — or Windows or macOS if you prefer. iOS needs macOS — there is no way around it Building an iOS app, and running the iOS Simulator, require Xcode, which only runs on macOS. For CI that means one of: Option Needs A macOS runner on your CI provider ("},{"u":"expo-build-and-release/icons-and-splash-screens.html","t":"App Icons and Splash Screens","s":"Expo Build and Release","d":"The icon and splash configuration a new SDK 57 project actually ships with — icon, ios.icon with an .icon asset, Android adaptive icon layers, and the expo-splash-screen plugin — and what each store expects.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","icon: the universal fallback","ios.icon: a file, an appearance set, or an .icon directory","android.adaptiveIcon: layers, not a picture","android.predictiveBackGestureEnabled","The splash screen: expo-splash-screen plugin options","Platform differences","Common patterns","Variant icons for non-production builds","Check the real result, not the config","Common mistakes","Related topics"],"b":"Icons and the splash screen are native assets: they are baked into the binary at build time, they are the first thing a store reviewer and a user see, and none of them can change through an EAS Update. This page starts from what generates for SDK 57, because the configuration shape changed from what most tutorials show. There is no top level key any more; the splash screen is configured through the config plugin. Why it exists / when to use it — and when NOT to Read this before your first store build and whenever you rebrand. Every change here needs a new native build . For controlling when the splash hides at runtime ( , ) and for fonts, see Fonts and Splash Screens. This page covers the assets and their configuration. Basic example This is the icon and splash configuration from a real SDK 57 project generated by with the default template. Only the unrelated keys have been removed: The asset files that template ships, with their dimensions measured from the generated files: File Size Used by 1024 × 1024 — the fallback for any platform without its own A directory: plus layer images 512 × 512 512 × 512 432 × 432 228 × 213 48 × 48 All PNGs in the template have an alpha channel. Expo Go vs development build Expo Go shows its own icon and splash screen. Nothing on this page is visible there. Check icons and the splash in a development build or, better, a release build installed from a real artifact. How it works : the universal fallback The SDK 57 config types describe as a local path or remote URL, and recommend a 1024 × 1024 PNG. Expo generates the platform sizes from it. and override it per platform, and overrides both on Android. : a file, an appearance set, or an directory In the SDK 57 types, accepts three shapes: Shape Example When A path to an image One icon for eve"},{"u":"expo-build-and-release/versioning.html","t":"Versioning and Runtime Versions","s":"Expo Build and Release","d":"The three numbers every Expo release carries — the user-facing version, the store build number, and the runtime version that decides which updates a binary accepts — and how to keep them from drifting.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Version: for people","Build number: for the stores","Who increments build numbers: cli.appVersionSource","Runtime version: for updates","Runtime version policies","Choosing a policy","Seeing what a build is running","Platform differences","Common patterns","Release branch flow with remote versioning and fingerprint","Bump version from a script, not by hand in three places","Common mistakes","Related topics"],"b":"A shipped Expo app carries three different version identifiers, and each one answers a different question: Identifier App config key Answers Who reads it Version \"Which release is this?\" Users, store listings Build number / \"Is this upload newer than the last one?\" App Store Connect, Google Play Runtime version \"Which updates can this binary run?\" and the update server Most versioning incidents come from treating these as one number. A store rejects an upload because the build number did not increase. An update crashes a binary because the runtime version said they were compatible when they were not. Why it exists / when to use it — and when NOT to You need all three once you ship to a store and use updates. Without there is no runtime version to manage, and only the first two matter. The decisions on this page are: 1. Who increments build numbers — you, in the app config, or EAS, remotely. 2. Which policy decides update compatibility. Basic example Types, from the installed for SDK 57: Key Type Maps to string iOS , Android string iOS number — a positive integer Android string, or The runtime version embedded in the build , , and also exist and override the top level value for one platform. Use them only when the platforms genuinely diverge. is a string, is a number and are both type errors in a typed . The config types also note that Apple's Transporter reads the version number from , not from . How it works Version: for people is what users see in the store and in your About screen. Follow whatever scheme your team uses (semantic versioning is common). Stores do not require it to increase on every upload, but each store release should have a distinct one so support conversations are unambiguous. Build number: for the stores Both stores require each upload to be distin"},{"u":"expo-build-and-release/environments.html","t":"Build Profiles per Environment","s":"Expo Build and Release","d":"Separating development, preview and production builds of an Expo app — distinct app identifiers installed side by side, eas.json profiles, EAS environments, update channels, and which values may differ between them.","h":["Why it exists / when to use it — and when NOT to","Basic example","1. A dynamic app config keyed on APP_VARIANT","2. Profiles that set the variant","3. Build each one","How it works","Four settings, four jobs","env versus environment","Why separate identifiers matter","Channels keep updates in their lane","What may differ between environments","Platform differences","Common patterns","Read the variant at runtime without shipping secrets","Check what a profile will produce before building","A staging profile that inherits preview","Common mistakes","Related topics"],"b":"Most apps need at least three builds that can coexist: a development build that loads code from your dev server, a preview build testers install to try a release candidate against staging, and the production build in the stores. If all three share an application identifier, installing one replaces the other, a tester's preview build can receive a production update, and a staging API URL can end up in a store build. This page sets them up so those mistakes cannot happen by accident. Why it exists / when to use it — and when NOT to Set this up before you hand a build to anyone else. Retrofitting it later means changing bundle identifiers, which on the stores means a new app. You do not need separate identifiers if you only ever install one build per device and do not use EAS Update. A single profile set with different values is enough. Do not use environments to hide secrets. Every value that reaches the JavaScript bundle is readable in every environment — see EXPO PUBLIC Variables and the Leak They Cause. Basic example 1. A dynamic app config keyed on This is the pattern from Expo's multiple app variants guide, typed with and from : The block is marked as a fragment only because is typed by the file Expo CLI generates in a real project, which a standalone snippet does not have. receives the static as and returns the final config. Keep everything that does not vary in ; put only the differences here. 2. Profiles that set the variant Every field above is in the 24.5.0 schema documented on eas.json and Build Profiles. 3. Build each one The three Android builds have three package names, so they install side by side. Cost and alternatives runs on EAS, which needs an Expo account and is a paid service with a free tier; queue times and concurrency depend on the plan. runs the s"},{"u":"expo-build-and-release/store-metadata.html","t":"App Store Metadata","s":"Expo Build and Release","d":"Store listings for an Expo app — what lives in the binary versus the store console, EAS Metadata's store.config.json for the App Store (beta, App Store only), and the privacy disclosures both stores require.","h":["Why it exists / when to use it — and when NOT to","Basic example","What the binary contributes","EAS Metadata for the App Store","How it works","Pull first, then push","Dynamic config","Privacy disclosures are metadata too — and they are checked","Platform differences","Common patterns","Review listing changes like code","Keep release notes next to the release","Keep an SDK data inventory","Common mistakes","Related topics"],"b":"A store listing is everything a user sees before installing: name, subtitle, description, keywords, screenshots, the privacy disclosures, support and privacy policy URLs, age rating and category. Almost none of it is in your binary. It lives in App Store Connect and the Google Play Console, and it is reviewed alongside the build. This page separates what your app config controls from what the stores control, and covers the one Expo tool for managing listings as files: EAS Metadata , which today supports the Apple App Store only and is in beta. Why it exists / when to use it — and when NOT to Read this before your first submission, and whenever a listing change needs review or has to match a release. Consider EAS Metadata when you want App Store listing text in version control, reviewed in pull requests, and pushed from the command line. Do not adopt it for Google Play — Expo's documentation states the Google Play Store is not implemented — and weigh its beta status before making it the only copy of your listing. Basic example What the binary contributes Listing item Comes from App name under the icon in the app config In app icon , , — see App Icons and Splash Screens Version shown on the listing Permissions and usage descriptions Native config — see Permissions Hygiene Everything else — store title, description, screenshots, privacy answers — is entered in the store. EAS Metadata for the App Store at the project root. This is the minimal example from Expo's documentation: Expo documents built in validation that runs before anything is sent to the store, which catches some listing problems without waiting for review. If the file is not at the default location, point at it with the verified field on the iOS submit profile: Beta, App Store only Expo's documentation states"},{"u":"expo-build-and-release/submission-checklists.html","t":"Submission Checklists","s":"Expo Build and Release","d":"Concrete pre-submission checks for an Expo SDK 57 release — build and version checks, security scans, native configuration, and the per-store items that most often cause a rejection or a bad release.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Build and version","Security","Native configuration","Behaviour on a release build","Platform differences","Common patterns","The update checklist","Make one person own the release","Common mistakes","Related topics"],"b":"A submission checklist exists to catch the mistakes that are cheap before upload and expensive after: a spent build number, a staging URL in production, a secret in the bundle, a permission string that fails review, an update channel pointing at the wrong branch. Each item below is something you can check with a command or a specific screen , not a reminder to \"make sure it works\". Copy the lists into your release process and delete what does not apply. Why it exists / when to use it — and when NOT to Run through this for every store submission. For JavaScript only changes shipped as an EAS Update, use the shorter update checklist at the end. The store review guidelines are long and change. This page does not restate them; it lists the technical checks an Expo project controls, and points at where the stores' own rules live. Basic example A script that automates the checks which can be automated: Adjust the host patterns in step 8 to your own staging domains. Step 8 in particular catches a mistake no store review will catch for you. How it works Build and version [ ] is set to this release. does not change it. [ ] Build numbers are managed: with , or a manual bump you have checked is higher than the last upload (not the last release). [ ] policy is what you intend. If it is and this release changes native code, changed too. [ ] The build profile is : is not , is not . [ ] The production profile's is . Check the built binary, not only , by surfacing on a debug screen of a release build. [ ] is pinned. See Versioning and Runtime Versions and Build Profiles per Environment. Security [ ] Bundle scan (step 7) is clean. See What Ships Inside the Bundle. [ ] Every variable is public by design. See EXPO PUBLIC Variables. [ ] No signing keys, , keystores or service account JSON "},{"u":"expo-build-and-release/staged-rollouts.html","t":"Staged Rollouts","s":"Expo Build and Release","d":"Releasing to a fraction of users first — Apple's phased release, Google Play staged rollouts via the console or eas submit, and EAS Update per-update and branch rollouts — with what each can and cannot undo.","h":["Why it exists / when to use it — and when NOT to","Basic example","EAS Update: publish to 10% first","Google Play: staged rollout from eas submit","App Store: phased release","How it works","App Store phased release","Google Play staged rollout","EAS Update rollouts","How they combine","Platform differences","Common patterns","Define stages and exit criteria before you start","Keep a known-good update ready","Use the Play Console for widening, eas submit for starting","Common mistakes","Related topics"],"b":"A staged rollout releases a change to a small share of users, lets you watch it, and then widens it. It limits the blast radius of a bad release: 1% of users hitting a crash is an incident; 100% is an outage. An Expo app has three independent rollout mechanisms, because it ships through two stores and, optionally, an update service: Mechanism Controls Who decides the percentage App Store phased release A new binary version on iOS Apple's fixed 7 day schedule Google Play staged rollout A new binary version on Android You EAS Update rollouts A JavaScript update on installed binaries You They do not coordinate with each other, and their undo stories are very different. Why it exists / when to use it — and when NOT to Use a staged rollout for every production release that you cannot fully test in advance — which is most of them. It is cheapest where the undo is also cheap: EAS Update rollouts. A staged rollout does not help if nobody is watching crash and error rates while it runs. Set up Monitoring first. It is also not a substitute for a fix path. A store rollout can be halted , but users who already received the build keep it. Basic example EAS Update: publish to 10% first is a verified flag in eas cli 24.5.0. Then widen, or back out: Google Play: staged rollout from and are verified fields of the 24.5.0 submit schema; is a number giving the staged rollout fraction. Widen or halt the rollout afterwards in the Play Console. App Store: phased release Enable phased release for automatic updates on the version in App Store Connect before it is released. There is no percentage to choose. How it works App Store phased release From Apple's App Store Connect documentation: Day 1 2 3 4 5 6 7 Share of users with automatic updates 1% 2% 5% 10% 20% 50% 100% It applies to users with "},{"u":"expo-build-and-release/monitoring.html","t":"Monitoring","s":"Expo Build and Release","d":"Knowing whether an Expo release is healthy — crash and error reporting with readable stack traces, which binary and update each report came from, update adoption, and the signals that decide a rollout.","h":["Why it exists / when to use it — and when NOT to","Basic example","Tag every report with the build and the update","A crash and error reporter","How it works","Stack traces need source maps","Crash-free rate by release, not overall","Update adoption and launch data","On-device update logs","Platform differences","Common patterns","Rollout gate: compare the new slice with the previous one","Send the release context to your own API","Test the pipeline with a deliberate error","Common mistakes","Related topics"],"b":"Monitoring a mobile release answers four questions, and you want each answer within minutes of a problem starting, not from a one star review: 1. Is it crashing? Native crashes and fatal JavaScript errors. 2. Is it erroring? Handled errors, failed requests, broken screens that did not crash. 3. Which build is it? Binary version, build number, runtime version, update ID and channel. 4. Who has it? How far a binary or an update has actually spread. Without the third answer, the first two are close to useless for an app that ships JavaScript updates: the same binary version can be running several different bundles. Why it exists / when to use it — and when NOT to Set monitoring up before your first staged rollout. A rollout without it is a slow full release. Collect what you need to find and fix problems. Monitoring SDKs collect device and usage data, which you must disclose in App Store privacy answers and the Play Data safety form — see App Store Metadata. Do not send tokens, passwords or personal content in error reports. Basic example Tag every report with the build and the update Whatever reporting tool you use, attach these values to every event. All of them are verified exports of the installed SDK 57 packages: A crash and error reporter Expo does not ship its own crash reporter. A common choice is Sentry, whose React Native SDK is in the SDK 57 native module map: On SDK 57 that resolves . The tag on npm is a newer major version (8.26.0 at the time of writing), so a bare package manager install would fetch a version SDK 57 does not pin — use . This block is a fragment: is not installed in this site's type check harness, so the calls above were not compiled here. , and are the entry points named in Sentry's own Expo guide. Sentry's guide also configures a config plug"},{"u":"expo-build-and-release/rollback-strategy.html","t":"Rollback Strategy","s":"Expo Build and Release","d":"Planning the undo before an Expo release goes out — rolling back an EAS Update to a previous update or the embedded bundle, why a store binary cannot be rolled back, server-side kill switches, and a minimum-version gate.","h":["Why it exists / when to use it — and when NOT to","Basic example","Rolling back an EAS Update","How it works","What an update rollback can reach","Rolling back takes effect at the next update check","Store binaries cannot be rolled back","expo-updates has its own safety net","Platform differences","Common patterns","A server-side kill switch for risky features","A minimum-version gate","Keep the backend compatible with every supported binary","The incident runbook","Common mistakes","Related topics"],"b":"A rollback strategy is a written answer to \"this release is broken; what do we do in the next ten minutes?\" For an Expo app, the answer depends entirely on what is broken: What went wrong Can you roll back? Fastest safe action JavaScript in an EAS Update Yes , in minutes JavaScript in the store binary's embedded bundle Partly Publish a fixed EAS Update to that runtime version Native code, permissions or config in a store binary No Stop the store rollout; fix forward with a new binary Backend change that breaks old app versions Yes, on your server Revert the server, or version the API A feature that misbehaves, in any of the above Yes, if you built a switch Turn the feature off server side Plan the actions in the right hand column before you release. During an incident is too late to discover that your channel points at the wrong branch or that the previous good update was on another runtime version. Why it exists / when to use it — and when NOT to Write this plan once per app and review it for each release that changes native code or the runtime version. Keep it short enough to follow under stress. Do not treat EAS Update rollback as a safety net for native changes. It cannot deliver or remove native code. Basic example Rolling back an EAS Update Expo documents this as an interactive command with two rollback types: To a previously published update — it re publishes an earlier update, so clients functionally return to it. To the embedded update — it instructs clients to run the bundle embedded in their build. After a rollback, publishing again delivers the new update to all clients as normal. The related commands, all present in the eas cli 24.5.0 command manifest: Run first during an incident: it shows which branch the channel points at and the newest update on it, whi"},{"u":"expo-vs-bare/comparison.html","t":"An Honest Comparison","s":"Expo vs Bare CLI","d":"Expo SDK 57 against the React Native Community CLI, side by side — React Native version, native project ownership, upgrades, libraries, builds, updates and cost — with the weaknesses of each stated plainly.","h":["Why it exists / when to use it — and when NOT to","The comparison","How it works — reading the table","Version control is the biggest structural difference","Native ownership cuts both ways","The SDK is a real asset and a real dependency","EAS is optional, and it is not free at scale","Common patterns","Weaknesses, stated plainly","Try both on your hardest requirement","Common mistakes","Related topics"],"b":"This site documents two toolchains for the same framework. The CLI half uses the React Native Community CLI on React Native 0.87.1 . This half uses Expo SDK 57 on React Native 0.86.3 . Both produce React Native apps with the same renderer (Fabric), the same engine (Hermes) and the same core components. The differences are in who owns the native projects, which versions you can run, and which tools surround the build. This page lays them out without recommending either as the default for everyone. Why it exists / when to use it — and when NOT to Read this page when you are choosing a toolchain for a new project, or when a team is arguing about switching. For a shorter, decision shaped version, read Choosing Expo or the Bare CLI. Do not read it as a verdict. Both toolchains are maintained, widely used and capable of shipping large apps. The right answer depends on your native surface, your team and your constraints. About the words \"managed\" and \"bare\" Older material divides Expo projects into a \"managed workflow\" and a \"bare workflow\". That framing is retired : it described an Expo project with or without native directories, with \"eject\" as a one way door between them. Today the live distinction inside Expo is CNG vs committed native directories, and it is reversible. On this site, \"bare\" means only the React Native Community CLI toolchain. The comparison Expo SDK 57 React Native Community CLI React Native version 0.86.3 , pinned by the SDK 0.87.1 , or whichever version you choose Upgrading React Native alone Not supported — upgrade the SDK Supported, via the Upgrade Helper Time to a new React Native release Waits for the next SDK Available on release day and Generated from config (or committed, if you choose) Committed and maintained by you Native configuration App conf"},{"u":"expo-vs-bare/what-expo-costs.html","t":"What Expo Gives You and What It Costs","s":"Expo vs Bare CLI","d":"A ledger of what Expo SDK 57 provides over the bare React Native CLI and what you pay for it — in React Native version control, native flexibility, app size, vendor dependency and money.","h":["Why it exists / when to use it — and when NOT to","What you get","Generated native projects","One SDK, one version line","Tooling around the build","Optional hosted services","What it costs","1. React Native version control","2. Native flexibility, or the upgrade benefit — pick one","3. Another layer to understand","4. App size","5. Pace and priorities set by someone else","6. Money, if you use EAS","7. Expo Go is not the product","Basic example","Common mistakes","Related topics"],"b":"Every toolchain trades something. This page is the ledger for Expo: each benefit next to the cost that comes with it, without promotional language and without pretending the costs are zero. The baseline for comparison is the React Native Community CLI, documented in the CLI half of this site. Why it exists / when to use it — and when NOT to Use this page when you need to justify a toolchain choice to someone else — a lead, a native team, a finance owner — and want both columns in one place. It is not a recommendation. If the costs below are acceptable for your project, Expo is usually cheaper to maintain. If one of them is a hard blocker, read When the Bare CLI Is Better. What you get Generated native projects and are produced from your app config by Continuous Native Generation. Upgrades do not involve merging native template changes into hand edited files, because there are no hand edited files. One SDK, one version line About 120 native packages ( , , , , and so on), released together, tested against one React Native version, with the community libraries the SDK tracks pinned alongside. picks matching versions for you. See expo install and SDK Alignment. Tooling around the build templates with Expo Router, typed routes and the React Compiler experiment already enabled in the SDK 57 default. Development builds via , with a launcher and dev menu. Config plugins as a structured way to change native projects. The Expo Modules API for writing native modules in Swift and Kotlin. Web output from the same project. Optional hosted services EAS Build, Submit, Update and Workflows. EAS Update in particular fills a gap the CLI has no first party answer for. What it costs 1. React Native version control SDK 57 ships React Native 0.86.3 . The CLI half of this site is on 0.87.1 . Y"},{"u":"expo-vs-bare/when-bare-is-better.html","t":"When the Bare CLI Is Better","s":"Expo vs Bare CLI","d":"The specific situations where the React Native Community CLI is the better toolchain than Expo SDK 57 — and the situations people cite that turn out not to be reasons.","h":["Why it exists / when to use it — and when NOT to","The cases where the CLI is better","1. You need a React Native version the SDK does not ship","2. You are adding React Native to an existing native app","3. Substantial hand-written native code lives in the app target","4. Native build files must be source-controlled and audited","5. A library you depend on cannot work with generation","6. You want no dependence on a vendor's release cadence","Basic example — the test in one table","How it works — what you take on with the CLI","Common patterns","Reasons that are not reasons","Prototype the blocker, not the app","Leaving Expo later is possible","Common mistakes","Related topics"],"b":"Expo is the lower maintenance default for most new React Native apps. It is not the right answer for every project. This page lists the cases where the React Native Community CLI genuinely fits better, with the reason each one bites, and then the common objections that do not hold up. \"Bare\" on this site means the Community CLI toolchain. It is not a mode of Expo. Why it exists / when to use it — and when NOT to Read this when Expo has been proposed and someone has a concern, or when you are deciding whether an existing Expo app has outgrown its toolchain. Check each case against your actual requirements — \"we might need that someday\" is a weak basis for giving up generated native projects. The cases where the CLI is better 1. You need a React Native version the SDK does not ship SDK 57 pins React Native 0.86.3 . The CLI can be on 0.87.1 today. Upgrading React Native independently of the SDK is not supported, so if you need something that landed in 0.87 — a bug fix, a platform requirement, an API — and cannot wait for the next SDK, the CLI is the only supported route. Test this claim honestly: name the specific change in the newer React Native you need. If you cannot, this is not your case. 2. You are adding React Native to an existing native app Brownfield integration — a React Native screen inside an established iOS or Android codebase — is what the CLI's model is built around. The native app is the host, and its project files are the source of truth. Expo's generated native model assumes the app is the Expo project. You can use Expo modules inside such an app, but the generation model does not apply. See Adopting Expo in an Existing Bare App for what that realistically looks like. 3. Substantial hand written native code lives in the app target If the app target conta"},{"u":"expo-vs-bare/ejecting-is-gone.html","t":"Ejecting Is Not a Thing Any More","s":"Expo vs Bare CLI","d":"expo eject was removed. What ejecting used to mean, what replaced it — prebuild plus a choice between generated and committed native directories — and why the new model is reversible.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works — what replaced ejecting","The old framing","The current framing","Why the change happened","Common patterns","Translating an old instruction","Recognising stale material","Common mistakes","Related topics"],"b":"\"Ejecting\" is a retired term has been removed from the Expo CLI. Nothing on this page describes a command you can run today. It exists so that a reader arriving from an older tutorial, Stack Overflow answer or team wiki knows what replaced it. \"Ejecting\" used to mean running to turn an Expo project that had no native directories into one that had them — and, in the process, leaving much of Expo's tooling behind. It was described as a one way door : once you ejected, you owned and by hand forever. That model is gone. There is no eject step, no one way door, and no point at which you \"leave\" the SDK by gaining native directories. Why it exists / when to use it — and when NOT to Read this page if: a tutorial tells you to run ; someone on your team argues against Expo because \"we will have to eject eventually\"; you are trying to understand why older material divides projects into a \"managed workflow\" and a \"bare workflow\". Do not treat it as a how to. The how to pages are expo prebuild and CNG vs Committed Native Directories. Basic example Run the old command against SDK 57's local CLI and it refuses. The migration map inside the installed redirects it, and the CLI exits with: The command it points to: Prebuild is not a one time eject ran once. is designed to run repeatedly , and in SDK 57 it clears and regenerates and by default. If you treat it like eject — run it, then hand edit the output — the next run deletes your edits. Decide on a strategy first. How it works — what replaced ejecting The old framing Old term What it meant Managed workflow An Expo project with no native directories; builds done by Expo's service Bare workflow A project with native directories, either created that way or ejected Eject The one way transition from the first to the second Those terms no "},{"u":"expo-vs-bare/adopting-expo.html","t":"Adopting Expo in an Existing Bare App","s":"Expo vs Bare CLI","d":"What it realistically takes to use Expo modules and tooling inside an existing React Native CLI app — install-expo-modules, the React Native version constraint, and why generation usually does not apply.","h":["Why it exists / when to use it — and when NOT to","The React Native version constraint comes first","Basic example","If the automated path fails","How it works","Native minimums","Common patterns","Adopt one package at a time","Never run prebuild on the existing app","Gate alignment in CI","Common mistakes","Related topics"],"b":"An existing React Native Community CLI app can use Expo SDK packages. It is a supported path, and it is a genuinely different thing from starting a project on Expo: you add the package and its module system to native projects you already own , and those projects stay committed and hand maintained. This page is honest about the three things that make adoption harder than a new project: the React Native version pin, the native changes, and the fact that Continuous Native Generation rarely fits an existing app. Why it exists / when to use it — and when NOT to Adopt Expo in an existing CLI app when: you want specific SDK packages — , , — without replacing your native projects; you want to write new native modules with the Expo Modules API; you want , Expo's Metro and Babel configuration, or EAS, while keeping native control. Do not expect adoption to give you: Generated native projects. Your and contain years of edits. Running on them clears and regenerates the directories by default in SDK 57, destroying those edits. Treat the app as using committed native directories, permanently or until you deliberately migrate. A way around the React Native version pin. See the next section. The React Native version constraint comes first Each Expo SDK targets exactly one React Native version, and the SDK's native packages are compiled against it: Expo SDK React Native SDK 55 0.83.10 SDK 56 0.85.3 SDK 57 0.86.3 An app built with the CLI half of this site is on React Native 0.87.1 . No stable SDK targets 0.87 yet , so SDK 57 packages in a 0.87.1 app are a mismatch that will report and that is not supported. Your realistic options: 1. Adopt when an SDK targeting your React Native version is stable. Nothing to force. 2. Keep the app on React Native 0.86.3 and adopt SDK 57. 3. Force it any"},{"u":"expo-vs-bare/moving-to-bare.html","t":"Moving from Expo to Bare","s":"Expo vs Bare CLI","d":"Two very different things get called \"moving to bare\" — committing native directories while keeping the SDK, and removing Expo entirely. What each involves, what it costs, and why most teams should stop at the first.","h":["Why it exists / when to use it — and when NOT to","Basic example — step 1: commit the native directories","How it works — step 2: removing Expo entirely","1. Inventory what Expo is doing","2. Replace each SDK package","3. Replace routing","4. Replace the entry point, Metro and Babel","5. Remove Expo from the native projects","6. Change the run commands","7. Only then upgrade React Native","Common patterns","Stop at step 1 and re-evaluate","Keep EAS without Expo modules","Common mistakes","Related topics"],"b":"\"Moving an Expo app to bare\" is used for two different jobs, and confusing them leads teams to plan months of work when they needed an afternoon — or an afternoon when they needed months. Step 1: commit native directories Step 2: remove Expo entirely What changes and become source; prebuild stops Every Expo package, config and build hook is replaced You keep SDK packages, , Expo Router, EAS, dev client React Native itself React Native version Still pinned by the SDK (0.86.3) Yours to choose Effort A command and a commit A migration project Reversible Yes, with effort Only by adopting Expo again Step 1 is almost always what people need. Step 2 is only justified by a case in When the Bare CLI Is Better. Why it exists / when to use it — and when NOT to Commit the native directories (step 1) when you need hand edited native code or audited native projects, but the SDK and tooling still serve you. Remove Expo entirely (step 2) only when you need something step 1 cannot give you — most often a React Native version the SDK does not ship, or organisational independence from the SDK's release cadence. Do not remove Expo because of Expo Go limitations, because of \"ejecting\", or to avoid EAS. None of those require it: development builds run any native library, ejecting no longer exists, and local builds are free. Basic example — step 1: commit the native directories Then remove and from , commit both directories, and stop running — remove it from scripts, CI and team habits. After this point, prebuild destroys work In SDK 57 clears and regenerates the native directories by default. Once they are committed and hand edited, one accidental run deletes the edits. Add a CI check that is empty after every build. What changes day to day: Native changes are edits to native files, reviewed"},{"u":"expo-migration/upgrading-sdk.html","t":"Upgrading Between SDK Versions","s":"Expo Migration and Reference","d":"The real upgrade flow for an Expo project — bump expo, run expo install --fix, read the changelog, and regenerate or hand-merge the native directories.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Step 1: bump expo","Step 2: npx expo install --fix","Step 3: the native directories","Step 4: read the changelog","Common patterns","Upgrade one SDK at a time","Verify before you celebrate","Clear the caches when the bundler misbehaves","expo.install.exclude for a package you must pin yourself","Migrating from the old upgrade command","Common mistakes","Related topics"],"b":"Upgrading an Expo project means moving the whole version line at once: the package, every package, the React Native version underneath, and the community libraries the SDK pins. You do not pick those versions yourself — the SDK does, and applies them. This page documents the upgrade to SDK 57 , which ships React Native 0.86.3 . Why it exists / when to use it — and when NOT to Upgrade when you want a newer SDK's modules, a newer React Native, or platform support the current SDK lacks (a new Android target API, a new Xcode requirement). Upgrading is also how you stay on a version that still receives fixes. Do not upgrade to solve a bug you have not diagnosed. An SDK bump changes 120 native modules and the React Native version in one step; if something was already broken, you now have two problems and no way to tell them apart. And do not try to upgrade React Native on its own. Each SDK targets exactly one React Native version and the SDK packages are compiled against it — see SDK to React Native Pairing. Basic example The flow documented on , in order: Then update the native side, which depends on which strategy your project uses — see Step 3: the native directories below. also works The Expo CLI special cases the package: it installs it first, then respawns itself so the new CLI version handles everything after. does steps 1 and 2 in one command. What you may not do is combine with other packages and / — the CLI rejects that with . The published walkthrough uses the two step form, so that is what this page leads with. How it works Step 1: bump is what pulls in the new CLI, the new , and the new the rest of the tooling reads. Nothing else can be resolved correctly until this package is on the target version, which is why it goes first and alone. Step 2: walks your , compa"},{"u":"expo-migration/install-check-and-fix.html","t":"expo install --check and --fix","s":"Expo Migration and Reference","d":"How the Expo CLI decides which dependency versions your project should have, what --check reports, what --fix rewrites, and why a bare npm install silently breaks an Expo app.","h":["Why it exists / when to use it — and when NOT to","Basic example","How it works","Where the expected versions come from","What --check prints and what it exits with","What --fix does","Why a bare npm install is the problem","Common patterns","Gate CI on it","Excluding a package you must pin yourself","Turning validation off (and why you probably should not)","Installing dev dependencies","Common mistakes","Related topics"],"b":"compares every dependency in your against the version the installed SDK expects, and reports the ones that do not match. rewrites them to the expected versions. These two flags exist because fetches , and is routinely built for a different Expo SDK than the one your project is on. That mismatch does not fail at install time. It fails at runtime, on a device, usually as a crash with a stack trace pointing into native code you did not write. Why it exists / when to use it — and when NOT to Run when you want to know, before you ship, whether your dependency tree still matches the SDK. It is a good CI step: it exits non zero when anything is out of line. Run after an SDK upgrade, after someone on the team has run a bare , or when has told you what is wrong and you agree with its answer. Do not run when you have deliberately pinned a package to a version the SDK does not choose. It will undo that decision. Record the exception in instead — see Excluding a package you must pin yourself. Basic example Verified from on : How it works Where the expected versions come from does not ask npm what the newest version of a package is. It asks the SDK what version of that package belongs to it. The answer lives in , inside the package you have installed. For SDK 57 ( ) that file maps 123 packages to versions. A sample, read out of the installed file: Package SDK 57 The SDK also pins community libraries, which is why the versions differ from the ones the CLI half of this site documents: Package SDK 57 pins CLI half (React Native 0.87) 3.3.0 4.6.0 0.12.2 4.27.0 5.9.1 14.0.1 3.1.1 Gesture Handler is a whole major version apart. Copying a version number from a CLI page into an Expo project is exactly the mistake is built to catch. You can read any entry yourself rather than trusting a tabl"},{"u":"expo-migration/sdk-react-native-pairing.html","t":"SDK to React Native Pairing","s":"Expo Migration and Reference","d":"Each Expo SDK targets exactly one React Native version. The verified pairing table, why you cannot upgrade React Native on its own, and why code does not transfer between the two halves of this site.","h":["The verified pairing table","Why it exists / when to use it — and when NOT to","Upgrading React Native independently of the SDK is not supported","What about going the other way?","Why the SDK trails the latest React Native release","The two halves of this site are on different React Natives","The differences that actually bite","Ref types: the concrete failure","The 0.87 removals are not removals for you","Common patterns","Check a library's peer range before you install it","Check what you actually have, in the project","Record the pairing where your team will see it","Common mistakes","Related topics"],"b":"An Expo SDK release is not a loose collection of packages that happen to work together. Every package in a given SDK is compiled against one React Native version. That version is not a recommendation or a minimum — it is the version the native code was built for. This page is the canonical reference for which React Native each SDK ships, and for what follows from that. The verified pairing table Read directly out of each release's own , which is the file itself consults: Expo SDK React Native SDK 55 0.83.10 SDK 56 0.85.3 SDK 57 0.86.3 You can confirm the row for your own project without trusting this page: On an SDK 57 project that prints . Secondary sources get this wrong Summaries of the SDK line frequently report SDK 55 as shipping React Native 0.85. It ships 0.83.10 . The table above was read out of the packages, not out of a blog post. When a source disagrees, run the command above. SDK 58 exists only as and dist tags. SDK 57 is the current stable release. Why it exists / when to use it — and when NOT to Come here whenever a version question arises: someone wants a React Native feature that landed after 0.86, a library's peer range names a React Native version, or a snippet from somewhere else will not compile. The one thing this page cannot give you is a way around the pairing. There isn't one. Upgrading React Native independently of the SDK is not supported Stated plainly, because it is the single most common request: You cannot pick your own React Native version on an Expo project Each Expo SDK targets one React Native version, and the SDK's 120 native modules are built against it . Forcing a different version in does not recompile them. What you get instead: reports the mismatch, prebuild generates native projects for a version your JavaScript dependencies were"},{"u":"expo-migration/removed-commands.html","t":"expo init and Other Removed Commands","s":"Expo Migration and Reference","d":"expo init, expo eject and the rest of the global expo-cli command surface were removed. The verified old-to-new mapping, read out of the local CLI itself.","h":["Why it exists / when to use it — and when NOT to","The two CLIs, and why commands \"disappeared\"","Basic example — the two that matter most","expo init is gone","expo eject is gone","How it works — the complete old-to-new map","The three destinations","expo doctor deserves its own note","expo upgrade has no one-command replacement","What did NOT change","Common patterns","Recognising a stale tutorial in ten seconds","Remove the global CLI if you still have it","Common mistakes","Related topics"],"b":"This page documents commands that no longer exist Everything named here has been removed . None of it is current advice. This page exists so that a reader arriving from an older tutorial can find out what to run instead. If a tutorial tells you to run , it predates the split between the old global package and the local CLI. That split is the root of almost every \"command not found\" or \"not supported in the local CLI\" message people hit today. Why it exists / when to use it — and when NOT to Use this page to translate an old command into a current one. Do not use it as a reason to install the old global CLI so the old command works again — the commands were not renamed for cosmetic reasons. Most of them moved to a different tool ( , , ) or were replaced by a different model entirely. The two CLIs, and why commands \"disappeared\" Old Current Package , installed globally , a dependency of in your project Invocation Versioning one global version for every project versioned with the SDK, per project The local CLI is versioned with your SDK. On SDK 57 you are running , and a different project on a different SDK runs a different CLI. That is the whole point: the CLI that resolves your dependency versions has to know which SDK you are on. The global CLI's command set was much larger because it also did account management, builds, credentials and publishing. Those responsibilities moved to (verified current version 24.5.0 ), which is a separate tool you install or run with . Basic example — the two that matter most is gone Running the old command against the local CLI prints, verbatim: The replacement, by package manager: also accepts and for alternative starting points. is gone The local CLI prints: That message is accurate about the command but understates the change. \"Ejecting"},{"u":"expo-migration/cli-reference.html","t":"Expo CLI Reference","s":"Expo Migration and Reference","d":"The npx expo command surface for SDK 57, read out of the installed @expo/cli 57.0.24 — every command, its verified flags, and what is deliberately not in it.","h":["Why it exists / when to use it — and when NOT to","Basic example","The command surface","npx expo start","npx expo install","npx expo prebuild","npx expo run:android","npx expo run:ios","npx expo export","npx expo config","npx expo customize","npx expo lint","npx expo serve","Account commands","What is NOT in npx expo","eas-cli is a separate tool","Common patterns","Check what you are actually running","Debug a config plugin without running prebuild","Start against a development build rather than Expo Go","Common mistakes","Related topics"],"b":"runs the local Expo CLI — , a dependency of the package in your project. It is versioned with your SDK, so an SDK 57 project runs and a project on a different SDK runs a different CLI. Everything on this page was read out of that installed package and confirmed by running locally. Where a flag exists in the CLI's argument parser but is not documented in its own help output, this page says so rather than guessing at its meaning. Why it exists / when to use it — and when NOT to Use this as the lookup table when you know roughly what you want and need the exact flag. For the narrative version of any given workflow, follow the links in Related topics — this page is deliberately a reference, not a tutorial. Do not reach for this page for EAS commands. , , and belong to ( 24.5.0 ), a separate tool covered in EAS Overview. Basic example The top level help, run against the installed CLI: with no subcommand runs . prints the CLI version, on SDK 57. The command surface Command What it does Start the local dev server (Metro) Install packages at the versions the SDK expects (alias: ) Generate the native and directories Build and run the native Android app locally Build and run the native iOS app locally Export the JavaScript bundle and assets for hosting Evaluate and print the resolved app config Generate static project files you want to own Run ESLint with Expo's setup Serve a previously exported build locally / / / Expo account authentication Three further entries exist in the CLI's command table but are hidden from on purpose: (a dispatcher — ), (an internal production bundling step) and (deprecated). Treat all three as implementation detail rather than part of the public surface. Starts the dev server. This is the default command. Flag Meaning Project directory. Default: curren"},{"u":"expo-migration/app-config-reference.html","t":"App Config Reference","s":"Expo Migration and Reference","d":"The app.json / app.config.ts keys that exist in Expo SDK 57, read out of the installed @expo/config-types 57.0.2 — including the ones that were removed.","h":["Why it exists / when to use it — and when NOT to","Basic example","The complete top-level key list","Keys the brief expected that are NOT top-level in SDK 57","splash is not a top-level key","Other keys that are absent","plugins","updates","runtimeVersion","ios","android","web","experiments","Common patterns","Print the resolved config instead of guessing","Dynamic config in TypeScript","Keep secrets out of it","Common mistakes","Related topics"],"b":"The app config is the single file that drives your app's identity, its native project generation and a large part of its runtime behaviour. It can be written as , or , and the keys are the same in all three. Every key on this page was read out of the installed in an SDK 57 project and cross checked against . Where a key you might expect is absent, this page says so — because an unrecognised key in the app config is silently ignored, which makes a typo or a stale key look exactly like a feature that does not work. Why it exists / when to use it — and when NOT to Use this page to check that a key exists and is spelled correctly before you spend an afternoon wondering why it has no effect. Use to see what the config resolved to after plugins and environment variables ran. Do not use this page as a substitute for reading the documentation of the key you need — this is a name and shape reference, not a guide to what each setting is for. Basic example The complete top level key list These are every key on the interface in SDK 57, in declaration order. The interface has no index signature, so this list is exhaustive. Key Type Notes Required. Display name on the home screen Free text Required. URL friendly project name, unique across your account Expo account that owns the project. Defaults to the current user Managed by tooling; you rarely set this Managed by tooling Usually inferred from the installed package Native/update compatibility. See below Maps to on iOS and on Android Defaults to ; adds when is installed Repository link on your Expo project page Defaults to . Needs on Android Root view background. Needs on iOS Android multitasker colour Path or URL. A 1024x1024 PNG is recommended object Deprecated — use the plugin configuration Behaviour when running in a development"},{"u":"expo-migration/cheat-sheet.html","t":"Expo Cheat Sheet","s":"Expo Migration and Reference","d":"Every Expo SDK 57 fact worth memorising on one page — commands, the SDK to React Native pairing, config keys, the prebuild danger and the Expo Go boundary.","h":["Versions","SDK to React Native pairing","The 0.86 vs 0.87 trap","Create a project","Run","The install rule","Upgrade an SDK","npx expo commands","The prebuild danger","Expo Go vs development build","App config keys","SDK 57 package versions","Shared libraries differ between the two halves","Stale-tutorial detector","Common mistakes","Related topics"],"b":"One page, scannable, SDK 57. Every number here was read out of the installed packages, not from memory. Each section links to the page that explains why. Versions Thing Value 57.0.22 — Expo SDK 57 57.0.24 57.0.21 24.5.0 — separate tool 1.20.4 — separate tool React Native 0.86.3 Prerequisites Node.js LTS; macOS, Windows (PowerShell or WSL 2), or Linux SDK 58 exists only as / . SDK 57 is current stable. SDK to React Native pairing Expo SDK React Native SDK 55 0.83.10 SDK 56 0.85.3 SDK 57 0.86.3 You cannot upgrade React Native independently of the SDK. Each SDK targets one React Native version and its 120 native modules are compiled against it. Move the SDK, and React Native moves with it. → SDK to React Native Pairing The 0.86 vs 0.87 trap This site's CLI half documents React Native 0.87.1 . You are on 0.86.3 . You (SDK 57 / 0.86) CLI half (0.87) Strict TypeScript API opt in default , , do not exist exist deep imports resolve (still a bad idea) type error Ref types: , never . Create a project Manager Command npm yarn pnpm bun Options: , . was removed It no longer exists. is gone too. → Removed Commands Run The install rule resolves the version matching your installed SDK from . A bare fetches , routinely built for a different SDK. Native module mismatches fail at runtime, not install time. Command Effect Install at the SDK matched version Report mismatches. Exit 1 if any Rewrite mismatched versions Machine readable, for CI Save to → expo install check and fix Pin something deliberately? Record it, do not fight the tool: Upgrade an SDK One SDK at a time. fixes versions, not your code. → Upgrading Between SDK Versions commands Command Use Dev server (the default) / SDK matched installs Generate and / Local native build and run Bundle and assets into Print the resolved app c"},{"u":"expo-migration/troubleshooting.html","t":"Expo Troubleshooting","s":"Expo Migration and Reference","d":"The failures Expo SDK 57 projects actually hit — stale tutorials, SDK-mismatched native modules, Expo Go limits, destroyed native edits and React Native 0.87 code on a 0.86 runtime — with cause and fix for each.","h":["Why it exists / when to use it — and when NOT to","A tutorial tells you to run expo init","npm install succeeded but the app crashes on launch","--check reports a version mismatch","Expo Go fails on a library with custom native code","expo prebuild deleted your hand-edited native code","Code copied from the React Native 0.87 docs does not compile","A \"removed in 0.87\" API still works, or vice versa","A config change appears to do nothing","npx expo doctor says it is not supported","After an SDK upgrade, the bundler reports errors that make no sense","Common mistakes","Related topics"],"b":"Every entry below follows the same shape: the symptom you see, the cause , and the fix . Most of these are not bugs in Expo. They are a stale instruction, a version mismatch, or a boundary nobody told you about. Why it exists / when to use it — and when NOT to Come here when something has already gone wrong. If you are choosing between approaches rather than debugging one, the Expo Cheat Sheet is the faster read. Nothing on this page is a workaround for an unverified problem. If you cannot reproduce the symptom, do not apply the fix. A tutorial tells you to run Symptom. You follow a tutorial, run , and get either or: Cause. was removed . It belonged to the old global , which was split apart: project creation went to , cloud operations went to , project checks went to , and what remains is the local CLI versioned with your SDK. Fix. The rest of that tutorial is suspect too was removed several SDKs ago. A tutorial still using it also predates prebuild, EAS, and the current app config. Check every command against expo init and Other Removed Commands before running it. Do not install the global to make the tutorial work — you will hit the next stale instruction shortly. succeeded but the app crashes on launch Symptom. You installed a package with , the install was clean, the bundle built, and then the app crashes at startup or the first time the module is used. Typical shapes: a null native module, a missing method on a native object, or a hard native crash with no JavaScript frames. Cause. An package is a JavaScript API plus compiled native code . fetches whatever npm calls , which is built for whatever SDK is current — not necessarily yours. The JavaScript resolves fine; the native half does not match your or your React Native 0.86.3. Nothing fails at install time. Native"}]